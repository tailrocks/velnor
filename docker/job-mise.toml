# Velnor job-image toolchain (Plan 008). This is the committed, locked mise
# config baked as the job container's global config. `mise install --locked`
# runs fail-closed against docker/job-mise.lock for linux-x64 and linux-arm64;
# there is no apt/Cargo/curl fallback (N8: one toolchain contract). Regenerate
# the adjacent lock with `mise lock --platform linux-x64,linux-arm64` — never
# hand-edit lock entries.
[settings]
cargo.binstall = true
lockfile = true
idiomatic_version_file_enable_tools = ["rust"]

[tools]
# Rust is selected from the adjacent rust-toolchain.toml through the idiomatic
# mise version-file setting above. Every other entry is an exact mise pin.
"aqua:nextest-rs/nextest/cargo-nextest" = "0.9.143"
"cargo:cargo-deny" = "0.20.2"
"github:fornwall/rust-script" = "0.36.0"
just = "1.58.0"
protoc = "35.1"
gh = "2.97.0"
mold = "2.41.0"
cosign = "3.1.3"
node = "26.5.0"
mr-boxington = "1.6.0"
sccache = "0.16.0"
hadolint = "2.15.1"
python = "3.14.7"
