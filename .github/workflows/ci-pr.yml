# Generated by velnor-workflow. Regenerate; do not hand-edit.
name: CI / PR
run-name: CI / PR · ${{ github.event_name }} · ${{ github.ref_name }}

on:
  pull_request:
  workflow_dispatch:
    inputs:
      runner:
        description: Execution backend
        required: false
        default: both
        type: choice
        options:
          - velnor
          - github
          - both
      scope:
        description: Verification scope
        required: true
        default: affected
        type: choice
        options:
          - affected
          - full
      base_sha:
        description: Git ref or SHA used as the affected-selection base
        required: false
        default: refs/heads/main
        type: string


concurrency:
  group: velnor-${{ github.repository }}-${{ github.event.pull_request.number || github.ref }}-pr-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  actions: read
  contents: read

jobs:
  plan:
    name: "Control / Planning"
    runs-on: ubuntu-24.04
    outputs:
      scope: ${{ steps.plan.outputs.scope }}
      base_sha: ${{ steps.plan.outputs.base_sha }}
      head_sha: ${{ steps.plan.outputs.head_sha }}
      units: ${{ steps.plan.outputs.units }}
      full_units: ${{ steps.plan.outputs.full_units }}
      bun_matrix: ${{ steps.plan.outputs.bun_matrix }}
      docker_matrix: ${{ steps.plan.outputs.docker_matrix }}
      docs_matrix: ${{ steps.plan.outputs.docs_matrix }}
      opentofu_matrix: ${{ steps.plan.outputs.opentofu_matrix }}
      rust_matrix: ${{ steps.plan.outputs.rust_matrix }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Set up Velnor workflow runtime
        if: ${{ runner.environment == 'github-hosted' }}
        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@e6fabca85499be62f939243d4452438449b69c53
        with:
          rev: ${{ (((github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository) || (github.ref == format('refs/heads/{0}', github.event.repository.default_branch) && (github.event_name == 'push' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'))) && github.sha) || 'e6fabca85499be62f939243d4452438449b69c53' }}
      - name: Set trusted workflow policy revision
        run: echo "VELNOR_WORKFLOW_POLICY_REVISION=e6fabca85499be62f939243d4452438449b69c53" >> "$GITHUB_ENV"
      - name: Select affected units
        id: plan
        env:
          EVENT_NAME: ${{ github.event_name }}
          CI_SCOPE_OVERRIDE: ${{ github.event.inputs.scope || '' }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_LANES: ${{ github.event.inputs.runner || 'both' }}
        run: |
          set -euo pipefail
          if [[ -z "${CI_SCOPE_OVERRIDE:-}" ]]; then unset CI_SCOPE_OVERRIDE; fi
          velnor-workflow plan --config .github/ci/project.toml

      - name: Prepare Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: e6fabca85499be62f939243d4452438449b69c53
        run: |
          set -euo pipefail
          stage="$RUNNER_TEMP/velnor-workflow-runtime"
          rm -rf "$stage"
          mkdir -p "$stage"
          src="$(command -v velnor-workflow)"
          install -m 0755 "$src" "$stage/velnor-workflow"
          digest="$(sha256sum "$stage/velnor-workflow" | awk '{print $1}')"
          jq -n --arg repository "$GITHUB_REPOSITORY" --arg revision "$EXPECTED_REVISION" --arg head_branch "${{ github.ref_name }}" --arg platform "${{ runner.os }}-${{ runner.arch }}" --arg run_id "$GITHUB_RUN_ID" --arg job_id "${{ github.job }}" --arg binary_sha256 "$digest" '{repository: $repository, revision: $revision, head_branch: $head_branch, platform: $platform, run_id: $run_id, job_id: $job_id, binary_sha256: $binary_sha256}' > "$stage/manifest.json"
      - name: Publish Velnor workflow runtime
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: velnor-workflow-runtime-e6fabca85499be62f939243d4452438449b69c53-${{ runner.os }}-${{ runner.arch }}
          path: ${{ runner.temp }}/velnor-workflow-runtime
          if-no-files-found: error
          retention-days: 7
  velnor-lane-admission:
    name: "Control / Velnor admission"
    if: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository }}
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Velnor lane omitted for fork
        run: |
          echo '::notice::Velnor lane omitted for fork pull request; validating GitHub lane only.'
  github-bun-velnor:
    name: "Bun · Bun package (velnor)"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',bun-velnor,') }}
    needs: [plan]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-bun.yml
    with:
      unit: bun-velnor
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  velnor-bun-velnor:
    name: "Bun · Bun package (velnor)"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',bun-velnor,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-bun.yml
    with:
      unit: bun-velnor
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  github-docker:
    name: "Docker · Docker"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',docker,') }}
    needs: [plan]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-docker.yml
    with:
      unit: docker
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  velnor-docker:
    name: "Docker · Docker"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',docker,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-docker.yml
    with:
      unit: docker
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  github-docs:
    name: "Documentation · Documentation"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',docs,') }}
    needs: [plan]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-docs.yml
    with:
      unit: docs
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  velnor-docs:
    name: "Documentation · Documentation"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',docs,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-docs.yml
    with:
      unit: docs
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  github-opentofu:
    name: "OpenTofu · OpenTofu"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',opentofu,') }}
    needs: [plan]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-opentofu.yml
    with:
      unit: opentofu
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  velnor-opentofu:
    name: "OpenTofu · OpenTofu"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',opentofu,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-opentofu.yml
    with:
      unit: opentofu
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  prepare-cargo:
    name: "Control / Prepare Cargo"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-policy,') }}
    needs: [plan]
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-policy
      lane: control
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  github-rust-policy:
    name: "Rust · Rust dependency policy"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-policy,') }}
    needs: [plan]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-policy
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  velnor-rust-policy:
    name: "Rust · Rust dependency policy"
    if: ${{ always() && needs.plan.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-policy,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, prepare-cargo]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-policy
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  github-rust-unit-collector:
    name: "Rust · unit-collector"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-unit-collector,') }}
    needs: [plan]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-unit-collector
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  velnor-rust-unit-collector:
    name: "Rust · unit-collector"
    if: ${{ always() && needs.plan.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-unit-collector,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, prepare-cargo]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-unit-collector
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  github-rust-velnor-bench:
    name: "Rust · velnor-bench"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-velnor-bench,') }}
    needs: [plan]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-velnor-bench
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  velnor-rust-velnor-bench:
    name: "Rust · velnor-bench"
    if: ${{ always() && needs.plan.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && (needs.velnor-rust-velnor-model.result == 'success' || needs.velnor-rust-velnor-model.result == 'skipped') && (needs.velnor-rust-velnor-runner.result == 'success' || needs.velnor-rust-velnor-runner.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-velnor-bench,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, prepare-cargo, velnor-rust-velnor-model, velnor-rust-velnor-runner]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-velnor-bench
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  github-rust-velnor-client:
    name: "Rust · velnor-client"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-velnor-client,') }}
    needs: [plan]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-velnor-client
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  velnor-rust-velnor-client:
    name: "Rust · velnor-client"
    if: ${{ always() && needs.plan.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && (needs.velnor-rust-velnor-model.result == 'success' || needs.velnor-rust-velnor-model.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-velnor-client,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, prepare-cargo, velnor-rust-velnor-model]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-velnor-client
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  github-rust-velnor-control:
    name: "Rust · velnor-control"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-velnor-control,') }}
    needs: [plan]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-velnor-control
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  velnor-rust-velnor-control:
    name: "Rust · velnor-control"
    if: ${{ always() && needs.plan.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && (needs.velnor-rust-velnor-model.result == 'success' || needs.velnor-rust-velnor-model.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-velnor-control,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, prepare-cargo, velnor-rust-velnor-model]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-velnor-control
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  github-rust-velnor-model:
    name: "Rust · velnor-model"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-velnor-model,') }}
    needs: [plan]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-velnor-model
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  velnor-rust-velnor-model:
    name: "Rust · velnor-model"
    if: ${{ always() && needs.plan.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-velnor-model,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, prepare-cargo]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-velnor-model
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  github-rust-velnor-render:
    name: "Rust · velnor-render"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-velnor-render,') }}
    needs: [plan]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-velnor-render
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  velnor-rust-velnor-render:
    name: "Rust · velnor-render"
    if: ${{ always() && needs.plan.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && (needs.velnor-rust-velnor-model.result == 'success' || needs.velnor-rust-velnor-model.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-velnor-render,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, prepare-cargo, velnor-rust-velnor-model]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-velnor-render
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  github-rust-velnor-runner:
    name: "Rust · velnor-runner"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-velnor-runner,') }}
    needs: [plan]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-velnor-runner
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  velnor-rust-velnor-runner:
    name: "Rust · velnor-runner"
    if: ${{ always() && needs.plan.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && (needs.velnor-rust-velnor-control.result == 'success' || needs.velnor-rust-velnor-control.result == 'skipped') && (needs.velnor-rust-velnor-model.result == 'success' || needs.velnor-rust-velnor-model.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-velnor-runner,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, prepare-cargo, velnor-rust-velnor-control, velnor-rust-velnor-model]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-velnor-runner
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  github-rust-velnor-tools:
    name: "Rust · velnor-tools"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-velnor-tools,') }}
    needs: [plan]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-velnor-tools
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  velnor-rust-velnor-tools:
    name: "Rust · velnor-tools"
    if: ${{ always() && needs.plan.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && (needs.velnor-rust-velnor-client.result == 'success' || needs.velnor-rust-velnor-client.result == 'skipped') && (needs.velnor-rust-velnor-model.result == 'success' || needs.velnor-rust-velnor-model.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-velnor-tools,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, prepare-cargo, velnor-rust-velnor-client, velnor-rust-velnor-model]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-velnor-tools
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  github-rust-velnor-workflow:
    name: "Rust · velnor-workflow"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-velnor-workflow,') }}
    needs: [plan]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-velnor-workflow
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  velnor-rust-velnor-workflow:
    name: "Rust · velnor-workflow"
    if: ${{ always() && needs.plan.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && (needs.velnor-rust-velnor-runner.result == 'success' || needs.velnor-rust-velnor-runner.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-velnor-workflow,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, prepare-cargo, velnor-rust-velnor-runner]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-velnor-workflow
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  github-rust-velnor-workflow-contract:
    name: "Rust · velnor-workflow-contract"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-velnor-workflow-contract,') }}
    needs: [plan]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-velnor-workflow-contract
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  velnor-rust-velnor-workflow-contract:
    name: "Rust · velnor-workflow-contract"
    if: ${{ always() && needs.plan.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-velnor-workflow-contract,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, prepare-cargo]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-velnor-workflow-contract
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  github-rust-velnorctl:
    name: "Rust · velnorctl"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-velnorctl,') }}
    needs: [plan]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-velnorctl
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  velnor-rust-velnorctl:
    name: "Rust · velnorctl"
    if: ${{ always() && needs.plan.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && (needs.velnor-rust-velnor-client.result == 'success' || needs.velnor-rust-velnor-client.result == 'skipped') && (needs.velnor-rust-velnor-control.result == 'success' || needs.velnor-rust-velnor-control.result == 'skipped') && (needs.velnor-rust-velnor-model.result == 'success' || needs.velnor-rust-velnor-model.result == 'skipped') && (needs.velnor-rust-velnor-render.result == 'success' || needs.velnor-rust-velnor-render.result == 'skipped') && (needs.velnor-rust-velnor-runner.result == 'success' || needs.velnor-rust-velnor-runner.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-velnorctl,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, prepare-cargo, velnor-rust-velnor-client, velnor-rust-velnor-control, velnor-rust-velnor-model, velnor-rust-velnor-render, velnor-rust-velnor-runner]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-velnorctl
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  github-rust-production-topology:
    name: "Rust · Rust production topology"
    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-production-topology,') }}
    needs: [plan]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-production-topology
      lane: github
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  velnor-rust-production-topology:
    name: "Rust · Rust production topology"
    if: ${{ always() && needs.plan.result == 'success' && (needs.prepare-cargo.result == 'success' || needs.prepare-cargo.result == 'skipped') && contains(format(',{0},', needs.plan.outputs.units), ',rust-production-topology,') && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) }}
    needs: [plan, prepare-cargo]
    cache-mode: read
    uses: ./.github/workflows/ci-unit-rust.yml
    with:
      unit: rust-production-topology
      lane: velnor
      selected_units: ${{ needs.plan.outputs.units }}
      scope: ${{ needs.plan.outputs.scope }}
      full_units: ${{ needs.plan.outputs.full_units }}
      base_sha: ${{ needs.plan.outputs.base_sha }}
      head_sha: ${{ needs.plan.outputs.head_sha }}
  ci-required:
    name: ci-required
    if: ${{ always() }}
    needs: [plan, velnor-lane-admission, github-bun-velnor, velnor-bun-velnor, github-docker, velnor-docker, github-docs, velnor-docs, github-opentofu, velnor-opentofu, prepare-cargo, github-rust-policy, velnor-rust-policy, github-rust-unit-collector, velnor-rust-unit-collector, github-rust-velnor-bench, velnor-rust-velnor-bench, github-rust-velnor-client, velnor-rust-velnor-client, github-rust-velnor-control, velnor-rust-velnor-control, github-rust-velnor-model, velnor-rust-velnor-model, github-rust-velnor-render, velnor-rust-velnor-render, github-rust-velnor-runner, velnor-rust-velnor-runner, github-rust-velnor-tools, velnor-rust-velnor-tools, github-rust-velnor-workflow, velnor-rust-velnor-workflow, github-rust-velnor-workflow-contract, velnor-rust-velnor-workflow-contract, github-rust-velnorctl, velnor-rust-velnorctl, github-rust-production-topology, velnor-rust-production-topology]
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Validate generated stack results
        env:
          NEEDS_JSON: ${{ toJSON(needs) }}
          SELECTED_UNITS: ${{ needs.plan.outputs.units }}
          FORK_PR: ${{ github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository }}
        shell: bash
        run: |
          set -euo pipefail
          result_for_job() {
            jq -r --arg job "$1" '.[$job].result // empty' <<<"$NEEDS_JSON"
          }
          result="$(result_for_job plan)"
          if [[ "$result" != success ]]; then
            echo "required CI prerequisite plan did not pass: $result" >&2
            exit 1
          fi
          result="$(result_for_job velnor-lane-admission)"
          case "$result" in
            success|skipped) ;;
            *) echo "required CI prerequisite velnor-lane-admission did not pass: $result" >&2; exit 1 ;;
          esac
          selected=",$SELECTED_UNITS,"
          if [[ "$selected" == *",bun-velnor,"* ]]; then
            result="$(result_for_job github-bun-velnor)"
            if [[ "$FORK_PR" == true && false ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job github-bun-velnor did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job github-bun-velnor did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-bun-velnor)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-bun-velnor failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",bun-velnor,"* ]]; then
            result="$(result_for_job velnor-bun-velnor)"
            if [[ "$FORK_PR" == true && true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-bun-velnor did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-bun-velnor did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-bun-velnor)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-bun-velnor failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",docker,"* ]]; then
            result="$(result_for_job github-docker)"
            if [[ "$FORK_PR" == true && false ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job github-docker did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job github-docker did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-docker)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-docker failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",docker,"* ]]; then
            result="$(result_for_job velnor-docker)"
            if [[ "$FORK_PR" == true && true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-docker did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-docker did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-docker)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-docker failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",docs,"* ]]; then
            result="$(result_for_job github-docs)"
            if [[ "$FORK_PR" == true && false ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job github-docs did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job github-docs did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-docs)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-docs failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",docs,"* ]]; then
            result="$(result_for_job velnor-docs)"
            if [[ "$FORK_PR" == true && true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-docs did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-docs did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-docs)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-docs failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",opentofu,"* ]]; then
            result="$(result_for_job github-opentofu)"
            if [[ "$FORK_PR" == true && false ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job github-opentofu did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job github-opentofu did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-opentofu)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-opentofu failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",opentofu,"* ]]; then
            result="$(result_for_job velnor-opentofu)"
            if [[ "$FORK_PR" == true && true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-opentofu did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-opentofu did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-opentofu)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-opentofu failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-policy,"* ]]; then
            result="$(result_for_job prepare-cargo)"
            if [[ "$result" != success ]]; then
              echo "selected CI prerequisite prepare-cargo did not pass: $result" >&2
              exit 1
            fi
          else
            result="$(result_for_job prepare-cargo)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI prerequisite prepare-cargo failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-policy,"* ]]; then
            result="$(result_for_job github-rust-policy)"
            if [[ "$FORK_PR" == true && false ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job github-rust-policy did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job github-rust-policy did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-rust-policy)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-policy failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-policy,"* ]]; then
            result="$(result_for_job velnor-rust-policy)"
            if [[ "$FORK_PR" == true && true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-rust-policy did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-policy did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-policy)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-policy failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-unit-collector,"* ]]; then
            result="$(result_for_job github-rust-unit-collector)"
            if [[ "$FORK_PR" == true && false ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job github-rust-unit-collector did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job github-rust-unit-collector did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-rust-unit-collector)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-unit-collector failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-unit-collector,"* ]]; then
            result="$(result_for_job velnor-rust-unit-collector)"
            if [[ "$FORK_PR" == true && true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-rust-unit-collector did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-unit-collector did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-unit-collector)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-unit-collector failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-velnor-bench,"* ]]; then
            result="$(result_for_job github-rust-velnor-bench)"
            if [[ "$FORK_PR" == true && false ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job github-rust-velnor-bench did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job github-rust-velnor-bench did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-rust-velnor-bench)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-velnor-bench failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-velnor-bench,"* ]]; then
            result="$(result_for_job velnor-rust-velnor-bench)"
            if [[ "$FORK_PR" == true && true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-rust-velnor-bench did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-velnor-bench did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-velnor-bench)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-velnor-bench failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-velnor-client,"* ]]; then
            result="$(result_for_job github-rust-velnor-client)"
            if [[ "$FORK_PR" == true && false ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job github-rust-velnor-client did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job github-rust-velnor-client did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-rust-velnor-client)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-velnor-client failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-velnor-client,"* ]]; then
            result="$(result_for_job velnor-rust-velnor-client)"
            if [[ "$FORK_PR" == true && true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-rust-velnor-client did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-velnor-client did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-velnor-client)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-velnor-client failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-velnor-control,"* ]]; then
            result="$(result_for_job github-rust-velnor-control)"
            if [[ "$FORK_PR" == true && false ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job github-rust-velnor-control did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job github-rust-velnor-control did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-rust-velnor-control)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-velnor-control failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-velnor-control,"* ]]; then
            result="$(result_for_job velnor-rust-velnor-control)"
            if [[ "$FORK_PR" == true && true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-rust-velnor-control did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-velnor-control did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-velnor-control)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-velnor-control failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-velnor-model,"* ]]; then
            result="$(result_for_job github-rust-velnor-model)"
            if [[ "$FORK_PR" == true && false ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job github-rust-velnor-model did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job github-rust-velnor-model did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-rust-velnor-model)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-velnor-model failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-velnor-model,"* ]]; then
            result="$(result_for_job velnor-rust-velnor-model)"
            if [[ "$FORK_PR" == true && true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-rust-velnor-model did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-velnor-model did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-velnor-model)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-velnor-model failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-velnor-render,"* ]]; then
            result="$(result_for_job github-rust-velnor-render)"
            if [[ "$FORK_PR" == true && false ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job github-rust-velnor-render did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job github-rust-velnor-render did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-rust-velnor-render)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-velnor-render failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-velnor-render,"* ]]; then
            result="$(result_for_job velnor-rust-velnor-render)"
            if [[ "$FORK_PR" == true && true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-rust-velnor-render did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-velnor-render did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-velnor-render)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-velnor-render failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-velnor-runner,"* ]]; then
            result="$(result_for_job github-rust-velnor-runner)"
            if [[ "$FORK_PR" == true && false ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job github-rust-velnor-runner did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job github-rust-velnor-runner did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-rust-velnor-runner)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-velnor-runner failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-velnor-runner,"* ]]; then
            result="$(result_for_job velnor-rust-velnor-runner)"
            if [[ "$FORK_PR" == true && true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-rust-velnor-runner did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-velnor-runner did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-velnor-runner)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-velnor-runner failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-velnor-tools,"* ]]; then
            result="$(result_for_job github-rust-velnor-tools)"
            if [[ "$FORK_PR" == true && false ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job github-rust-velnor-tools did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job github-rust-velnor-tools did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-rust-velnor-tools)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-velnor-tools failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-velnor-tools,"* ]]; then
            result="$(result_for_job velnor-rust-velnor-tools)"
            if [[ "$FORK_PR" == true && true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-rust-velnor-tools did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-velnor-tools did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-velnor-tools)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-velnor-tools failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-velnor-workflow,"* ]]; then
            result="$(result_for_job github-rust-velnor-workflow)"
            if [[ "$FORK_PR" == true && false ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job github-rust-velnor-workflow did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job github-rust-velnor-workflow did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-rust-velnor-workflow)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-velnor-workflow failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-velnor-workflow,"* ]]; then
            result="$(result_for_job velnor-rust-velnor-workflow)"
            if [[ "$FORK_PR" == true && true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-rust-velnor-workflow did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-velnor-workflow did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-velnor-workflow)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-velnor-workflow failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-velnor-workflow-contract,"* ]]; then
            result="$(result_for_job github-rust-velnor-workflow-contract)"
            if [[ "$FORK_PR" == true && false ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job github-rust-velnor-workflow-contract did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job github-rust-velnor-workflow-contract did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-rust-velnor-workflow-contract)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-velnor-workflow-contract failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-velnor-workflow-contract,"* ]]; then
            result="$(result_for_job velnor-rust-velnor-workflow-contract)"
            if [[ "$FORK_PR" == true && true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-rust-velnor-workflow-contract did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-velnor-workflow-contract did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-velnor-workflow-contract)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-velnor-workflow-contract failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-velnorctl,"* ]]; then
            result="$(result_for_job github-rust-velnorctl)"
            if [[ "$FORK_PR" == true && false ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job github-rust-velnorctl did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job github-rust-velnorctl did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-rust-velnorctl)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-velnorctl failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-velnorctl,"* ]]; then
            result="$(result_for_job velnor-rust-velnorctl)"
            if [[ "$FORK_PR" == true && true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-rust-velnorctl did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-velnorctl did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-velnorctl)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-velnorctl failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-production-topology,"* ]]; then
            result="$(result_for_job github-rust-production-topology)"
            if [[ "$FORK_PR" == true && false ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job github-rust-production-topology did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job github-rust-production-topology did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job github-rust-production-topology)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job github-rust-production-topology failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
          if [[ "$selected" == *",rust-production-topology,"* ]]; then
            result="$(result_for_job velnor-rust-production-topology)"
            if [[ "$FORK_PR" == true && true ]]; then
              case "$result" in
                success|skipped) ;;
                *) echo "selected CI job velnor-rust-production-topology did not pass: $result" >&2; exit 1 ;;
              esac
            else
              case "$result" in
                success) ;;
                *) echo "selected CI job velnor-rust-production-topology did not pass: $result" >&2; exit 1 ;;
              esac
            fi
          else
            result="$(result_for_job velnor-rust-production-topology)"
            case "$result" in
              success|skipped) ;;
              *) echo "unselected CI job velnor-rust-production-topology failed unexpectedly: $result" >&2; exit 1 ;;
            esac
          fi
  required:
    name: "Control / Required"
    if: ${{ always() }}
    needs: [ci-required]
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Mirror CI / Required
        if: ${{ needs.ci-required.result != 'success' }}
        run: exit 1
