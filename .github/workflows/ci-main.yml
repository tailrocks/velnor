# Generated by github-actions-generator. Regenerate; do not hand-edit.
name: CI
run-name: CI / main · ${{ github.event_name }} · ${{ github.ref_name }}

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: false

permissions:
  contents: read


env:
  CARGO_INCREMENTAL: "0"
  RUSTC_WRAPPER: sccache
  SCCACHE_GHA_ENABLED: "true"

jobs:
  plan:
    name: Plan
    runs-on: ubuntu-24.04
    outputs:
      scope: ${{ steps.plan.outputs.scope }}
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Select affected units
        id: plan
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh plan

  policy:
    name: Policy
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout workflow data
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Checkout trusted policy
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        continue-on-error: true
        with:
          ref: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.sha }}
          path: .github-policy
          persist-credentials: false
      - name: Enforce workflow policy
        env:
          EVENT_NAME: ${{ github.event_name }}
          WORKFLOW_ROOT: ${{ github.workspace }}
        run: |
          set -euo pipefail
          policy=.github-policy/.github/ci/policy.sh
          if [[ ! -f "$policy" ]]; then
            policy=.github/ci/policy.sh
          fi
          bash "$policy"
  github-bun-traced-node-modules:
    name: "GitHub / Bun package (traced-node-modules)"
    needs: [plan, policy]
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6
      - name: Restore "Bun package (traced-node-modules)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.bun/install/cache
          key: ci-${{ runner.os }}-bun-traced-node-modules-${{ hashFiles('.output/server/package.json', 'bun.lock') }}
          restore-keys: |
            ci-${{ runner.os }}-bun-traced-node-modules-
      - name: Run "Bun package (traced-node-modules)" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: bun-traced-node-modules
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"
      - name: Save "Bun package (traced-node-modules)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.bun/install/cache
          key: ci-${{ runner.os }}-bun-traced-node-modules-${{ hashFiles('.output/server/package.json', 'bun.lock') }}

  github-bun-velnor:
    name: "GitHub / Bun package (velnor)"
    needs: [plan, policy]
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6
      - name: Restore "Bun package (velnor)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.bun/install/cache
          key: ci-${{ runner.os }}-bun-velnor-${{ hashFiles('package.json', 'bun.lock') }}
          restore-keys: |
            ci-${{ runner.os }}-bun-velnor-
      - name: Run "Bun package (velnor)" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: bun-velnor
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"
      - name: Save "Bun package (velnor)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.bun/install/cache
          key: ci-${{ runner.os }}-bun-velnor-${{ hashFiles('package.json', 'bun.lock') }}

  github-docker:
    name: "GitHub / Docker"
    needs: [plan, policy]
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e
      - name: Run Docker checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: docker
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"

  github-docs:
    name: "GitHub / Documentation"
    needs: [plan, policy]
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Restore Documentation cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.npm
          key: ci-${{ runner.os }}-docs-${{ hashFiles('package-lock.json', 'bun.lock') }}
          restore-keys: |
            ci-${{ runner.os }}-docs-
      - name: Run Documentation checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: docs
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"
      - name: Save Documentation cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.npm
          key: ci-${{ runner.os }}-docs-${{ hashFiles('package-lock.json', 'bun.lock') }}

  github-rust-policy:
    name: "GitHub / Rust dependency policy"
    needs: [plan, policy]
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up cargo-deny
        uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf
        with:
          tool: cargo-deny
          fallback: none
      - name: Set up sccache
        uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba
        with:
          version: v0.16.0
      - name: Set up mold
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444
        with:
          make-default: true
      - name: Restore "Rust dependency policy" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-policy-${{ hashFiles('Cargo.toml', 'Cargo.lock') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-policy-
      - name: Run "Rust dependency policy" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: rust-policy
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"
      - name: Save "Rust dependency policy" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-policy-${{ hashFiles('Cargo.toml', 'Cargo.lock') }}

  github-rust-unit-collector:
    name: "GitHub / Rust crate (unit-collector)"
    needs: [plan, policy]
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up cargo-nextest
        uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf
        with:
          tool: nextest
          fallback: none
      - name: Set up sccache
        uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba
        with:
          version: v0.16.0
      - name: Set up mold
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444
        with:
          make-default: true
      - name: Restore "Rust crate (unit-collector)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-unit-collector-${{ hashFiles('tools/unit-collector/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-unit-collector-
      - name: Run "Rust crate (unit-collector)" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: rust-unit-collector
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"
      - name: Save "Rust crate (unit-collector)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-unit-collector-${{ hashFiles('tools/unit-collector/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  github-rust-velnor-action-journal:
    name: "GitHub / Rust crate (velnor-action-journal)"
    needs: [plan, policy, github-rust-velnor-action-model, github-rust-velnor-model]
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up cargo-nextest
        uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf
        with:
          tool: nextest
          fallback: none
      - name: Set up sccache
        uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba
        with:
          version: v0.16.0
      - name: Set up mold
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444
        with:
          make-default: true
      - name: Restore "Rust crate (velnor-action-journal)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-action-journal-${{ hashFiles('crates/velnor-action-journal/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-velnor-action-journal-
      - name: Run "Rust crate (velnor-action-journal)" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: rust-velnor-action-journal
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"
      - name: Save "Rust crate (velnor-action-journal)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-action-journal-${{ hashFiles('crates/velnor-action-journal/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  github-rust-velnor-action-model:
    name: "GitHub / Rust crate (velnor-action-model)"
    needs: [plan, policy]
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up cargo-nextest
        uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf
        with:
          tool: nextest
          fallback: none
      - name: Set up sccache
        uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba
        with:
          version: v0.16.0
      - name: Set up mold
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444
        with:
          make-default: true
      - name: Restore "Rust crate (velnor-action-model)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-action-model-${{ hashFiles('crates/velnor-action-model/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-velnor-action-model-
      - name: Run "Rust crate (velnor-action-model)" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: rust-velnor-action-model
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"
      - name: Save "Rust crate (velnor-action-model)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-action-model-${{ hashFiles('crates/velnor-action-model/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  github-rust-velnor-cache-service:
    name: "GitHub / Rust crate (velnor-cache-service)"
    needs: [plan, policy, github-rust-velnor-action-journal, github-rust-velnor-action-model, github-rust-velnor-cas]
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up cargo-nextest
        uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf
        with:
          tool: nextest
          fallback: none
      - name: Set up sccache
        uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba
        with:
          version: v0.16.0
      - name: Set up mold
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444
        with:
          make-default: true
      - name: Restore "Rust crate (velnor-cache-service)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-cache-service-${{ hashFiles('crates/velnor-cache-service/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-velnor-cache-service-
      - name: Run "Rust crate (velnor-cache-service)" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: rust-velnor-cache-service
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"
      - name: Save "Rust crate (velnor-cache-service)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-cache-service-${{ hashFiles('crates/velnor-cache-service/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  github-rust-velnor-cas:
    name: "GitHub / Rust crate (velnor-cas)"
    needs: [plan, policy, github-rust-velnor-action-model]
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up cargo-nextest
        uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf
        with:
          tool: nextest
          fallback: none
      - name: Set up sccache
        uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba
        with:
          version: v0.16.0
      - name: Set up mold
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444
        with:
          make-default: true
      - name: Restore "Rust crate (velnor-cas)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-cas-${{ hashFiles('crates/velnor-cas/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-velnor-cas-
      - name: Run "Rust crate (velnor-cas)" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: rust-velnor-cas
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"
      - name: Save "Rust crate (velnor-cas)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-cas-${{ hashFiles('crates/velnor-cas/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  github-rust-velnor-client:
    name: "GitHub / Rust crate (velnor-client)"
    needs: [plan, policy, github-rust-velnor-model]
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up cargo-nextest
        uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf
        with:
          tool: nextest
          fallback: none
      - name: Set up sccache
        uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba
        with:
          version: v0.16.0
      - name: Set up mold
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444
        with:
          make-default: true
      - name: Restore "Rust crate (velnor-client)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-client-${{ hashFiles('crates/velnor-client/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-velnor-client-
      - name: Run "Rust crate (velnor-client)" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: rust-velnor-client
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"
      - name: Save "Rust crate (velnor-client)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-client-${{ hashFiles('crates/velnor-client/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  github-rust-velnor-control:
    name: "GitHub / Rust crate (velnor-control)"
    needs: [plan, policy, github-rust-velnor-action-journal, github-rust-velnor-model]
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up cargo-nextest
        uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf
        with:
          tool: nextest
          fallback: none
      - name: Set up sccache
        uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba
        with:
          version: v0.16.0
      - name: Set up mold
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444
        with:
          make-default: true
      - name: Restore "Rust crate (velnor-control)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-control-${{ hashFiles('crates/velnor-control/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-velnor-control-
      - name: Run "Rust crate (velnor-control)" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: rust-velnor-control
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"
      - name: Save "Rust crate (velnor-control)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-control-${{ hashFiles('crates/velnor-control/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  github-rust-velnor-model:
    name: "GitHub / Rust crate (velnor-model)"
    needs: [plan, policy]
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up cargo-nextest
        uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf
        with:
          tool: nextest
          fallback: none
      - name: Set up sccache
        uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba
        with:
          version: v0.16.0
      - name: Set up mold
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444
        with:
          make-default: true
      - name: Restore "Rust crate (velnor-model)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-model-${{ hashFiles('crates/velnor-model/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-velnor-model-
      - name: Run "Rust crate (velnor-model)" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: rust-velnor-model
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"
      - name: Save "Rust crate (velnor-model)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-model-${{ hashFiles('crates/velnor-model/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  github-rust-velnor-render:
    name: "GitHub / Rust crate (velnor-render)"
    needs: [plan, policy, github-rust-velnor-model]
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up cargo-nextest
        uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf
        with:
          tool: nextest
          fallback: none
      - name: Set up sccache
        uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba
        with:
          version: v0.16.0
      - name: Set up mold
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444
        with:
          make-default: true
      - name: Restore "Rust crate (velnor-render)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-render-${{ hashFiles('crates/velnor-render/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-velnor-render-
      - name: Run "Rust crate (velnor-render)" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: rust-velnor-render
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"
      - name: Save "Rust crate (velnor-render)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-render-${{ hashFiles('crates/velnor-render/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  github-rust-velnor-runner:
    name: "GitHub / Rust crate (velnor-runner)"
    needs: [plan, policy, github-rust-velnor-action-model, github-rust-velnor-cache-service, github-rust-velnor-control, github-rust-velnor-model]
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up cargo-nextest
        uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf
        with:
          tool: nextest
          fallback: none
      - name: Set up sccache
        uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba
        with:
          version: v0.16.0
      - name: Set up mold
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444
        with:
          make-default: true
      - name: Restore "Rust crate (velnor-runner)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-runner-${{ hashFiles('crates/velnor-runner/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-velnor-runner-
      - name: Run "Rust crate (velnor-runner)" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: rust-velnor-runner
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"
      - name: Save "Rust crate (velnor-runner)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-runner-${{ hashFiles('crates/velnor-runner/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  github-rust-velnor-tools:
    name: "GitHub / Rust crate (velnor-tools)"
    needs: [plan, policy]
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up cargo-nextest
        uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf
        with:
          tool: nextest
          fallback: none
      - name: Set up sccache
        uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba
        with:
          version: v0.16.0
      - name: Set up mold
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444
        with:
          make-default: true
      - name: Restore "Rust crate (velnor-tools)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-tools-${{ hashFiles('crates/velnor-tools/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-velnor-tools-
      - name: Run "Rust crate (velnor-tools)" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: rust-velnor-tools
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"
      - name: Save "Rust crate (velnor-tools)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-tools-${{ hashFiles('crates/velnor-tools/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  github-rust-velnorctl:
    name: "GitHub / Rust crate (velnorctl)"
    needs: [plan, policy, github-rust-velnor-client, github-rust-velnor-control, github-rust-velnor-model, github-rust-velnor-render, github-rust-velnor-runner]
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up cargo-nextest
        uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf
        with:
          tool: nextest
          fallback: none
      - name: Set up sccache
        uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba
        with:
          version: v0.16.0
      - name: Set up mold
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444
        with:
          make-default: true
      - name: Restore "Rust crate (velnorctl)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnorctl-${{ hashFiles('crates/velnorctl/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-velnorctl-
      - name: Run "Rust crate (velnorctl)" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: rust-velnorctl
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"
      - name: Save "Rust crate (velnorctl)" cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnorctl-${{ hashFiles('crates/velnorctl/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}

  velnor-bun-traced-node-modules:
    name: "Velnor / Bun package (traced-node-modules)"
    if: ${{ github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') }}
    needs: [plan, policy]
    runs-on: [self-hosted, velnor]
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6
      - name: Restore "Bun package (traced-node-modules)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.bun/install/cache
          key: ci-${{ runner.os }}-bun-traced-node-modules-${{ hashFiles('.output/server/package.json', 'bun.lock') }}
          restore-keys: |
            ci-${{ runner.os }}-bun-traced-node-modules-
      - name: Run "Bun package (traced-node-modules)" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: bun-traced-node-modules
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"

  velnor-bun-velnor:
    name: "Velnor / Bun package (velnor)"
    if: ${{ github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') }}
    needs: [plan, policy]
    runs-on: [self-hosted, velnor]
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6
      - name: Restore "Bun package (velnor)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.bun/install/cache
          key: ci-${{ runner.os }}-bun-velnor-${{ hashFiles('package.json', 'bun.lock') }}
          restore-keys: |
            ci-${{ runner.os }}-bun-velnor-
      - name: Run "Bun package (velnor)" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: bun-velnor
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"

  velnor-docker:
    name: "Velnor / Docker"
    if: ${{ github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') }}
    needs: [plan, policy]
    runs-on: [self-hosted, velnor]
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e
      - name: Run Docker checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: docker
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"

  velnor-docs:
    name: "Velnor / Documentation"
    if: ${{ github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') }}
    needs: [plan, policy]
    runs-on: [self-hosted, velnor]
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Restore Documentation cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.npm
          key: ci-${{ runner.os }}-docs-${{ hashFiles('package-lock.json', 'bun.lock') }}
          restore-keys: |
            ci-${{ runner.os }}-docs-
      - name: Run Documentation checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: docs
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"

  velnor-rust-policy:
    name: "Velnor / Rust dependency policy"
    if: ${{ github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') }}
    needs: [plan, policy]
    runs-on: [self-hosted, velnor]
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up cargo-deny
        uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf
        with:
          tool: cargo-deny
          fallback: none
      - name: Set up sccache
        uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba
        with:
          version: v0.16.0
      - name: Set up mold
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444
        with:
          make-default: true
      - name: Restore "Rust dependency policy" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-policy-${{ hashFiles('Cargo.toml', 'Cargo.lock') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-policy-
      - name: Run "Rust dependency policy" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: rust-policy
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"

  velnor-rust-unit-collector:
    name: "Velnor / Rust crate (unit-collector)"
    if: ${{ github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') }}
    needs: [plan, policy]
    runs-on: [self-hosted, velnor]
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up cargo-nextest
        uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf
        with:
          tool: nextest
          fallback: none
      - name: Set up sccache
        uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba
        with:
          version: v0.16.0
      - name: Set up mold
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444
        with:
          make-default: true
      - name: Restore "Rust crate (unit-collector)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-unit-collector-${{ hashFiles('tools/unit-collector/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-unit-collector-
      - name: Run "Rust crate (unit-collector)" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: rust-unit-collector
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"

  velnor-rust-velnor-action-journal:
    name: "Velnor / Rust crate (velnor-action-journal)"
    if: ${{ github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') }}
    needs: [plan, policy, velnor-rust-velnor-action-model, velnor-rust-velnor-model]
    runs-on: [self-hosted, velnor]
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up cargo-nextest
        uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf
        with:
          tool: nextest
          fallback: none
      - name: Set up sccache
        uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba
        with:
          version: v0.16.0
      - name: Set up mold
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444
        with:
          make-default: true
      - name: Restore "Rust crate (velnor-action-journal)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-action-journal-${{ hashFiles('crates/velnor-action-journal/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-velnor-action-journal-
      - name: Run "Rust crate (velnor-action-journal)" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: rust-velnor-action-journal
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"

  velnor-rust-velnor-action-model:
    name: "Velnor / Rust crate (velnor-action-model)"
    if: ${{ github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') }}
    needs: [plan, policy]
    runs-on: [self-hosted, velnor]
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up cargo-nextest
        uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf
        with:
          tool: nextest
          fallback: none
      - name: Set up sccache
        uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba
        with:
          version: v0.16.0
      - name: Set up mold
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444
        with:
          make-default: true
      - name: Restore "Rust crate (velnor-action-model)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-action-model-${{ hashFiles('crates/velnor-action-model/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-velnor-action-model-
      - name: Run "Rust crate (velnor-action-model)" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: rust-velnor-action-model
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"

  velnor-rust-velnor-cache-service:
    name: "Velnor / Rust crate (velnor-cache-service)"
    if: ${{ github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') }}
    needs: [plan, policy, velnor-rust-velnor-action-journal, velnor-rust-velnor-action-model, velnor-rust-velnor-cas]
    runs-on: [self-hosted, velnor]
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up cargo-nextest
        uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf
        with:
          tool: nextest
          fallback: none
      - name: Set up sccache
        uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba
        with:
          version: v0.16.0
      - name: Set up mold
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444
        with:
          make-default: true
      - name: Restore "Rust crate (velnor-cache-service)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-cache-service-${{ hashFiles('crates/velnor-cache-service/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-velnor-cache-service-
      - name: Run "Rust crate (velnor-cache-service)" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: rust-velnor-cache-service
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"

  velnor-rust-velnor-cas:
    name: "Velnor / Rust crate (velnor-cas)"
    if: ${{ github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') }}
    needs: [plan, policy, velnor-rust-velnor-action-model]
    runs-on: [self-hosted, velnor]
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up cargo-nextest
        uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf
        with:
          tool: nextest
          fallback: none
      - name: Set up sccache
        uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba
        with:
          version: v0.16.0
      - name: Set up mold
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444
        with:
          make-default: true
      - name: Restore "Rust crate (velnor-cas)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-cas-${{ hashFiles('crates/velnor-cas/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-velnor-cas-
      - name: Run "Rust crate (velnor-cas)" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: rust-velnor-cas
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"

  velnor-rust-velnor-client:
    name: "Velnor / Rust crate (velnor-client)"
    if: ${{ github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') }}
    needs: [plan, policy, velnor-rust-velnor-model]
    runs-on: [self-hosted, velnor]
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up cargo-nextest
        uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf
        with:
          tool: nextest
          fallback: none
      - name: Set up sccache
        uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba
        with:
          version: v0.16.0
      - name: Set up mold
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444
        with:
          make-default: true
      - name: Restore "Rust crate (velnor-client)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-client-${{ hashFiles('crates/velnor-client/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-velnor-client-
      - name: Run "Rust crate (velnor-client)" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: rust-velnor-client
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"

  velnor-rust-velnor-control:
    name: "Velnor / Rust crate (velnor-control)"
    if: ${{ github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') }}
    needs: [plan, policy, velnor-rust-velnor-action-journal, velnor-rust-velnor-model]
    runs-on: [self-hosted, velnor]
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up cargo-nextest
        uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf
        with:
          tool: nextest
          fallback: none
      - name: Set up sccache
        uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba
        with:
          version: v0.16.0
      - name: Set up mold
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444
        with:
          make-default: true
      - name: Restore "Rust crate (velnor-control)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-control-${{ hashFiles('crates/velnor-control/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-velnor-control-
      - name: Run "Rust crate (velnor-control)" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: rust-velnor-control
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"

  velnor-rust-velnor-model:
    name: "Velnor / Rust crate (velnor-model)"
    if: ${{ github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') }}
    needs: [plan, policy]
    runs-on: [self-hosted, velnor]
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up cargo-nextest
        uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf
        with:
          tool: nextest
          fallback: none
      - name: Set up sccache
        uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba
        with:
          version: v0.16.0
      - name: Set up mold
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444
        with:
          make-default: true
      - name: Restore "Rust crate (velnor-model)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-model-${{ hashFiles('crates/velnor-model/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-velnor-model-
      - name: Run "Rust crate (velnor-model)" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: rust-velnor-model
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"

  velnor-rust-velnor-render:
    name: "Velnor / Rust crate (velnor-render)"
    if: ${{ github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') }}
    needs: [plan, policy, velnor-rust-velnor-model]
    runs-on: [self-hosted, velnor]
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up cargo-nextest
        uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf
        with:
          tool: nextest
          fallback: none
      - name: Set up sccache
        uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba
        with:
          version: v0.16.0
      - name: Set up mold
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444
        with:
          make-default: true
      - name: Restore "Rust crate (velnor-render)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-render-${{ hashFiles('crates/velnor-render/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-velnor-render-
      - name: Run "Rust crate (velnor-render)" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: rust-velnor-render
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"

  velnor-rust-velnor-runner:
    name: "Velnor / Rust crate (velnor-runner)"
    if: ${{ github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') }}
    needs: [plan, policy, velnor-rust-velnor-action-model, velnor-rust-velnor-cache-service, velnor-rust-velnor-control, velnor-rust-velnor-model]
    runs-on: [self-hosted, velnor]
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up cargo-nextest
        uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf
        with:
          tool: nextest
          fallback: none
      - name: Set up sccache
        uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba
        with:
          version: v0.16.0
      - name: Set up mold
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444
        with:
          make-default: true
      - name: Restore "Rust crate (velnor-runner)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-runner-${{ hashFiles('crates/velnor-runner/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-velnor-runner-
      - name: Run "Rust crate (velnor-runner)" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: rust-velnor-runner
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"

  velnor-rust-velnor-tools:
    name: "Velnor / Rust crate (velnor-tools)"
    if: ${{ github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') }}
    needs: [plan, policy]
    runs-on: [self-hosted, velnor]
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up cargo-nextest
        uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf
        with:
          tool: nextest
          fallback: none
      - name: Set up sccache
        uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba
        with:
          version: v0.16.0
      - name: Set up mold
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444
        with:
          make-default: true
      - name: Restore "Rust crate (velnor-tools)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnor-tools-${{ hashFiles('crates/velnor-tools/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-velnor-tools-
      - name: Run "Rust crate (velnor-tools)" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: rust-velnor-tools
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"

  velnor-rust-velnorctl:
    name: "Velnor / Rust crate (velnorctl)"
    if: ${{ github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') }}
    needs: [plan, policy, velnor-rust-velnor-client, velnor-rust-velnor-control, velnor-rust-velnor-model, velnor-rust-velnor-render, velnor-rust-velnor-runner]
    runs-on: [self-hosted, velnor]
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Set up cargo-nextest
        uses: taiki-e/install-action@1ed6d7be6168f6c9046541087ff549b6bc581fdf
        with:
          tool: nextest
          fallback: none
      - name: Set up sccache
        uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba
        with:
          version: v0.16.0
      - name: Set up mold
        uses: rui314/setup-mold@9c9c13bf4c3f1adef0cc596abc155580bcb04444
        with:
          make-default: true
      - name: Restore "Rust crate (velnorctl)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-velnorctl-${{ hashFiles('crates/velnorctl/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-velnorctl-
      - name: Run "Rust crate (velnorctl)" checks
        env:
          CI_SCOPE: ${{ needs.plan.outputs.scope }}
          CI_UNIT_ID: rust-velnorctl
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh run --scope "$CI_SCOPE"

  required:
    name: main
    if: ${{ always() && needs['github-bun-traced-node-modules'].result == 'success' && needs['github-bun-velnor'].result == 'success' && needs['github-docker'].result == 'success' && needs['github-docs'].result == 'success' && needs['github-rust-policy'].result == 'success' && needs['github-rust-unit-collector'].result == 'success' && needs['github-rust-velnor-action-journal'].result == 'success' && needs['github-rust-velnor-action-model'].result == 'success' && needs['github-rust-velnor-cache-service'].result == 'success' && needs['github-rust-velnor-cas'].result == 'success' && needs['github-rust-velnor-client'].result == 'success' && needs['github-rust-velnor-control'].result == 'success' && needs['github-rust-velnor-model'].result == 'success' && needs['github-rust-velnor-render'].result == 'success' && needs['github-rust-velnor-runner'].result == 'success' && needs['github-rust-velnor-tools'].result == 'success' && needs['github-rust-velnorctl'].result == 'success' && (needs['velnor-bun-traced-node-modules'].result == 'success' || needs['velnor-bun-traced-node-modules'].result == 'skipped') && (needs['velnor-bun-velnor'].result == 'success' || needs['velnor-bun-velnor'].result == 'skipped') && (needs['velnor-docker'].result == 'success' || needs['velnor-docker'].result == 'skipped') && (needs['velnor-docs'].result == 'success' || needs['velnor-docs'].result == 'skipped') && (needs['velnor-rust-policy'].result == 'success' || needs['velnor-rust-policy'].result == 'skipped') && (needs['velnor-rust-unit-collector'].result == 'success' || needs['velnor-rust-unit-collector'].result == 'skipped') && (needs['velnor-rust-velnor-action-journal'].result == 'success' || needs['velnor-rust-velnor-action-journal'].result == 'skipped') && (needs['velnor-rust-velnor-action-model'].result == 'success' || needs['velnor-rust-velnor-action-model'].result == 'skipped') && (needs['velnor-rust-velnor-cache-service'].result == 'success' || needs['velnor-rust-velnor-cache-service'].result == 'skipped') && (needs['velnor-rust-velnor-cas'].result == 'success' || needs['velnor-rust-velnor-cas'].result == 'skipped') && (needs['velnor-rust-velnor-client'].result == 'success' || needs['velnor-rust-velnor-client'].result == 'skipped') && (needs['velnor-rust-velnor-control'].result == 'success' || needs['velnor-rust-velnor-control'].result == 'skipped') && (needs['velnor-rust-velnor-model'].result == 'success' || needs['velnor-rust-velnor-model'].result == 'skipped') && (needs['velnor-rust-velnor-render'].result == 'success' || needs['velnor-rust-velnor-render'].result == 'skipped') && (needs['velnor-rust-velnor-runner'].result == 'success' || needs['velnor-rust-velnor-runner'].result == 'skipped') && (needs['velnor-rust-velnor-tools'].result == 'success' || needs['velnor-rust-velnor-tools'].result == 'skipped') && (needs['velnor-rust-velnorctl'].result == 'success' || needs['velnor-rust-velnorctl'].result == 'skipped') }}
    needs: [plan, policy, github-bun-traced-node-modules, github-bun-velnor, github-docker, github-docs, github-rust-policy, github-rust-unit-collector, github-rust-velnor-action-journal, github-rust-velnor-action-model, github-rust-velnor-cache-service, github-rust-velnor-cas, github-rust-velnor-client, github-rust-velnor-control, github-rust-velnor-model, github-rust-velnor-render, github-rust-velnor-runner, github-rust-velnor-tools, github-rust-velnorctl, velnor-bun-traced-node-modules, velnor-bun-velnor, velnor-docker, velnor-docs, velnor-rust-policy, velnor-rust-unit-collector, velnor-rust-velnor-action-journal, velnor-rust-velnor-action-model, velnor-rust-velnor-cache-service, velnor-rust-velnor-cas, velnor-rust-velnor-client, velnor-rust-velnor-control, velnor-rust-velnor-model, velnor-rust-velnor-render, velnor-rust-velnor-runner, velnor-rust-velnor-tools, velnor-rust-velnorctl]
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: All unit jobs passed
        run: echo 'all generated CI units passed'
