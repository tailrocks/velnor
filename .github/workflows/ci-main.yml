# Generated by github-actions-generator. Regenerate; do not hand-edit.
name: CI
run-name: CI / main · ${{ github.event_name }} · ${{ github.ref_name }}

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  plan:
    name: Planning
    runs-on: ubuntu-24.04
    outputs:
      scope: ${{ steps.plan.outputs.scope }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Select affected units
        id: plan
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh plan

  policy:
    name: Policy
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout workflow data
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Checkout trusted policy
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        continue-on-error: true
        with:
          ref: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.sha }}
          path: .github-policy
          persist-credentials: false
      - name: Enforce workflow policy
        env:
          EVENT_NAME: ${{ github.event_name }}
          WORKFLOW_ROOT: ${{ github.workspace }}
        run: |
          set -euo pipefail
          policy=.github-policy/.github/ci/policy.sh
          if [[ ! -f "$policy" ]]; then
            policy=.github/ci/policy.sh
          fi
          bash "$policy"
  group-unit-bun-velnor:
    name: "Bun / Packages / Bun package (velnor)"
    needs: [plan, policy]
    uses: ./.github/workflows/ci-bun-velnor.yml
    with:
      scope: ${{ needs.plan.outputs.scope }}
  group-unit-docker:
    name: "Docker / Images / Docker"
    needs: [plan, policy]
    uses: ./.github/workflows/ci-docker-docker.yml
    with:
      scope: ${{ needs.plan.outputs.scope }}
  group-unit-docs:
    name: "Documentation / Documentation"
    needs: [plan, policy]
    uses: ./.github/workflows/ci-docs-docs.yml
    with:
      scope: ${{ needs.plan.outputs.scope }}
  group-unit-rust-policy:
    name: "Rust dependency policy"
    needs: [plan, policy]
    uses: ./.github/workflows/ci-rust-policy.yml
    with:
      scope: ${{ needs.plan.outputs.scope }}
  group-unit-rust-unit-collector:
    name: unit-collector
    needs: [plan, policy]
    uses: ./.github/workflows/ci-rust-unit-collector.yml
    with:
      scope: ${{ needs.plan.outputs.scope }}
  group-unit-rust-velnor-action-journal:
    name: velnor-action-journal
    needs: [plan, policy, group-unit-rust-velnor-action-model, group-unit-rust-velnor-model]
    uses: ./.github/workflows/ci-rust-velnor-action-journal.yml
    with:
      scope: ${{ needs.plan.outputs.scope }}
  group-unit-rust-velnor-action-model:
    name: velnor-action-model
    needs: [plan, policy]
    uses: ./.github/workflows/ci-rust-velnor-action-model.yml
    with:
      scope: ${{ needs.plan.outputs.scope }}
  group-unit-rust-velnor-cache-service:
    name: velnor-cache-service
    needs: [plan, policy, group-unit-rust-velnor-action-journal, group-unit-rust-velnor-action-model, group-unit-rust-velnor-cas]
    uses: ./.github/workflows/ci-rust-velnor-cache-service.yml
    with:
      scope: ${{ needs.plan.outputs.scope }}
  group-unit-rust-velnor-cas:
    name: velnor-cas
    needs: [plan, policy, group-unit-rust-velnor-action-model]
    uses: ./.github/workflows/ci-rust-velnor-cas.yml
    with:
      scope: ${{ needs.plan.outputs.scope }}
  group-unit-rust-velnor-client:
    name: velnor-client
    needs: [plan, policy, group-unit-rust-velnor-model]
    uses: ./.github/workflows/ci-rust-velnor-client.yml
    with:
      scope: ${{ needs.plan.outputs.scope }}
  group-unit-rust-velnor-control:
    name: velnor-control
    needs: [plan, policy, group-unit-rust-velnor-action-journal, group-unit-rust-velnor-model]
    uses: ./.github/workflows/ci-rust-velnor-control.yml
    with:
      scope: ${{ needs.plan.outputs.scope }}
  group-unit-rust-velnor-model:
    name: velnor-model
    needs: [plan, policy]
    uses: ./.github/workflows/ci-rust-velnor-model.yml
    with:
      scope: ${{ needs.plan.outputs.scope }}
  group-unit-rust-velnor-render:
    name: velnor-render
    needs: [plan, policy, group-unit-rust-velnor-model]
    uses: ./.github/workflows/ci-rust-velnor-render.yml
    with:
      scope: ${{ needs.plan.outputs.scope }}
  group-unit-rust-velnor-runner:
    name: velnor-runner
    needs: [plan, policy, group-unit-rust-velnor-action-model, group-unit-rust-velnor-cache-service, group-unit-rust-velnor-control, group-unit-rust-velnor-model]
    uses: ./.github/workflows/ci-rust-velnor-runner.yml
    with:
      scope: ${{ needs.plan.outputs.scope }}
  group-unit-rust-velnor-tools:
    name: velnor-tools
    needs: [plan, policy]
    uses: ./.github/workflows/ci-rust-velnor-tools.yml
    with:
      scope: ${{ needs.plan.outputs.scope }}
  group-unit-rust-velnorctl:
    name: velnorctl
    needs: [plan, policy, group-unit-rust-velnor-client, group-unit-rust-velnor-control, group-unit-rust-velnor-model, group-unit-rust-velnor-render, group-unit-rust-velnor-runner]
    uses: ./.github/workflows/ci-rust-velnorctl.yml
    with:
      scope: ${{ needs.plan.outputs.scope }}
  required:
    name: main
    if: ${{ always() && needs['plan'].result == 'success' && needs['policy'].result == 'success' && needs['group-unit-bun-velnor'].result == 'success' && needs['group-unit-docker'].result == 'success' && needs['group-unit-docs'].result == 'success' && needs['group-unit-rust-policy'].result == 'success' && needs['group-unit-rust-unit-collector'].result == 'success' && needs['group-unit-rust-velnor-action-journal'].result == 'success' && needs['group-unit-rust-velnor-action-model'].result == 'success' && needs['group-unit-rust-velnor-cache-service'].result == 'success' && needs['group-unit-rust-velnor-cas'].result == 'success' && needs['group-unit-rust-velnor-client'].result == 'success' && needs['group-unit-rust-velnor-control'].result == 'success' && needs['group-unit-rust-velnor-model'].result == 'success' && needs['group-unit-rust-velnor-render'].result == 'success' && needs['group-unit-rust-velnor-runner'].result == 'success' && needs['group-unit-rust-velnor-tools'].result == 'success' && needs['group-unit-rust-velnorctl'].result == 'success' }}
    needs: [plan, policy, group-unit-bun-velnor, group-unit-docker, group-unit-docs, group-unit-rust-policy, group-unit-rust-unit-collector, group-unit-rust-velnor-action-journal, group-unit-rust-velnor-action-model, group-unit-rust-velnor-cache-service, group-unit-rust-velnor-cas, group-unit-rust-velnor-client, group-unit-rust-velnor-control, group-unit-rust-velnor-model, group-unit-rust-velnor-render, group-unit-rust-velnor-runner, group-unit-rust-velnor-tools, group-unit-rust-velnorctl]
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: All generated stack workflows passed
        run: echo 'all generated CI stacks passed'
