# Generated by velnor-workflow. Regenerate; do not hand-edit.
name: Rust
on:
  workflow_call:
    inputs:
      unit:
        required: true
        type: string
      selected_units:
        required: true
        type: string
      scope:
        required: true
        type: string
      full_units:
        required: true
        type: string
      base_sha:
        required: true
        type: string
      head_sha:
        required: true
        type: string
      lane:
        required: true
        type: string
      mise_tools:
        required: false
        type: string
        default: ""
      mise_runner:
        required: false
        type: boolean
        default: false
      mbx_compat:
        required: false
        type: string
        default: ""
      mbx_dependency_files:
        required: false
        type: string
        default: ""
      mbx_freshness_files:
        required: false
        type: string
        default: ""
      cargo_bin_tools:
        required: false
        type: string
        default: ""
      tool_version:
        required: false
        type: string
        default: ""
      node_cache_dependency_path:
        required: false
        type: string
        default: ""
      cache_paths:
        required: false
        type: string
        default: ""
      cache_key_files:
        required: false
        type: string
        default: ""
      seed_compat:
        required: false
        type: string
        default: ""
      seed_dependency_files:
        required: false
        type: string
        default: ""
      seed_freshness_files:
        required: false
        type: string
        default: ""
      cargo_root:
        required: false
        type: string
        default: ""
      cargo_fetch_skip_when_warm:
        required: false
        type: boolean
        default: false
      cargo_net_offline:
        required: false
        type: boolean
        default: false
      host_warm_layers:
        required: false
        type: string
        default: ""
      policy_runtime:
        required: false
        type: boolean
        default: false
      candidate_publish:
        required: false
        type: boolean
        default: false
      apple_executor:
        required: false
        type: boolean
        default: false
      unit_dependencies:
        required: false
        type: string
        default: ""
      unit_admission:
        required: false
        type: string
        default: ""

jobs:
  velnor-prepare-cargo-sources:
    name: prepare-cargo
    if: ${{ inputs.lane == 'control' && (contains(format(',{0},', inputs.selected_units), ',rust-unit-collector,') || contains(format(',{0},', inputs.selected_units), ',rust-velnor-bench,') || contains(format(',{0},', inputs.selected_units), ',rust-velnor-client,') || contains(format(',{0},', inputs.selected_units), ',rust-velnor-control,') || contains(format(',{0},', inputs.selected_units), ',rust-velnor-model,') || contains(format(',{0},', inputs.selected_units), ',rust-velnor-render,') || contains(format(',{0},', inputs.selected_units), ',rust-velnor-runner,') || contains(format(',{0},', inputs.selected_units), ',rust-velnor-tools,') || contains(format(',{0},', inputs.selected_units), ',rust-velnor-workflow,') || contains(format(',{0},', inputs.selected_units), ',rust-velnor-workflow-contract,') || contains(format(',{0},', inputs.selected_units), ',rust-velnorctl,') || contains(format(',{0},', inputs.selected_units), ',rust-production-topology,')) && (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'velnor' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          ref: ${{ inputs.head_sha }}
      - name: Materialize Velnor CI selection
        shell: bash
        env:
          SELECTION_BASE_SHA: ${{ inputs.base_sha }}
          SELECTION_HEAD_SHA: ${{ inputs.head_sha }}
          SELECTION_SCOPE: ${{ inputs.scope }}
          SELECTION_UNITS: ${{ inputs.selected_units }}
          SELECTION_FULL_UNITS: ${{ inputs.full_units }}
        run: |
          set -euo pipefail
          mkdir -p .velnor-ci-selection
          {
            printf 'version=1\n'
            printf 'base_sha=%s\n' "$SELECTION_BASE_SHA"
            printf 'head_sha=%s\n' "$SELECTION_HEAD_SHA"
            printf 'scope=%s\n' "$SELECTION_SCOPE"
            printf 'units=%s\n' "$SELECTION_UNITS"
            printf 'full_units=%s\n' "$SELECTION_FULL_UNITS"
          } > .velnor-ci-selection/velnor-ci-selection
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          if cargo metadata --locked --offline --all-features --format-version 1 >/dev/null 2>&1; then
            echo "Cargo sources warm; skipping fetch"
          else
            cargo fetch --locked
          fi
          cd -- 'crates/velnor-workflow-contract'
          if cargo metadata --locked --offline --all-features --format-version 1 >/dev/null 2>&1; then
            echo "Cargo sources warm; skipping fetch"
          else
            cargo fetch --locked
          fi
          cd -- "$GITHUB_WORKSPACE"
  verify-github:
    name: GitHub
    if: ${{ inputs.lane == 'github' && contains(format(',{0},', inputs.selected_units), format(',{0},', inputs.unit)) && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Mark CI job start
        if: always()
        run: |
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/JOB_STARTED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          ref: ${{ inputs.head_sha }}
      - name: Record unit dependencies
        env:
          UNIT_ID: ${{ inputs.unit }}
          UNIT_DEPENDENCIES: ${{ inputs.unit_dependencies }}
          UNIT_ADMISSION: ${{ inputs.unit_admission }}
          UNIT_LANE: ${{ inputs.lane }}
        run: |
          {
            echo '## Unit dependencies'
            echo
            echo "- Unit: $UNIT_ID"
            echo "- Lane: $UNIT_LANE"
            echo "- Admission: $UNIT_ADMISSION"
            if [[ -z "$UNIT_DEPENDENCIES" ]]; then
              echo '- Dependencies: none'
            else
              echo "- Dependencies: $UNIT_DEPENDENCIES"
            fi
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-a6fa8d4a5096e6df37250b59a8105abfebdb9013-${{ runner.os }}-${{ runner.arch }}
          path: .velnor-workflow-runtime
      - name: Verify Velnor workflow runtime
        shell: bash
        env:
          EXPECTED_REVISION: a6fa8d4a5096e6df37250b59a8105abfebdb9013
        run: |
          set -euo pipefail
          manifest=.velnor-workflow-runtime/manifest.json
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg run_id "$GITHUB_RUN_ID" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and (.run_id | test("^[0-9]+$")) and .job_id != "" and (.binary_sha256 | test("^[0-9a-f]{64}$")) and (.policy_binary_sha256 | test("^[0-9a-f]{64}$")) and (.closure | test("^[0-9a-f]{64}$")) and (.policy_closure | test("^[0-9a-f]{64}$"))' "$manifest" >/dev/null
          expected="$(jq -er '.binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch" >&2; exit 1; }
          expected="$(jq -er '.policy_binary_sha256' "$manifest")"
          actual="$(sha256sum .velnor-workflow-runtime/velnor-workflow-policy | awk '{print $1}')"
          [[ "$actual" == "$expected" ]] || { echo "::error::policy runtime digest mismatch" >&2; exit 1; }
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        env:
          EXPECTED_REVISION: a6fa8d4a5096e6df37250b59a8105abfebdb9013
        run: |
          set -euo pipefail
          home="$RUNNER_TEMP/velnor-workflow-runtime-artifact"
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$home/bin/velnor-workflow"
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow-policy "$home/bin/velnor-workflow-policy"
          expected_closure="$(jq -er '.policy_closure' .velnor-workflow-runtime/manifest.json)"
          reported="$("$home/bin/velnor-workflow-policy" --closure)"
          [[ "$reported" == "$expected_closure" ]] || { echo "::error::policy runtime reports closure $reported, expected $expected_closure" >&2; exit 1; }
          echo "$home/bin" >> "$GITHUB_PATH"
          echo "VELNOR_WORKFLOW_PINNED_BINARY=$home/bin/velnor-workflow-policy" >> "$GITHUB_ENV"
      - name: Mark runner setup end
        if: always()
        run: |
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/RUNNER_SETUP_ENDED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
      - name: Materialize Velnor CI selection
        shell: bash
        env:
          SELECTION_BASE_SHA: ${{ inputs.base_sha }}
          SELECTION_HEAD_SHA: ${{ inputs.head_sha }}
          SELECTION_SCOPE: ${{ inputs.scope }}
          SELECTION_UNITS: ${{ inputs.selected_units }}
          SELECTION_FULL_UNITS: ${{ inputs.full_units }}
        run: |
          set -euo pipefail
          mkdir -p .velnor-ci-selection
          {
            printf 'version=1\n'
            printf 'base_sha=%s\n' "$SELECTION_BASE_SHA"
            printf 'head_sha=%s\n' "$SELECTION_HEAD_SHA"
            printf 'scope=%s\n' "$SELECTION_SCOPE"
            printf 'units=%s\n' "$SELECTION_UNITS"
            printf 'full_units=%s\n' "$SELECTION_FULL_UNITS"
          } > .velnor-ci-selection/velnor-ci-selection
      - name: Mark selection transport end
        if: always()
        run: |
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/SELECTION_ENDED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
      - name: Restore Rust toolchain
        id: rustup-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Provision Rust toolchain
        shell: bash
        run: |
          set -euo pipefail
          rustup toolchain install
          rustup target add 'aarch64-apple-darwin' 'aarch64-unknown-linux-gnu' 'x86_64-apple-darwin' 'x86_64-unknown-linux-gnu' 'x86_64-unknown-linux-musl'
      - name: Save Rust toolchain
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup
          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
      - name: Set up Mise tools
        if: ${{ inputs.mise_tools != '' }}
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install_args: ${{ inputs.mise_tools }}
          cache: true
          cache_save: ${{ (github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main') }}
      - name: Set up Mise
        if: ${{ inputs.mise_runner }}
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install: false
      - name: Bound the Mr. Boxington store
        shell: bash
        run: echo "MBX_GC_MAX_SIZE=12GiB" >> "$GITHUB_ENV"
      - name: Set up Mr. Boxington
        id: mbx-cache
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.11.1
          cache-key: velnor-mbx-v3-${{ inputs.mbx_compat }}-${{ runner.os }}-${{ runner.arch }}-${{ inputs.unit }}-${{ hashFiles(inputs.mbx_dependency_files) }}-${{ hashFiles(inputs.mbx_freshness_files) }}
          restore-keys: |
            velnor-mbx-v3-${{ inputs.mbx_compat }}-${{ runner.os }}-${{ runner.arch }}-${{ inputs.unit }}-${{ hashFiles(inputs.mbx_dependency_files) }}-
            velnor-mbx-v3-${{ inputs.mbx_compat }}-${{ runner.os }}-${{ runner.arch }}-${{ inputs.unit }}-
          save-on-workflow-dispatch: true
      - name: Restore cargo bin toolchain
        if: ${{ inputs.cargo_bin_tools != '' }}
        id: cargo-bin-toolchain
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cargo/bin
          key: velnor-cargo-bin-v2-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('mise.lock', 'mise.toml') }}
      - name: Verify cargo bin tools
        if: ${{ inputs.cargo_bin_tools != '' && (steps.cargo-bin-toolchain.outputs.cache-hit == 'true') }}
        id: cargo-bin-verify
        shell: bash
        env:
          CARGO_BIN_TOOLS: ${{ inputs.cargo_bin_tools }}
        run: |
          set -euo pipefail
          missing=0
          IFS=',' read -ra tools <<< "$CARGO_BIN_TOOLS"
          for tool in "${tools[@]}"; do
            tool="${tool#"${tool%%[![:space:]]*}"}"
            tool="${tool%"${tool##*[![:space:]]}"}"
            [[ -z "$tool" ]] && continue
            if ! command -v "$tool" >/dev/null; then
              missing=1
              break
            fi
            if ! "$tool" --version >/dev/null 2>&1; then
              missing=1
              break
            fi
            if [[ "$tool" == cargo-* ]]; then
              subcommand="${tool#cargo-}"
              if ! cargo "$subcommand" --version >/dev/null 2>&1; then
                missing=1
                break
              fi
            fi
          done
          echo "missing=$missing" >> "$GITHUB_OUTPUT"
      - name: Set up cargo bin tools
        if: ${{ inputs.cargo_bin_tools != '' && (steps.cargo-bin-toolchain.outputs.cache-hit != 'true' || steps.cargo-bin-verify.outputs.missing == 'true') }}
        uses: taiki-e/install-action@0758d235715de2f3551eacc980d9ae8fce9342c3 # v2.87.3
        with:
          tool: ${{ inputs.cargo_bin_tools }}
          fallback: none
      - name: Save cargo bin toolchain
        if: ${{ inputs.cargo_bin_tools != '' && (((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && (steps.cargo-bin-toolchain.outputs.cache-hit != 'true' || steps.cargo-bin-verify.outputs.missing == 'true')) }}
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cargo/bin
          key: velnor-cargo-bin-v2-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('mise.lock', 'mise.toml') }}
      - name: Restore mold 2.42.0 cache
        id: mold-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Set up mold 2.42.0
        shell: bash
        env:
          MOLD_VERSION: 2.42.0
        run: |
          set -euo pipefail
          case "$(uname -m)" in
            x86_64) mold_arch=x86_64; expected='f5ed2f6e31d1ada4f07fe766fe0de7a73104d1c5cdc59086fcecc16a43720b6d' ;;
            aarch64) mold_arch=aarch64; expected='3c9a0a3624aac8a2007569ae50c33b3129a0f0ae8bcc974aeee2f8939d295190' ;;
            *) echo "unsupported mold architecture: $(uname -m)" >&2; exit 1 ;;
          esac
          cache_dir="$HOME/.cache/velnor/mold/$MOLD_VERSION"
          archive="$cache_dir/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
          mkdir -p "$cache_dir"
          if [[ ! -s "$archive" ]]; then
            temporary="$archive.download"
            trap 'rm -f "$temporary"' EXIT
            curl --fail --show-error --silent --location --http1.1 --continue-at - --retry 20 --retry-all-errors --retry-delay 5 --retry-max-time 1800 --connect-timeout 30 --max-time 900 \
              --output "$temporary" \
              "https://github.com/rui314/mold/releases/download/v$MOLD_VERSION/mold-$MOLD_VERSION-$mold_arch-linux.tar.gz"
            printf '%s  %s\n' "$expected" "$temporary" | sha256sum --check --strict
            mv "$temporary" "$archive"
            trap - EXIT
          fi
          printf '%s  %s\n' "$expected" "$archive" | sha256sum --check --strict
          if [[ "$(id -u)" -eq 0 ]]; then
            tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
          else
            sudo tar --directory /usr/local --strip-components=1 --no-overwrite-dir -xzf "$archive"
          fi
          command -v mold >/dev/null
          mold --version | grep -F "$MOLD_VERSION"
      - name: Save mold 2.42.0 cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.mold-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ~/.cache/velnor/mold/2.42.0
          key: velnor-mold-2.42.0-${{ runner.os }}-${{ runner.arch }}
      - name: Mark tool bootstrap end
        if: always()
        run: |
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/TOOL_BOOTSTRAP_ENDED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
      - name: Restore unit cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ${{ inputs.cache_paths }}
          key: ci-${{ runner.os }}-${{ runner.arch }}-rust-${{ hashFiles(inputs.cache_key_files) }}
          restore-keys: |
            ci-${{ runner.os }}-${{ runner.arch }}-rust-
      - name: Mark cache prep end
        if: always()
        run: |
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/CACHE_PREP_ENDED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
      - name: Prepare Cargo sources
        if: ${{ inputs.cargo_root != '' && steps.cache.outputs.cache-hit != 'true' }}
        env:
          CARGO_FETCH_ROOT: ${{ inputs.cargo_root }}
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          root="$CARGO_FETCH_ROOT"
          if [[ "$root" != "." ]]; then
            cd -- "$root"
          fi
          cargo fetch --locked
      - name: Mark cargo fetch end
        if: always()
        run: |
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/CARGO_FETCH_ENDED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
      - name: Fetch D19 pin history
        env:
          CI_UNIT_ID: ${{ inputs.unit }}
        run: |
          set -euo pipefail
          case "$CI_UNIT_ID" in
            'rust-policy') exit 0 ;;
            'rust-unit-collector') exit 0 ;;
            'rust-velnor-bench') exit 0 ;;
            'rust-velnor-client') exit 0 ;;
            'rust-velnor-control') exit 0 ;;
            'rust-velnor-model') exit 0 ;;
            'rust-velnor-render') exit 0 ;;
            'rust-velnor-runner') exit 0 ;;
            'rust-velnor-tools') exit 0 ;;
            'rust-velnor-workflow') : ;;
            'rust-velnor-workflow-contract') exit 0 ;;
            'rust-velnorctl') exit 0 ;;
            'rust-production-topology') exit 0 ;;
            *) echo "unknown unit for pin fetch: $CI_UNIT_ID" >&2; exit 1 ;;
          esac
          pin="$(sed -n -E 's/^[[:space:]]*revision[[:space:]]*=[[:space:]]*"([0-9a-f]{40})".*/\1/p' .github-gen/velnor-workflow.toml | head -n 1)"
          test "$pin" != '' || { echo "::error::D19 pin missing from .github-gen/velnor-workflow.toml" >&2; exit 1; }
          if ! git cat-file -e "$pin^{commit}" 2>/dev/null; then
            git fetch --no-tags --depth 1 "$GITHUB_SERVER_URL/$GITHUB_REPOSITORY" "$pin"
          fi
      - name: Run unit checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ inputs.base_sha }}
          HEAD_SHA: ${{ inputs.head_sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: ${{ inputs.cargo_net_offline }}
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/CHECKS_STARTED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/CHECKS_ENDED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
          exit $rc
      - name: Prepare candidate generator product
        if: ${{ inputs.candidate_publish && (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository) }}
        id: candidate
        env:
          CANDIDATE_MERGE_SHA: ${{ inputs.head_sha }}
          CANDIDATE_PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          CANDIDATE_BASE_SHA: ${{ inputs.base_sha }}
        run: |
          set -euo pipefail
          PR_HEAD="$CANDIDATE_PR_HEAD_SHA"
          BASE="$CANDIDATE_BASE_SHA"
          if ! git cat-file -e "$PR_HEAD^{commit}" 2>/dev/null; then
            git fetch --no-tags --depth 1 "$GITHUB_SERVER_URL/$GITHUB_REPOSITORY" "$PR_HEAD"
          fi
          head_closure="$(velnor-workflow closure --rev="$PR_HEAD" --candidate)"
          if ! git cat-file -e "$BASE^{commit}" 2>/dev/null; then
            git fetch --no-tags --depth 1 "$GITHUB_SERVER_URL/$GITHUB_REPOSITORY" "$BASE"
          fi
          base_pin="$(git show "$BASE:.github-gen/velnor-workflow.toml" 2>/dev/null | sed -n -E 's/^[[:space:]]*revision[[:space:]]*=[[:space:]]*"([0-9a-f]{40})".*/\1/p' | head -n 1)"
          test "$base_pin" != '' || base_pin="$(git show "$BASE:.github/workflows/ci-policy.yml" 2>/dev/null | sed -n -E 's/^.*VELNOR_WORKFLOW_POLICY_REVISION:[[:space:]]*([0-9a-f]{40}).*/\1/p' | head -n 1)"
          test "$base_pin" != '' || { echo "::error::base $BASE declares no generator pin" >&2; exit 1; }
          if ! git cat-file -e "$base_pin^{commit}" 2>/dev/null; then
            git fetch --no-tags --depth 1 "$GITHUB_SERVER_URL/$GITHUB_REPOSITORY" "$base_pin"
          fi
          base_closure="$(velnor-workflow closure --rev="$base_pin" --candidate)"
          if [[ "$head_closure" == "$base_closure" ]]; then
            echo "head $PR_HEAD shares the base pin's candidate closure; no candidate to publish"
            echo "skip=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          merge_closure="$(velnor-workflow closure --rev="$CANDIDATE_MERGE_SHA" --candidate)"
          worktree=""
          use_unit_binary=false
          if [[ "$merge_closure" == "$head_closure" ]]; then
            test -x target/debug/velnor-workflow || { echo "::error::candidate packaging needs the unit's own target/debug/velnor-workflow; the checks must build the generator binary" >&2; exit 1; }
            # The checks may build with different features than the candidate
            # profile stamps, so only reuse their binary when it already
            # reports the head closure; otherwise fall through to a clean
            # default-features build below.
            if [[ "$(target/debug/velnor-workflow --closure)" == "$head_closure" ]]; then
              use_unit_binary=true
            fi
          fi
          if [[ "$use_unit_binary" == true ]]; then
            binary="target/debug/velnor-workflow"
            build_rev="$CANDIDATE_MERGE_SHA"
          else
            worktree="$RUNNER_TEMP/velnor-workflow-head"
            rm -rf "$worktree"
            git worktree add --detach "$worktree" "$PR_HEAD"
            trap 'git worktree remove --force "$worktree"' EXIT
            cargo build --locked -p velnor-workflow --manifest-path "$worktree/crates/velnor-workflow/Cargo.toml"
            binary="$worktree/target/debug/velnor-workflow"
            build_rev="$PR_HEAD"
          fi
          stage="$RUNNER_TEMP/velnor-workflow-candidate"
          rm -rf "$stage"
          mkdir -p "$stage"
          install -m 0755 "$binary" "$stage/velnor-workflow"
          digest="$(sha256sum "$stage/velnor-workflow" | awk '{print $1}')"
          reported="$("$stage/velnor-workflow" --closure)"
          [[ "$reported" == "$head_closure" ]] || { echo "::error::candidate reports closure $reported, head $PR_HEAD declares $head_closure" >&2; exit 1; }
          jq -n --arg profile debug --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg repository "$GITHUB_REPOSITORY" --arg run_id "$GITHUB_RUN_ID" --arg revision "$PR_HEAD" --arg closure "$head_closure" --arg build_revision "$build_rev" --arg binary_sha256 "$digest" '{profile: $profile, platform: $platform, repository: $repository, run_id: $run_id, revision: $revision, closure: $closure, build_revision: $build_revision, binary_sha256: $binary_sha256}' > "$stage/candidate-manifest.json"
          if [[ "$worktree" != "" ]]; then
            git worktree remove --force "$worktree"
            trap - EXIT
          fi
          echo "name=velnor-workflow-candidate-${head_closure:0:16}-${RUNNER_OS}-${RUNNER_ARCH}" >> "$GITHUB_OUTPUT"
      - name: Publish candidate generator product
        if: ${{ inputs.candidate_publish && (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository && steps.candidate.outputs.skip != 'true') }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ${{ steps.candidate.outputs.name }}
          path: ${{ runner.temp }}/velnor-workflow-candidate
          if-no-files-found: error
          retention-days: 1
      - name: Save unit cache
        if: always() && ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ${{ inputs.cache_paths }}
          key: ci-${{ runner.os }}-${{ runner.arch }}-rust-${{ hashFiles(inputs.cache_key_files) }}
      - name: Mark cleanup end
        if: always()
        run: |
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/CLEANUP_ENDED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
      - name: Report phase timings and cache outcomes
        if: always()
        uses: ./.github/actions/report-velnor-ci-outcomes
        with:
          job_label: ${{ inputs.unit }}
          ci_lane: github
          cache_rustup_primary: ${{ steps.rustup-toolchain.outputs.cache-primary-key }}
          cache_rustup_matched: ${{ steps.rustup-toolchain.outputs.cache-matched-key }}
          cache_mold_primary: ${{ steps.mold-cache.outputs.cache-primary-key }}
          cache_mold_matched: ${{ steps.mold-cache.outputs.cache-matched-key }}
          cache_mbx_hit: ${{ steps.mbx-cache.outputs.cache-hit }}
          cache_mbx_primary: ${{ steps.mbx-cache.outputs.cache-primary-key }}
          cache_cargo_primary: ${{ steps.cache.outputs.cache-primary-key }}
          cache_cargo_matched: ${{ steps.cache.outputs.cache-matched-key }}

  verify-velnor:
    name: Velnor
    if: ${{ inputs.lane == 'velnor' && contains(format(',{0},', inputs.selected_units), format(',{0},', inputs.unit)) && (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'velnor' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    timeout-minutes: 45
    steps:
      - name: Mark CI job start
        if: always()
        run: |
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/JOB_STARTED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          ref: ${{ inputs.head_sha }}
      - name: Record unit dependencies
        env:
          UNIT_ID: ${{ inputs.unit }}
          UNIT_DEPENDENCIES: ${{ inputs.unit_dependencies }}
          UNIT_ADMISSION: ${{ inputs.unit_admission }}
          UNIT_LANE: ${{ inputs.lane }}
        run: |
          {
            echo '## Unit dependencies'
            echo
            echo "- Unit: $UNIT_ID"
            echo "- Lane: $UNIT_LANE"
            echo "- Admission: $UNIT_ADMISSION"
            if [[ -z "$UNIT_DEPENDENCIES" ]]; then
              echo '- Dependencies: none'
            else
              echo "- Dependencies: $UNIT_DEPENDENCIES"
            fi
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Velnor runner identity
        shell: bash
        run: |
          set -euo pipefail
          {
            echo '## Velnor runner identity'
            echo
            echo "- Host: ${VELNOR_HOST:-unset}"
            echo "- Instance: ${VELNOR_INSTANCE:-unset}"
            echo "- Slot: ${VELNOR_SLOT:-unset}"
            echo "- GitHub runner: ${RUNNER_NAME:-unset}"
            echo "- OS/arch: ${RUNNER_OS:-unset}/${RUNNER_ARCH:-unset}"
            echo "- Execution backend: ${VELNOR_EXECUTION_BACKEND:-unset}"
            echo "- Velnor version: ${VELNOR_MANIFEST_VERSION:-${VELNOR_SOURCE_SHA:-unset}}"
          } | tee -a "${GITHUB_STEP_SUMMARY:-/dev/null}"
      - name: Mark runner setup end
        if: always()
        run: |
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/RUNNER_SETUP_ENDED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
      - name: Materialize Velnor CI selection
        shell: bash
        env:
          SELECTION_BASE_SHA: ${{ inputs.base_sha }}
          SELECTION_HEAD_SHA: ${{ inputs.head_sha }}
          SELECTION_SCOPE: ${{ inputs.scope }}
          SELECTION_UNITS: ${{ inputs.selected_units }}
          SELECTION_FULL_UNITS: ${{ inputs.full_units }}
        run: |
          set -euo pipefail
          mkdir -p .velnor-ci-selection
          {
            printf 'version=1\n'
            printf 'base_sha=%s\n' "$SELECTION_BASE_SHA"
            printf 'head_sha=%s\n' "$SELECTION_HEAD_SHA"
            printf 'scope=%s\n' "$SELECTION_SCOPE"
            printf 'units=%s\n' "$SELECTION_UNITS"
            printf 'full_units=%s\n' "$SELECTION_FULL_UNITS"
          } > .velnor-ci-selection/velnor-ci-selection
      - name: Mark selection transport end
        if: always()
        run: |
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/SELECTION_ENDED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
      - name: Provision pinned Velnor workflow policy runtime
        if: ${{ inputs.policy_runtime }}
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          PINNED_REVISION: a6fa8d4a5096e6df37250b59a8105abfebdb9013
          CHECKOUT_PATH: ${{ github.workspace }}
        run: |
          set -euo pipefail
          if ! git -C "$CHECKOUT_PATH" cat-file -e "$PINNED_REVISION^{commit}" 2>/dev/null; then
            git fetch --no-tags --depth 1 "$GITHUB_SERVER_URL/$GITHUB_REPOSITORY" "$PINNED_REVISION"
          fi
          listing="$(git -C "$CHECKOUT_PATH" ls-tree -r "$PINNED_REVISION" -- crates/velnor-workflow Cargo.toml Cargo.lock rust-toolchain.toml rust-toolchain .cargo)"
          test "$listing" != '' || { echo "::error::revision $PINNED_REVISION has no closure inputs" >&2; exit 1; }
          if command -v sha256sum >/dev/null 2>&1; then
            closure="$(printf '%s\nclosure-version:1\nfeatures:\nprofile:release\n' "$(LC_ALL=C sort <<<"$listing")" | sha256sum | awk '{print $1}')"
          else
            closure="$(printf '%s\nclosure-version:1\nfeatures:\nprofile:release\n' "$(LC_ALL=C sort <<<"$listing")" | shasum -a 256 | awk '{print $1}')"
          fi
          binary="${CARGO_HOME:-$HOME/.cargo}/bin/velnor-workflow-policy"
          tag="velnor-workflow-runtime-v1-${closure:0:16}"
          asset="velnor-workflow-${RUNNER_OS}-${RUNNER_ARCH}"
          temporary="$(mktemp -d)"
          trap 'rm -rf "$temporary"' EXIT
          if ! gh release download "$tag" --repo tailrocks/velnor --pattern manifest.json --dir "$temporary"; then
            echo "::error::no policy runtime product for revision $PINNED_REVISION (closure ${closure:0:16}); the mainline runtime-product publisher builds it after merge" >&2
            exit 1
          fi
          gh attestation verify "$temporary/manifest.json" --owner tailrocks --signer-workflow tailrocks/velnor/.github/workflows/ci-runtime-products.yml
          jq -e --arg closure "$closure" --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" --arg asset "$asset" '.closure == $closure and .profile == "release" and .features == "" and (.products[$platform].binary | test("^[0-9a-f]{64}$")) and .products[$platform].asset == $asset' "$temporary/manifest.json" >/dev/null
          expected="$(jq -er --arg platform "${RUNNER_OS}-${RUNNER_ARCH}" '.products[$platform].binary' "$temporary/manifest.json")"
          existing=""
          if [[ -x "$binary" ]]; then
            if command -v sha256sum >/dev/null 2>&1; then
              existing="$(sha256sum "$binary" | awk '{print $1}')"
            else
              existing="$(shasum -a 256 "$binary" | awk '{print $1}')"
            fi
          fi
          if [[ "$existing" != "$expected" ]]; then
            gh release download "$tag" --repo tailrocks/velnor --pattern "$asset" --dir "$temporary"
            gh attestation verify "$temporary/$asset" --owner tailrocks --signer-workflow tailrocks/velnor/.github/workflows/ci-runtime-products.yml
            if command -v sha256sum >/dev/null 2>&1; then
              actual="$(sha256sum "$temporary/$asset" | awk '{print $1}')"
            else
              actual="$(shasum -a 256 "$temporary/$asset" | awk '{print $1}')"
            fi
            [[ "$actual" == "$expected" ]] || { echo "::error::policy runtime digest mismatch" >&2; exit 1; }
            install -Dm0755 "$temporary/$asset" "$binary"
          fi
          reported="$("$binary" --closure)"
          [[ "$reported" == "$closure" ]] || { echo "::error::pinned workflow policy runtime reports closure $reported, expected $closure" >&2; exit 1; }
          echo "VELNOR_WORKFLOW_PINNED_BINARY=$binary" >> "$GITHUB_ENV"
      - name: Install declared Mise tools
        if: ${{ inputs.mise_tools != '' }}
        env:
          MISE_TOOLS: ${{ inputs.mise_tools }}
        run: |
          set -euo pipefail
          read -ra tools <<<"$MISE_TOOLS"
          mise --yes install "${tools[@]}"
      - name: Set up Mr. Boxington
        id: mbx-cache
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Mark tool bootstrap end
        if: always()
        run: |
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/TOOL_BOOTSTRAP_ENDED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
      - name: Mark cache prep end
        if: always()
        run: |
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/CACHE_PREP_ENDED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
      - name: Mark cargo fetch end
        if: always()
        run: |
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/CARGO_FETCH_ENDED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
      - name: Run unit checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ inputs.base_sha }}
          HEAD_SHA: ${{ inputs.head_sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: ${{ inputs.cargo_net_offline }}
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/CHECKS_STARTED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/CHECKS_ENDED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
          exit $rc
      - name: Mark cleanup end
        if: always()
        run: |
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/CLEANUP_ENDED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
      - name: Report phase timings and cache outcomes
        if: always()
        uses: ./.github/actions/report-velnor-ci-outcomes
        with:
          job_label: ${{ inputs.unit }}
          ci_lane: velnor
          host_warm_layers: rustup,mold,mbx,cargo

