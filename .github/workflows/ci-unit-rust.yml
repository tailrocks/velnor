# Generated by velnor-workflow. Regenerate; do not hand-edit.
name: Rust
on:
  workflow_call:
    inputs:
      unit:
        required: true
        type: string
      scope:
        required: true
        type: string
      selection-artifact:
        required: true
        type: string

jobs:
  velnor-rust-policy:
    name: "Velnor / rust-policy"
    if: ${{ inputs.unit == 'rust-policy' && (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'velnor' || github.event.inputs.runner == 'both')))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Install declared Mise tools
        run: |
          set -euo pipefail
          mise --yes install
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust dependency policy" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'Cargo.lock') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Run "Rust dependency policy" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · Rust dependency policy"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  velnor-rust-unit-collector:
    name: "Velnor / rust-unit-collector"
    if: ${{ inputs.unit == 'rust-unit-collector' && (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'velnor' || github.event.inputs.runner == 'both')))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Install declared Mise tools
        run: |
          set -euo pipefail
          mise --yes install
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (unit-collector)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'tools/unit-collector/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'tools/unit-collector' && cargo fetch --locked
      - name: Run "Rust crate (unit-collector)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · unit-collector"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  velnor-rust-velnor-bench:
    name: "Velnor / rust-velnor-bench"
    if: ${{ inputs.unit == 'rust-velnor-bench' && (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'velnor' || github.event.inputs.runner == 'both')))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Install declared Mise tools
        run: |
          set -euo pipefail
          mise --yes install
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (velnor-bench)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/velnor-bench/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/velnor-bench' && cargo fetch --locked
      - name: Run "Rust crate (velnor-bench)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · velnor-bench"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  velnor-rust-velnor-client:
    name: "Velnor / rust-velnor-client"
    if: ${{ inputs.unit == 'rust-velnor-client' && (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'velnor' || github.event.inputs.runner == 'both')))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Install declared Mise tools
        run: |
          set -euo pipefail
          mise --yes install
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (velnor-client)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/velnor-client/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/velnor-client' && cargo fetch --locked
      - name: Run "Rust crate (velnor-client)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · velnor-client"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  velnor-rust-velnor-control:
    name: "Velnor / rust-velnor-control"
    if: ${{ inputs.unit == 'rust-velnor-control' && (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'velnor' || github.event.inputs.runner == 'both')))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Install declared Mise tools
        run: |
          set -euo pipefail
          mise --yes install
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (velnor-control)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/velnor-control/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/velnor-control' && cargo fetch --locked
      - name: Run "Rust crate (velnor-control)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · velnor-control"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  velnor-rust-velnor-model:
    name: "Velnor / rust-velnor-model"
    if: ${{ inputs.unit == 'rust-velnor-model' && (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'velnor' || github.event.inputs.runner == 'both')))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Install declared Mise tools
        run: |
          set -euo pipefail
          mise --yes install
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (velnor-model)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/velnor-model/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/velnor-model' && cargo fetch --locked
      - name: Run "Rust crate (velnor-model)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · velnor-model"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  velnor-rust-velnor-render:
    name: "Velnor / rust-velnor-render"
    if: ${{ inputs.unit == 'rust-velnor-render' && (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'velnor' || github.event.inputs.runner == 'both')))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Install declared Mise tools
        run: |
          set -euo pipefail
          mise --yes install
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (velnor-render)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/velnor-render/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/velnor-render' && cargo fetch --locked
      - name: Run "Rust crate (velnor-render)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · velnor-render"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  velnor-rust-velnor-runner:
    name: "Velnor / rust-velnor-runner"
    if: ${{ inputs.unit == 'rust-velnor-runner' && (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'velnor' || github.event.inputs.runner == 'both')))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Install declared Mise tools
        run: |
          set -euo pipefail
          mise --yes install
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (velnor-runner)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/velnor-runner/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/velnor-runner' && cargo fetch --locked
      - name: Run "Rust crate (velnor-runner)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · velnor-runner"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  velnor-rust-velnor-tools:
    name: "Velnor / rust-velnor-tools"
    if: ${{ inputs.unit == 'rust-velnor-tools' && (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'velnor' || github.event.inputs.runner == 'both')))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Install declared Mise tools
        run: |
          set -euo pipefail
          mise --yes install
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (velnor-tools)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/velnor-tools/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/velnor-tools' && cargo fetch --locked
      - name: Run "Rust crate (velnor-tools)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · velnor-tools"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  velnor-rust-velnor-workflow:
    name: "Velnor / rust-velnor-workflow"
    if: ${{ inputs.unit == 'rust-velnor-workflow' && (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'velnor' || github.event.inputs.runner == 'both')))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Install declared Mise tools
        run: |
          set -euo pipefail
          mise --yes install
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (velnor-workflow)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/velnor-workflow/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/velnor-workflow' && cargo fetch --locked
      - name: Run "Rust crate (velnor-workflow)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · velnor-workflow"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  velnor-rust-velnor-workflow-contract:
    name: "Velnor / rust-velnor-workflow-contract"
    if: ${{ inputs.unit == 'rust-velnor-workflow-contract' && (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'velnor' || github.event.inputs.runner == 'both')))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Install declared Mise tools
        run: |
          set -euo pipefail
          mise --yes install
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (velnor-workflow-contract)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/velnor-workflow-contract/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/velnor-workflow-contract' && cargo fetch --locked
      - name: Run "Rust crate (velnor-workflow-contract)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · velnor-workflow-contract"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  velnor-rust-velnorctl:
    name: "Velnor / rust-velnorctl"
    if: ${{ inputs.unit == 'rust-velnorctl' && (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'velnor' || github.event.inputs.runner == 'both')))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Install declared Mise tools
        run: |
          set -euo pipefail
          mise --yes install
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust crate (velnorctl)" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('.cargo/**', 'Cargo.toml', 'crates/velnorctl/Cargo.toml', 'Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cd -- 'crates/velnorctl' && cargo fetch --locked
      - name: Run "Rust crate (velnorctl)" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · velnorctl"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

  velnor-rust-production-topology:
    name: "Velnor / rust-production-topology"
    if: ${{ inputs.unit == 'rust-production-topology' && (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'velnor' || github.event.inputs.runner == 'both')))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    env:
      RUSTFLAGS: "-C link-arg=-fuse-ld=mold"
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Install declared Mise tools
        run: |
          set -euo pipefail
          mise --yes install
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: local
      - name: Restore "Rust production topology" cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ci-${{ runner.os }}-rust-${{ hashFiles('Cargo.toml', 'Cargo.lock') }}
          restore-keys: |
            ci-${{ runner.os }}-rust-
      - name: Prepare Cargo sources
        env:
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -euo pipefail
          cargo fetch --locked
      - name: Run "Rust production topology" checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.inputs.base_sha || github.event.before || 'refs/heads/main' }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          CARGO_NET_OFFLINE: "true"
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          echo "VELNOR_CHECKS_STARTED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          echo "VELNOR_CHECKS_ENDED_EPOCH=$(date +%s)" >> "$GITHUB_ENV"
          exit $rc
      - name: Report phase timings and cache outcomes
        if: always()
        env:
          VELNOR_WORKFLOW_NAME: "Rust · Rust production topology"
          VELNOR_LANE_NAME: Velnor
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -uo pipefail
          log="$RUNNER_TEMP/velnor-unit-log.txt"
          log_present=false
          if [[ -s "$log" ]]; then log_present=true; fi
          started="${VELNOR_CHECKS_STARTED_EPOCH:-}"
          ended="${VELNOR_CHECKS_ENDED_EPOCH:-}"
          wall=""
          if [[ "$started" =~ ^[0-9]+$ && "$ended" =~ ^[0-9]+$ ]]; then
            wall=$((ended - started))
          fi
          queue=""
          job_name=""
          expected_job="$VELNOR_WORKFLOW_NAME / $VELNOR_LANE_NAME"
          if command -v gh >/dev/null 2>&1 && command -v jq >/dev/null 2>&1; then
            run_started="$(gh api "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID" --jq '.run_started_at // empty' 2>/dev/null || true)"
            jobs_json="$(gh api --paginate "repos/$GH_REPO/actions/runs/$GITHUB_RUN_ID/jobs?per_page=100" 2>/dev/null || true)"
            job_name="$(printf '%s' "$jobs_json" | jq -r --arg wf "$VELNOR_WORKFLOW_NAME" --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name == ($wf + " / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            if [[ -z "$job_name" ]]; then
              job_name="$(printf '%s' "$jobs_json" | jq -r --arg lane "$VELNOR_LANE_NAME" '[.jobs[]? | select(.name | endswith(" / " + $lane))][0].name // empty' 2>/dev/null | head -n1 || true)"
            fi
            if [[ -n "$job_name" && -n "$run_started" ]]; then
              job_started="$(printf '%s' "$jobs_json" | jq -r --arg name "$job_name" '.jobs[]? | select(.name == $name) | .started_at' 2>/dev/null | head -n1 || true)"
              if [[ -n "$job_started" ]]; then
                queue="$(jq -n --arg job "$job_started" --arg run "$run_started" '($job | fromdateiso8601) - ($run | fromdateiso8601)' 2>/dev/null || true)"
              fi
            fi
          fi
          if [[ -z "$job_name" ]]; then
            job_name="$expected_job"
          fi
          updating_index=0
          updating_git=0
          downloading_crates=0
          compiling_count=0
          downloaded_lines=""
          mbx_lines=""
          finished_lines=""
          if [[ -s "$log" ]]; then
            updating_index="$(grep -c 'Updating crates.io index' "$log" || true)"
            updating_git="$(grep -cE 'Updating git (repository|submodule)' "$log" || true)"
            downloading_crates="$(grep -c 'Downloading crates' "$log" || true)"
            compiling_count="$(grep -c 'Compiling ' "$log" || true)"
            downloaded_lines="$(grep -E 'Downloaded [0-9]+ crate' "$log" || true)"
            mbx_lines="$(grep -E 'mbx(\[cache\])?:' "$log" | grep -E 'hits|misses' || true)"
            finished_lines="$(grep -E 'Finished .* in ' "$log" || true)"
          fi
          positive_int() {
            local value="$1"
            [[ "$value" =~ ^[0-9]+$ ]] || value=0
            printf '%s' "$value"
          }
          report_json="$(jq -nc \
            --arg job "$job_name" \
            --arg queue "$queue" \
            --arg wall "$wall" \
            --argjson log_present "$log_present" \
            --argjson updating_index "$(positive_int "$updating_index")" \
            --argjson updating_git "$(positive_int "$updating_git")" \
            --argjson downloading_crates "$(positive_int "$downloading_crates")" \
            --argjson compiling_count "$(positive_int "$compiling_count")" \
            --argjson downloaded_lines "$(printf '%s' "$downloaded_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson mbx "$(printf '%s' "$mbx_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            --argjson finished "$(printf '%s' "$finished_lines" | jq -Rsc 'split("\n") | map(select(length > 0))' || true)" \
            '{
              job: $job,
              log_present: $log_present,
              queue_seconds: ($queue | if test("^[0-9]+$") then tonumber else null end),
              checks_wall_seconds: ($wall | if test("^[0-9]+$") then tonumber else null end),
              origin_downloads: {
                updating_crates_io_index: $updating_index,
                updating_git: $updating_git,
                downloading_crates: $downloading_crates,
                downloaded_lines: $downloaded_lines
              },
              compiler: {
                mbx_outcomes: $mbx,
                finished_segments: $finished,
                compiling_lines: $compiling_count
              }
            }' 2>/dev/null || true)"
          if [[ -z "$report_json" ]]; then
            report_json="VELNOR_CI_REPORT_FALLBACK job=$job_name"
          fi
          echo "VELNOR_CI_REPORT $report_json"
          {
            echo ""
            echo '### Phase timings and cache outcomes'
            echo ""
            echo '```json'
            echo "$report_json"
            echo '```'
            echo ""
            echo "| metric | value |"
            echo "| --- | --- |"
            echo "| queue_seconds | ${queue:-unknown} |"
            echo "| checks_wall_seconds | ${wall:-unknown} |"
            echo "| updating_crates_io_index | $updating_index |"
            echo "| updating_git | $updating_git |"
            echo "| downloading_crates | $downloading_crates |"
            echo "| compiling_lines | $compiling_count |"
          } >> "$GITHUB_STEP_SUMMARY" 2>/dev/null || true
          exit 0

