# Generated by velnor-workflow. Regenerate; do not hand-edit.
name: "Documentation · Documentation"
on:
  workflow_call:
    inputs:
      scope:
        required: true
        type: string
      selection-artifact:
        required: true
        type: string

env:
  RUSTFLAGS: "-C link-arg=-fuse-ld=mold"

jobs:
  github:
    name: GitHub
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-97e53e779426ccb7b196342df13e6cd6144c8502
          path: .velnor-workflow-runtime
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        run: |
          set -euo pipefail
          install -Dm0755 .velnor-workflow-runtime/velnor-workflow "$HOME/.cargo/bin/velnor-workflow"
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Documentation cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.npm
          key: ci-${{ runner.os }}-docs-${{ hashFiles('package-lock.json', 'bun.lock') }}
          restore-keys: |
            ci-${{ runner.os }}-docs-
      - name: Run Documentation checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: docs
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
        run: velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit docs
      - name: Save Documentation cache
        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.npm
          key: ci-${{ runner.os }}-docs-${{ hashFiles('package-lock.json', 'bun.lock') }}

  velnor:
    name: Velnor
    if: ${{ github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') }}
    runs-on: [self-hosted, velnor-target-mvp]
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Download Velnor CI selection
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: ${{ inputs.selection-artifact }}
          path: .velnor-ci-selection
      - name: Restore Documentation cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.npm
          key: ci-${{ runner.os }}-docs-${{ hashFiles('package-lock.json', 'bun.lock') }}
          restore-keys: |
            ci-${{ runner.os }}-docs-
      - name: Run Documentation checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: docs
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
        run: velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit docs

