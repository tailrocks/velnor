# Generated by velnor-workflow. Regenerate; do not hand-edit.
name: Documentation
on:
  workflow_call:
    inputs:
      unit:
        required: true
        type: string
      selected_units:
        required: true
        type: string
      selected_unit_ids:
        required: true
        type: string
      scope:
        required: true
        type: string
      full_units:
        required: true
        type: string
      base_sha:
        required: true
        type: string
      head_sha:
        required: true
        type: string
      plan_digest:
        required: true
        type: string
      provider:
        required: true
        type: string
      mise_tools:
        required: false
        type: string
        default: ""
      mise_runner:
        required: false
        type: boolean
        default: false
      mbx_enabled:
        required: false
        type: boolean
        default: false
      mbx_compat:
        required: false
        type: string
        default: ""
      mbx_dependency_files:
        required: false
        type: string
        default: ""
      mbx_freshness_files:
        required: false
        type: string
        default: ""
      cargo_bin_tools:
        required: false
        type: string
        default: ""
      tool_version:
        required: false
        type: string
        default: ""
      node_cache_dependency_path:
        required: false
        type: string
        default: ""
      cache_paths:
        required: false
        type: string
        default: ""
      cache_key_files:
        required: false
        type: string
        default: ""
      seed_compat:
        required: false
        type: string
        default: ""
      seed_dependency_files:
        required: false
        type: string
        default: ""
      seed_freshness_files:
        required: false
        type: string
        default: ""
      cargo_root:
        required: false
        type: string
        default: ""
      cargo_fetch_skip_when_warm:
        required: false
        type: boolean
        default: false
      cargo_net_offline:
        required: false
        type: boolean
        default: false
      host_warm_layers:
        required: false
        type: string
        default: ""
      policy_runtime:
        required: false
        type: boolean
        default: false
      unit_platform:
        required: false
        type: string
        default: ""
      unit_trust:
        required: false
        type: string
        default: ""
      unit_dependencies:
        required: false
        type: string
        default: ""
      unit_admission:
        required: false
        type: string
        default: ""

jobs:
  verify-github-hosted:
    name: "GitHub · hosted"
    if: ${{ inputs.provider == 'github-hosted' && contains(inputs.selected_units, format('"unit_id":"{0}"', inputs.unit)) && ((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',github-hosted,')) || (github.event_name != 'workflow_dispatch')) }}
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Mark CI job start
        if: always()
        run: |
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/JOB_STARTED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          ref: ${{ inputs.head_sha }}
      - name: Record unit dependencies
        env:
          UNIT_ID: ${{ inputs.unit }}
          UNIT_DEPENDENCIES: ${{ inputs.unit_dependencies }}
          UNIT_ADMISSION: ${{ inputs.unit_admission }}
          UNIT_PROVIDER: ${{ inputs.provider }}
        run: |
          {
            echo '## Unit dependencies'
            echo
            echo "- Unit: $UNIT_ID"
            echo "- Provider: $UNIT_PROVIDER"
            echo "- Admission: $UNIT_ADMISSION"
            if [[ -z "$UNIT_DEPENDENCIES" ]]; then
              echo '- Dependencies: none'
            else
              echo "- Dependencies: $UNIT_DEPENDENCIES"
            fi
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-e1c589eb5012a705aa735cb553a5230ff9fec490-${{ runner.os }}-${{ runner.arch }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/velnor-workflow-runtime-download
      - name: Verify Velnor workflow runtime
        shell: bash
        working-directory: .
        env:
          EXPECTED_REVISION: e1c589eb5012a705aa735cb553a5230ff9fec490
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          sha256() { if command -v sha256sum >/dev/null 2>&1; then sha256sum "$@"; else shasum -a 256 "$@"; fi; }
          stage="$RUNNER_TEMP/velnor-workflow-runtime-download"
          manifest="$stage/manifest.json"
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "$RUNNER_OS-$RUNNER_ARCH" --arg run_id "$GITHUB_RUN_ID" --arg run_attempt "$GITHUB_RUN_ATTEMPT" --arg job "runtime" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and .run_attempt == $run_attempt and .job_id == $job and (.policy_revision | test("^[0-9a-f]{40}$")) and (.profile != "debug" or .policy_revision == $revision) and ((.profile == "release" and .features == "") or (.profile == "debug" and .features == "tui")) and (.binary_sha256 | test("^[0-9a-f]{64}$")) and (.policy_binary_sha256 | test("^[0-9a-f]{64}$")) and (.closure | test("^[0-9a-f]{64}$")) and .closure == .policy_closure' "$manifest" >/dev/null
          if [[ 'true' == true ]]; then
            if ! git cat-file -e "$EXPECTED_REVISION^{commit}" 2>/dev/null; then
              auth="$(printf 'x-access-token:%s' "$GH_TOKEN" | base64 | tr -d '\n')"
              git -c "http.extraheader=AUTHORIZATION: basic $auth" fetch --no-tags --depth 1 "$GITHUB_SERVER_URL/$GITHUB_REPOSITORY" "$EXPECTED_REVISION"
            fi
          listing="$(git ls-tree -r "$EXPECTED_REVISION" -- 'crates/velnor-workflow' 'Cargo.toml' 'Cargo.lock' 'rust-toolchain.toml' 'rust-toolchain' '.cargo')"
          test -n "$listing"
          features="$(jq -er '.features | strings' "$manifest")"
          profile="$(jq -er .profile "$manifest")"
          closure="$(printf '%s\nclosure-version:1\nfeatures:%s\nprofile:%s\n' "$(LC_ALL=C sort <<<"$listing")" "$features" "$profile" | sha256 | awk '{print $1}')"
          [[ "$(jq -er .closure "$manifest")" == "$closure" ]] || { echo "::error::runtime source closure mismatch" >&2; exit 1; }
          fi
          for pair in 'velnor-workflow binary_sha256' 'velnor-workflow-policy policy_binary_sha256'; do
            read -r binary field <<< "$pair"
            expected="$(jq -er --arg field "$field" '.[$field]' "$manifest")"
            actual="$(sha256 "$stage/$binary" | awk '{print $1}')"
            [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch: $binary" >&2; exit 1; }
          done
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        env:
          GH_TOKEN: ""
          GITHUB_TOKEN: ""
        run: |
          set -euo pipefail
          sha256() { if command -v sha256sum >/dev/null 2>&1; then sha256sum "$@"; else shasum -a 256 "$@"; fi; }
          stage="$RUNNER_TEMP/velnor-workflow-runtime-download"
          home="$RUNNER_TEMP/velnor-workflow-runtime-artifact"
          mkdir -p "$home/bin"
          install -m 0755 "$stage/velnor-workflow" "$home/bin/velnor-workflow"
          install -m 0755 "$stage/velnor-workflow-policy" "$home/bin/velnor-workflow-policy"
          expected_closure="$(jq -er '.policy_closure' "$stage/manifest.json")"
          for binary in velnor-workflow velnor-workflow-policy; do
            reported="$("$home/bin/$binary" --closure)"
            [[ "$reported" == "$expected_closure" ]] || { echo "::error::runtime reports closure $reported, expected $expected_closure" >&2; exit 1; }
          done
          echo "$home/bin" >> "$GITHUB_PATH"
          echo "VELNOR_WORKFLOW_PINNED_BINARY=$home/bin/velnor-workflow-policy" >> "$GITHUB_ENV"
      - name: Mark runner setup end
        if: always()
        run: |
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/RUNNER_SETUP_ENDED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
      - name: Materialize Velnor CI selection
        shell: bash
        env:
          SELECTION_BASE_SHA: ${{ inputs.base_sha }}
          SELECTION_HEAD_SHA: ${{ inputs.head_sha }}
          SELECTION_SCOPE: ${{ inputs.scope }}
          SELECTION_UNITS: ${{ inputs.selected_unit_ids }}
          SELECTION_FULL_UNITS: ${{ inputs.full_units }}
          SELECTION_PLAN_DIGEST: ${{ inputs.plan_digest }}
        run: |
          set -euo pipefail
          mkdir -p .velnor-ci-selection
          {
            printf 'version=2\n'
            printf 'base_sha=%s\n' "$SELECTION_BASE_SHA"
            printf 'head_sha=%s\n' "$SELECTION_HEAD_SHA"
            printf 'scope=%s\n' "$SELECTION_SCOPE"
            printf 'units=%s\n' "$SELECTION_UNITS"
            printf 'full_units=%s\n' "$SELECTION_FULL_UNITS"
            printf 'plan_digest=%s\n' "$SELECTION_PLAN_DIGEST"
          } > .velnor-ci-selection/velnor-ci-selection
      - name: Mark selection transport end
        if: always()
        run: |
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/SELECTION_ENDED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
      - name: Mark tool bootstrap end
        if: always()
        run: |
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/TOOL_BOOTSTRAP_ENDED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
      - name: Restore unit cache
        id: cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ${{ inputs.cache_paths }}
          key: ci-${{ runner.os }}-${{ runner.arch }}-${{ inputs.provider }}-${{ inputs.unit_platform }}-${{ inputs.unit_trust }}-docs-${{ hashFiles(inputs.cache_key_files) }}
          restore-keys: |
            ci-${{ runner.os }}-${{ runner.arch }}-${{ inputs.provider }}-${{ inputs.unit_platform }}-${{ inputs.unit_trust }}-docs-
      - name: Mark cache prep end
        if: always()
        run: |
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/CACHE_PREP_ENDED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
      - name: Mark cargo fetch end
        if: always()
        run: |
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/CARGO_FETCH_ENDED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
      - name: Run unit checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ inputs.base_sha }}
          HEAD_SHA: ${{ inputs.head_sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/CHECKS_STARTED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/CHECKS_ENDED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
          exit $rc
      - name: Save unit cache
        if: always() && ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ${{ inputs.cache_paths }}
          key: ci-${{ runner.os }}-${{ runner.arch }}-${{ inputs.provider }}-${{ inputs.unit_platform }}-${{ inputs.unit_trust }}-docs-${{ hashFiles(inputs.cache_key_files) }}
      - name: Mark cleanup end
        if: always()
        run: |
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/CLEANUP_ENDED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
      - name: Report phase timings and cache outcomes
        if: always()
        uses: ./.github/actions/report-velnor-ci-outcomes
        with:
          job_label: ${{ inputs.unit }}
          ci_lane: github
          cache_cargo_primary: ${{ steps.cache.outputs.cache-primary-key }}
          cache_cargo_matched: ${{ steps.cache.outputs.cache-matched-key }}

  verify-velnor:
    name: velnor
    if: ${{ inputs.provider == 'velnor' && contains(inputs.selected_units, format('"unit_id":"{0}"', inputs.unit)) && (((github.event_name == 'workflow_dispatch' && contains(format(',{0},', github.event.inputs.providers), ',velnor,')) || (false)) && (!(github.event_name == 'pull_request' && (github.event.pull_request.head.repo.fork || github.event.pull_request.user.type == 'Bot')))) }}
    runs-on: [self-hosted, velnor-target-mvp]
    timeout-minutes: 45
    steps:
      - name: Mark CI job start
        if: always()
        run: |
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/JOB_STARTED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
          ref: ${{ inputs.head_sha }}
      - name: Record unit dependencies
        env:
          UNIT_ID: ${{ inputs.unit }}
          UNIT_DEPENDENCIES: ${{ inputs.unit_dependencies }}
          UNIT_ADMISSION: ${{ inputs.unit_admission }}
          UNIT_PROVIDER: ${{ inputs.provider }}
        run: |
          {
            echo '## Unit dependencies'
            echo
            echo "- Unit: $UNIT_ID"
            echo "- Provider: $UNIT_PROVIDER"
            echo "- Admission: $UNIT_ADMISSION"
            if [[ -z "$UNIT_DEPENDENCIES" ]]; then
              echo '- Dependencies: none'
            else
              echo "- Dependencies: $UNIT_DEPENDENCIES"
            fi
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Download Velnor workflow runtime
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: velnor-workflow-runtime-e1c589eb5012a705aa735cb553a5230ff9fec490-${{ runner.os }}-${{ runner.arch }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/velnor-workflow-runtime-download
      - name: Verify Velnor workflow runtime
        shell: bash
        working-directory: .
        env:
          EXPECTED_REVISION: e1c589eb5012a705aa735cb553a5230ff9fec490
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          sha256() { if command -v sha256sum >/dev/null 2>&1; then sha256sum "$@"; else shasum -a 256 "$@"; fi; }
          stage="$RUNNER_TEMP/velnor-workflow-runtime-download"
          manifest="$stage/manifest.json"
          jq -e --arg revision "$EXPECTED_REVISION" --arg repository "$GITHUB_REPOSITORY" --arg platform "$RUNNER_OS-$RUNNER_ARCH" --arg run_id "$GITHUB_RUN_ID" --arg run_attempt "$GITHUB_RUN_ATTEMPT" --arg job "runtime" '.revision == $revision and .repository == $repository and .platform == $platform and .run_id == $run_id and .run_attempt == $run_attempt and .job_id == $job and (.policy_revision | test("^[0-9a-f]{40}$")) and (.profile != "debug" or .policy_revision == $revision) and ((.profile == "release" and .features == "") or (.profile == "debug" and .features == "tui")) and (.binary_sha256 | test("^[0-9a-f]{64}$")) and (.policy_binary_sha256 | test("^[0-9a-f]{64}$")) and (.closure | test("^[0-9a-f]{64}$")) and .closure == .policy_closure' "$manifest" >/dev/null
          if [[ 'true' == true ]]; then
            if ! git cat-file -e "$EXPECTED_REVISION^{commit}" 2>/dev/null; then
              auth="$(printf 'x-access-token:%s' "$GH_TOKEN" | base64 | tr -d '\n')"
              git -c "http.extraheader=AUTHORIZATION: basic $auth" fetch --no-tags --depth 1 "$GITHUB_SERVER_URL/$GITHUB_REPOSITORY" "$EXPECTED_REVISION"
            fi
          listing="$(git ls-tree -r "$EXPECTED_REVISION" -- 'crates/velnor-workflow' 'Cargo.toml' 'Cargo.lock' 'rust-toolchain.toml' 'rust-toolchain' '.cargo')"
          test -n "$listing"
          features="$(jq -er '.features | strings' "$manifest")"
          profile="$(jq -er .profile "$manifest")"
          closure="$(printf '%s\nclosure-version:1\nfeatures:%s\nprofile:%s\n' "$(LC_ALL=C sort <<<"$listing")" "$features" "$profile" | sha256 | awk '{print $1}')"
          [[ "$(jq -er .closure "$manifest")" == "$closure" ]] || { echo "::error::runtime source closure mismatch" >&2; exit 1; }
          fi
          for pair in 'velnor-workflow binary_sha256' 'velnor-workflow-policy policy_binary_sha256'; do
            read -r binary field <<< "$pair"
            expected="$(jq -er --arg field "$field" '.[$field]' "$manifest")"
            actual="$(sha256 "$stage/$binary" | awk '{print $1}')"
            [[ "$actual" == "$expected" ]] || { echo "::error::runtime digest mismatch: $binary" >&2; exit 1; }
          done
      - name: Add Velnor workflow runtime to PATH
        shell: bash
        env:
          GH_TOKEN: ""
          GITHUB_TOKEN: ""
        run: |
          set -euo pipefail
          sha256() { if command -v sha256sum >/dev/null 2>&1; then sha256sum "$@"; else shasum -a 256 "$@"; fi; }
          stage="$RUNNER_TEMP/velnor-workflow-runtime-download"
          home="$RUNNER_TEMP/velnor-workflow-runtime-artifact"
          mkdir -p "$home/bin"
          install -m 0755 "$stage/velnor-workflow" "$home/bin/velnor-workflow"
          install -m 0755 "$stage/velnor-workflow-policy" "$home/bin/velnor-workflow-policy"
          expected_closure="$(jq -er '.policy_closure' "$stage/manifest.json")"
          for binary in velnor-workflow velnor-workflow-policy; do
            reported="$("$home/bin/$binary" --closure)"
            [[ "$reported" == "$expected_closure" ]] || { echo "::error::runtime reports closure $reported, expected $expected_closure" >&2; exit 1; }
          done
          echo "$home/bin" >> "$GITHUB_PATH"
          echo "VELNOR_WORKFLOW_PINNED_BINARY=$home/bin/velnor-workflow-policy" >> "$GITHUB_ENV"
      - name: Mark runner setup end
        if: always()
        run: |
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/RUNNER_SETUP_ENDED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
      - name: Materialize Velnor CI selection
        shell: bash
        env:
          SELECTION_BASE_SHA: ${{ inputs.base_sha }}
          SELECTION_HEAD_SHA: ${{ inputs.head_sha }}
          SELECTION_SCOPE: ${{ inputs.scope }}
          SELECTION_UNITS: ${{ inputs.selected_unit_ids }}
          SELECTION_FULL_UNITS: ${{ inputs.full_units }}
          SELECTION_PLAN_DIGEST: ${{ inputs.plan_digest }}
        run: |
          set -euo pipefail
          mkdir -p .velnor-ci-selection
          {
            printf 'version=2\n'
            printf 'base_sha=%s\n' "$SELECTION_BASE_SHA"
            printf 'head_sha=%s\n' "$SELECTION_HEAD_SHA"
            printf 'scope=%s\n' "$SELECTION_SCOPE"
            printf 'units=%s\n' "$SELECTION_UNITS"
            printf 'full_units=%s\n' "$SELECTION_FULL_UNITS"
            printf 'plan_digest=%s\n' "$SELECTION_PLAN_DIGEST"
          } > .velnor-ci-selection/velnor-ci-selection
      - name: Mark selection transport end
        if: always()
        run: |
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/SELECTION_ENDED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
      - name: Mark tool bootstrap end
        if: always()
        run: |
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/TOOL_BOOTSTRAP_ENDED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
      - name: Mark cache prep end
        if: always()
        run: |
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/CACHE_PREP_ENDED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
      - name: Mark cargo fetch end
        if: always()
        run: |
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/CARGO_FETCH_ENDED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
      - name: Run unit checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: ${{ inputs.unit }}
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ inputs.base_sha }}
          HEAD_SHA: ${{ inputs.head_sha }}
          VELNOR_SELECTION_FILE: .velnor-ci-selection/velnor-ci-selection
          MISE_AUTO_INSTALL: "false"
          MISE_EXEC_AUTO_INSTALL: "false"
          MISE_NOT_FOUND_AUTO_INSTALL: "false"
        run: |
          set -o pipefail
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/CHECKS_STARTED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
          rc=0
          velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit "$CI_UNIT_ID" 2>&1 | tee "$RUNNER_TEMP/velnor-unit-log.txt" || rc=$?
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/CHECKS_ENDED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
          exit $rc
      - name: Mark cleanup end
        if: always()
        run: |
          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
          umask 077
          mkdir -p "$timing_dir"
          marker="$timing_dir/CLEANUP_ENDED"
          if [[ ! -e "$marker" ]]; then
            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
              chmod 0444 "$marker" 2>/dev/null || true
            fi
          fi
      - name: Report phase timings and cache outcomes
        if: always()
        uses: ./.github/actions/report-velnor-ci-outcomes
        with:
          job_label: ${{ inputs.unit }}
          ci_lane: velnor
          host_warm_layers: cargo

