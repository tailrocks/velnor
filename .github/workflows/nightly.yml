# Generated by github-actions-generator. Regenerate; do not hand-edit.
name: Nightly
run-name: Nightly · ${{ github.event_name }} · ${{ github.ref_name }}

on:
  schedule:
    - cron: '17 3 * * *'
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  plan:
    name: Planning
    runs-on: ubuntu-24.04
    outputs:
      scope: ${{ steps.plan.outputs.scope }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Select affected units
        id: plan
        env:
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
        run: bash .github/ci/run.sh plan

  policy:
    name: Policy
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout workflow data
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Checkout trusted policy
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        continue-on-error: true
        with:
          ref: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.sha }}
          path: .github-policy
          persist-credentials: false
      - name: Enforce workflow policy
        env:
          EVENT_NAME: ${{ github.event_name }}
          WORKFLOW_ROOT: ${{ github.workspace }}
        run: |
          set -euo pipefail
          policy=.github-policy/.github/ci/policy.sh
          if [[ ! -f "$policy" ]]; then
            policy=.github/ci/policy.sh
          fi
          bash "$policy"
  group-rust:
    name: Rust
    needs: [plan, policy]
    uses: ./.github/workflows/ci-rust.yml
    with:
      scope: ${{ needs.plan.outputs.scope }}
  group-bun:
    name: "Bun / Packages"
    needs: [plan, policy]
    uses: ./.github/workflows/ci-bun.yml
    with:
      scope: ${{ needs.plan.outputs.scope }}
  group-docker:
    name: "Docker / Images"
    needs: [plan, policy]
    uses: ./.github/workflows/ci-docker.yml
    with:
      scope: ${{ needs.plan.outputs.scope }}
  group-docs:
    name: Documentation
    needs: [plan, policy]
    uses: ./.github/workflows/ci-docs.yml
    with:
      scope: ${{ needs.plan.outputs.scope }}
