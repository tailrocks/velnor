# Generated by github-actions-generator. Regenerate; do not hand-edit.
name: "Bun / Packages"
on:
  workflow_call:
    inputs:
      scope:
        required: true
        type: string

permissions:
  contents: read

jobs:
  group-unit-bun-velnor:
    name: "Bun package (velnor)"
    uses: ./.github/workflows/ci-bun-velnor.yml
    with:
      scope: ${{ inputs.scope }}
