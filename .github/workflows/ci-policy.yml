# Generated by velnor-workflow. Regenerate; do not hand-edit.
name: Velnor workflow policy

on:
  pull_request_target:
    types: [opened, synchronize, reopened]

concurrency:
  group: policy-${{ github.repository }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  policy:
    name: Policy
    uses: tailrocks/velnor/.github/workflows/velnor-workflow-policy.yml@de3f79fe2cb2d493e95023abfbbcd51a48d77346
    with:
      policy-revision: de3f79fe2cb2d493e95023abfbbcd51a48d77346
    permissions:
      contents: read
