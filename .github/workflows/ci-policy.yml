# Generated by velnor-workflow. Regenerate; do not hand-edit.
name: Velnor workflow policy

on:
  pull_request_target:
    types: [opened, synchronize, reopened]

concurrency:
  group: policy-${{ github.repository }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  policy:
    name: Policy
    uses: tailrocks/velnor/.github/workflows/velnor-workflow-policy.yml@07d083181537b3b51c98e35879ffc16bd7f9b288
    with:
      policy-revision: 07d083181537b3b51c98e35879ffc16bd7f9b288
    permissions:
      contents: read
