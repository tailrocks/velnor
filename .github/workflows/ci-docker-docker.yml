# Generated by velnor-workflow. Regenerate; do not hand-edit.
name: "Docker · Docker"
on:
  workflow_call:
    inputs:
      scope:
        required: true
        type: string

jobs:
  github:
    name: GitHub
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up Velnor workflow runtime
        if: ${{ runner.environment == 'github-hosted' }}
        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@c3a38b5ffffb844ab0dffc730a67f953b6af9eb0
        with:
          rev: c3a38b5ffffb844ab0dffc730a67f953b6af9eb0
      - name: Set trusted workflow policy revision
        run: echo "VELNOR_WORKFLOW_POLICY_REVISION=f15ff2e8449a34f77f04746fa461a349bad22e79" >> "$GITHUB_ENV"
      - name: Expose GitHub Actions runtime
        uses: crazy-max/ghaction-github-runtime@04d248b84655b509d8c44dc1d6f990c879747487 # v4.0.0
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0
      - name: Run Docker checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: docker
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
          CI_RUNNER_LANE: github
          CI_CACHE_WRITE: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main') }}
        run: velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit docker

  velnor:
    name: Velnor
    if: ${{ github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch') }}
    runs-on: [self-hosted, velnor-target-mvp]
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0
      - name: Run Docker checks
        env:
          CI_SCOPE: ${{ inputs.scope }}
          CI_UNIT_ID: docker
          EVENT_NAME: ${{ github.event_name }}
          BASE_SHA: ${{ github.event.pull_request.base.sha || github.event.merge_group.base_sha || github.event.before }}
          HEAD_SHA: ${{ github.sha }}
          CI_RUNNER_LANE: velnor
          CI_CACHE_WRITE: false
        run: velnor-workflow run --config .github/ci/project.toml --scope "$CI_SCOPE" --unit docker

