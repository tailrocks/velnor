# Immediate recovery checkpoint

User requested all work committed and pushed immediately. Agent source archives
preserve interrupted drafts. They are not installed implementations or accepted
performance improvements. Review findings, failed probes and incomplete validation
remain open. The manifest binds each source archive by SHA-256.

The condition composer is checkpointed separately in commit `5618e8ee`.
Older preserved working trees are checkpointed on their existing campaign branches:
Velnor `155d6b81`, Jackin `14141e0e`, Parallax `3a784c6a`.
The latter Parallax draft is superseded by the validated `8e1262d7` integration;
it must not replace that integration without review.

The iteration-credit audit is a recommendation, not a ledger update. Its rollback
entry conflates two hypotheses and requires correction before acceptance.
The second condition probe could not compile because the expected dependency
archive was unavailable; its captured failure is not a passing regression result.
