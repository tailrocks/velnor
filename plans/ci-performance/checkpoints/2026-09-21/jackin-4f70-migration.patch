diff --git a/.github-gen/velnor-workflow.toml b/.github-gen/velnor-workflow.toml
index 1616aaa3..12ae0037 100644
--- a/.github-gen/velnor-workflow.toml
+++ b/.github-gen/velnor-workflow.toml
@@ -3,9 +3,10 @@ schema = 1
 [generator]
 repository = "jackin-project/jackin"
 # D19 pin: immutable Velnor source for this generated tree. The matching
-# published runtime is velnor-workflow-runtime-v1-af140ad4d8d84326
-# (closure af140ad4d8d84326d5676f626ce4261fd7914ac41d521d851484dcb8253270e7).
-revision = "4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d"
+# The exact 4f70 candidate was built and attested by Velnor run 35523884300.
+# Consumer rollout still requires the immutable cross-platform runtime product
+# for this revision; the Linux debug candidate is policy bootstrap evidence only.
+revision = "4f70cf741179e5a499a6e43f62dac2f913188b35"
 
 [scan]
 exclude = ["native/Package.swift", "native/.swiftpm/**"]
@@ -122,10 +123,6 @@ runner = "github"
 tasks = ["renovate-upstream-sources"]
 timeout_minutes = 10
 status = "required"
-[check_profile.env]
-# The probe needs only sh + curl: skip mise's whole-toolset install
-# (30 tools, ~9 cold minutes) before running the task.
-MISE_TASK_RUN_AUTO_INSTALL = "false"
 
 [[declare]]
 primitive = "scheduled-checks"
diff --git a/.github/ci/.github-actions-generator-state b/.github/ci/.github-actions-generator-state
index e0f89dd3..534e60c0 100644
--- a/.github/ci/.github-actions-generator-state
+++ b/.github/ci/.github-actions-generator-state
@@ -1,25 +1,25 @@
 # Generated ownership state; do not edit.
 schema = 2
 [inputs]
-config	b376b11ddcb7cf5b
-scan	79aad67cd9452b50
-generator	54
+config	6d886bff427980b3
+scan	c83bb8901b87fc64
+generator	65
 [outputs]
 .github/actionlint.yaml	2c1dbf178fa8baa1
-.github/ci/project.toml	5b4cfa3490420801
-.github/workflows/ci-main.yml	8ccdbf26d85a92b8
-.github/workflows/ci-policy.yml	846c6e540f111b8c
-.github/workflows/ci-pr.yml	35bbba9456bb9e97
-.github/workflows/ci-unit-bun.yml	6ee57fd5e25bd570
-.github/workflows/ci-unit-docker.yml	53ba26f2a83bfb94
-.github/workflows/ci-unit-rust.yml	9a7fc6279d159712
-.github/workflows/ci-unit-swift.yml	7cb31cb8c822dd7a
-.github/workflows/desktop-merge.yml	f9cb2a63c3d833ff
-.github/workflows/desktop-scheduled.yml	ede17b880d391a6d
-.github/workflows/maintenance.yml	ea77e9b6fa6c14bd
+.github/ci/project.toml	ad607080d7a03539
+.github/workflows/ci-main.yml	45f3b7cdea55f8e4
+.github/workflows/ci-policy.yml	3169aaea0aa45cf0
+.github/workflows/ci-pr.yml	f4811986eb0a6e83
+.github/workflows/ci-unit-bun.yml	91c2c978976e4dcb
+.github/workflows/ci-unit-docker.yml	6ab70cc3d402aab9
+.github/workflows/ci-unit-rust.yml	1381bbc16969aac3
+.github/workflows/ci-unit-swift.yml	2370109d8a56fb49
+.github/workflows/desktop-merge.yml	2c595ca5359cdc64
+.github/workflows/desktop-scheduled.yml	1c00097dc34a1192
+.github/workflows/maintenance.yml	e646a1b4fede30ad
 .github/workflows/nightly.yml	0bbe34967df0f3ed
 .github/workflows/release.yml	1aace61e7093c352
-.github/workflows/renovate-upstream-sources.yml	aea51fd10f50526e
+.github/workflows/renovate-upstream-sources.yml	bdcf7725525a83b2
 .github/workflows/renovate-validate.yml	0aac9a7e6ffb76ca
 .github/workflows/renovate.yml	0fc56f6b1d71c76a
 config/fleet/velnor-host.env	d14b419216449423
diff --git a/.github/ci/project.toml b/.github/ci/project.toml
index fc32abda..1ea2dc6d 100644
--- a/.github/ci/project.toml
+++ b/.github/ci/project.toml
@@ -55,10 +55,10 @@ label = "Rust crate (arrayref)"
 kind = "rust"
 root = "vendor/arrayref"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml", "vendor/arrayref/**/*.rs", "vendor/arrayref/Cargo.lock", "vendor/arrayref/Cargo.toml", "vendor/arrayref/benches/**", "vendor/arrayref/examples/**", "vendor/arrayref/src/**", "vendor/arrayref/tests/**"]
-github_pr_commands = ["cd -- 'vendor/arrayref' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'vendor/arrayref' && mbx nextest run --locked --all-features --package 'arrayref' --no-tests pass", "cd -- 'vendor/arrayref' && mbx clippy --locked --profile test --all-targets --all-features --package 'arrayref' -- -D warnings"]
-github_full_commands = ["cd -- 'vendor/arrayref' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'vendor/arrayref' && mbx nextest run --locked --all-features --package 'arrayref' --no-tests pass", "cd -- 'vendor/arrayref' && mbx clippy --locked --profile test --all-targets --all-features --package 'arrayref' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'vendor/arrayref' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'vendor/arrayref' && mbx nextest run --locked --all-features --package 'arrayref' --no-tests pass", "cd -- 'vendor/arrayref' && mbx clippy --locked --profile test --all-targets --all-features --package 'arrayref' -- -D warnings"]
-velnor_full_commands = ["cd -- 'vendor/arrayref' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'vendor/arrayref' && mbx nextest run --locked --all-features --package 'arrayref' --no-tests pass", "cd -- 'vendor/arrayref' && mbx clippy --locked --profile test --all-targets --all-features --package 'arrayref' -- -D warnings"]
+github_pr_commands = ["cd -- 'vendor/arrayref' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'vendor/arrayref' && mbx clippy --locked --profile test --all-targets --all-features --package 'arrayref' -- -D warnings", "cd -- 'vendor/arrayref' && mbx nextest run --locked --all-features --package 'arrayref' --no-tests pass"]
+github_full_commands = ["cd -- 'vendor/arrayref' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'vendor/arrayref' && mbx clippy --locked --profile test --all-targets --all-features --package 'arrayref' -- -D warnings", "cd -- 'vendor/arrayref' && mbx nextest run --locked --all-features --package 'arrayref' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'vendor/arrayref' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'vendor/arrayref' && mbx clippy --locked --profile test --all-targets --all-features --package 'arrayref' -- -D warnings", "cd -- 'vendor/arrayref' && mbx nextest run --locked --all-features --package 'arrayref' --no-tests pass"]
+velnor_full_commands = ["cd -- 'vendor/arrayref' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'vendor/arrayref' && mbx clippy --locked --profile test --all-targets --all-features --package 'arrayref' -- -D warnings", "cd -- 'vendor/arrayref' && mbx nextest run --locked --all-features --package 'arrayref' --no-tests pass"]
 [unit.cache]
 key_files = [".cargo/**", "vendor/arrayref/Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
 paths = ["~/.cargo/registry", "~/.cargo/git"]
@@ -69,10 +69,10 @@ label = "Rust crate (jackin)"
 kind = "rust"
 root = "crates/jackin"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin/**/*.rs", "crates/jackin/Cargo.toml", "crates/jackin/benches/**", "crates/jackin/build.rs", "crates/jackin/examples/**", "crates/jackin/src/**", "crates/jackin/tests/**", "docker/construct/Dockerfile", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin' && mbx nextest run --locked --all-features --package 'jackin' --no-tests pass", "cd -- 'crates/jackin' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin' && mbx nextest run --locked --all-features --package 'jackin' --no-tests pass", "cd -- 'crates/jackin' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin' && mbx nextest run --locked --all-features --package 'jackin' --no-tests pass", "cd -- 'crates/jackin' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin' && mbx nextest run --locked --all-features --package 'jackin' --no-tests pass", "cd -- 'crates/jackin' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin' -- -D warnings", "cd -- 'crates/jackin' && mbx nextest run --locked --all-features --package 'jackin' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin' -- -D warnings", "cd -- 'crates/jackin' && mbx nextest run --locked --all-features --package 'jackin' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin' -- -D warnings", "cd -- 'crates/jackin' && mbx nextest run --locked --all-features --package 'jackin' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin' -- -D warnings", "cd -- 'crates/jackin' && mbx nextest run --locked --all-features --package 'jackin' --no-tests pass"]
 depends_on = ["rust-jackin-brand", "rust-jackin-build-meta", "rust-jackin-config", "rust-jackin-console", "rust-jackin-core", "rust-jackin-diagnostics", "rust-jackin-docker", "rust-jackin-env", "rust-jackin-image", "rust-jackin-launch", "rust-jackin-manifest", "rust-jackin-otlp-testbed", "rust-jackin-process", "rust-jackin-protocol", "rust-jackin-runtime", "rust-jackin-telemetry", "rust-jackin-test-support", "rust-jackin-tui", "rust-jackin-usage"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
@@ -84,10 +84,10 @@ label = "Rust crate (jackin-agent-status)"
 kind = "rust"
 root = "crates/jackin-agent-status"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-agent-status/**/*.rs", "crates/jackin-agent-status/Cargo.toml", "crates/jackin-agent-status/benches/**", "crates/jackin-agent-status/examples/**", "crates/jackin-agent-status/packs/amp.toml", "crates/jackin-agent-status/packs/claude.toml", "crates/jackin-agent-status/packs/codex.toml", "crates/jackin-agent-status/packs/grok.toml", "crates/jackin-agent-status/packs/kimi.toml", "crates/jackin-agent-status/packs/opencode.toml", "crates/jackin-agent-status/src/**", "crates/jackin-agent-status/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-agent-status' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-agent-status' && mbx nextest run --locked --all-features --package 'jackin-agent-status' --no-tests pass", "cd -- 'crates/jackin-agent-status' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-agent-status' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-agent-status' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-agent-status' && mbx nextest run --locked --all-features --package 'jackin-agent-status' --no-tests pass", "cd -- 'crates/jackin-agent-status' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-agent-status' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-agent-status' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-agent-status' && mbx nextest run --locked --all-features --package 'jackin-agent-status' --no-tests pass", "cd -- 'crates/jackin-agent-status' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-agent-status' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-agent-status' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-agent-status' && mbx nextest run --locked --all-features --package 'jackin-agent-status' --no-tests pass", "cd -- 'crates/jackin-agent-status' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-agent-status' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-agent-status' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-agent-status' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-agent-status' -- -D warnings", "cd -- 'crates/jackin-agent-status' && mbx nextest run --locked --all-features --package 'jackin-agent-status' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-agent-status' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-agent-status' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-agent-status' -- -D warnings", "cd -- 'crates/jackin-agent-status' && mbx nextest run --locked --all-features --package 'jackin-agent-status' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-agent-status' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-agent-status' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-agent-status' -- -D warnings", "cd -- 'crates/jackin-agent-status' && mbx nextest run --locked --all-features --package 'jackin-agent-status' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-agent-status' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-agent-status' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-agent-status' -- -D warnings", "cd -- 'crates/jackin-agent-status' && mbx nextest run --locked --all-features --package 'jackin-agent-status' --no-tests pass"]
 depends_on = ["rust-jackin-core", "rust-jackin-protocol"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
@@ -99,10 +99,10 @@ label = "Rust crate (jackin-brand)"
 kind = "rust"
 root = "crates/jackin-brand"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-brand/**/*.rs", "crates/jackin-brand/Cargo.toml", "crates/jackin-brand/benches/**", "crates/jackin-brand/examples/**", "crates/jackin-brand/src/**", "crates/jackin-brand/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-brand' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-brand' && mbx nextest run --locked --all-features --package 'jackin-brand' --no-tests pass", "cd -- 'crates/jackin-brand' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-brand' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-brand' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-brand' && mbx nextest run --locked --all-features --package 'jackin-brand' --no-tests pass", "cd -- 'crates/jackin-brand' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-brand' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-brand' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-brand' && mbx nextest run --locked --all-features --package 'jackin-brand' --no-tests pass", "cd -- 'crates/jackin-brand' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-brand' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-brand' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-brand' && mbx nextest run --locked --all-features --package 'jackin-brand' --no-tests pass", "cd -- 'crates/jackin-brand' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-brand' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-brand' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-brand' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-brand' -- -D warnings", "cd -- 'crates/jackin-brand' && mbx nextest run --locked --all-features --package 'jackin-brand' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-brand' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-brand' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-brand' -- -D warnings", "cd -- 'crates/jackin-brand' && mbx nextest run --locked --all-features --package 'jackin-brand' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-brand' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-brand' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-brand' -- -D warnings", "cd -- 'crates/jackin-brand' && mbx nextest run --locked --all-features --package 'jackin-brand' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-brand' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-brand' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-brand' -- -D warnings", "cd -- 'crates/jackin-brand' && mbx nextest run --locked --all-features --package 'jackin-brand' --no-tests pass"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
 paths = ["~/.cargo/registry", "~/.cargo/git"]
@@ -113,10 +113,10 @@ label = "Rust crate (jackin-build-meta)"
 kind = "rust"
 root = "crates/jackin-build-meta"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-build-meta/**/*.rs", "crates/jackin-build-meta/Cargo.toml", "crates/jackin-build-meta/benches/**", "crates/jackin-build-meta/examples/**", "crates/jackin-build-meta/src/**", "crates/jackin-build-meta/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-build-meta' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-build-meta' && mbx nextest run --locked --all-features --package 'jackin-build-meta' --no-tests pass", "cd -- 'crates/jackin-build-meta' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-build-meta' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-build-meta' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-build-meta' && mbx nextest run --locked --all-features --package 'jackin-build-meta' --no-tests pass", "cd -- 'crates/jackin-build-meta' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-build-meta' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-build-meta' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-build-meta' && mbx nextest run --locked --all-features --package 'jackin-build-meta' --no-tests pass", "cd -- 'crates/jackin-build-meta' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-build-meta' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-build-meta' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-build-meta' && mbx nextest run --locked --all-features --package 'jackin-build-meta' --no-tests pass", "cd -- 'crates/jackin-build-meta' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-build-meta' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-build-meta' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-build-meta' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-build-meta' -- -D warnings", "cd -- 'crates/jackin-build-meta' && mbx nextest run --locked --all-features --package 'jackin-build-meta' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-build-meta' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-build-meta' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-build-meta' -- -D warnings", "cd -- 'crates/jackin-build-meta' && mbx nextest run --locked --all-features --package 'jackin-build-meta' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-build-meta' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-build-meta' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-build-meta' -- -D warnings", "cd -- 'crates/jackin-build-meta' && mbx nextest run --locked --all-features --package 'jackin-build-meta' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-build-meta' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-build-meta' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-build-meta' -- -D warnings", "cd -- 'crates/jackin-build-meta' && mbx nextest run --locked --all-features --package 'jackin-build-meta' --no-tests pass"]
 depends_on = ["rust-jackin-process"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
@@ -128,10 +128,10 @@ label = "Rust crate (jackin-capsule)"
 kind = "rust"
 root = "crates/jackin-capsule"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-capsule/**/*.rs", "crates/jackin-capsule/Cargo.toml", "crates/jackin-capsule/benches/**", "crates/jackin-capsule/build.rs", "crates/jackin-capsule/examples/**", "crates/jackin-capsule/src/**", "crates/jackin-capsule/src/daemon.rs", "crates/jackin-capsule/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-capsule' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-capsule' && mbx nextest run --locked --all-features --package 'jackin-capsule' --no-tests pass", "cd -- 'crates/jackin-capsule' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-capsule' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-capsule' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-capsule' && mbx nextest run --locked --all-features --package 'jackin-capsule' --no-tests pass", "cd -- 'crates/jackin-capsule' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-capsule' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-capsule' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-capsule' && mbx nextest run --locked --all-features --package 'jackin-capsule' --no-tests pass", "cd -- 'crates/jackin-capsule' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-capsule' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-capsule' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-capsule' && mbx nextest run --locked --all-features --package 'jackin-capsule' --no-tests pass", "cd -- 'crates/jackin-capsule' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-capsule' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-capsule' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-capsule' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-capsule' -- -D warnings", "cd -- 'crates/jackin-capsule' && mbx nextest run --locked --all-features --package 'jackin-capsule' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-capsule' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-capsule' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-capsule' -- -D warnings", "cd -- 'crates/jackin-capsule' && mbx nextest run --locked --all-features --package 'jackin-capsule' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-capsule' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-capsule' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-capsule' -- -D warnings", "cd -- 'crates/jackin-capsule' && mbx nextest run --locked --all-features --package 'jackin-capsule' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-capsule' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-capsule' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-capsule' -- -D warnings", "cd -- 'crates/jackin-capsule' && mbx nextest run --locked --all-features --package 'jackin-capsule' --no-tests pass"]
 depends_on = ["rust-jackin-agent-status", "rust-jackin-brand", "rust-jackin-build-meta", "rust-jackin-core", "rust-jackin-diagnostics", "rust-jackin-otlp-testbed", "rust-jackin-process", "rust-jackin-protocol", "rust-jackin-telemetry", "rust-jackin-tui", "rust-jackin-usage"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
@@ -143,10 +143,10 @@ label = "Rust crate (jackin-config)"
 kind = "rust"
 root = "crates/jackin-config"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-config/**/*.rs", "crates/jackin-config/Cargo.toml", "crates/jackin-config/benches/**", "crates/jackin-config/examples/**", "crates/jackin-config/src/**", "crates/jackin-config/src/fixtures/config.round_trip.toml", "crates/jackin-config/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-config' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-config' && mbx nextest run --locked --all-features --package 'jackin-config' --no-tests pass", "cd -- 'crates/jackin-config' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-config' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-config' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-config' && mbx nextest run --locked --all-features --package 'jackin-config' --no-tests pass", "cd -- 'crates/jackin-config' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-config' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-config' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-config' && mbx nextest run --locked --all-features --package 'jackin-config' --no-tests pass", "cd -- 'crates/jackin-config' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-config' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-config' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-config' && mbx nextest run --locked --all-features --package 'jackin-config' --no-tests pass", "cd -- 'crates/jackin-config' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-config' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-config' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-config' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-config' -- -D warnings", "cd -- 'crates/jackin-config' && mbx nextest run --locked --all-features --package 'jackin-config' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-config' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-config' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-config' -- -D warnings", "cd -- 'crates/jackin-config' && mbx nextest run --locked --all-features --package 'jackin-config' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-config' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-config' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-config' -- -D warnings", "cd -- 'crates/jackin-config' && mbx nextest run --locked --all-features --package 'jackin-config' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-config' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-config' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-config' -- -D warnings", "cd -- 'crates/jackin-config' && mbx nextest run --locked --all-features --package 'jackin-config' --no-tests pass"]
 depends_on = ["rust-jackin-core", "rust-jackin-diagnostics", "rust-jackin-otlp-testbed", "rust-jackin-telemetry"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
@@ -158,10 +158,10 @@ label = "Rust crate (jackin-config-fuzz)"
 kind = "rust"
 root = "crates/jackin-config/fuzz"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-config/fuzz/**/*.rs", "crates/jackin-config/fuzz/Cargo.lock", "crates/jackin-config/fuzz/Cargo.toml", "crates/jackin-config/fuzz/benches/**", "crates/jackin-config/fuzz/examples/**", "crates/jackin-config/fuzz/src/**", "crates/jackin-config/fuzz/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-config/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-config/fuzz' && mbx nextest run --locked --all-features --package 'jackin-config-fuzz' --no-tests pass", "cd -- 'crates/jackin-config/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-config-fuzz' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-config/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-config/fuzz' && mbx nextest run --locked --all-features --package 'jackin-config-fuzz' --no-tests pass", "cd -- 'crates/jackin-config/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-config-fuzz' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-config/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-config/fuzz' && mbx nextest run --locked --all-features --package 'jackin-config-fuzz' --no-tests pass", "cd -- 'crates/jackin-config/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-config-fuzz' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-config/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-config/fuzz' && mbx nextest run --locked --all-features --package 'jackin-config-fuzz' --no-tests pass", "cd -- 'crates/jackin-config/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-config-fuzz' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-config/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-config/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-config-fuzz' -- -D warnings", "cd -- 'crates/jackin-config/fuzz' && mbx nextest run --locked --all-features --package 'jackin-config-fuzz' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-config/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-config/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-config-fuzz' -- -D warnings", "cd -- 'crates/jackin-config/fuzz' && mbx nextest run --locked --all-features --package 'jackin-config-fuzz' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-config/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-config/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-config-fuzz' -- -D warnings", "cd -- 'crates/jackin-config/fuzz' && mbx nextest run --locked --all-features --package 'jackin-config-fuzz' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-config/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-config/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-config-fuzz' -- -D warnings", "cd -- 'crates/jackin-config/fuzz' && mbx nextest run --locked --all-features --package 'jackin-config-fuzz' --no-tests pass"]
 depends_on = ["rust-jackin-config"]
 [unit.cache]
 key_files = [".cargo/**", "crates/jackin-config/fuzz/Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
@@ -173,10 +173,10 @@ label = "Rust crate (jackin-console)"
 kind = "rust"
 root = "crates/jackin-console"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-console/**/*.rs", "crates/jackin-console/Cargo.toml", "crates/jackin-console/benches/**", "crates/jackin-console/examples/**", "crates/jackin-console/src/**", "crates/jackin-console/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-console' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-console' && mbx nextest run --locked --all-features --package 'jackin-console' --no-tests pass", "cd -- 'crates/jackin-console' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-console' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-console' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-console' && mbx nextest run --locked --all-features --package 'jackin-console' --no-tests pass", "cd -- 'crates/jackin-console' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-console' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-console' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-console' && mbx nextest run --locked --all-features --package 'jackin-console' --no-tests pass", "cd -- 'crates/jackin-console' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-console' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-console' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-console' && mbx nextest run --locked --all-features --package 'jackin-console' --no-tests pass", "cd -- 'crates/jackin-console' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-console' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-console' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-console' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-console' -- -D warnings", "cd -- 'crates/jackin-console' && mbx nextest run --locked --all-features --package 'jackin-console' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-console' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-console' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-console' -- -D warnings", "cd -- 'crates/jackin-console' && mbx nextest run --locked --all-features --package 'jackin-console' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-console' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-console' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-console' -- -D warnings", "cd -- 'crates/jackin-console' && mbx nextest run --locked --all-features --package 'jackin-console' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-console' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-console' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-console' -- -D warnings", "cd -- 'crates/jackin-console' && mbx nextest run --locked --all-features --package 'jackin-console' --no-tests pass"]
 depends_on = ["rust-jackin-config", "rust-jackin-core", "rust-jackin-diagnostics", "rust-jackin-env", "rust-jackin-oppicker", "rust-jackin-otlp-testbed", "rust-jackin-protocol", "rust-jackin-telemetry", "rust-jackin-tui"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
@@ -188,10 +188,10 @@ label = "Rust crate (jackin-core)"
 kind = "rust"
 root = "crates/jackin-core"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-core/**/*.rs", "crates/jackin-core/Cargo.toml", "crates/jackin-core/benches/**", "crates/jackin-core/examples/**", "crates/jackin-core/src/**", "crates/jackin-core/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-core' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-core' && mbx nextest run --locked --all-features --package 'jackin-core' --no-tests pass", "cd -- 'crates/jackin-core' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-core' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-core' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-core' && mbx nextest run --locked --all-features --package 'jackin-core' --no-tests pass", "cd -- 'crates/jackin-core' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-core' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-core' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-core' && mbx nextest run --locked --all-features --package 'jackin-core' --no-tests pass", "cd -- 'crates/jackin-core' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-core' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-core' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-core' && mbx nextest run --locked --all-features --package 'jackin-core' --no-tests pass", "cd -- 'crates/jackin-core' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-core' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-core' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-core' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-core' -- -D warnings", "cd -- 'crates/jackin-core' && mbx nextest run --locked --all-features --package 'jackin-core' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-core' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-core' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-core' -- -D warnings", "cd -- 'crates/jackin-core' && mbx nextest run --locked --all-features --package 'jackin-core' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-core' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-core' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-core' -- -D warnings", "cd -- 'crates/jackin-core' && mbx nextest run --locked --all-features --package 'jackin-core' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-core' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-core' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-core' -- -D warnings", "cd -- 'crates/jackin-core' && mbx nextest run --locked --all-features --package 'jackin-core' --no-tests pass"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
 paths = ["~/.cargo/registry", "~/.cargo/git"]
@@ -202,10 +202,10 @@ label = "Rust crate (jackin-dev)"
 kind = "rust"
 root = "crates/jackin-dev"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-dev/**/*.rs", "crates/jackin-dev/Cargo.toml", "crates/jackin-dev/benches/**", "crates/jackin-dev/examples/**", "crates/jackin-dev/src/**", "crates/jackin-dev/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-dev' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-dev' && mbx nextest run --locked --all-features --package 'jackin-dev' --no-tests pass", "cd -- 'crates/jackin-dev' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-dev' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-dev' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-dev' && mbx nextest run --locked --all-features --package 'jackin-dev' --no-tests pass", "cd -- 'crates/jackin-dev' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-dev' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-dev' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-dev' && mbx nextest run --locked --all-features --package 'jackin-dev' --no-tests pass", "cd -- 'crates/jackin-dev' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-dev' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-dev' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-dev' && mbx nextest run --locked --all-features --package 'jackin-dev' --no-tests pass", "cd -- 'crates/jackin-dev' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-dev' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-dev' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-dev' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-dev' -- -D warnings", "cd -- 'crates/jackin-dev' && mbx nextest run --locked --all-features --package 'jackin-dev' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-dev' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-dev' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-dev' -- -D warnings", "cd -- 'crates/jackin-dev' && mbx nextest run --locked --all-features --package 'jackin-dev' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-dev' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-dev' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-dev' -- -D warnings", "cd -- 'crates/jackin-dev' && mbx nextest run --locked --all-features --package 'jackin-dev' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-dev' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-dev' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-dev' -- -D warnings", "cd -- 'crates/jackin-dev' && mbx nextest run --locked --all-features --package 'jackin-dev' --no-tests pass"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
 paths = ["~/.cargo/registry", "~/.cargo/git"]
@@ -216,10 +216,10 @@ label = "Rust crate (jackin-diagnostics)"
 kind = "rust"
 root = "crates/jackin-diagnostics"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-diagnostics/**/*.rs", "crates/jackin-diagnostics/Cargo.toml", "crates/jackin-diagnostics/benches/**", "crates/jackin-diagnostics/build.rs", "crates/jackin-diagnostics/examples/**", "crates/jackin-diagnostics/src/**", "crates/jackin-diagnostics/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-diagnostics' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-diagnostics' && mbx nextest run --locked --all-features --package 'jackin-diagnostics' --no-tests pass", "cd -- 'crates/jackin-diagnostics' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-diagnostics' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-diagnostics' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-diagnostics' && mbx nextest run --locked --all-features --package 'jackin-diagnostics' --no-tests pass", "cd -- 'crates/jackin-diagnostics' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-diagnostics' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-diagnostics' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-diagnostics' && mbx nextest run --locked --all-features --package 'jackin-diagnostics' --no-tests pass", "cd -- 'crates/jackin-diagnostics' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-diagnostics' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-diagnostics' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-diagnostics' && mbx nextest run --locked --all-features --package 'jackin-diagnostics' --no-tests pass", "cd -- 'crates/jackin-diagnostics' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-diagnostics' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-diagnostics' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-diagnostics' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-diagnostics' -- -D warnings", "cd -- 'crates/jackin-diagnostics' && mbx nextest run --locked --all-features --package 'jackin-diagnostics' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-diagnostics' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-diagnostics' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-diagnostics' -- -D warnings", "cd -- 'crates/jackin-diagnostics' && mbx nextest run --locked --all-features --package 'jackin-diagnostics' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-diagnostics' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-diagnostics' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-diagnostics' -- -D warnings", "cd -- 'crates/jackin-diagnostics' && mbx nextest run --locked --all-features --package 'jackin-diagnostics' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-diagnostics' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-diagnostics' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-diagnostics' -- -D warnings", "cd -- 'crates/jackin-diagnostics' && mbx nextest run --locked --all-features --package 'jackin-diagnostics' --no-tests pass"]
 depends_on = ["rust-jackin-core", "rust-jackin-otlp-testbed", "rust-jackin-telemetry"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
@@ -231,10 +231,10 @@ label = "Rust crate (jackin-docker)"
 kind = "rust"
 root = "crates/jackin-docker"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-docker/**/*.rs", "crates/jackin-docker/Cargo.toml", "crates/jackin-docker/benches/**", "crates/jackin-docker/build.rs", "crates/jackin-docker/examples/**", "crates/jackin-docker/src/**", "crates/jackin-docker/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-docker' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-docker' && mbx nextest run --locked --all-features --package 'jackin-docker' --no-tests pass", "cd -- 'crates/jackin-docker' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-docker' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-docker' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-docker' && mbx nextest run --locked --all-features --package 'jackin-docker' --no-tests pass", "cd -- 'crates/jackin-docker' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-docker' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-docker' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-docker' && mbx nextest run --locked --all-features --package 'jackin-docker' --no-tests pass", "cd -- 'crates/jackin-docker' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-docker' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-docker' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-docker' && mbx nextest run --locked --all-features --package 'jackin-docker' --no-tests pass", "cd -- 'crates/jackin-docker' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-docker' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-docker' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-docker' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-docker' -- -D warnings", "cd -- 'crates/jackin-docker' && mbx nextest run --locked --all-features --package 'jackin-docker' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-docker' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-docker' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-docker' -- -D warnings", "cd -- 'crates/jackin-docker' && mbx nextest run --locked --all-features --package 'jackin-docker' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-docker' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-docker' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-docker' -- -D warnings", "cd -- 'crates/jackin-docker' && mbx nextest run --locked --all-features --package 'jackin-docker' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-docker' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-docker' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-docker' -- -D warnings", "cd -- 'crates/jackin-docker' && mbx nextest run --locked --all-features --package 'jackin-docker' --no-tests pass"]
 depends_on = ["rust-jackin-build-meta", "rust-jackin-core", "rust-jackin-diagnostics", "rust-jackin-otlp-testbed", "rust-jackin-process", "rust-jackin-telemetry"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
@@ -246,10 +246,10 @@ label = "Rust crate (jackin-env)"
 kind = "rust"
 root = "crates/jackin-env"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-env/**/*.rs", "crates/jackin-env/Cargo.toml", "crates/jackin-env/benches/**", "crates/jackin-env/examples/**", "crates/jackin-env/src/**", "crates/jackin-env/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-env' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-env' && mbx nextest run --locked --all-features --package 'jackin-env' --no-tests pass", "cd -- 'crates/jackin-env' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-env' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-env' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-env' && mbx nextest run --locked --all-features --package 'jackin-env' --no-tests pass", "cd -- 'crates/jackin-env' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-env' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-env' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-env' && mbx nextest run --locked --all-features --package 'jackin-env' --no-tests pass", "cd -- 'crates/jackin-env' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-env' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-env' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-env' && mbx nextest run --locked --all-features --package 'jackin-env' --no-tests pass", "cd -- 'crates/jackin-env' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-env' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-env' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-env' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-env' -- -D warnings", "cd -- 'crates/jackin-env' && mbx nextest run --locked --all-features --package 'jackin-env' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-env' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-env' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-env' -- -D warnings", "cd -- 'crates/jackin-env' && mbx nextest run --locked --all-features --package 'jackin-env' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-env' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-env' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-env' -- -D warnings", "cd -- 'crates/jackin-env' && mbx nextest run --locked --all-features --package 'jackin-env' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-env' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-env' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-env' -- -D warnings", "cd -- 'crates/jackin-env' && mbx nextest run --locked --all-features --package 'jackin-env' --no-tests pass"]
 depends_on = ["rust-jackin-config", "rust-jackin-core", "rust-jackin-diagnostics", "rust-jackin-process", "rust-jackin-protocol", "rust-jackin-telemetry"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
@@ -261,10 +261,10 @@ label = "Rust crate (jackin-env-fuzz)"
 kind = "rust"
 root = "crates/jackin-env/fuzz"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-env/fuzz/**/*.rs", "crates/jackin-env/fuzz/Cargo.lock", "crates/jackin-env/fuzz/Cargo.toml", "crates/jackin-env/fuzz/benches/**", "crates/jackin-env/fuzz/examples/**", "crates/jackin-env/fuzz/src/**", "crates/jackin-env/fuzz/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-env/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-env/fuzz' && mbx nextest run --locked --all-features --package 'jackin-env-fuzz' --no-tests pass", "cd -- 'crates/jackin-env/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-env-fuzz' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-env/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-env/fuzz' && mbx nextest run --locked --all-features --package 'jackin-env-fuzz' --no-tests pass", "cd -- 'crates/jackin-env/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-env-fuzz' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-env/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-env/fuzz' && mbx nextest run --locked --all-features --package 'jackin-env-fuzz' --no-tests pass", "cd -- 'crates/jackin-env/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-env-fuzz' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-env/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-env/fuzz' && mbx nextest run --locked --all-features --package 'jackin-env-fuzz' --no-tests pass", "cd -- 'crates/jackin-env/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-env-fuzz' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-env/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-env/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-env-fuzz' -- -D warnings", "cd -- 'crates/jackin-env/fuzz' && mbx nextest run --locked --all-features --package 'jackin-env-fuzz' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-env/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-env/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-env-fuzz' -- -D warnings", "cd -- 'crates/jackin-env/fuzz' && mbx nextest run --locked --all-features --package 'jackin-env-fuzz' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-env/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-env/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-env-fuzz' -- -D warnings", "cd -- 'crates/jackin-env/fuzz' && mbx nextest run --locked --all-features --package 'jackin-env-fuzz' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-env/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-env/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-env-fuzz' -- -D warnings", "cd -- 'crates/jackin-env/fuzz' && mbx nextest run --locked --all-features --package 'jackin-env-fuzz' --no-tests pass"]
 depends_on = ["rust-jackin-config", "rust-jackin-core", "rust-jackin-env"]
 [unit.cache]
 key_files = [".cargo/**", "crates/jackin-env/fuzz/Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
@@ -276,10 +276,10 @@ label = "Rust crate (jackin-host)"
 kind = "rust"
 root = "crates/jackin-host"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-host/**/*.rs", "crates/jackin-host/Cargo.toml", "crates/jackin-host/benches/**", "crates/jackin-host/examples/**", "crates/jackin-host/src/**", "crates/jackin-host/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-host' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-host' && mbx nextest run --locked --all-features --package 'jackin-host' --no-tests pass", "cd -- 'crates/jackin-host' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-host' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-host' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-host' && mbx nextest run --locked --all-features --package 'jackin-host' --no-tests pass", "cd -- 'crates/jackin-host' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-host' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-host' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-host' && mbx nextest run --locked --all-features --package 'jackin-host' --no-tests pass", "cd -- 'crates/jackin-host' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-host' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-host' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-host' && mbx nextest run --locked --all-features --package 'jackin-host' --no-tests pass", "cd -- 'crates/jackin-host' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-host' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-host' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-host' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-host' -- -D warnings", "cd -- 'crates/jackin-host' && mbx nextest run --locked --all-features --package 'jackin-host' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-host' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-host' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-host' -- -D warnings", "cd -- 'crates/jackin-host' && mbx nextest run --locked --all-features --package 'jackin-host' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-host' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-host' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-host' -- -D warnings", "cd -- 'crates/jackin-host' && mbx nextest run --locked --all-features --package 'jackin-host' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-host' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-host' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-host' -- -D warnings", "cd -- 'crates/jackin-host' && mbx nextest run --locked --all-features --package 'jackin-host' --no-tests pass"]
 depends_on = ["rust-jackin-core", "rust-jackin-diagnostics", "rust-jackin-docker", "rust-jackin-process", "rust-jackin-protocol", "rust-jackin-telemetry", "rust-jackin-test-support"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
@@ -291,10 +291,10 @@ label = "Rust crate (jackin-image)"
 kind = "rust"
 root = "crates/jackin-image"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-agent-status/packs/amp.toml", "crates/jackin-agent-status/packs/claude.toml", "crates/jackin-agent-status/packs/codex.toml", "crates/jackin-agent-status/packs/grok.toml", "crates/jackin-agent-status/packs/kimi.toml", "crates/jackin-agent-status/packs/opencode.toml", "crates/jackin-image/**/*.rs", "crates/jackin-image/Cargo.toml", "crates/jackin-image/benches/**", "crates/jackin-image/build.rs", "crates/jackin-image/examples/**", "crates/jackin-image/src/**", "crates/jackin-image/tests/**", "docker/runtime/agent-status/hooks/claude/report-hook.sh", "docker/runtime/agent-status/hooks/codex/report-hook.sh", "docker/runtime/agent-status/hooks/opencode/plugin.js", "docker/runtime/agent-status/hooks/opencode/report-hook.sh", "docker/runtime/entrypoint.sh", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-image' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-image' && mbx nextest run --locked --all-features --package 'jackin-image' --no-tests pass", "cd -- 'crates/jackin-image' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-image' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-image' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-image' && mbx nextest run --locked --all-features --package 'jackin-image' --no-tests pass", "cd -- 'crates/jackin-image' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-image' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-image' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-image' && mbx nextest run --locked --all-features --package 'jackin-image' --no-tests pass", "cd -- 'crates/jackin-image' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-image' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-image' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-image' && mbx nextest run --locked --all-features --package 'jackin-image' --no-tests pass", "cd -- 'crates/jackin-image' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-image' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-image' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-image' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-image' -- -D warnings", "cd -- 'crates/jackin-image' && mbx nextest run --locked --all-features --package 'jackin-image' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-image' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-image' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-image' -- -D warnings", "cd -- 'crates/jackin-image' && mbx nextest run --locked --all-features --package 'jackin-image' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-image' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-image' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-image' -- -D warnings", "cd -- 'crates/jackin-image' && mbx nextest run --locked --all-features --package 'jackin-image' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-image' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-image' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-image' -- -D warnings", "cd -- 'crates/jackin-image' && mbx nextest run --locked --all-features --package 'jackin-image' --no-tests pass"]
 depends_on = ["rust-jackin-build-meta", "rust-jackin-core", "rust-jackin-diagnostics", "rust-jackin-docker", "rust-jackin-manifest", "rust-jackin-otlp-testbed", "rust-jackin-process", "rust-jackin-telemetry"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
@@ -306,10 +306,10 @@ label = "Rust crate (jackin-instance)"
 kind = "rust"
 root = "crates/jackin-instance"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-instance/**/*.rs", "crates/jackin-instance/Cargo.toml", "crates/jackin-instance/benches/**", "crates/jackin-instance/examples/**", "crates/jackin-instance/src/**", "crates/jackin-instance/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-instance' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-instance' && mbx nextest run --locked --all-features --package 'jackin-instance' --no-tests pass", "cd -- 'crates/jackin-instance' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-instance' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-instance' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-instance' && mbx nextest run --locked --all-features --package 'jackin-instance' --no-tests pass", "cd -- 'crates/jackin-instance' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-instance' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-instance' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-instance' && mbx nextest run --locked --all-features --package 'jackin-instance' --no-tests pass", "cd -- 'crates/jackin-instance' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-instance' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-instance' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-instance' && mbx nextest run --locked --all-features --package 'jackin-instance' --no-tests pass", "cd -- 'crates/jackin-instance' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-instance' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-instance' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-instance' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-instance' -- -D warnings", "cd -- 'crates/jackin-instance' && mbx nextest run --locked --all-features --package 'jackin-instance' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-instance' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-instance' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-instance' -- -D warnings", "cd -- 'crates/jackin-instance' && mbx nextest run --locked --all-features --package 'jackin-instance' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-instance' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-instance' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-instance' -- -D warnings", "cd -- 'crates/jackin-instance' && mbx nextest run --locked --all-features --package 'jackin-instance' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-instance' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-instance' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-instance' -- -D warnings", "cd -- 'crates/jackin-instance' && mbx nextest run --locked --all-features --package 'jackin-instance' --no-tests pass"]
 depends_on = ["rust-jackin-config", "rust-jackin-core", "rust-jackin-diagnostics", "rust-jackin-manifest", "rust-jackin-process", "rust-jackin-telemetry"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
@@ -321,10 +321,10 @@ label = "Rust crate (jackin-isolation)"
 kind = "rust"
 root = "crates/jackin-isolation"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-isolation/**/*.rs", "crates/jackin-isolation/Cargo.toml", "crates/jackin-isolation/benches/**", "crates/jackin-isolation/examples/**", "crates/jackin-isolation/src/**", "crates/jackin-isolation/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-isolation' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-isolation' && mbx nextest run --locked --all-features --package 'jackin-isolation' --no-tests pass", "cd -- 'crates/jackin-isolation' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-isolation' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-isolation' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-isolation' && mbx nextest run --locked --all-features --package 'jackin-isolation' --no-tests pass", "cd -- 'crates/jackin-isolation' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-isolation' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-isolation' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-isolation' && mbx nextest run --locked --all-features --package 'jackin-isolation' --no-tests pass", "cd -- 'crates/jackin-isolation' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-isolation' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-isolation' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-isolation' && mbx nextest run --locked --all-features --package 'jackin-isolation' --no-tests pass", "cd -- 'crates/jackin-isolation' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-isolation' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-isolation' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-isolation' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-isolation' -- -D warnings", "cd -- 'crates/jackin-isolation' && mbx nextest run --locked --all-features --package 'jackin-isolation' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-isolation' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-isolation' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-isolation' -- -D warnings", "cd -- 'crates/jackin-isolation' && mbx nextest run --locked --all-features --package 'jackin-isolation' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-isolation' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-isolation' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-isolation' -- -D warnings", "cd -- 'crates/jackin-isolation' && mbx nextest run --locked --all-features --package 'jackin-isolation' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-isolation' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-isolation' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-isolation' -- -D warnings", "cd -- 'crates/jackin-isolation' && mbx nextest run --locked --all-features --package 'jackin-isolation' --no-tests pass"]
 depends_on = ["rust-jackin-config", "rust-jackin-core", "rust-jackin-diagnostics", "rust-jackin-docker", "rust-jackin-process", "rust-jackin-protocol", "rust-jackin-telemetry", "rust-jackin-test-support"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
@@ -336,10 +336,10 @@ label = "Rust crate (jackin-launch)"
 kind = "rust"
 root = "crates/jackin-launch"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-launch/**/*.rs", "crates/jackin-launch/Cargo.toml", "crates/jackin-launch/benches/**", "crates/jackin-launch/build.rs", "crates/jackin-launch/examples/**", "crates/jackin-launch/src/**", "crates/jackin-launch/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-launch' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-launch' && mbx nextest run --locked --all-features --package 'jackin-launch' --no-tests pass", "cd -- 'crates/jackin-launch' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-launch' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-launch' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-launch' && mbx nextest run --locked --all-features --package 'jackin-launch' --no-tests pass", "cd -- 'crates/jackin-launch' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-launch' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-launch' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-launch' && mbx nextest run --locked --all-features --package 'jackin-launch' --no-tests pass", "cd -- 'crates/jackin-launch' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-launch' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-launch' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-launch' && mbx nextest run --locked --all-features --package 'jackin-launch' --no-tests pass", "cd -- 'crates/jackin-launch' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-launch' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-launch' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-launch' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-launch' -- -D warnings", "cd -- 'crates/jackin-launch' && mbx nextest run --locked --all-features --package 'jackin-launch' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-launch' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-launch' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-launch' -- -D warnings", "cd -- 'crates/jackin-launch' && mbx nextest run --locked --all-features --package 'jackin-launch' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-launch' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-launch' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-launch' -- -D warnings", "cd -- 'crates/jackin-launch' && mbx nextest run --locked --all-features --package 'jackin-launch' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-launch' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-launch' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-launch' -- -D warnings", "cd -- 'crates/jackin-launch' && mbx nextest run --locked --all-features --package 'jackin-launch' --no-tests pass"]
 depends_on = ["rust-jackin-brand", "rust-jackin-build-meta", "rust-jackin-core", "rust-jackin-diagnostics", "rust-jackin-telemetry", "rust-jackin-tui"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
@@ -351,10 +351,10 @@ label = "Rust crate (jackin-manifest)"
 kind = "rust"
 root = "crates/jackin-manifest"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-manifest/**/*.rs", "crates/jackin-manifest/Cargo.toml", "crates/jackin-manifest/benches/**", "crates/jackin-manifest/examples/**", "crates/jackin-manifest/src/**", "crates/jackin-manifest/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-manifest' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-manifest' && mbx nextest run --locked --all-features --package 'jackin-manifest' --no-tests pass", "cd -- 'crates/jackin-manifest' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-manifest' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-manifest' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-manifest' && mbx nextest run --locked --all-features --package 'jackin-manifest' --no-tests pass", "cd -- 'crates/jackin-manifest' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-manifest' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-manifest' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-manifest' && mbx nextest run --locked --all-features --package 'jackin-manifest' --no-tests pass", "cd -- 'crates/jackin-manifest' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-manifest' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-manifest' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-manifest' && mbx nextest run --locked --all-features --package 'jackin-manifest' --no-tests pass", "cd -- 'crates/jackin-manifest' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-manifest' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-manifest' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-manifest' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-manifest' -- -D warnings", "cd -- 'crates/jackin-manifest' && mbx nextest run --locked --all-features --package 'jackin-manifest' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-manifest' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-manifest' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-manifest' -- -D warnings", "cd -- 'crates/jackin-manifest' && mbx nextest run --locked --all-features --package 'jackin-manifest' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-manifest' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-manifest' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-manifest' -- -D warnings", "cd -- 'crates/jackin-manifest' && mbx nextest run --locked --all-features --package 'jackin-manifest' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-manifest' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-manifest' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-manifest' -- -D warnings", "cd -- 'crates/jackin-manifest' && mbx nextest run --locked --all-features --package 'jackin-manifest' --no-tests pass"]
 depends_on = ["rust-jackin-config", "rust-jackin-core"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
@@ -366,10 +366,10 @@ label = "Rust crate (jackin-manifest-fuzz)"
 kind = "rust"
 root = "crates/jackin-manifest/fuzz"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-manifest/fuzz/**/*.rs", "crates/jackin-manifest/fuzz/Cargo.lock", "crates/jackin-manifest/fuzz/Cargo.toml", "crates/jackin-manifest/fuzz/benches/**", "crates/jackin-manifest/fuzz/examples/**", "crates/jackin-manifest/fuzz/src/**", "crates/jackin-manifest/fuzz/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-manifest/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-manifest/fuzz' && mbx nextest run --locked --all-features --package 'jackin-manifest-fuzz' --no-tests pass", "cd -- 'crates/jackin-manifest/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-manifest-fuzz' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-manifest/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-manifest/fuzz' && mbx nextest run --locked --all-features --package 'jackin-manifest-fuzz' --no-tests pass", "cd -- 'crates/jackin-manifest/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-manifest-fuzz' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-manifest/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-manifest/fuzz' && mbx nextest run --locked --all-features --package 'jackin-manifest-fuzz' --no-tests pass", "cd -- 'crates/jackin-manifest/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-manifest-fuzz' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-manifest/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-manifest/fuzz' && mbx nextest run --locked --all-features --package 'jackin-manifest-fuzz' --no-tests pass", "cd -- 'crates/jackin-manifest/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-manifest-fuzz' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-manifest/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-manifest/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-manifest-fuzz' -- -D warnings", "cd -- 'crates/jackin-manifest/fuzz' && mbx nextest run --locked --all-features --package 'jackin-manifest-fuzz' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-manifest/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-manifest/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-manifest-fuzz' -- -D warnings", "cd -- 'crates/jackin-manifest/fuzz' && mbx nextest run --locked --all-features --package 'jackin-manifest-fuzz' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-manifest/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-manifest/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-manifest-fuzz' -- -D warnings", "cd -- 'crates/jackin-manifest/fuzz' && mbx nextest run --locked --all-features --package 'jackin-manifest-fuzz' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-manifest/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-manifest/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-manifest-fuzz' -- -D warnings", "cd -- 'crates/jackin-manifest/fuzz' && mbx nextest run --locked --all-features --package 'jackin-manifest-fuzz' --no-tests pass"]
 depends_on = ["rust-jackin-manifest"]
 [unit.cache]
 key_files = [".cargo/**", "crates/jackin-manifest/fuzz/Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
@@ -381,10 +381,10 @@ label = "Rust crate (jackin-oppicker)"
 kind = "rust"
 root = "crates/jackin-oppicker"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-oppicker/**/*.rs", "crates/jackin-oppicker/Cargo.toml", "crates/jackin-oppicker/benches/**", "crates/jackin-oppicker/examples/**", "crates/jackin-oppicker/src/**", "crates/jackin-oppicker/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-oppicker' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-oppicker' && mbx nextest run --locked --all-features --package 'jackin-oppicker' --no-tests pass", "cd -- 'crates/jackin-oppicker' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-oppicker' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-oppicker' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-oppicker' && mbx nextest run --locked --all-features --package 'jackin-oppicker' --no-tests pass", "cd -- 'crates/jackin-oppicker' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-oppicker' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-oppicker' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-oppicker' && mbx nextest run --locked --all-features --package 'jackin-oppicker' --no-tests pass", "cd -- 'crates/jackin-oppicker' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-oppicker' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-oppicker' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-oppicker' && mbx nextest run --locked --all-features --package 'jackin-oppicker' --no-tests pass", "cd -- 'crates/jackin-oppicker' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-oppicker' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-oppicker' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-oppicker' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-oppicker' -- -D warnings", "cd -- 'crates/jackin-oppicker' && mbx nextest run --locked --all-features --package 'jackin-oppicker' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-oppicker' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-oppicker' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-oppicker' -- -D warnings", "cd -- 'crates/jackin-oppicker' && mbx nextest run --locked --all-features --package 'jackin-oppicker' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-oppicker' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-oppicker' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-oppicker' -- -D warnings", "cd -- 'crates/jackin-oppicker' && mbx nextest run --locked --all-features --package 'jackin-oppicker' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-oppicker' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-oppicker' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-oppicker' -- -D warnings", "cd -- 'crates/jackin-oppicker' && mbx nextest run --locked --all-features --package 'jackin-oppicker' --no-tests pass"]
 depends_on = ["rust-jackin-core", "rust-jackin-telemetry"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
@@ -396,10 +396,10 @@ label = "Rust crate (jackin-otlp-testbed)"
 kind = "rust"
 root = "crates/jackin-otlp-testbed"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-otlp-testbed/**/*.rs", "crates/jackin-otlp-testbed/Cargo.toml", "crates/jackin-otlp-testbed/benches/**", "crates/jackin-otlp-testbed/examples/**", "crates/jackin-otlp-testbed/src/**", "crates/jackin-otlp-testbed/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-otlp-testbed' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-otlp-testbed' && mbx nextest run --locked --all-features --package 'jackin-otlp-testbed' --no-tests pass", "cd -- 'crates/jackin-otlp-testbed' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-otlp-testbed' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-otlp-testbed' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-otlp-testbed' && mbx nextest run --locked --all-features --package 'jackin-otlp-testbed' --no-tests pass", "cd -- 'crates/jackin-otlp-testbed' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-otlp-testbed' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-otlp-testbed' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-otlp-testbed' && mbx nextest run --locked --all-features --package 'jackin-otlp-testbed' --no-tests pass", "cd -- 'crates/jackin-otlp-testbed' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-otlp-testbed' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-otlp-testbed' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-otlp-testbed' && mbx nextest run --locked --all-features --package 'jackin-otlp-testbed' --no-tests pass", "cd -- 'crates/jackin-otlp-testbed' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-otlp-testbed' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-otlp-testbed' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-otlp-testbed' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-otlp-testbed' -- -D warnings", "cd -- 'crates/jackin-otlp-testbed' && mbx nextest run --locked --all-features --package 'jackin-otlp-testbed' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-otlp-testbed' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-otlp-testbed' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-otlp-testbed' -- -D warnings", "cd -- 'crates/jackin-otlp-testbed' && mbx nextest run --locked --all-features --package 'jackin-otlp-testbed' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-otlp-testbed' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-otlp-testbed' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-otlp-testbed' -- -D warnings", "cd -- 'crates/jackin-otlp-testbed' && mbx nextest run --locked --all-features --package 'jackin-otlp-testbed' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-otlp-testbed' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-otlp-testbed' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-otlp-testbed' -- -D warnings", "cd -- 'crates/jackin-otlp-testbed' && mbx nextest run --locked --all-features --package 'jackin-otlp-testbed' --no-tests pass"]
 depends_on = ["rust-jackin-telemetry"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
@@ -411,10 +411,10 @@ label = "Rust crate (jackin-pr-trailers)"
 kind = "rust"
 root = "crates/jackin-pr-trailers"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-pr-trailers/**/*.rs", "crates/jackin-pr-trailers/Cargo.toml", "crates/jackin-pr-trailers/benches/**", "crates/jackin-pr-trailers/examples/**", "crates/jackin-pr-trailers/src/**", "crates/jackin-pr-trailers/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-pr-trailers' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-pr-trailers' && mbx nextest run --locked --all-features --package 'jackin-pr-trailers' --no-tests pass", "cd -- 'crates/jackin-pr-trailers' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-pr-trailers' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-pr-trailers' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-pr-trailers' && mbx nextest run --locked --all-features --package 'jackin-pr-trailers' --no-tests pass", "cd -- 'crates/jackin-pr-trailers' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-pr-trailers' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-pr-trailers' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-pr-trailers' && mbx nextest run --locked --all-features --package 'jackin-pr-trailers' --no-tests pass", "cd -- 'crates/jackin-pr-trailers' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-pr-trailers' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-pr-trailers' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-pr-trailers' && mbx nextest run --locked --all-features --package 'jackin-pr-trailers' --no-tests pass", "cd -- 'crates/jackin-pr-trailers' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-pr-trailers' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-pr-trailers' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-pr-trailers' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-pr-trailers' -- -D warnings", "cd -- 'crates/jackin-pr-trailers' && mbx nextest run --locked --all-features --package 'jackin-pr-trailers' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-pr-trailers' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-pr-trailers' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-pr-trailers' -- -D warnings", "cd -- 'crates/jackin-pr-trailers' && mbx nextest run --locked --all-features --package 'jackin-pr-trailers' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-pr-trailers' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-pr-trailers' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-pr-trailers' -- -D warnings", "cd -- 'crates/jackin-pr-trailers' && mbx nextest run --locked --all-features --package 'jackin-pr-trailers' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-pr-trailers' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-pr-trailers' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-pr-trailers' -- -D warnings", "cd -- 'crates/jackin-pr-trailers' && mbx nextest run --locked --all-features --package 'jackin-pr-trailers' --no-tests pass"]
 depends_on = ["rust-jackin-process"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
@@ -426,10 +426,10 @@ label = "Rust crate (jackin-process)"
 kind = "rust"
 root = "crates/jackin-process"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-process/**/*.rs", "crates/jackin-process/Cargo.toml", "crates/jackin-process/benches/**", "crates/jackin-process/examples/**", "crates/jackin-process/src/**", "crates/jackin-process/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-process' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-process' && mbx nextest run --locked --all-features --package 'jackin-process' --no-tests pass", "cd -- 'crates/jackin-process' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-process' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-process' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-process' && mbx nextest run --locked --all-features --package 'jackin-process' --no-tests pass", "cd -- 'crates/jackin-process' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-process' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-process' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-process' && mbx nextest run --locked --all-features --package 'jackin-process' --no-tests pass", "cd -- 'crates/jackin-process' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-process' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-process' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-process' && mbx nextest run --locked --all-features --package 'jackin-process' --no-tests pass", "cd -- 'crates/jackin-process' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-process' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-process' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-process' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-process' -- -D warnings", "cd -- 'crates/jackin-process' && mbx nextest run --locked --all-features --package 'jackin-process' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-process' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-process' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-process' -- -D warnings", "cd -- 'crates/jackin-process' && mbx nextest run --locked --all-features --package 'jackin-process' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-process' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-process' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-process' -- -D warnings", "cd -- 'crates/jackin-process' && mbx nextest run --locked --all-features --package 'jackin-process' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-process' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-process' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-process' -- -D warnings", "cd -- 'crates/jackin-process' && mbx nextest run --locked --all-features --package 'jackin-process' --no-tests pass"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
 paths = ["~/.cargo/registry", "~/.cargo/git"]
@@ -440,10 +440,10 @@ label = "Rust crate (jackin-protocol)"
 kind = "rust"
 root = "crates/jackin-protocol"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-protocol/**/*.rs", "crates/jackin-protocol/Cargo.toml", "crates/jackin-protocol/benches/**", "crates/jackin-protocol/examples/**", "crates/jackin-protocol/src/**", "crates/jackin-protocol/tests/**", "crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-protocol' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-protocol' && mbx nextest run --locked --all-features --package 'jackin-protocol' --no-tests pass", "cd -- 'crates/jackin-protocol' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-protocol' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-protocol' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-protocol' && mbx nextest run --locked --all-features --package 'jackin-protocol' --no-tests pass", "cd -- 'crates/jackin-protocol' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-protocol' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-protocol' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-protocol' && mbx nextest run --locked --all-features --package 'jackin-protocol' --no-tests pass", "cd -- 'crates/jackin-protocol' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-protocol' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-protocol' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-protocol' && mbx nextest run --locked --all-features --package 'jackin-protocol' --no-tests pass", "cd -- 'crates/jackin-protocol' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-protocol' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-protocol' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-protocol' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-protocol' -- -D warnings", "cd -- 'crates/jackin-protocol' && mbx nextest run --locked --all-features --package 'jackin-protocol' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-protocol' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-protocol' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-protocol' -- -D warnings", "cd -- 'crates/jackin-protocol' && mbx nextest run --locked --all-features --package 'jackin-protocol' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-protocol' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-protocol' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-protocol' -- -D warnings", "cd -- 'crates/jackin-protocol' && mbx nextest run --locked --all-features --package 'jackin-protocol' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-protocol' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-protocol' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-protocol' -- -D warnings", "cd -- 'crates/jackin-protocol' && mbx nextest run --locked --all-features --package 'jackin-protocol' --no-tests pass"]
 depends_on = ["rust-jackin-core", "rust-jackin-telemetry"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
@@ -455,10 +455,10 @@ label = "Rust crate (jackin-protocol-fuzz)"
 kind = "rust"
 root = "crates/jackin-protocol/fuzz"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-protocol/fuzz/**/*.rs", "crates/jackin-protocol/fuzz/Cargo.lock", "crates/jackin-protocol/fuzz/Cargo.toml", "crates/jackin-protocol/fuzz/benches/**", "crates/jackin-protocol/fuzz/examples/**", "crates/jackin-protocol/fuzz/src/**", "crates/jackin-protocol/fuzz/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-protocol/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-protocol/fuzz' && mbx nextest run --locked --all-features --package 'jackin-protocol-fuzz' --no-tests pass", "cd -- 'crates/jackin-protocol/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-protocol-fuzz' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-protocol/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-protocol/fuzz' && mbx nextest run --locked --all-features --package 'jackin-protocol-fuzz' --no-tests pass", "cd -- 'crates/jackin-protocol/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-protocol-fuzz' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-protocol/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-protocol/fuzz' && mbx nextest run --locked --all-features --package 'jackin-protocol-fuzz' --no-tests pass", "cd -- 'crates/jackin-protocol/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-protocol-fuzz' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-protocol/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-protocol/fuzz' && mbx nextest run --locked --all-features --package 'jackin-protocol-fuzz' --no-tests pass", "cd -- 'crates/jackin-protocol/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-protocol-fuzz' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-protocol/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-protocol/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-protocol-fuzz' -- -D warnings", "cd -- 'crates/jackin-protocol/fuzz' && mbx nextest run --locked --all-features --package 'jackin-protocol-fuzz' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-protocol/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-protocol/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-protocol-fuzz' -- -D warnings", "cd -- 'crates/jackin-protocol/fuzz' && mbx nextest run --locked --all-features --package 'jackin-protocol-fuzz' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-protocol/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-protocol/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-protocol-fuzz' -- -D warnings", "cd -- 'crates/jackin-protocol/fuzz' && mbx nextest run --locked --all-features --package 'jackin-protocol-fuzz' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-protocol/fuzz' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-protocol/fuzz' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-protocol-fuzz' -- -D warnings", "cd -- 'crates/jackin-protocol/fuzz' && mbx nextest run --locked --all-features --package 'jackin-protocol-fuzz' --no-tests pass"]
 depends_on = ["rust-jackin-protocol"]
 [unit.cache]
 key_files = [".cargo/**", "crates/jackin-protocol/fuzz/Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
@@ -470,10 +470,10 @@ label = "Rust crate (jackin-runtime)"
 kind = "rust"
 root = "crates/jackin-runtime"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-runtime/**/*.rs", "crates/jackin-runtime/Cargo.toml", "crates/jackin-runtime/benches/**", "crates/jackin-runtime/build.rs", "crates/jackin-runtime/examples/**", "crates/jackin-runtime/src/**", "crates/jackin-runtime/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-runtime' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-runtime' && mbx nextest run --locked --all-features --package 'jackin-runtime' --no-tests pass", "cd -- 'crates/jackin-runtime' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-runtime' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-runtime' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-runtime' && mbx nextest run --locked --all-features --package 'jackin-runtime' --no-tests pass", "cd -- 'crates/jackin-runtime' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-runtime' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-runtime' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-runtime' && mbx nextest run --locked --all-features --package 'jackin-runtime' --no-tests pass", "cd -- 'crates/jackin-runtime' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-runtime' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-runtime' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-runtime' && mbx nextest run --locked --all-features --package 'jackin-runtime' --no-tests pass", "cd -- 'crates/jackin-runtime' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-runtime' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-runtime' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-runtime' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-runtime' -- -D warnings", "cd -- 'crates/jackin-runtime' && mbx nextest run --locked --all-features --package 'jackin-runtime' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-runtime' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-runtime' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-runtime' -- -D warnings", "cd -- 'crates/jackin-runtime' && mbx nextest run --locked --all-features --package 'jackin-runtime' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-runtime' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-runtime' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-runtime' -- -D warnings", "cd -- 'crates/jackin-runtime' && mbx nextest run --locked --all-features --package 'jackin-runtime' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-runtime' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-runtime' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-runtime' -- -D warnings", "cd -- 'crates/jackin-runtime' && mbx nextest run --locked --all-features --package 'jackin-runtime' --no-tests pass"]
 depends_on = ["rust-jackin-brand", "rust-jackin-build-meta", "rust-jackin-config", "rust-jackin-core", "rust-jackin-diagnostics", "rust-jackin-docker", "rust-jackin-env", "rust-jackin-host", "rust-jackin-image", "rust-jackin-instance", "rust-jackin-isolation", "rust-jackin-launch", "rust-jackin-manifest", "rust-jackin-otlp-testbed", "rust-jackin-process", "rust-jackin-protocol", "rust-jackin-telemetry", "rust-jackin-test-support", "rust-jackin-usage"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
@@ -485,10 +485,10 @@ label = "Rust crate (jackin-telemetry)"
 kind = "rust"
 root = "crates/jackin-telemetry"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-telemetry/**/*.rs", "crates/jackin-telemetry/Cargo.toml", "crates/jackin-telemetry/benches/**", "crates/jackin-telemetry/examples/**", "crates/jackin-telemetry/src/**", "crates/jackin-telemetry/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-telemetry' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-telemetry' && mbx nextest run --locked --all-features --package 'jackin-telemetry' --no-tests pass", "cd -- 'crates/jackin-telemetry' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-telemetry' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-telemetry' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-telemetry' && mbx nextest run --locked --all-features --package 'jackin-telemetry' --no-tests pass", "cd -- 'crates/jackin-telemetry' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-telemetry' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-telemetry' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-telemetry' && mbx nextest run --locked --all-features --package 'jackin-telemetry' --no-tests pass", "cd -- 'crates/jackin-telemetry' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-telemetry' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-telemetry' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-telemetry' && mbx nextest run --locked --all-features --package 'jackin-telemetry' --no-tests pass", "cd -- 'crates/jackin-telemetry' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-telemetry' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-telemetry' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-telemetry' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-telemetry' -- -D warnings", "cd -- 'crates/jackin-telemetry' && mbx nextest run --locked --all-features --package 'jackin-telemetry' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-telemetry' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-telemetry' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-telemetry' -- -D warnings", "cd -- 'crates/jackin-telemetry' && mbx nextest run --locked --all-features --package 'jackin-telemetry' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-telemetry' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-telemetry' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-telemetry' -- -D warnings", "cd -- 'crates/jackin-telemetry' && mbx nextest run --locked --all-features --package 'jackin-telemetry' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-telemetry' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-telemetry' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-telemetry' -- -D warnings", "cd -- 'crates/jackin-telemetry' && mbx nextest run --locked --all-features --package 'jackin-telemetry' --no-tests pass"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
 paths = ["~/.cargo/registry", "~/.cargo/git"]
@@ -499,10 +499,10 @@ label = "Rust crate (jackin-test-support)"
 kind = "rust"
 root = "crates/jackin-test-support"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-test-support/**/*.rs", "crates/jackin-test-support/Cargo.toml", "crates/jackin-test-support/benches/**", "crates/jackin-test-support/examples/**", "crates/jackin-test-support/src/**", "crates/jackin-test-support/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-test-support' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-test-support' && mbx nextest run --locked --all-features --package 'jackin-test-support' --no-tests pass", "cd -- 'crates/jackin-test-support' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-test-support' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-test-support' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-test-support' && mbx nextest run --locked --all-features --package 'jackin-test-support' --no-tests pass", "cd -- 'crates/jackin-test-support' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-test-support' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-test-support' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-test-support' && mbx nextest run --locked --all-features --package 'jackin-test-support' --no-tests pass", "cd -- 'crates/jackin-test-support' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-test-support' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-test-support' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-test-support' && mbx nextest run --locked --all-features --package 'jackin-test-support' --no-tests pass", "cd -- 'crates/jackin-test-support' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-test-support' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-test-support' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-test-support' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-test-support' -- -D warnings", "cd -- 'crates/jackin-test-support' && mbx nextest run --locked --all-features --package 'jackin-test-support' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-test-support' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-test-support' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-test-support' -- -D warnings", "cd -- 'crates/jackin-test-support' && mbx nextest run --locked --all-features --package 'jackin-test-support' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-test-support' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-test-support' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-test-support' -- -D warnings", "cd -- 'crates/jackin-test-support' && mbx nextest run --locked --all-features --package 'jackin-test-support' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-test-support' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-test-support' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-test-support' -- -D warnings", "cd -- 'crates/jackin-test-support' && mbx nextest run --locked --all-features --package 'jackin-test-support' --no-tests pass"]
 depends_on = ["rust-jackin-core", "rust-jackin-manifest"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
@@ -514,10 +514,10 @@ label = "Rust crate (jackin-tui)"
 kind = "rust"
 root = "crates/jackin-tui"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-tui/**/*.rs", "crates/jackin-tui/Cargo.toml", "crates/jackin-tui/benches/**", "crates/jackin-tui/examples/**", "crates/jackin-tui/src/**", "crates/jackin-tui/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-tui' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-tui' && mbx nextest run --locked --all-features --package 'jackin-tui' --no-tests pass", "cd -- 'crates/jackin-tui' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-tui' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-tui' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-tui' && mbx nextest run --locked --all-features --package 'jackin-tui' --no-tests pass", "cd -- 'crates/jackin-tui' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-tui' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-tui' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-tui' && mbx nextest run --locked --all-features --package 'jackin-tui' --no-tests pass", "cd -- 'crates/jackin-tui' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-tui' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-tui' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-tui' && mbx nextest run --locked --all-features --package 'jackin-tui' --no-tests pass", "cd -- 'crates/jackin-tui' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-tui' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-tui' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-tui' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-tui' -- -D warnings", "cd -- 'crates/jackin-tui' && mbx nextest run --locked --all-features --package 'jackin-tui' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-tui' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-tui' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-tui' -- -D warnings", "cd -- 'crates/jackin-tui' && mbx nextest run --locked --all-features --package 'jackin-tui' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-tui' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-tui' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-tui' -- -D warnings", "cd -- 'crates/jackin-tui' && mbx nextest run --locked --all-features --package 'jackin-tui' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-tui' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-tui' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-tui' -- -D warnings", "cd -- 'crates/jackin-tui' && mbx nextest run --locked --all-features --package 'jackin-tui' --no-tests pass"]
 depends_on = ["rust-jackin-brand"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
@@ -529,10 +529,10 @@ label = "Rust crate (jackin-usage)"
 kind = "rust"
 root = "crates/jackin-usage"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-usage/**/*.rs", "crates/jackin-usage/Cargo.toml", "crates/jackin-usage/benches/**", "crates/jackin-usage/examples/**", "crates/jackin-usage/src/**", "crates/jackin-usage/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-usage' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-usage' && mbx nextest run --locked --all-features --package 'jackin-usage' --no-tests pass", "cd -- 'crates/jackin-usage' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-usage' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-usage' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-usage' && mbx nextest run --locked --all-features --package 'jackin-usage' --no-tests pass", "cd -- 'crates/jackin-usage' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-usage' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-usage' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-usage' && mbx nextest run --locked --all-features --package 'jackin-usage' --no-tests pass", "cd -- 'crates/jackin-usage' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-usage' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-usage' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-usage' && mbx nextest run --locked --all-features --package 'jackin-usage' --no-tests pass", "cd -- 'crates/jackin-usage' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-usage' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-usage' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-usage' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-usage' -- -D warnings", "cd -- 'crates/jackin-usage' && mbx nextest run --locked --all-features --package 'jackin-usage' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-usage' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-usage' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-usage' -- -D warnings", "cd -- 'crates/jackin-usage' && mbx nextest run --locked --all-features --package 'jackin-usage' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-usage' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-usage' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-usage' -- -D warnings", "cd -- 'crates/jackin-usage' && mbx nextest run --locked --all-features --package 'jackin-usage' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-usage' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-usage' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-usage' -- -D warnings", "cd -- 'crates/jackin-usage' && mbx nextest run --locked --all-features --package 'jackin-usage' --no-tests pass"]
 depends_on = ["rust-jackin-config", "rust-jackin-core", "rust-jackin-diagnostics", "rust-jackin-otlp-testbed", "rust-jackin-protocol", "rust-jackin-telemetry"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
@@ -544,10 +544,10 @@ label = "Rust crate (jackin-usage-ffi)"
 kind = "rust"
 root = "crates/jackin-usage-ffi"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-usage-ffi/**/*.rs", "crates/jackin-usage-ffi/Cargo.toml", "crates/jackin-usage-ffi/benches/**", "crates/jackin-usage-ffi/examples/**", "crates/jackin-usage-ffi/src/**", "crates/jackin-usage-ffi/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-usage-ffi' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-usage-ffi' && mbx nextest run --locked --all-features --package 'jackin-usage-ffi' --no-tests pass", "cd -- 'crates/jackin-usage-ffi' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-usage-ffi' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-usage-ffi' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-usage-ffi' && mbx nextest run --locked --all-features --package 'jackin-usage-ffi' --no-tests pass", "cd -- 'crates/jackin-usage-ffi' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-usage-ffi' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-usage-ffi' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-usage-ffi' && mbx nextest run --locked --all-features --package 'jackin-usage-ffi' --no-tests pass", "cd -- 'crates/jackin-usage-ffi' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-usage-ffi' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-usage-ffi' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-usage-ffi' && mbx nextest run --locked --all-features --package 'jackin-usage-ffi' --no-tests pass", "cd -- 'crates/jackin-usage-ffi' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-usage-ffi' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-usage-ffi' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-usage-ffi' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-usage-ffi' -- -D warnings", "cd -- 'crates/jackin-usage-ffi' && mbx nextest run --locked --all-features --package 'jackin-usage-ffi' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-usage-ffi' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-usage-ffi' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-usage-ffi' -- -D warnings", "cd -- 'crates/jackin-usage-ffi' && mbx nextest run --locked --all-features --package 'jackin-usage-ffi' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-usage-ffi' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-usage-ffi' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-usage-ffi' -- -D warnings", "cd -- 'crates/jackin-usage-ffi' && mbx nextest run --locked --all-features --package 'jackin-usage-ffi' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-usage-ffi' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-usage-ffi' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-usage-ffi' -- -D warnings", "cd -- 'crates/jackin-usage-ffi' && mbx nextest run --locked --all-features --package 'jackin-usage-ffi' --no-tests pass"]
 depends_on = ["rust-jackin-config", "rust-jackin-core", "rust-jackin-env", "rust-jackin-protocol", "rust-jackin-telemetry", "rust-jackin-usage"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
@@ -559,10 +559,10 @@ label = "Rust crate (jackin-xtask)"
 kind = "rust"
 root = "crates/jackin-xtask"
 watch = [".cargo/**", "Cargo.lock", "Cargo.toml", "crates/jackin-xtask/**/*.rs", "crates/jackin-xtask/Cargo.toml", "crates/jackin-xtask/benches/**", "crates/jackin-xtask/examples/**", "crates/jackin-xtask/src/**", "crates/jackin-xtask/tests/**", "mise.lock", "mise.toml", "rust-toolchain", "rust-toolchain.toml"]
-github_pr_commands = ["cd -- 'crates/jackin-xtask' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-xtask' && mbx nextest run --locked --all-features --package 'jackin-xtask' --no-tests pass", "cd -- 'crates/jackin-xtask' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-xtask' -- -D warnings"]
-github_full_commands = ["cd -- 'crates/jackin-xtask' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-xtask' && mbx nextest run --locked --all-features --package 'jackin-xtask' --no-tests pass", "cd -- 'crates/jackin-xtask' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-xtask' -- -D warnings"]
-velnor_pr_commands = ["cd -- 'crates/jackin-xtask' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-xtask' && mbx nextest run --locked --all-features --package 'jackin-xtask' --no-tests pass", "cd -- 'crates/jackin-xtask' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-xtask' -- -D warnings"]
-velnor_full_commands = ["cd -- 'crates/jackin-xtask' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-xtask' && mbx nextest run --locked --all-features --package 'jackin-xtask' --no-tests pass", "cd -- 'crates/jackin-xtask' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-xtask' -- -D warnings"]
+github_pr_commands = ["cd -- 'crates/jackin-xtask' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-xtask' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-xtask' -- -D warnings", "cd -- 'crates/jackin-xtask' && mbx nextest run --locked --all-features --package 'jackin-xtask' --no-tests pass"]
+github_full_commands = ["cd -- 'crates/jackin-xtask' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-xtask' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-xtask' -- -D warnings", "cd -- 'crates/jackin-xtask' && mbx nextest run --locked --all-features --package 'jackin-xtask' --no-tests pass"]
+velnor_pr_commands = ["cd -- 'crates/jackin-xtask' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-xtask' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-xtask' -- -D warnings", "cd -- 'crates/jackin-xtask' && mbx nextest run --locked --all-features --package 'jackin-xtask' --no-tests pass"]
+velnor_full_commands = ["cd -- 'crates/jackin-xtask' && mbx fmt --manifest-path 'Cargo.toml' -- --check", "cd -- 'crates/jackin-xtask' && mbx clippy --locked --profile test --all-targets --all-features --package 'jackin-xtask' -- -D warnings", "cd -- 'crates/jackin-xtask' && mbx nextest run --locked --all-features --package 'jackin-xtask' --no-tests pass"]
 depends_on = ["rust-jackin-process"]
 [unit.cache]
 key_files = [".cargo/**", "Cargo.lock", "rust-toolchain.toml", "rust-toolchain"]
diff --git a/.github/workflows/ci-main.yml b/.github/workflows/ci-main.yml
index 62a3a38f..60f7542e 100644
--- a/.github/workflows/ci-main.yml
+++ b/.github/workflows/ci-main.yml
@@ -60,11 +60,11 @@ jobs:
       - name: Set up Velnor workflow runtime
         id: runtime
         if: ${{ runner.environment == 'github-hosted' }}
-        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
+        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@4f70cf741179e5a499a6e43f62dac2f913188b35
         with:
-          rev: 4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
+          rev: 4f70cf741179e5a499a6e43f62dac2f913188b35
       - name: Set trusted workflow policy revision
-        run: echo "VELNOR_WORKFLOW_POLICY_REVISION=4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d" >> "$GITHUB_ENV"
+        run: echo "VELNOR_WORKFLOW_POLICY_REVISION=4f70cf741179e5a499a6e43f62dac2f913188b35" >> "$GITHUB_ENV"
       - name: Select affected units
         id: plan
         env:
@@ -80,7 +80,7 @@ jobs:
       - name: Prepare Velnor workflow runtime
         shell: bash
         env:
-          EXPECTED_REVISION: 4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
+          EXPECTED_REVISION: 4f70cf741179e5a499a6e43f62dac2f913188b35
         run: |
           set -euo pipefail
           stage="$RUNNER_TEMP/velnor-workflow-runtime"
@@ -99,7 +99,7 @@ jobs:
       - name: Publish Velnor workflow runtime
         uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
         with:
-          name: velnor-workflow-runtime-4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d-${{ runner.os }}-${{ runner.arch }}
+          name: velnor-workflow-runtime-4f70cf741179e5a499a6e43f62dac2f913188b35-${{ runner.os }}-${{ runner.arch }}
           path: ${{ runner.temp }}/velnor-workflow-runtime
           if-no-files-found: error
           retention-days: 7
@@ -138,9 +138,9 @@ jobs:
           fi
           git checkout --quiet --detach "$HEAD_SHA"
       - name: Set up Velnor workflow runtime
-        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
+        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@4f70cf741179e5a499a6e43f62dac2f913188b35
         with:
-          rev: 4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
+          rev: 4f70cf741179e5a499a6e43f62dac2f913188b35
           checkout-path: ${{ github.workspace }}/policy-checkout
       - name: Read declared generator pin
         id: pin
@@ -153,13 +153,13 @@ jobs:
           echo "value=$pin" >> "$GITHUB_OUTPUT"
       - name: Set up declared generator product
         id: renderer
-        if: steps.pin.outputs.value != '4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d'
-        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
+        if: steps.pin.outputs.value != '4f70cf741179e5a499a6e43f62dac2f913188b35'
+        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@4f70cf741179e5a499a6e43f62dac2f913188b35
         with:
           rev: ${{ steps.pin.outputs.value }}
           checkout-path: ${{ github.workspace }}/policy-checkout
       - name: Resolve declared generator product
-        if: steps.pin.outputs.value != '4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d'
+        if: steps.pin.outputs.value != '4f70cf741179e5a499a6e43f62dac2f913188b35'
         run: |
           set -euo pipefail
           binary="$HOME/.cache/velnor/workflow-runtime/${{ steps.renderer.outputs.closure }}/bin/velnor-workflow"
@@ -193,7 +193,7 @@ jobs:
           WORKFLOW_ROOT: ${{ github.workspace }}/policy-checkout
           HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
           BASE_SHA: ${{ github.event.pull_request.base.sha || github.sha }}
-          VELNOR_WORKFLOW_POLICY_REVISION: 4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
+          VELNOR_WORKFLOW_POLICY_REVISION: 4f70cf741179e5a499a6e43f62dac2f913188b35
         run: |
           set -euo pipefail
           velnor-workflow policy \
diff --git a/.github/workflows/ci-policy.yml b/.github/workflows/ci-policy.yml
index e9eba78d..6cba0e76 100644
--- a/.github/workflows/ci-policy.yml
+++ b/.github/workflows/ci-policy.yml
@@ -49,9 +49,9 @@ jobs:
           fi
           git checkout --quiet --detach "$HEAD_SHA"
       - name: Set up Velnor workflow runtime
-        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
+        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@4f70cf741179e5a499a6e43f62dac2f913188b35
         with:
-          rev: 4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
+          rev: 4f70cf741179e5a499a6e43f62dac2f913188b35
           checkout-path: ${{ github.workspace }}/policy-checkout
       - name: Read declared generator pin
         id: pin
@@ -64,13 +64,13 @@ jobs:
           echo "value=$pin" >> "$GITHUB_OUTPUT"
       - name: Set up declared generator product
         id: renderer
-        if: steps.pin.outputs.value != '4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d'
-        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
+        if: steps.pin.outputs.value != '4f70cf741179e5a499a6e43f62dac2f913188b35'
+        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@4f70cf741179e5a499a6e43f62dac2f913188b35
         with:
           rev: ${{ steps.pin.outputs.value }}
           checkout-path: ${{ github.workspace }}/policy-checkout
       - name: Resolve declared generator product
-        if: steps.pin.outputs.value != '4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d'
+        if: steps.pin.outputs.value != '4f70cf741179e5a499a6e43f62dac2f913188b35'
         run: |
           set -euo pipefail
           binary="$HOME/.cache/velnor/workflow-runtime/${{ steps.renderer.outputs.closure }}/bin/velnor-workflow"
@@ -104,7 +104,7 @@ jobs:
           WORKFLOW_ROOT: ${{ github.workspace }}/policy-checkout
           HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
           BASE_SHA: ${{ github.event.pull_request.base.sha || github.sha }}
-          VELNOR_WORKFLOW_POLICY_REVISION: 4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
+          VELNOR_WORKFLOW_POLICY_REVISION: 4f70cf741179e5a499a6e43f62dac2f913188b35
         run: |
           set -euo pipefail
           velnor-workflow policy \
diff --git a/.github/workflows/ci-pr.yml b/.github/workflows/ci-pr.yml
index 5a24d5aa..6f2449c6 100644
--- a/.github/workflows/ci-pr.yml
+++ b/.github/workflows/ci-pr.yml
@@ -59,11 +59,11 @@ jobs:
       - name: Set up Velnor workflow runtime
         id: runtime
         if: ${{ runner.environment == 'github-hosted' }}
-        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
+        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@4f70cf741179e5a499a6e43f62dac2f913188b35
         with:
-          rev: 4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
+          rev: 4f70cf741179e5a499a6e43f62dac2f913188b35
       - name: Set trusted workflow policy revision
-        run: echo "VELNOR_WORKFLOW_POLICY_REVISION=4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d" >> "$GITHUB_ENV"
+        run: echo "VELNOR_WORKFLOW_POLICY_REVISION=4f70cf741179e5a499a6e43f62dac2f913188b35" >> "$GITHUB_ENV"
       - name: Select affected units
         id: plan
         env:
@@ -79,7 +79,7 @@ jobs:
       - name: Prepare Velnor workflow runtime
         shell: bash
         env:
-          EXPECTED_REVISION: 4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
+          EXPECTED_REVISION: 4f70cf741179e5a499a6e43f62dac2f913188b35
         run: |
           set -euo pipefail
           stage="$RUNNER_TEMP/velnor-workflow-runtime"
@@ -98,13 +98,13 @@ jobs:
       - name: Publish Velnor workflow runtime
         uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
         with:
-          name: velnor-workflow-runtime-4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d-${{ runner.os }}-${{ runner.arch }}
+          name: velnor-workflow-runtime-4f70cf741179e5a499a6e43f62dac2f913188b35-${{ runner.os }}-${{ runner.arch }}
           path: ${{ runner.temp }}/velnor-workflow-runtime
           if-no-files-found: error
           retention-days: 7
   github-bun-jackin-docs:
     name: "Bun · Bun package (jackin-docs)"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',bun-jackin-docs,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',bun-jackin-docs,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-bun.yml
     with:
@@ -122,7 +122,7 @@ jobs:
       unit_admission: github
   github-docker-docker-construct:
     name: "Docker · Docker (docker/construct)"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',docker-docker-construct,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',docker-docker-construct,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-docker.yml
     with:
@@ -136,7 +136,7 @@ jobs:
       unit_admission: github
   github-rust-arrayref:
     name: "Rust · arrayref"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-arrayref,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-arrayref,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -176,7 +176,7 @@ jobs:
       unit_admission: github
   github-rust-jackin:
     name: "Rust · jackin"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -344,7 +344,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-agent-status:
     name: "Rust · jackin-agent-status"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-agent-status,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-agent-status,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -400,7 +400,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-brand:
     name: "Rust · jackin-brand"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-brand,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-brand,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -439,7 +439,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-build-meta:
     name: "Rust · jackin-build-meta"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-build-meta,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-build-meta,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -484,7 +484,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-capsule:
     name: "Rust · jackin-capsule"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-capsule,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-capsule,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -588,7 +588,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-config:
     name: "Rust · jackin-config"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-config,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-config,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -649,7 +649,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-config-fuzz:
     name: "Rust · jackin-config-fuzz"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-config-fuzz,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-config-fuzz,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -717,7 +717,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-console:
     name: "Rust · jackin-console"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-console,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-console,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -814,7 +814,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-core:
     name: "Rust · jackin-core"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-core,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-core,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -853,7 +853,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-dev:
     name: "Rust · jackin-dev"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-dev,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-dev,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -892,7 +892,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-diagnostics:
     name: "Rust · jackin-diagnostics"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-diagnostics,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-diagnostics,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -948,7 +948,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-docker:
     name: "Rust · jackin-docker"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-docker,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-docker,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -1020,7 +1020,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-env:
     name: "Rust · jackin-env"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-env,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-env,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -1097,7 +1097,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-env-fuzz:
     name: "Rust · jackin-env-fuzz"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-env-fuzz,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-env-fuzz,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -1181,7 +1181,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-host:
     name: "Rust · jackin-host"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-host,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-host,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -1279,7 +1279,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-image:
     name: "Rust · jackin-image"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-image,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-image,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -1372,7 +1372,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-instance:
     name: "Rust · jackin-instance"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-instance,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-instance,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -1448,7 +1448,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-isolation:
     name: "Rust · jackin-isolation"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-isolation,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-isolation,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -1546,7 +1546,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-launch:
     name: "Rust · jackin-launch"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-launch,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-launch,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -1628,7 +1628,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-manifest:
     name: "Rust · jackin-manifest"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-manifest,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-manifest,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -1694,7 +1694,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-manifest-fuzz:
     name: "Rust · jackin-manifest-fuzz"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-manifest-fuzz,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-manifest-fuzz,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -1767,7 +1767,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-oppicker:
     name: "Rust · jackin-oppicker"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-oppicker,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-oppicker,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -1817,7 +1817,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-otlp-testbed:
     name: "Rust · jackin-otlp-testbed"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-otlp-testbed,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-otlp-testbed,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -1862,7 +1862,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-pr-trailers:
     name: "Rust · jackin-pr-trailers"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-pr-trailers,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-pr-trailers,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -1907,7 +1907,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-process:
     name: "Rust · jackin-process"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-process,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-process,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -1946,7 +1946,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-protocol:
     name: "Rust · jackin-protocol"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-protocol,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-protocol,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -1997,7 +1997,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-protocol-fuzz:
     name: "Rust · jackin-protocol-fuzz"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-protocol-fuzz,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-protocol-fuzz,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -2055,7 +2055,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-runtime:
     name: "Rust · jackin-runtime"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-runtime,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-runtime,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -2206,7 +2206,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-telemetry:
     name: "Rust · jackin-telemetry"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-telemetry,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-telemetry,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -2245,7 +2245,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-test-support:
     name: "Rust · jackin-test-support"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-test-support,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-test-support,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -2316,7 +2316,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-tui:
     name: "Rust · jackin-tui"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-tui,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-tui,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -2361,7 +2361,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-usage:
     name: "Rust · jackin-usage"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-usage,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-usage,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -2433,7 +2433,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-usage-ffi:
     name: "Rust · jackin-usage-ffi"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-usage-ffi,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-usage-ffi,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -2520,7 +2520,7 @@ jobs:
       unit_admission: github
   github-rust-jackin-xtask:
     name: "Rust · jackin-xtask"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-xtask,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-jackin-xtask,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -2565,7 +2565,7 @@ jobs:
       unit_admission: github
   github-rust-policy:
     name: "Rust · Rust dependency policy"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-policy,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',rust-policy,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-rust.yml
     with:
@@ -2600,7 +2600,7 @@ jobs:
       unit_admission: github
   github-swift-package-native-design-prototypes-unifiedagentusage:
     name: "Swift · Apple · Swift package (native/Design/Prototypes/UnifiedAgentUsage)"
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',swift-package-native-design-prototypes-unifiedagentusage,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',swift-package-native-design-prototypes-unifiedagentusage,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-swift.yml
     with:
@@ -2620,7 +2620,7 @@ jobs:
       unit_admission: github
   github-swift-package-native:
     name: "Swift · Apple · "
-    if: ${{ always() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',swift-package-native,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
+    if: ${{ !cancelled() && needs.plan.result == 'success' && contains(format(',{0},', needs.plan.outputs.units), ',swift-package-native,') && (github.event_name == 'pull_request' || (github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'schedule')) || (github.event_name == 'workflow_dispatch' && (github.event.inputs.runner == 'github' || github.event.inputs.runner == 'both' || github.event.inputs.runner == ''))) }}
     needs: [plan]
     uses: ./.github/workflows/ci-unit-swift.yml
     with:
@@ -2637,7 +2637,7 @@ jobs:
       unit_admission: github
   ci-required:
     name: ci-required
-    if: ${{ always() }}
+    if: ${{ !cancelled() }}
     needs: [plan, github-bun-jackin-docs, github-docker-docker-construct, github-rust-arrayref, github-rust-jackin, github-rust-jackin-agent-status, github-rust-jackin-brand, github-rust-jackin-build-meta, github-rust-jackin-capsule, github-rust-jackin-config, github-rust-jackin-config-fuzz, github-rust-jackin-console, github-rust-jackin-core, github-rust-jackin-dev, github-rust-jackin-diagnostics, github-rust-jackin-docker, github-rust-jackin-env, github-rust-jackin-env-fuzz, github-rust-jackin-host, github-rust-jackin-image, github-rust-jackin-instance, github-rust-jackin-isolation, github-rust-jackin-launch, github-rust-jackin-manifest, github-rust-jackin-manifest-fuzz, github-rust-jackin-oppicker, github-rust-jackin-otlp-testbed, github-rust-jackin-pr-trailers, github-rust-jackin-process, github-rust-jackin-protocol, github-rust-jackin-protocol-fuzz, github-rust-jackin-runtime, github-rust-jackin-telemetry, github-rust-jackin-test-support, github-rust-jackin-tui, github-rust-jackin-usage, github-rust-jackin-usage-ffi, github-rust-jackin-xtask, github-rust-policy, github-swift-package-native-design-prototypes-unifiedagentusage, github-swift-package-native]
     runs-on: ubuntu-26.04
     timeout-minutes: 5
@@ -3461,7 +3461,7 @@ jobs:
           fi
   required:
     name: "Control / Required"
-    if: ${{ always() }}
+    if: ${{ !cancelled() }}
     needs: [ci-required]
     runs-on: ubuntu-26.04
     timeout-minutes: 5
diff --git a/.github/workflows/ci-unit-bun.yml b/.github/workflows/ci-unit-bun.yml
index d68c0a2a..38eed4c2 100644
--- a/.github/workflows/ci-unit-bun.yml
+++ b/.github/workflows/ci-unit-bun.yml
@@ -163,12 +163,12 @@ jobs:
       - name: Download Velnor workflow runtime
         uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
         with:
-          name: velnor-workflow-runtime-4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d-${{ runner.os }}-${{ runner.arch }}
+          name: velnor-workflow-runtime-4f70cf741179e5a499a6e43f62dac2f913188b35-${{ runner.os }}-${{ runner.arch }}
           path: .velnor-workflow-runtime
       - name: Verify Velnor workflow runtime
         shell: bash
         env:
-          EXPECTED_REVISION: 4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
+          EXPECTED_REVISION: 4f70cf741179e5a499a6e43f62dac2f913188b35
         run: |
           set -euo pipefail
           manifest=.velnor-workflow-runtime/manifest.json
@@ -182,7 +182,7 @@ jobs:
       - name: Add Velnor workflow runtime to PATH
         shell: bash
         env:
-          EXPECTED_REVISION: 4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
+          EXPECTED_REVISION: 4f70cf741179e5a499a6e43f62dac2f913188b35
         run: |
           set -euo pipefail
           home="$RUNNER_TEMP/velnor-workflow-runtime-artifact"
@@ -317,20 +317,33 @@ jobs:
             fi
           fi
           exit $rc
+      - name: Mark BUNDLE cache-save start
+        if: always() && ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
+        run: |
+          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
+          umask 077
+          mkdir -p "$timing_dir"
+          marker="$timing_dir/CACHE_BUNDLE_SAVE_STARTED"
+          if [[ ! -e "$marker" ]]; then
+            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
+              chmod 0444 "$marker" 2>/dev/null || true
+            fi
+          fi
       - name: Save unit cache
+        id: unit_cache_save
         if: always() && ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
         uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
         with:
           path: |
             ${{ inputs.cache_paths }}
           key: ci-${{ runner.os }}-${{ runner.arch }}-bun-${{ hashFiles(inputs.cache_key_files) }}
-      - name: Mark cleanup end
-        if: always()
+      - name: Mark BUNDLE cache-save end
+        if: always() && (always() && ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true') && steps.unit_cache_save.outcome == 'success'
         run: |
           timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
           umask 077
           mkdir -p "$timing_dir"
-          marker="$timing_dir/CLEANUP_ENDED"
+          marker="$timing_dir/CACHE_BUNDLE_SAVE_ENDED"
           if [[ ! -e "$marker" ]]; then
             if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
               chmod 0444 "$marker" 2>/dev/null || true
@@ -338,7 +351,7 @@ jobs:
           fi
       - name: Report phase timings and cache outcomes
         if: always()
-        uses: tailrocks/velnor/.github/actions/report-velnor-ci-outcomes@4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
+        uses: tailrocks/velnor/.github/actions/report-velnor-ci-outcomes@4f70cf741179e5a499a6e43f62dac2f913188b35
         with:
           job_label: ${{ inputs.unit }}
           ci_lane: github
diff --git a/.github/workflows/ci-unit-docker.yml b/.github/workflows/ci-unit-docker.yml
index 2d5f130f..699fc08e 100644
--- a/.github/workflows/ci-unit-docker.yml
+++ b/.github/workflows/ci-unit-docker.yml
@@ -163,12 +163,12 @@ jobs:
       - name: Download Velnor workflow runtime
         uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
         with:
-          name: velnor-workflow-runtime-4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d-${{ runner.os }}-${{ runner.arch }}
+          name: velnor-workflow-runtime-4f70cf741179e5a499a6e43f62dac2f913188b35-${{ runner.os }}-${{ runner.arch }}
           path: .velnor-workflow-runtime
       - name: Verify Velnor workflow runtime
         shell: bash
         env:
-          EXPECTED_REVISION: 4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
+          EXPECTED_REVISION: 4f70cf741179e5a499a6e43f62dac2f913188b35
         run: |
           set -euo pipefail
           manifest=.velnor-workflow-runtime/manifest.json
@@ -182,7 +182,7 @@ jobs:
       - name: Add Velnor workflow runtime to PATH
         shell: bash
         env:
-          EXPECTED_REVISION: 4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
+          EXPECTED_REVISION: 4f70cf741179e5a499a6e43f62dac2f913188b35
         run: |
           set -euo pipefail
           home="$RUNNER_TEMP/velnor-workflow-runtime-artifact"
@@ -311,21 +311,9 @@ jobs:
             fi
           fi
           exit $rc
-      - name: Mark cleanup end
-        if: always()
-        run: |
-          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
-          umask 077
-          mkdir -p "$timing_dir"
-          marker="$timing_dir/CLEANUP_ENDED"
-          if [[ ! -e "$marker" ]]; then
-            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
-              chmod 0444 "$marker" 2>/dev/null || true
-            fi
-          fi
       - name: Report phase timings and cache outcomes
         if: always()
-        uses: tailrocks/velnor/.github/actions/report-velnor-ci-outcomes@4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
+        uses: tailrocks/velnor/.github/actions/report-velnor-ci-outcomes@4f70cf741179e5a499a6e43f62dac2f913188b35
         with:
           job_label: ${{ inputs.unit }}
           ci_lane: github
diff --git a/.github/workflows/ci-unit-rust.yml b/.github/workflows/ci-unit-rust.yml
index eb322482..32f28a2e 100644
--- a/.github/workflows/ci-unit-rust.yml
+++ b/.github/workflows/ci-unit-rust.yml
@@ -163,12 +163,12 @@ jobs:
       - name: Download Velnor workflow runtime
         uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
         with:
-          name: velnor-workflow-runtime-4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d-${{ runner.os }}-${{ runner.arch }}
+          name: velnor-workflow-runtime-4f70cf741179e5a499a6e43f62dac2f913188b35-${{ runner.os }}-${{ runner.arch }}
           path: .velnor-workflow-runtime
       - name: Verify Velnor workflow runtime
         shell: bash
         env:
-          EXPECTED_REVISION: 4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
+          EXPECTED_REVISION: 4f70cf741179e5a499a6e43f62dac2f913188b35
         run: |
           set -euo pipefail
           manifest=.velnor-workflow-runtime/manifest.json
@@ -182,7 +182,7 @@ jobs:
       - name: Add Velnor workflow runtime to PATH
         shell: bash
         env:
-          EXPECTED_REVISION: 4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
+          EXPECTED_REVISION: 4f70cf741179e5a499a6e43f62dac2f913188b35
         run: |
           set -euo pipefail
           home="$RUNNER_TEMP/velnor-workflow-runtime-artifact"
@@ -466,20 +466,33 @@ jobs:
             fi
           fi
           exit $rc
+      - name: Mark BUNDLE cache-save start
+        if: always() && ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
+        run: |
+          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
+          umask 077
+          mkdir -p "$timing_dir"
+          marker="$timing_dir/CACHE_BUNDLE_SAVE_STARTED"
+          if [[ ! -e "$marker" ]]; then
+            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
+              chmod 0444 "$marker" 2>/dev/null || true
+            fi
+          fi
       - name: Save unit cache
+        id: unit_cache_save
         if: always() && ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
         uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
         with:
           path: |
             ${{ inputs.cache_paths }}
           key: ci-${{ runner.os }}-${{ runner.arch }}-rust-${{ hashFiles(inputs.cache_key_files) }}
-      - name: Mark cleanup end
-        if: always()
+      - name: Mark BUNDLE cache-save end
+        if: always() && (always() && ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true') && steps.unit_cache_save.outcome == 'success'
         run: |
           timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
           umask 077
           mkdir -p "$timing_dir"
-          marker="$timing_dir/CLEANUP_ENDED"
+          marker="$timing_dir/CACHE_BUNDLE_SAVE_ENDED"
           if [[ ! -e "$marker" ]]; then
             if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
               chmod 0444 "$marker" 2>/dev/null || true
@@ -487,7 +500,7 @@ jobs:
           fi
       - name: Report phase timings and cache outcomes
         if: always()
-        uses: tailrocks/velnor/.github/actions/report-velnor-ci-outcomes@4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
+        uses: tailrocks/velnor/.github/actions/report-velnor-ci-outcomes@4f70cf741179e5a499a6e43f62dac2f913188b35
         with:
           job_label: ${{ inputs.unit }}
           ci_lane: github
diff --git a/.github/workflows/ci-unit-swift.yml b/.github/workflows/ci-unit-swift.yml
index 2f6206b9..96c8de92 100644
--- a/.github/workflows/ci-unit-swift.yml
+++ b/.github/workflows/ci-unit-swift.yml
@@ -163,11 +163,11 @@ jobs:
       - name: Set up Velnor workflow runtime
         id: runtime
         if: ${{ runner.environment == 'github-hosted' }}
-        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
+        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@4f70cf741179e5a499a6e43f62dac2f913188b35
         with:
-          rev: 4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
+          rev: 4f70cf741179e5a499a6e43f62dac2f913188b35
       - name: Set trusted workflow policy revision
-        run: echo "VELNOR_WORKFLOW_POLICY_REVISION=4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d" >> "$GITHUB_ENV"
+        run: echo "VELNOR_WORKFLOW_POLICY_REVISION=4f70cf741179e5a499a6e43f62dac2f913188b35" >> "$GITHUB_ENV"
       - name: Mark runner setup end
         if: always()
         run: |
@@ -298,20 +298,34 @@ jobs:
             fi
           fi
           exit $rc
-      - name: Save unit cache
+      - name: Mark BUNDLE cache-save start
         if: ${{ inputs.cache_key_files != '' && (always() && ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true') }}
+        run: |
+          timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
+          umask 077
+          mkdir -p "$timing_dir"
+          marker="$timing_dir/CACHE_BUNDLE_SAVE_STARTED"
+          if [[ ! -e "$marker" ]]; then
+            if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
+              chmod 0444 "$marker" 2>/dev/null || true
+            fi
+          fi
+      - name: Save unit cache
+        if: ${{ inputs.cache_key_files != '' }}
+        id: unit_cache_save
+        if: always() && ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true'
         uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
         with:
           path: |
             ${{ inputs.cache_paths }}
           key: ci-${{ runner.os }}-${{ runner.arch }}-swift-${{ hashFiles(inputs.cache_key_files) }}
-      - name: Mark cleanup end
-        if: always()
+      - name: Mark BUNDLE cache-save end
+        if: ${{ inputs.cache_key_files != '' && (always() && (always() && ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.cache.outputs.cache-hit != 'true') && steps.unit_cache_save.outcome == 'success') }}
         run: |
           timing_dir="$RUNNER_TEMP/velnor-ci-timing-${GITHUB_RUN_ID:-unknown}-${GITHUB_RUN_ATTEMPT:-0}-${GITHUB_JOB:-unknown}"
           umask 077
           mkdir -p "$timing_dir"
-          marker="$timing_dir/CLEANUP_ENDED"
+          marker="$timing_dir/CACHE_BUNDLE_SAVE_ENDED"
           if [[ ! -e "$marker" ]]; then
             if (set -C; printf '%s\n' "$(date +%s)" > "$marker") 2>/dev/null; then
               chmod 0444 "$marker" 2>/dev/null || true
@@ -319,7 +333,7 @@ jobs:
           fi
       - name: Report phase timings and cache outcomes
         if: always()
-        uses: tailrocks/velnor/.github/actions/report-velnor-ci-outcomes@4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
+        uses: tailrocks/velnor/.github/actions/report-velnor-ci-outcomes@4f70cf741179e5a499a6e43f62dac2f913188b35
         with:
           job_label: ${{ inputs.unit }}
           ci_lane: github
diff --git a/.github/workflows/desktop-merge.yml b/.github/workflows/desktop-merge.yml
index d9faf33f..5217eec5 100644
--- a/.github/workflows/desktop-merge.yml
+++ b/.github/workflows/desktop-merge.yml
@@ -23,20 +23,46 @@ jobs:
       CARGO_TERM_COLOR: always
       DEVELOPER_DIR: "/Applications/Xcode_26.6.app/Contents/Developer"
       MACOSX_DEPLOYMENT_TARGET: "26.0"
+      MISE_AUTO_INSTALL: "false"
+      MISE_EXEC_AUTO_INSTALL: "false"
+      MISE_NOT_FOUND_AUTO_INSTALL: "false"
+      MISE_TASK_RUN_AUTO_INSTALL: "false"
+      RUSTUP_TOOLCHAIN: "1.97.1"
     steps:
       - name: Checkout repository
         uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
         with:
           persist-credentials: false
-      - name: Set up Mise tools
-        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
+      - name: Restore Rust toolchain
+        id: rustup-toolchain
+        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
         with:
-          install_args: "cargo-binstall rust aqua:nextest-rs/nextest/cargo-nextest cargo:sccache cargo:boltffi_cli xcodegen swiftlint xcbeautify ripgrep"
-          cache: true
-          cache_save: ${{ (github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main') }}
+          path: |
+            ~/.rustup
+          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
+      - name: Provision Rust toolchain
+        shell: bash
+        run: |
+          set -euo pipefail
+          rustup toolchain install
+          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
+      - name: Save Rust toolchain
+        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
+        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
+        with:
+          path: |
+            ~/.rustup
+          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
+      - name: Activate pinned Rust shims
+        shell: bash
+        run: |
+          set -euo pipefail
+          printf '%s\n' "$HOME/.cargo/bin" >> "$GITHUB_PATH"
       - name: Set up Mise
         uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
         with:
-          install: false
+          install_args: "cargo-binstall aqua:nextest-rs/nextest/cargo-nextest cargo:sccache cargo:boltffi_cli xcodegen swiftlint xcbeautify ripgrep"
+          cache: true
+          cache_save: ${{ (github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main') }}
       - name: Run desktop-merge
         run: mise run desktop-merge
diff --git a/.github/workflows/desktop-scheduled.yml b/.github/workflows/desktop-scheduled.yml
index 06209ab6..bcc91387 100644
--- a/.github/workflows/desktop-scheduled.yml
+++ b/.github/workflows/desktop-scheduled.yml
@@ -23,20 +23,46 @@ jobs:
       CARGO_TERM_COLOR: always
       DEVELOPER_DIR: "/Applications/Xcode_26.6.app/Contents/Developer"
       MACOSX_DEPLOYMENT_TARGET: "26.0"
+      MISE_AUTO_INSTALL: "false"
+      MISE_EXEC_AUTO_INSTALL: "false"
+      MISE_NOT_FOUND_AUTO_INSTALL: "false"
+      MISE_TASK_RUN_AUTO_INSTALL: "false"
+      RUSTUP_TOOLCHAIN: "1.97.1"
     steps:
       - name: Checkout repository
         uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
         with:
           persist-credentials: false
-      - name: Set up Mise tools
-        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
+      - name: Restore Rust toolchain
+        id: rustup-toolchain
+        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
         with:
-          install_args: "cargo-binstall rust aqua:nextest-rs/nextest/cargo-nextest cargo:sccache cargo:boltffi_cli xcodegen swiftlint xcbeautify periphery ripgrep"
-          cache: true
-          cache_save: ${{ (github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main') }}
+          path: |
+            ~/.rustup
+          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
+      - name: Provision Rust toolchain
+        shell: bash
+        run: |
+          set -euo pipefail
+          rustup toolchain install
+          rustup target add 'aarch64-unknown-linux-gnu' 'x86_64-unknown-linux-gnu'
+      - name: Save Rust toolchain
+        if: ((github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main')) && steps.rustup-toolchain.outputs.cache-hit != 'true'
+        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
+        with:
+          path: |
+            ~/.rustup
+          key: velnor-rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml', 'rust-toolchain') }}
+      - name: Activate pinned Rust shims
+        shell: bash
+        run: |
+          set -euo pipefail
+          printf '%s\n' "$HOME/.cargo/bin" >> "$GITHUB_PATH"
       - name: Set up Mise
         uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
         with:
-          install: false
+          install_args: "cargo-binstall aqua:nextest-rs/nextest/cargo-nextest cargo:sccache cargo:boltffi_cli xcodegen swiftlint xcbeautify periphery ripgrep"
+          cache: true
+          cache_save: ${{ (github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'schedule' || (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main') }}
       - name: Run desktop-scheduled
         run: mise run desktop-scheduled
diff --git a/.github/workflows/maintenance.yml b/.github/workflows/maintenance.yml
index bdf1917e..6db5a0d6 100644
--- a/.github/workflows/maintenance.yml
+++ b/.github/workflows/maintenance.yml
@@ -113,12 +113,12 @@ jobs:
       - name: Set up Velnor workflow runtime
         id: runtime
         if: ${{ steps.retention-gate.outputs.skip != 'true' && runner.environment == 'github-hosted' }}
-        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
+        uses: tailrocks/velnor/.github/actions/setup-velnor-workflow@4f70cf741179e5a499a6e43f62dac2f913188b35
         with:
-          rev: 4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d
+          rev: 4f70cf741179e5a499a6e43f62dac2f913188b35
       - name: Set trusted workflow policy revision
         if: steps.retention-gate.outputs.skip != 'true'
-        run: echo "VELNOR_WORKFLOW_POLICY_REVISION=4fa7a3a85f141a6bb95bc9bdf0eef9e3ddde165d" >> "$GITHUB_ENV"
+        run: echo "VELNOR_WORKFLOW_POLICY_REVISION=4f70cf741179e5a499a6e43f62dac2f913188b35" >> "$GITHUB_ENV"
       - name: Sweep closed-PR merge-ref caches
         if: steps.retention-gate.outputs.skip != 'true'
         env:
diff --git a/.github/workflows/renovate-upstream-sources.yml b/.github/workflows/renovate-upstream-sources.yml
index f46835bf..a08b7ec2 100644
--- a/.github/workflows/renovate-upstream-sources.yml
+++ b/.github/workflows/renovate-upstream-sources.yml
@@ -21,6 +21,9 @@ jobs:
     runs-on: ubuntu-26.04
     timeout-minutes: 10
     env:
+      MISE_AUTO_INSTALL: "false"
+      MISE_EXEC_AUTO_INSTALL: "false"
+      MISE_NOT_FOUND_AUTO_INSTALL: "false"
       MISE_TASK_RUN_AUTO_INSTALL: "false"
     steps:
       - name: Checkout repository
