# Generator pin review

Reviewed `/private/tmp/velnor-pin0dc-21799655` read-only. The isolated
checkout is detached at `217996550d737fc2d26b99f08be7e938c7b4af43`; its source
config changes `[generator].revision` from `325719f1e05d3d46322c9fd3eeb9ad545e175638`
to `0dc79895ff1c5e88be7c3822c437e1c5b5282e12`. The exact generated artifact is
`/private/tmp/velnor-pin0dc-21799655-generated-3.patch`, SHA-256
`a59ccad09705e17ccd971af9518be8f39e1963bf6d0c7056352c495ac9685146`, with 13
paths. It updates every consumer runtime and policy revision reference.

## Verdict

**HOLD.** This is a global runtime downgrade, not an old-base-validator-only
slot. The current source renderer reports revision `217996550d737fc2d26b99f08be7e938c7b4af43`
and closure `a67d55386f0034c7ac4c86ac12db2aaba47f8d670704700c1898a831f8fc0aa0`.
The current renderer's `--check --default-branch main` exits zero but explicitly
warns that the tree matches the candidate render, not the declared `0dc` render.

I built a clean `0dc79895` source archive in
`/private/tmp/velnor-old0dc-review-20260920`. Its `plan --config` accepts the
current schema-3 `.github/ci/project.toml` and reports all 17 units, so basic
runtime project parsing is compatible. Its `--check --default-branch main`
against the pin checkout exits 1 with generated differences in `actionlint`,
ownership state, `project.toml`, main/PR/runtime-product workflows, every
unit-family workflow, preview, and release. Its policy invocation likewise
reports `FAIL generated-tree` for those paths. The candidate route remains
present in the generated policy, but cannot make this global execution pin
safe: normal main/consumer execution still uses the old renderer, and the old
renderer cannot validate the current generated tree.

The old render proves concrete semantic regressions:

- `.github/ci/project.toml` changes Mr. Boxington from `1.12.0` to `1.11.1`
  and removes root Bun watch globs `**/*.js`, `**/*.json`, `**/*.jsx`,
  `**/*.ts`, and `**/*.tsx`.
- Rust units put `nextest` before `clippy`; current `217` output puts clippy
  before tests.
- Runtime products route the Apple builder to `macos-15`; current output uses
  `macos-26`.
- All Rust/preview/release generated jobs use the old Mr. Boxington action
  `v1.3.1`/`1.11.1` instead of pinned `v1.4.0`/`1.12.0`.
- The source range `0dc..325` also contains the e94/325 package-release
  verification, source-bound setup, and immutable consumer-update fixes. A
  consumer using those primitives would lose those fixes under the old
  runtime.

## Bounded executable follow-up

The exact follow-up was run against `/private/tmp/velnor-ci-integration` at
`df9fb272c025f76cc8711560209afcdfd6cc4e00`. The old-render `--check` failure
is not, by itself, a runtime regression: a `0dc` renderer necessarily writes
different generated bytes. The old published binary's runtime project parser
does accept the current generated schema-3 project. Its `plan --config`
output is byte-identical to the `325` binary: all 17 units and
`plan_digest=1ef6d65741969647`.

There is a concrete policy incompatibility for package-release consumers. I
generated the same package-release fixture with the `325` binary at
`/private/tmp/velnor-pin-policy-fixture-20260920b`. The publish job contains
`uses: ./source/.github/actions/setup-velnor-workflow`; the current policy
accepts this exact source-checkout action, while the published `0dc` policy
reports:

```
FAIL action-pins  .github/workflows/preview.yml: job publish: action is not a full SHA pin: ./source/.github/actions/setup-velnor-workflow
```

The current package-release integration tests pass (3/3), including
`package_release_owner_publish_uses_source_checkout_for_runtime_action` and
`package_release_hook_renders_and_passes_policy`. Velnor's own current
`release`/`preview` render does not use the `source/` checkout path, so this is
not a failure in the present owner workflow; it is a real failure for any
consumer rendered by the generic package-release primitive. A global runtime
pin must cover both surfaces.

The executable source range is larger than a renderer-only diff: e94 adds
declared verification-task validation and execution, locked task-tool setup,
source-bound runtime setup, and verification at producer/handoff/published
boundaries; 325 replaces consumer branch rewriting with immutable branch
handling, stale-PR closure, untracked-output staging, and cached whitespace
validation. These changes live in
`crates/velnor-workflow/src/s2/primitives/package_release.rs`. The selected
`s2/policy.rs` change is the exact source-checkout action allowance above.
The old runtime accepts ordinary `plan` semantics, but its policy cannot
accept the package-release workflow emitted by the current generator.

## Validation

- `MISE_NO_CONFIG=1 mise exec actionlint@1.7.12 -- actionlint`: **PASS** on
  the current generated tree.
- Current `217` generator `mbx run --locked --manifest-path
  crates/velnor-workflow/Cargo.toml -- --plain --check --default-branch main .`:
  **PASS with candidate-render notice**.
- Current isolated `velnor-workflow` package contract:
  `MISE_TRUSTED_CONFIG_PATHS=/private/tmp/velnor-pin0dc-21799655 mbx nextest
  run --locked --all-features --package velnor-workflow --no-tests pass`:
  **1919 passed, 1 leaky, 0 skipped**.
- Old `0dc` `plan --config` current project: **PASS**, 17 units.
- Old `0dc` `--check` and policy generated-tree validation: **FAIL** as above.
- Old `0dc` policy on the current package-release fixture: **FAIL
  action-pins** for `./source/.github/actions/setup-velnor-workflow`; current
  `325` policy on the same fixture: **PASS action-pins**.
- Old and current `plan --config` on the current Velnor project: **PASS**, exact
  17-unit output and digest match.

## Required shape

Keep the old validator only in an explicit policy/bootstrap slot, with its
candidate manifest path and trust checks. Keep all consumer planning,
execution, release, preview, and runtime-product jobs on a published runtime
that renders the current tree. Do not globally set `[generator].revision` to
`0dc` until the current renderer's execution semantics are either incorporated
into the published base product or the generated workflow carries distinct,
independently validated base-policy and consumer-runtime identities.
