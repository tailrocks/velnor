# Required admission verdict independent review

Shared ir.rs patch changes selected-but-unadmitted verdict from accepting skipped to failure. Correct: runtime.rs1500–1560 computes effective providers, platform/trust eligibility and explicit exclusions before freezing PlannedUnit.providers. No supported reason to erase a frozen selected obligation downstream. Required prerequisite selection uses plan_expects_local, so hosted-only selection does not mistakenly require local cargo warming. Trusted-only/local caller admissions follow the same trusted-event predicate sent to the planner.

Regression executes actual emitted Bash with false/empty/unknown admission values: selected+skipped fails; empty selected set+skipped passes. Existing expected-set tests cover successful selected jobs and missing/skipped/cancelled cases. Source review approves this strict verdict change.

Separate concrete input-parity gap discovered: dispatch_provider_expression matches raw comma-delimited github.event.inputs.providers; plan_providers_for_value trims whitespace around CSV entries and treats empty as full universe; generated plan env uses automatic providers for explicitly empty dispatch input. For providers=github-hosted, velnor, plan includes velnor but raw admission cannot match ,velnor,. For explicit empty dispatch, plan includes automatic providers while every dispatch admission false. Previously false-green skipped work; strict patch now fails visibly. Normalize effective provider input once and consume the result across planner/admission, or reject noncanonical explicit inputs. Parent notified; patch itself should remain strict.
