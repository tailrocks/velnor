# Parallax run 35481807065 failure diagnosis

Run: [35481807065](https://github.com/tailrocks/parallax/actions/runs/35481807065), attempt 1, PR #112 merge ref `eb9810b41a68260194ee380b5139db01f4da8a72` (source `0da45dafabc7a46cf1a5c1ff461e2d193d115cb1`, base `6a12bf47a816b63e848b563aaa45ef9694159c79`). The jobs API reported 77 jobs. At collection time the run API still said `status=queued`, so this is a failure observation from terminal jobs, not a verified run-level conclusion.

| Job | Evidence | Actual failure | Candidate versus base |
| --- | --- | --- | --- |
| [106000898238](https://github.com/tailrocks/parallax/actions/runs/35481807065/job/106000898238) | [raw log](parallax-35481807065-106000898238-failure.log) | Docker BuildKit tried to pull `parallax-checkout:latest` and failed `pull access denied ... insufficient_scope`. | The Dockerfile's named `from=parallax-checkout` mount and generated command were unchanged. The command supplies no `--build-context parallax-checkout=...`; this is a pre-existing generator/context contract defect. |
| [106000898372](https://github.com/tailrocks/parallax/actions/runs/35481807065/job/106000898372) | [raw log](parallax-35481807065-106000898372-failure.log) | `nextest-evidence-fixture` ran five tests; intentional `persistent_fail` and state-dependent `retry_pass` failed. | Fixture source and generated command were unchanged. The unit command runs the negative fixture set instead of the positive `pass` selection or the repository's evidence script. Preserve the fixture and failure evidence. |
| [106000898473](https://github.com/tailrocks/parallax/actions/runs/35481807065/job/106000898473) | [raw log](parallax-35481807065-106000898473-failure.log) | TypeScript `TS2769` at `load-app-status.ts:7` and `status.ts:8`: `AbortSignal | undefined` violates `exactOptionalPropertyTypes` for `RequestInit`. | Both files were unchanged from base. This is a pre-existing UI typing failure. |
| [106000898719](https://github.com/tailrocks/parallax/actions/runs/35481807065/job/106000898719) | job log | Rust CLI embed failed because `crates/parallax-server/../../ui/dist/client` did not exist. | The RustEmbed consumer and UI prerequisite declaration were unchanged. Isolated Cargo consumers have no declared UI build artifact/prerequisite. |
| [106000898827](https://github.com/tailrocks/parallax/actions/runs/35481807065/job/106000898827) | job log | Rust server hit the same missing `ui/dist/client` embed directory. | Same pre-existing build-order defect. |
| [106000898849](https://github.com/tailrocks/parallax/actions/runs/35481807065/job/106000898849) | job log | `parallax-xtask` release tests tried to read absent `.github/workflows/preview.yml`, `release.yml`, and `setup-macos-sdk.yml` paths. | Test and paths were unchanged. A scan exclusion does not prevent the generated nextest command from executing this test. |

The first experiments should preserve coverage while making prerequisites explicit: run the Docker build from repository root with a declared `parallax-checkout` build context; run the fixture's positive selector or `scripts/ci/test-nextest-evidence.sh`; build UI once as an artifact dependency before isolated RustEmbed consumers; and either restore the release-test fixture files or make their fixture contract explicit. Re-run each affected unit on the same merge ref and compare job and step timestamps with the collector output.

The workflow policy is also independently wrong: `.github-gen/velnor-workflow.toml` declares `automatic_lanes = "github"`, but generated PR/manual workflows default `runner` to `both` and set `VELNOR_LANES` from `github.event.inputs.runner || 'both'`. A pull request therefore admits both lanes despite the automatic policy. This needs the planned typed-provider/source-2 migration; reducing the matrix would hide the policy defect.
