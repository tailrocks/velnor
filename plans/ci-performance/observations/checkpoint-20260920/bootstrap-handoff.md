# Exact source bootstrap handoff

Worktree: `/tmp/velnor-bootstrap-97bac4c`, baseline `97bac4c4582bbe18ee607a1dd7a41b4854345c7e`. No commits, index changes, generated outputs, settings mutations, or source publication by this agent.

## Architecture

Owner only: a readonly hosted native runtime root precedes planning and all runtime consumers. Exact declared source pin must be an ancestor of the checked-out integration candidate, and full generator closures of pin, audited PR head, and actual tested checkout must agree. Published release product remains preferred and verified by existing attested setup action. Source fallback is permitted only after explicit HTTP404. Authorization/network/attestation/digest failures never trigger fallback.

Fallback compiles one clean detached pinned worktree per native platform with isolated Cargo home/target and env-i, explicit debug/tui features from DEV_FEATURES. Formatting and strict Clippy precede compilation. Exact binary revision and full closure are checked. Published release and source debug identities remain distinct; no false attestation or profile claim.

Transport binds repository, exact pin, native platform, full closure, profile/features, producer job, run ID, run attempt, and both executable digests before any executable probe. Files stage under RUNNER_TEMP; downloaded artifacts cannot dirty release-build source identity. Source-equivalent published revision may differ from declared pin; source debug revision must equal it. The job CLI is provisioned on all owner providers; fleet controller/daemon authority is unchanged.

Current-run policy consumes this artifact, removing mainline PR-artifact lookup. Standalone trusted policy can bootstrap only its exact trusted base pin, captures validator absolute path before acquiring the declared renderer, and retains source-bound early candidate compatibility with the previous trusted adapter. Both Candidate PR-success/main-failure waivers are removed. No new generation-config field.

Maintenance root checks out GitHub-declared protected default branch and rejects branch-only pin before any network lookup. It has contents:read only; privileged consumer depends on it. Control platform derives supported native selector labels; ambiguous owner selectors reject explicitly. Runtime producer matrices include needed native platforms. Generic consumers retain published-only setup.

Native ARM GNU release targets now run on ubuntu-24.04-arm, validate native compiler and explicit Cargo/CC linker selection. Hosted Debian package-record emission executes the exact packaged native binary; single canonical metadata pair remains unchanged. Poisoned metadata-tool regression checks both architectures and both preview/stable scripts. Independent review approved this handoff; separate runner stable-channel mapping is reviewer-owned.

## Migration preconditions

1. Commit complete generator source A; rebuild exact clean A (build.rs stamps git HEAD, so dirty development binaries prove tests only).
2. Declare pin A and regenerate B; prove generator closure A == B and prospective integration tree, exact pinned render, no second-generation drift.
3. Confirm closure A product explicitly absent before first migration PR, then observe early debug candidate artifact and previous-base Policy consuming it. Old adapter has no general published-release-to-debug conversion; do not claim that.
4. Run real hosted native Linux X64/ARM64 preflight and preserve strict required checks before merge. Current Velnor Docker image Ubuntu26.04 supports the Ubuntu24.04 GNU runtime ABI; arbitrary older custom images are not proven by architecture labels.
5. Preserve parent gate/admission fixes when applying baseline diff with three-way merge. s2 revision70 is intentional; parent legacy revision64 stays untouched.

## Corrected hypothesis

No custom publication transaction was retained. Official gh release create source v2.65.0 and v2.101.0 already creates a draft, uploads assets, then publishes, cleaning up normal failures. Initial alleged partial-publication race was disproven. Narrow pre-existing gh release view draft-as-success/error-classification issue remains separate; runtime root REST release-by-tag lookup reads published releases.

## Validation

Focused source_bootstrap suite passed10 before added selector regression. Full library run now passed1833 with only checked-in generation drift intentionally excluded; integration package assertion updated to new transport ownership. Final full generator suite PASS1961 (22 suites), one existing checked-in render-drift test excluded pending parent regeneration. Strict generator Clippy --all-targets -- -D warnings passed. cargo fmt --all -- --check passed; git diff --check passed. Runner emission command cancelled immediately before work when parent took cached-target validation; parent owns that proof. Commands used CARGO_BUILD_JOBS=3; no timing/performance claims.

## Files

Generator: s2/mod.rs; s2/policy.rs; s2/policy/tests.rs; s2/primitives/ir.rs; s2/primitives/mod.rs; s2/primitives/release.rs; s2/primitives/package_release.rs; new s2/primitives/runtime_bootstrap.rs; tests/package_release.rs.
Separate reviewer patch: velnor-runner/src/release.rs and release/tests.rs maps embedded release→stable package channel, preserves preview and rejects development/cross-kind.

Final generator patch including new module: `/tmp/velnor-integration/bootstrap-generator.patch`. No reviewer runner files included in this patch.

Review correction: hosted control platform always follows selectors[GithubHosted] with existing ubuntu24 fallback, irrespective provider universe. Local maintenance/build runtime platforms come from existing typed provider_caps LinuxX64, preserving opaque Velnor-only/self-hosted-only selectors. New regression covers both modes with an ARM hosted control selector and without any hosted selector. Post-correction focused source_bootstrap suite PASS12; strict all-targets Clippy PASS. Final fmt verification follows.
