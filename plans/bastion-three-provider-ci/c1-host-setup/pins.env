# shellcheck shell=bash
# C1 bastion host-setup pins — tailrocks/velnor campaign, gate C1 prep.
# Rechecked read-only on 2026-09-19 against the official Docker Trixie package
# index. Re-resolve from the Source URL, record newest stanzas and date. Never float.
#
# Sourced by provision-bastion.sh (same directory). Package and repository
# values can be overridden during a reviewed re-resolve. Key fingerprints are
# readonly policy constants so caller environment cannot weaken authentication.

# --- Docker Engine (Debian 13 "trixie", amd64) -------------------------------
# Source: https://download.docker.com/linux/debian/dists/trixie/stable/binary-amd64/Packages
# Resolved: 2026-09-19 (newest Version stanza per package in that file).
: "${VELNOR_C1_DOCKER_CE_VERSION:=5:29.8.1-1~debian.13~trixie}"
: "${VELNOR_C1_DOCKER_CE_CLI_VERSION:=5:29.8.1-1~debian.13~trixie}"
: "${VELNOR_C1_CONTAINERD_VERSION:=2.3.5-1~debian.13~trixie}"
: "${VELNOR_C1_BUILDX_VERSION:=0.37.1-1~debian.13~trixie}"
: "${VELNOR_C1_COMPOSE_VERSION:=5.5.1-1~debian.13~trixie}"
: "${VELNOR_C1_DOCKER_REPO_URL:=https://download.docker.com/linux/debian}"
: "${VELNOR_C1_DOCKER_DIST:=trixie}"
: "${VELNOR_C1_DOCKER_COMPONENT:=stable}"

# --- Docker APT key (fingerprinted, fail-closed) ------------------------------
# Key URL: https://download.docker.com/linux/debian/gpg
# Expected primary fingerprint (no spaces, uppercase):
#   Authenticated 2026-09-18 against TWO independent references:
#   1. Ubuntu keyserver, same keyid, identical fingerprints + creation date:
#      https://keyserver.ubuntu.com/pks/lookup?op=get&search=0x8D81803C0EBFCD88
#   2. Published fingerprint in official Docker install docs (historical
#      docker.github.io mirrors, e.g. install/linux/docker-ce/debian.md):
#      "9DC8 5822 9FC7 DD38 854A E2D8 8D81 803C 0EBF CD88",
#      uid "Docker Release (CE deb) <docker@docker.com>".
#   Functional proof: live trixie InRelease (signed 2026-09-15) verifies
#   "Good signature" from signing subkey 7EA0A9C3F273FCD8 of this key.
readonly VELNOR_C1_DOCKER_KEY_FPR=9DC858229FC7DD38854AE2D88D81803C0EBFCD88
readonly VELNOR_C1_DOCKER_KEY_SUB=D3306A018370199E527AE7997EA0A9C3F273FCD8
: "${VELNOR_C1_DOCKER_KEY_URL:=https://download.docker.com/linux/debian/gpg}"

# --- Source derivation --------------------------------------------------------
# Steps derived from ChainArgos/java-monorepo ansible-configs at audited live
# head 218a44b28984acf4ceee24cd1d9d6ccb8c38ae37 (verified no-drift 2026-09-18).
# See /tmp/c1-ansible-reresolve.md (campaign working note, not committed).
: "${VELNOR_C1_SOURCE_REPO:=ChainArgos/java-monorepo}"
: "${VELNOR_C1_SOURCE_HEAD:=218a44b28984acf4ceee24cd1d9d6ccb8c38ae37}"
