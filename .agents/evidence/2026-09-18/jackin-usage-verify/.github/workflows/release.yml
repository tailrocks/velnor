# Generated by velnor-workflow. Regenerate; do not hand-edit.
name: Release
run-name: Release · ${{ github.ref_name }}

on:
  push:
    tags: ["v[0-9]*"]
  workflow_dispatch:
    inputs:
      mode:
        description: Release drill mode (publication is tag-triggered, never dispatched)
        required: false
        default: validate
        type: choice
        options:
          - validate

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  build:
    name: build
    if: ${{ github.event_name == 'workflow_dispatch' }}
    runs-on: macos-26
    timeout-minutes: 90
    env:
      DEVELOPER_DIR: "/Applications/Xcode_26.6.app/Contents/Developer"
      MACOSX_DEPLOYMENT_TARGET: "26.0"
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up Mise
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install: false

      - name: Run desktop-release-env
        run: mise run desktop-release-env
      - name: Run desktop-build
        run: mise run desktop-build
      - name: Run desktop-verify
        run: mise run desktop-verify
      - name: Run desktop-release-state
        run: mise run desktop-release-state
  sign:
    name: "Sign release"
    if: ${{ github.event_name != 'workflow_dispatch' }}
    runs-on: macos-26
    timeout-minutes: 90
    environment: release-macos
    permissions:
      attestations: write
      contents: read
      id-token: write
    env:
      APP_STORE_CONNECT_API_KEY_P8: "${{ secrets.APP_STORE_CONNECT_API_KEY_P8 }}"
      APP_STORE_CONNECT_ISSUER_ID: "${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}"
      APP_STORE_CONNECT_KEY_ID: "${{ secrets.APP_STORE_CONNECT_KEY_ID }}"
      DEVELOPER_DIR: "/Applications/Xcode_26.6.app/Contents/Developer"
      DEVELOPER_ID_APPLICATION_P12_BASE64: "${{ secrets.DEVELOPER_ID_APPLICATION_P12_BASE64 }}"
      DEVELOPER_ID_APPLICATION_P12_PASSWORD: "${{ secrets.DEVELOPER_ID_APPLICATION_P12_PASSWORD }}"
      EXPECTED_CERT_SHA256: "${{ vars.JACKIN_DEVELOPER_ID_CERT_SHA256 }}"
      EXPECTED_TEAM_ID: "${{ vars.JACKIN_DEVELOPER_ID_TEAM_ID }}"
      MACOSX_DEPLOYMENT_TARGET: "26.0"
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up Mise
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install: false

      - name: Run desktop-release-env
        run: mise run desktop-release-env
      - name: Run desktop-build
        run: mise run desktop-build
      - name: Run desktop-verify
        run: mise run desktop-verify
      - name: Run desktop-release-state
        run: mise run desktop-release-state
      - name: Run desktop-sign-notarize
        run: mise run desktop-sign-notarize
      - name: Attest release artifacts
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8 # v4.2.2
        with:
          subject-path: |
            native/dist/jackin-desktop-*-aarch64-apple-darwin.zip

