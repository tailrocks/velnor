# Generated by velnor-workflow. Regenerate; do not hand-edit.
name: "Renovate upstream sources"
run-name: "Renovate upstream sources · ${{ github.event_name }}"

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: renovate-upstream-sources-${{ github.repository }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  renovate-upstream-sources:
    name: "Renovate upstream sources"
    runs-on: ubuntu-26.04
    timeout-minutes: 10
    env:
      MISE_TASK_RUN_AUTO_INSTALL: "false"
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up Mise
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
        with:
          install: false
      - name: Run renovate-upstream-sources
        run: mise run renovate-upstream-sources
