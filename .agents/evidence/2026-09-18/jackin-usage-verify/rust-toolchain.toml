# SPDX-FileCopyrightText: 2026 Alexey Zhokhov
# SPDX-License-Identifier: Apache-2.0

# Source of truth for the latest tested Rust channel and required components.
# Read by rustup, rust-analyzer, mise (via idiomatic_version_file),
# and CI (via jdx/mise-action).
# `rust-version` in Cargo.toml matches this channel; older compilers are unsupported.

[toolchain]
channel = "1.97.1"
components = ["clippy", "rustfmt"]
targets = ["aarch64-unknown-linux-gnu", "x86_64-unknown-linux-gnu"]
