# Plan 001: Clear RUSTSEC-2026-0204 by bumping crossbeam-epoch in the lockfile

> **Executor instructions**: Follow this plan step by step. Run every
> verification command and confirm the expected result before moving to the
> next step. If anything in the "STOP conditions" section occurs, stop and
> report — do not improvise. When done, update the status row for this plan
> in `security-review/README.md`.
>
> **Drift check (run first)**: `git diff --stat a4761957d..HEAD -- Cargo.lock deny.toml .cargo/audit.toml`
> If `Cargo.lock` already lists `crossbeam-epoch` at `0.9.20` or higher, this
> plan is already DONE — mark it so and stop.

## Status

- **Priority**: P0
- **Effort**: S
- **Risk**: LOW
- **Depends on**: none
- **Category**: migration / security
- **Planned at**: commit `a4761957d`, 2026-07-09

## Why this matters

`cargo audit` and `cargo deny check advisories` both currently FAIL on
**RUSTSEC-2026-0204** (invalid pointer dereference in the `fmt::Pointer` impl
for `crossbeam-epoch`'s `Atomic`/`Shared`, advisory dated 2026-07-06, fixed in
`>=0.9.20`). The workspace pins `crossbeam-epoch 0.9.18` transitively (pulled at
runtime via `turso_core → crossbeam-deque → crossbeam-epoch`, and dev-only via
`criterion → rayon → crossbeam-epoch`). `crates/AGENTS.md` documents `cargo
audit` as a "deliberately duplicated RustSec PR gate," so the next
`Cargo.lock`-touching PR and the scheduled hygiene workflow go red until this is
bumped. Runtime exploitability is low (requires `Debug`/`Pointer`-formatting an
already-invalid atomic), so the concrete cost is a blocked CI gate. The fix is a
single lockfile bump with no first-party code change.

## Current state

- `Cargo.lock` — `crossbeam-epoch` is resolved at `0.9.18`. `crossbeam-epoch`
  is a transitive dependency; it does **not** appear in any `Cargo.toml`
  `[dependencies]` table, so there is no version constraint pinning it — a
  precise `cargo update` will move it freely.
- `deny.toml` — has an `[advisories]` section with an `ignore = [...]` list
  (currently `RUSTSEC-2023-0071`, `RUSTSEC-2026-0173`). RUSTSEC-2026-0204 is
  **not** ignored, and must not be added to the ignore list — the correct fix
  is to bump, not to suppress.
- `.cargo/audit.toml` — mirrors the same two advisory ignores. Do not touch
  unless the bump proves impossible (see STOP conditions).

## Commands you will need

| Purpose | Command | Expected on success |
|---|---|---|
| Bump | `cargo update -p crossbeam-epoch --precise 0.9.20` | writes `Cargo.lock`, exit 0 |
| Audit | `cargo audit` | `0 vulnerabilities found` (or no RUSTSEC-2026-0204) |
| Deny advisories | `cargo deny check advisories` | `advisories ok` |
| Deny full | `cargo deny check bans licenses sources` | all `ok` (no new duplicate-version break) |
| Build sanity | `cargo check --workspace --locked` | exit 0 |

If `cargo audit`'s advisory DB is stale or offline, run `cargo audit fetch`
first. If the environment blocks network access entirely and neither tool can
run, complete the bump + `cargo check` and record in your status note that the
advisory tools could not be run here.

## Scope

**In scope** (the only files you should modify):
- `Cargo.lock` (regenerated by `cargo update`)

**Out of scope** (do NOT touch):
- Any `Cargo.toml` — `crossbeam-epoch` is transitive; do not add a direct
  dependency or an explicit pin.
- `deny.toml` / `.cargo/audit.toml` — do not add RUSTSEC-2026-0204 to any
  ignore list. Suppression is the wrong fix here.
- The `turso` / `criterion` `=` version pins — the crossbeam bump does not
  require changing them; if it appears to, that is a STOP condition.

## Git workflow

- Branch: `chore/bump-crossbeam-epoch` (or the operator's active branch if one
  is already checked out for this work).
- One commit; conventional-commit style. Example message:
  `chore(deps): bump crossbeam-epoch to 0.9.20 (RUSTSEC-2026-0204)`
- Sign the commit (`git commit -s`). Do NOT push or open a PR unless the
  operator instructed it.

## Steps

### Step 1: Bump the transitive dependency

Run `cargo update -p crossbeam-epoch --precise 0.9.20`. If `0.9.20` is yanked or
unavailable, retry with the next patch (`0.9.21`, etc.) — any version `>=0.9.20`
clears the advisory.

**Verify**: `git diff Cargo.lock | grep -A1 'name = "crossbeam-epoch"'` shows
`version = "0.9.20"` (or higher). No other crate's version line changed beyond
what a crossbeam-epoch bump pulls in (crossbeam-epoch may itself nudge
`crossbeam-utils`; that is fine).

### Step 2: Confirm the advisory is cleared

Run `cargo audit` and `cargo deny check advisories`.

**Verify**: neither output contains `RUSTSEC-2026-0204`. `cargo audit` prints
`0 vulnerabilities found` (or lists only advisories already in the ignore list,
not this one). `cargo deny check advisories` prints `advisories ok`.

### Step 3: Confirm no supply-chain regression

Run `cargo deny check bans licenses sources` and `cargo check --workspace --locked`.

**Verify**: `bans` stays `ok` — if the bump introduced a *new* duplicate
version of crossbeam-epoch (both 0.9.18 and 0.9.20 in the tree), `cargo deny
check bans` will flag it; in that case run `cargo update` without `--precise` to
let both consumers converge, then re-check. `cargo check` exits 0.

## Test plan

No new tests — this is a lockfile-only change. The verification gates ARE the
test: `cargo audit` and `cargo deny check advisories` moving from red to green.

## Done criteria

Machine-checkable. ALL must hold:

- [ ] `grep -A1 'name = "crossbeam-epoch"' Cargo.lock` shows `version = "0.9.20"` or higher
- [ ] `cargo audit` does not report RUSTSEC-2026-0204 (or advisory tools unavailable, noted)
- [ ] `cargo deny check advisories` → `advisories ok`
- [ ] `cargo deny check bans licenses sources` → all `ok`
- [ ] `cargo check --workspace --locked` exits 0
- [ ] Only `Cargo.lock` is modified (`git status` shows no other file)
- [ ] `security-review/README.md` status row updated

## STOP conditions

Stop and report back (do not improvise) if:

- `cargo update -p crossbeam-epoch --precise 0.9.20` fails because a `Cargo.toml`
  constraint blocks it — that would mean crossbeam-epoch is directly pinned
  somewhere the drift check missed; report the constraint.
- The bump forces a version change to `turso`, `turso_core`, `criterion`, or any
  `=`-pinned crate — those pins are deliberate; report which pin conflicts.
- `cargo deny check bans` reports a new unresolved duplicate-version error that
  `cargo update` (non-precise) does not resolve.
- The advisory is still reported after the bump (wrong crate in the reach path).

## Maintenance notes

- This is a transitive bump; Renovate's `lockFileMaintenance` will keep
  crossbeam-epoch current going forward, so no manual pin is added or needed.
- Reviewer should confirm the diff is `Cargo.lock`-only and that no advisory was
  added to an ignore list as a shortcut.
- Related deps-hygiene follow-ups (stale `ring@0.17.14` license exception,
  duplicated advisory-ignore lists) are tracked separately in
  `security-review/README.md` under "considered / deferred" — not part of this
  plan.
