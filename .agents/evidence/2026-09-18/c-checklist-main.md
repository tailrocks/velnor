# Bastion acceptance checklist: 1:1 with work-plan steps

Status: **acceptance tracker** — branch `docs/bastion-final-plan` (PR `tailrocks/velnor#912`).
Authority: `plans/bastion-three-provider-ci/spec.md`. Steps: `plans/bastion-three-provider-ci/work-plan.md`. Inputs: `plans/bastion-three-provider-ci/evidence.md`.

**DONE = every item below is checked, each with cited evidence, and each signed off by an independent verifier who is not the step's author.** Author assertions, unchecked boxes, screenshots, and PR bodies saying "all green" are not certification. All boxes start **unchecked**.

- [ ] **A0 — Live evidence ledger refreshed.** Done when: current `main` SHAs recorded for all three repos; PR #904/#901 status re-checked; recorded failure runs re-read with attempts/logs; releases/tags re-resolved; upstream Scale Set main re-resolved; APT omission blob re-read; permissions verified without values; read-only bastion inventory captured; every correction logged as `old observation → new evidence → consequence`. Evidence: ledger with exact SHAs, paginated inventories, failing logs, permission facts (no secret values), SSH read-only inventory output, correction list. Verifier: evidence verifier.
- [ ] **A1 — Velnor CI/policy/ownership/test failures repaired.** Done when: each live failure reproduced → root-fixed → regression-tested; regeneration exact (0 files); ownership inventory complete incl. unexpected files; structured policy + actionlint clean; coverage/caches/streaming/parallel-main/supersession preserved; timing baseline captured with critical-path improvement; `cargo test -p velnor-workflow`, contract tests, clippy `-D warnings`, `cargo fmt --check` all green. Evidence: reproduction→fix→regression-test records, regen/policy/lint logs, timing baseline + improved critical path. Verifier: CI verifier.
- [ ] **A2 — Immutable runtime producer/consumer + publish-before-pin qualified.** Done when: trusted `R` stays the normal consumer; candidate `C` builds once per closure/platform with full closure coverage; negatives reject (missing/bad/untrusted products, lookup confusion, PR-checksum-as-trust); zero cargo builds in normal consumer paths; cold-consumer verification proof; atomic pin+tree promotion proof; daemon never via `releases/latest`. Evidence: implementation + negative-suite logs, zero-cargo consumer logs, cold-consumer + atomic-promotion proofs, deduplicated-build proof. Verifier: runtime/supply-chain verifier.
- [ ] **A3 — Three consecutive full green bootstrap main runs.** Done when: three consecutive FULL green `main` runs, zero manual reruns, zero hidden source failures, bootstrap provider scope explicitly declared (labeled bootstrap, never triple-qualified); each run has URL, source SHA, plan digest, complete expected-result set, per-job timings; required checks (incl. DCO) enforced, no protection bypass. Evidence: three run URLs with identities, expected results, timings, bootstrap-scope record. Verifier: CI/result verifier.
- [ ] **B1 — Generic typed APT primitives implemented.** Done when: all spec §7 capabilities typed (source+identity, package, arch set, suites, signer+secrets, coherence, assembly, records, retention, Pages, channel tasks); `verify-release.sh` reused via narrow typed contract; config executes no shell/YAML; generator/IR tests green; renamed fixture proves genericity; negatives reject (bad source/digest/key/arch/incoherent); generated APT workflow coverage owned + policy/actionlint clean. Evidence: generator/IR tests, renamed-fixture proof, negative logs, generated coverage + ownership/policy/lint proofs. Verifier: APT generator verifier.
- [ ] **B2 — Generator product published; velnor-apt pinned + regenerated.** Done when: approved generator product published/attested via trusted hosted producer with all platform assets verified; ONE atomic velnor-apt commit updates pin + full tree; omission notice + stale direct-install docs removed only on real coverage; sole ownership/reference checks pass; policy/actionlint clean; release-verifier tests cover the new tree. Evidence: atomic pin/tree diff, publication record, ownership/policy/lint logs, verifier-test proof. Verifier: ownership/APT verifier.
- [ ] **B3 — Coherent bootstrap Velnor release produced.** Done when: exact tag/commit/version with coherent amd64+arm64 products, manifests/record/checksums, OCI/payload identity (incl. native arm64), trusted attestations — all via generated hosted CI; unbounded/global-native prerequisites included; coherence negatives fail the release; release contracts kept; KVM tests stay declared-separate, no VMs on bastion. Evidence: tag/commit/version, digests, manifests/record, OCI/payload identity, attestations, negative coherence logs. Verifier: release/package verifier.
- [ ] **B4 — Signed APT published via single-writer flow; live candidate verified.** Done when: independent verification passed BEFORE mutation; staged index + retained previous version + signed InRelease/Release + publication record; single-writer Pages deployment; same-feed serialization with no-rollback proof; LIVE candidate independently verified (signed chain, exact version, both archs, hashes, source/record, origin); signer fingerprint recorded. Evidence: pre-mutation verification log, live metadata/package chain proof, exact candidate identity, previous-version recovery artifacts. Verifier: APT supply-chain verifier.
- [ ] **C1 — Exact candidate installed on bastion via locked APT transaction.** Done when: fingerprint authenticated against independent reference, repository `Signed-By` configured; pre-install verification complete (metadata/version/arch/hash/source/origin/attestation-as-distinct-check); idempotent host setup complete per spec §6+§6.1 (SSH preserved, NVMe untouched, no libvirt); jobs drained, config/secrets preserved; `flock` transaction log + `dpkg-query` identity; binary/record/manifest match; `release verify-installed` passed pre-start; activation/drain/health derived from package/help and verified; no `dpkg -i`/sideload/signature-bypass. Evidence: transaction log, `dpkg-query`, identity + verify-installed + activation/health proofs, idempotent-setup proof. Verifier: deployment verifier.
- [ ] **C2 — Quota-free + native-backed global N enabled; smoke + provisional N done.** Done when: unbounded/global-native active BEFORE first job; HostConfig + cgroup ancestry + units/drop-ins + build env inspected (inherited limits included) with zero ceilings; no raw-socket mount; authenticated health records flowing; real native smoke job green with URL + engine identity + occupancy ledger + cleanup receipt; provisional native N benchmarked (16/24/32/48/64+, comparable conditions, full metric set, unrelated jobs unharmed). Evidence: quota-free inspection bundle, no-raw-socket proof, health + smoke URL, occupancy/cleanup ledger, benchmark data. Verifier: resource/native verifier.
- [ ] **D1 — Rust Scale Set adapter + credentials + runner/DinD + allocator finished.** Done when: upstream revision pinned; full Rust protocol per spec §5.1 9-step order; recorded fixtures + LIVE canary conformance (mocks alone rejected); App credentials wired with management keys out of jobs + in-process refresh; homogeneous digest-pinned runner+DinD (never `latest`, version recorded, tools verified); private-Docker semantics proven on real jobs (private socket, shared netns, identical binds+ownership, log export, collision-free); shared-allocator race tests prove ≤N/no-per-scope-N/no-starvation/oldest-observed; every lifecycle boundary reconciled; set identity survives restart; no Go controller; native path unrewritten. Evidence: fixtures + canary logs, digest pins, credential/trust wiring, private-Docker proof, allocator race proofs, restart/recovery proofs. Verifier: protocol/Docker/capacity verifiers.
- [ ] **D2 — Three-provider model + watchdog + strict results finished.** Done when: one provider-set schema everywhere with mechanical no-legacy proof (no aliases/deprecation/dual parsers/no inference); typed platform/trust/capabilities with explicit-failure negatives; planner→3-provider fanout with identical inputs, provider display names, disjoint local selectors, per-lane bootstrap; strict expected-result set with per-class negatives (missing/skip/cancel/timeout/fail/dup-conflict/identity-mismatch/stale/wrong-provider); fail-fast disabled; capability tests green; hosted watchdog per spec §8 with outage detection proven against 180s/120s/5m/10m; trust negatives (fork/spoof/substitution) deny bastion. Evidence: no-legacy mechanical proof, routing/fanout proof, strict-result negatives, capability tests, watchdog + outage proof, trust-denial proof. Verifier: generator/result/trust verifiers.
- [ ] **D3 — Scale Set package + feed published; bastion upgraded on locked path.** Done when: new coherent release + signed feed via B3/B4 procedures; bastion upgraded via C1 locked path (checks + `flock` + `dpkg-query` + identity + `verify-installed` + package-derived activation); no sideloaded/untracked binaries; post-upgrade package/config/state identity + image pins + restart health verified; FULL C2 quota-free inspection repeated after upgrade. Evidence: release/feed chain proof, upgrade transaction log, installed identity, repeated quota-free proof. Verifier: release/deployment verifier.
- [ ] **E1 — Velnor qualified on all three providers.** Done when: 17-unit baseline confirmed (or reviewed correction); every eligible Linux unit fanned to all three providers with identical inputs incl. production-topology + Docker conformance on both local engines; actual unit×provider matrix recorded (engine identities, digests, tests/JUnit, cache/timing, cleanup); planner-declared exceptions only; fail-fast disabled; per-provider evidence independent; sole-ownership proof on final tree. Evidence: unit×provider matrix with identities + counts, ownership proof, exception list. Verifier: Velnor parity verifier.
- [ ] **E2 — Fault matrix passed; mixed N measured; triple-green streak + PR.** Done when: every spec §8 matrix row executed on canaries with per-row invariants proven (no false success, diagnostics, owned-only cleanup, capacity-once, reconciled reservations, dedup + returned-IDs, monotonic ≤N, degraded visibility, pre-execution rejection, trust denial, aggregate fails bad/missing, quota-free descendants); coordinated window preserved unrelated work/SSH with no destructive ops; mixed-engine cold/warm N benchmarked per spec §4.4; three consecutive full triple-green mains + one representative PR recorded (URLs/identities/attempts/timings); gate report signed. Evidence: per-row fault records, fork-denial proof, ≤N ledger, N data, three run URLs + PR URL, gate report. Verifier: adversarial/capacity/result verifiers.
- [ ] **F1 — Jackin migrated + routing/release/E2E repaired (only after E).** Done when: E gate signed first; authorization + scope + typed config only, published pin selected, no per-Jackin infra; tree regenerated + owned + policy/lint clean; 40-unit ledger holds (38 Linux×3 + 2 Apple Silicon Swift + E2E/controls, never 120 Linux); native Apple routing fixed with same-job XCFramework→Swift; tools re-resolved without silent userspace/toolchain substitution; `release.yml` reconciled without legacy restore or release-enabling; `docker-e2e` explicit with same-source capsule ELF + `JACKIN_CAPSULE_BIN`, Docker/Buildx/Compose/`script(1)`/nested-DinD/Testcontainers/TLS-relay proven, serial groups kept, 20-capsule fanout exercised, zero fixture weakening; generic bugs fixed via §0.8 requalification. Evidence: migrated tree + ledger, routing/release diffs, explicit E2E proof, tool-resolution record. Verifier: Jackin/platform/E2E verifier.
- [ ] **F2 — Jackin full parity + PR/main qualified; N remeasured if needed.** Done when: full unit×provider results with counts/identities/digests/caches/no-quotas/cleanup; representative PR + main recorded; N remeasured iff mix changed; Velnor non-regression proven for any F1 generic change; independent gate report signed. Evidence: parity set, PR + main URLs, N data (if triggered), Velnor regression proof, gate report. Verifier: independent Jackin parity verifier.
- [ ] **G1 — ChainArgos migrated + repaired + parity qualified (only after F).** Done when: F gate signed first; authorization + scope + typed config only, published pin, same controller + global N; tree regenerated; ownership/trust + missing local actions repaired first; 71-unit/213-execution ledger holds; job-local PostgreSQL with graph-justified dedup of the 16 `flywayMigrate jooqCodegen` preparations, same job DB via build-supported vars (`POSTGRESQL_DB_HOST/PORT`), never prod/shared; Testcontainers PG/RabbitMQ/Redis/RustFS with explicit nextest profile + serial RustFS group + verified effective config; digests locked (`rustfs/rustfs:1.0.0-beta.8`, `postgres:18-alpine`, …); root-context 12-target bake proven; no unchanged global-name root Compose on shared host; GraalVM-25/wrapper-9.5.1/Rust-1.98.1/protobuf/Node-24.20.0/Bun-1.3.14/browsers preserved or tested-updated; Playwright audited; Micronaut `1.44` checked; single-writer + release-disabled kept; external/RPC failures labeled; PR + main recorded; generic bugs via §0.8. Evidence: migrated tree + ledger, DB/Testcontainers/RustFS/Docker/browser proofs, PR + main URLs. Verifier: ChainArgos Java/Rust/Docker/frontend verifiers.
- [ ] **G2 — Campaign closed: onboarding, pins, runbooks, report.** Done when: mechanical no-regression proofs across all three trees (no VM/Go/quota/reservations/legacy/hand-YAML); fresh renamed-fixture repo onboarded end to end (trust→auth→config→published-pin→regen→qualify→inventory) with no new infra; new-org adds auth metadata only; pins + deployed identities recorded (generators, `velnor-runner`, digests, protocol ref, signer, APT record); idempotent setup + reinstall + recovery paths verified (APT-downgrade-only-with-proven-snapshot else forward recovery, no shims); ops/recovery runbook matches implemented package/help; final mixed-engine N recorded with pool/provenance/fault bundle; acceptance report covers all three final trees + every gate, signed by the final independent verifier. Evidence: no-regression logs, onboarding proof, pin/identity record, runbooks, final N evidence, signed acceptance report. Verifier: final independent verifier.
