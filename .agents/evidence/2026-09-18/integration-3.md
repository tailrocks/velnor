# Integration evidence — batch 3 (bastion campaign)

Target: `docs/bastion-final-plan`, shared workspace
`/Users/donbeave/Projects/tailrocks/velnor-project/velnor3`.
Pushed: `origin/docs/bastion-final-plan` `411b8a99`..`40ff66ec`.

## Inputs (certifications read first)

- `/tmp/v-a2-signer.md`: CERTIFIED — `feat/a2-signer-order` @
  `f79ab248b56a49b24ad40c23f98a179ebece05d1` (stack `754c1cd6`+`f79ab248`,
  base `38ffbfd7`).
- `/tmp/v-fix-timing.md`: CERTIFIED — `perf/main-docker-gha-cache` @
  `511ffd7e8a20bbce85eb6cba1fcb6b863514d8fa` (base `38ffbfd7`).

Pre-merge state: `git fetch origin`; on `docs/bastion-final-plan` @
`411b8a99`, clean tree. Local tips of both source branches matched the
certified commits and `origin/*`.

## Merge 1: feat/a2-signer-order → `8f030b7c` (signed off)

`git merge --no-commit` → 8 conflicts: 6 generated, 2 source
(`velnor_first_ci.rs` auto-merged).

Generated (reset to HEAD, never hand-edited, then regenerated):
`.github/actions/setup-velnor-workflow/action.yml`,
`.github/ci/.github-actions-generator-state`, `ci-runtime-products.yml`,
`ci-unit-rust.yml`, `release.yml`.

Source resolutions (script `/tmp/resolve-merge1.py`, all assertions held):

1. `.github-gen/.../setup-velnor-workflow/action.yml` (comment only; the
   two `--source-ref` verify lines auto-merged): combined HEAD's
   `--revision` wording with a2's default-branch-ref wording.
2. `lib.rs` provisioner format string: HEAD body (live pin-parse, no
   baked `PINNED_REVISION`, revision probe) + a2's
   `--source-ref refs/heads/main` on both `gh attestation verify` lines.
   A2's test hunks auto-merged.
3. `runtime_products.rs` C1 (doc comment): combined HEAD revision wording
   with a2 verify-before-create wording.
4. C2 (assemble tail): kept HEAD's `manifest_revision == HEAD_SHA` check,
   took a2's deletion of the early-skip + in-assemble `gh release
   create` (required: merged tests assert zero `skipped` output; the
   `if: steps.assemble.outputs.skipped` gates auto-removed with a2's
   step headers).
5. C3 (create-step body): took a2's side verbatim (re-check + create),
   and carried HEAD's `--revision` probe into a2's new dist-based
   "Smoke-test the release" step (`$temporary` → `dist`), preserving
   batch-2 behavior the merged tests assert.
6. C4 (pinned digest): set to the true merged render
   `e3224ae82935c03707cb7060f7a5b242cdd71356dcbe1e891c7c2bd2fdcaeab5`
   after structural tests passed.

Post-merge source-only fixes (same breakage class as batch 2):

- a2's `all_consumers_pin_the_same_producer_ref` test called the
  pre-batch-1 2-arg `workflow_pinned_policy_runtime_velnor` → E0061.
  Dropped the removed arg (test only counts `--source-ref`).
- Batch-2's `smoke_test_installs_the_consumer_layout` asserted the
  revision probe reads `$temporary/manifest.json` (download-based smoke
  test a2 restructured to dist-based): updated the path to
  `dist/manifest.json` (+ message); the probe itself is preserved, not
  weakened. Plus a `cargo fmt` reflow of that assertion.

Regen: rebuilt `cargo build -p velnor-workflow`, ran
`./target/debug/velnor-workflow --plain --force`, then
`--plain --dry-run .` → **0 files would change**.

Rendered-output checks (regenerated bytes): guard first (producer :47);
assemble + manifest-revision check (:256); smoke (:261) with both
`--source-ref refs/heads/main` pins (:274/275) + revision probe (:291);
create (:292) with the single `gh release create` (:308).
`create=1 download=0 skipped=0`; consumers carry 2 pins each
(ci-unit-rust, release, setup action). Merge diff: 9 files (+376/-98),
no reversals.

Gates at `8f030b7c`: `cargo test -p velnor-workflow` 498 lib +
2/6/5/9/33, 0 failed; clippy `-D warnings` clean; fmt clean;
actionlint on the 3 touched workflows clean; `git diff --check` clean.

## Merge 2: perf/main-docker-gha-cache → `40ff66ec` (signed off)

`git merge --no-commit` → 1 conflict: generated
`.github/ci/.github-actions-generator-state` only (reset to HEAD, then
regenerated; never hand-edited). `lib.rs`, `project.toml`,
`ci-main.yml`, `ci-pr.yml` auto-merged; the merged `lib.rs` +/- lines
are byte-identical to the fix commit's `lib.rs` diff
(`docker_hosted_full_command` + call site + new test).

Regen: `--plain --force` changed only the generator state beyond the
auto-merged files (perf-side renders already byte-exact);
`--plain --dry-run .` → **0 files would change**. Rendered
`github_full_commands`: first entry buildx + both `scope=docker` flags,
no `--target ci`; second entry byte-unchanged; `seed_compat
4ad4d42d64a2` — matches the cert.

Gates at `40ff66ec` (pushed bytes): `cargo test -p velnor-workflow`
**499 lib** + 2/6/5/9/33, 0 failed; clippy `-D warnings` clean; fmt
clean; repo-wide `actionlint` exit 0; `git diff --check` clean.

## Final state

- `origin/docs/bastion-final-plan` = `40ff66ec63fc2f8a468a6b31ea45823e9cda152a`.
- Both merges signed off (`Signed-off-by` trailer verified on each).
- Working tree clean, in sync with origin. No hand edits to generated
  YAML at any step.
