BESTLEN=5586
HITS=1
Migration inventory — Jackin @ `0be3fcf`, tree clean, read-only audit.

## 1. Contributor instructions (CI-relevant)

- `AGENTS.md:1-10` (+ `CLAUDE.md` symlink): no-legacy rule — migrations must remove old paths completely, no shims. Research project, break things freely.
- `CONTRIBUTING.md:1-20`: DCO sign-off required (DCO2 app blocks unsigned), "CI must pass".
- `PULL_REQUESTS.md:28,42,76`: every PR needs copy-pasteable "Verify locally" section. Agent-only rules referenced under `.github/` — but **`.github/AGENTS.md` does not exist** (template `PULL_REQUEST_TEMPLATE.md:6-11` points at it). Doc gap.
- `TESTING.md:86-93,184-187`: GitHub/Velnor lane parity rules; PR gate is generated `ci-pr.yml` dispatching `ci-unit-swift.yml`.
- No repo doc describes the regen command; regen is evidenced only in commit messages (`velnor-workflow --force --check`, #992).

## 2. `.github-gen/velnor-workflow.toml` (50 lines, read fully)

Sections: `[generator]` (L3-5), `[scan]` (L7-8), `[workflow]` (L13-20), `[policy]` (L22-24), 3× `[[units]]` (L30-50). **Zero `[[static_files]]` mappings, zero declared source workflows, zero declared generated outputs.** `grep static` → no match. Generated outputs are instead declared generator-side in `.github/ci/project.toml` `[workflow].files` (9 workflows) and the ownership state file.

## 3. File ownership

Generated-by-Velnor (all carry `# Generated by velnor-workflow. Regenerate; do not hand-edit.` on line 1, and all 12 are listed in `.github/ci/.github-actions-generator-state:8-20`):

- `.github/workflows/`: `ci-main.yml`, `ci-pr.yml`, `ci-policy.yml`, `ci-unit-bun.yml`, `ci-unit-docker.yml`, `ci-unit-rust.yml`, `ci-unit-swift.yml`, `maintenance.yml`, `nightly.yml`
- `.github/actionlint.yaml`, `.github/ci/project.toml`, `config/fleet/velnor-host.env`

Product-owned (no generated header, absent from state):

- `.github/PULL_REQUEST_TEMPLATE.md`, `.github/problem-matchers/xtask.json`, `.github/scripts/zig-for-cargo-zigbuild`
- `.github/actions/**` and `.github/ci/**` beyond `project.toml` + state file: **do not exist** on main.

## 4. Static overrides: none left on main

- #992 (`fecda3c`) removed all **27 `[[static_files]]`** (pre-image `velnor-workflow.toml:72-184`), all of `.github-gen/sources/**`, all of `.github/actions/**`, plus `[policy].exclude_workflows` (10 interim #965 entries) and the `sources/**` scan exclusion.
- The 27 removed mappings were: 11 adopted workflows (`cache-cleanup`, `construct`, `desktop-cadence`, `docs`, `hygiene`, `jackin-dev`, `preview`, `release`, `renovate`, `renovate-validate`, `reuse-compliance`) + `ci-unit-swift.yml` override + 15 action files (`aggregate-needs`, `build-release-archive`, `cache-cargo-registry`, `check-deployed-docs`, `download-ci-xtask` + 6 runtime files, `download-codebook`, `sign-and-attest-archive`, `sign-capsule-manifest`).
- #993 (`92f347a`): pure pin bump `92c34786→b9c3156c`, no behavior change.
- #995 (`0be3fcf`): `[policy]` external checks trimmed to `["DCO"]` (`velnor-workflow.toml:24`; was + `construct-required`, `docs-link-check`, `docs-required`, `validate`) plus product code only.
- The restore attempt exists **only** on unmerged side branch `fix/restore-static-ci-after-992` (`ab5b0c4` = `origin/pr/994`, 63 files, +13408). Per AGENTS.md no-legacy rule it must not merge as static copies. Its commit message is the capability gap list the generator must absorb: macOS Swift lane (today `ci-unit-swift.yml:104` is `runs-on: ubuntu-26.04`, claimed to break `boltffi_cli`/`swift-package-native-ci`), DCO policy (already in `[policy]`), MBX env, and the release/preview/docs/hygiene/renovate/reuse behaviors.

## 5. Generator pin

`b9c3156cdb88e63c11b9e595a3e694b02238c09a`, pinned in exactly one source: `.github-gen/velnor-workflow.toml:5` (`[generator].revision`; `.repository = "jackin-project/jackin"` names the target repo, not the generator). All other occurrences are generated downstream copies: `tailrocks/velnor@<rev>` action refs (`ci-main.yml:62`, `ci-pr.yml:61`), `VELNOR_WORKFLOW_POLICY_REVISION` env, cache keys. No pin in `mise.toml`, `ratchet.toml`, or `Cargo.toml`. Branch `velnor/fleet-b9c31…` mirrors the rev name.

## Deliverable: migration inventory

- **Config sections to change (in Velnor, then re-pin here):** generator must natively emit the macOS Swift lane, release/preview signing, docs/hygiene/renovate/reuse-cadence, and fleet env currently achievable only via static copies; Jackin side then only bumps `[generator].revision`.
- **Static mappings expected to be removed:** already zero on main — keep it that way; reject `origin/pr/994`'s 27 re-added `[[static_files]]`.
- **Product-owned files to retain:** `PULL_REQUEST_TEMPLATE.md`, `problem-matchers/xtask.json`, `scripts/zig-for-cargo-zigbuild` (plus fix the dangling `.github/AGENTS.md` reference or create the file).
