# B3 de-risk scout — read-only, main@58a94b12

Scope: `tailrocks/velnor` origin/main = `58a94b12d0139c303ddf1dfdd57a36260c39fa02`
("Merge pull request #912"). No mutations performed (no tags/releases/dispatches/pushes/PRs).
Plan sources read first: `plans/bastion-three-provider-ci/work-plan.md` (STEP B3, lines 210–229),
`checklist.md` (B3 line 14), `spec.md` (§7 APT/delivery, lines 242–289).

Generator state post-flip: `.github-gen/velnor-workflow.toml:1` → `schema = 2`,
`revision = "ec399527..."`; `.github/workflows/release.yml:1` → "Generated by velnor-workflow".

## 1. RELEASE PATH

**Release producer: `.github/workflows/release.yml` (4030 lines, generated).**
Triggers (`release.yml:5-20`): `push.tags: ["v*"]` + `workflow_dispatch`
(providers subset + `existing-image-digest` recovery input). Tag-push path publishes;
dispatch path is verification/recovery-oriented (`image-platform` skipped on dispatch,
`release.yml:2726`).

Pipeline jobs (`release.yml`): `admit-provider` (:29) → `verify` (:40, incl.
`velnor-workflow release verify-tag --branch 'main' --package 'velnor-runner'` at :68
+ version resolve `vX → X` at :69-84) → 36 per-provider check lanes → `build` (:2583) →
`metadata` (:3004) → `guest-payload` (:3121) → `image-admission` (:2661) →
`image-platform` (:2723) → `image` (:2903) → `debian` (:3338) → `sign-deb` (:3647) →
`publish` (:3667, `environment: github-release`, `contents: write`).

`publish` produces via `gh release create "v${VERSION}" --verify-tag` (`release.yml:4003-4017`)
exactly 14 assets: 2 tarballs + 2 tarball sidecars, 2 debs + 2 deb sidecars,
`release-record.json` + sidecar, `manifest.json` + sidecar, `release-manifest.json`,
`SHA256SUMS`. Plus multi-arch GHCR image `ghcr.io/tailrocks/velnor-job-ubuntu:${VERSION}`
(index digest pinned in record, `release.yml:2946-2989`). Attestations:
`actions/attest-build-provenance` on tarballs (`release.yml:2649-2652`), OCI
provenance+SBOM (`release.yml:2857-2858`), debs via called signer (below); verified
pre-publish with `gh attestation verify` (`release.yml:3711-3722`).

**Supporting / adjacent files:**
- `.github/workflows/ci-release-package-signer.yml` — callable-only (`workflow_call`,
  no push/dispatch trigger, :10-24); attests one deb subject per call; admitted refs
  `refs/tags/v*` or `refs/heads/main` only. Called by `sign-deb` matrix {amd64, arm64}.
- `.github/workflows/ci-runtime-products.yml` — SEPARATE release identity
  (`velnor-workflow-runtime-v1-<closure16>`, stage-0 generator binaries, spec §3). NOT
  the Velnor release. Trigger: main push + dispatch.
- `.github/workflows/preview.yml` — rolling `preview` prerelease from main, separate
  identity. NOT the bootstrap release.
- No APT publication in this repo: `release.yml:4019-4021` (N6) — velnor-apt pulls the
  record itself; no APT credential here.

## 2. VERSION SOURCE

Tag-driven AND crate-bound (three-way equality enforced):
- `verify-tag` (`crates/velnor-workflow/src/s2/runtime.rs:3076-3134`): requires
  `GITHUB_REF=refs/tags/v<semver>`, tag commit == `origin/main` tip, and (with
  `--package`) tag version == `cargo metadata` version of `velnor-runner`.
- `crates/velnor-runner/build.rs:26-30` (release-build mode): build FAILS unless
  tree clean + exactly one `v*` tag at HEAD + `tag == v<crate version> == Cargo.lock`.
- `release.yml:3619-3620`: deb Version/Architecture must equal lane version/arch.

Current values at 58a94b12: `crates/velnor-runner/Cargo.toml:3` → `0.1.275`;
`Cargo.lock` → `velnor-runner 0.1.275`; highest numeric tag on origin → `v0.1.274`.
**A bootstrap cut from 58a94b12 would carry version `0.1.275` as tag `v0.1.275`
cut exactly at 58a94b12** (tag must equal main tip per verify-tag).

## 3. UNBOUNDED PREREQS (B3 action 2) — MISSING

`unbounded polic*`, `quota-free*`, `global.native*`, `native-backed`,
`native admission` appear ONLY in plan docs (`spec.md:204,287`, `work-plan.md:220,295,366`).
Zero hits in `crates/` or `config/` — no flag, env var, config key, or unit/drop-in
implements an unbounded/quota-free mode or a global-N native admission control.

What the tree carries instead is the BOUNDED policy the campaign must replace:
- `crates/velnor-runner/debian/velnor-jobs.slice:14-19` — `MemoryHigh=90%`,
  `MemoryMax=95%`, `MemorySwapMax=0` + required `10-host-cpu.conf` drop-in.
- `crates/velnor-runner/debian/postinst:31-52,252-261` — writes + enforces
  `CPUQuota=<95% of online CPUs>` drop-in, fails closed on mismatch.
- `crates/velnor-runner/src/execution/docker.rs:154-162` — boundary check enforces
  `NanoCpus=500000000, Memory=67108864` per job container.
- `crates/velnor-runner/src/admission.rs` — action-admission (workflow parsing), NOT
  native capacity admission. `capacity.rs` Docker-lifecycle permits are unrelated.
- No `velnor.env` / unit file carries an unbounded toggle
  (`crates/velnor-runner/debian/velnor.env` checked via packaging list).

Consistent with work-plan §0.7 (`work-plan.md:39-43`): the unbounded/global-native
foundation is "developed in parallel with A/B and included before C2" — i.e. parallel
source work, not yet landed at 58a94b12.

## 4. COHERENCE GATES (B3 action 3) — 3/4 present, 1 gap

All in `crates/velnor-runner/src/release/tests.rs` (2398 lines) unless noted:

- Mismatched arch — PRESENT: `:556 every_single_field_mismatch_is_caught`,
  `:642 per_arch_completeness_requires_both`, `:710 verify_installed_rejects_missing_host_arch`,
  `:945 assemble_rejects_incoherent_input`; error variants `ArchitectureSet`,
  `ArchTarget`, `InstalledArchMissing` (`release.rs:608+`).
- Missing manifest entries — PRESENT: `:784 assemble_command_requires_complete_binary_and_deb_artifacts`,
  `:792 ..._rejects_missing_binary_sidecar`, `:802 ..._rejects_missing_deb_sidecar`,
  `:812 ..._rejects_missing_deb_payload`.
- Bad digests — PRESENT: `:519 verify_record_bytes_rejects_wrong_checksum`,
  `:822 ..._rejects_malformed_artifact_checksum`, `:866 ..._rejects_deb_sidecar_mismatch`,
  `:882 ..._rejects_tampered_deb_even_with_refreshed_sidecar`; plus `:530` non-canonical,
  `:542` malformed JSON.
- Missing attestation — TEST GAP (enforcement present, negative test absent):
  enforcement = `release.yml:3711-3722` (`gh attestation verify` tarballs + debs with
  `--signer-workflow .../ci-release-package-signer.yml`, fail-closed pre-publish);
  production = `release.yml:2649` + `ci-release-package-signer.yml:36-39` +
  OCI `:2857-2858`. But no in-tree test feeds an unattested artifact through a
  verify path and asserts failure. Nearest neighbors are generator render-string
  assertions (`s2/primitives/release.rs:6503`, `velnor_first_ci.rs:1007-1008`) and
  job-manifest permission tests (`manifest.rs:3054,3068` — attest-action config
  validation, not release-attestation absence).

Related: `scripts/verify-release.sh` (spec §7: "audited and reused") does NOT exist
at origin/main (`git ls-tree | grep verify-release` → empty). Independent
verification today = inline `publish`-job steps + `release assemble`/`verify-record`
CLI (`release.rs:1758,1871`), not a standalone audited script.

## 5. NATIVE ARM64 — PRESENT (payload + image; binaries/debs are cross)

- `guest-payload` (`release.yml:3121-3134`): matrix `aarch64 → runner:
  ubuntu-24.04-arm`, `runs-on: ${{ matrix.runner }}` (:3123). Native arm64 build of
  guest agent + vmlinux + rootfs (`mbx build --target aarch64-...` on an aarch64
  host, :3251-3258; staging + digest pinning :3300-3313).
- `image-platform` (`release.yml:2723-2738`): matrix `arm64 → runner:
  ubuntu-24.04-arm`, native binary build (:2827-2837) + `platforms: linux/arm64`
  push (:2849-2869); assembled into one immutable 2-platform index by `image`
  (:2965-2980, platform-set asserted == 2).
- `ci-runtime-products.yml` build matrix: Linux/ARM64 → `ubuntu-24.04-arm` (native).
- NOT native (cross on x64, stated for precision): `build` job
  (`release.yml:2596`, static `runs-on: ubuntu-24.04`, both targets incl.
  `aarch64-unknown-linux-gnu` via `rustup target add` + `--target`) and `debian`
  job (`release.yml:3341`, static `runs-on: ubuntu-24.04`, both arches incl. arm64).

## Verdict

**B3-GAPS** (2 items):
1. **Unbounded-policy/global-native-admission prerequisites MISSING in-tree.**
   No flag/config/unit implements them; packaged defaults enforce the bounded
   policy (CPUQuota drop-in, MemoryHigh/Max, per-container NanoCpus/Memory).
   Bootstrap package cannot activate what does not exist — §0.7 parallel source
   work must land first (or B3 explicitly re-scopes).
2. **Missing-attestation negative TEST missing.** Enforcement exists in the
   generated `publish` job (`gh attestation verify`, fail-closed); a dedicated
   negative test proving "missing attestation fails the release" does not.
   (Also note: `scripts/verify-release.sh` referenced by spec §7 is absent.)

Present: release path via generated hosted CI (`release.yml`, tag `v*` + dispatch),
14-asset product set + GHCR multi-arch index + attestations, version source
(tag == crate == lockfile, bootstrap = `0.1.275`/`v0.1.275` @ 58a94b12), 3/4
coherence negative suites, native-arm64 guest-payload + OCI producers.
