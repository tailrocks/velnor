# Integration evidence — batch 2 (bastion campaign)

Target: `docs/bastion-final-plan`, shared workspace
`/Users/donbeave/Projects/tailrocks/velnor-project/velnor3`.
Pushed: `origin/docs/bastion-final-plan` `96ccc0f1`..`411b8a99`.

## Inputs (certifications read first)

- `/tmp/v-fix-opstore.md`: CERTIFIED — `fix/opstore-rejection-cause` @
  `915ed0725ed316cd240a86c26efb1a343c7a9808` (base `38ffbfd7`).
- `/tmp/v-a2-srcid.md`: CERTIFIED — `feat/a2-source-identity` @
  `65beb2edec4b186a1ba86052fd6a8429a6acb16c` (base `38ffbfd7`).

Pre-merge state: `git fetch origin`; on `docs/bastion-final-plan` @
`96ccc0f1`, clean tree. Local tips of both source branches matched the
certified commits and `origin/*`. Both merge-bases = `38ffbfd7`.

## Merge 1: fix/opstore-rejection-cause

`git merge --no-ff` → clean (disjoint files: runner/model vs batch-1's
workflow files). Commit `00656e0f` (signed off):

> Merge fix/opstore-rejection-cause into docs/bastion-final-plan

6 files: telemetry.rs, executor.rs, ops.rs, runner.rs, test_support.rs,
schemas/velnor.telemetry.v1.json (+450/-89).

## Merge 2: feat/a2-source-identity

`git merge --no-ff` → 2 conflicts:

1. `crates/velnor-workflow/src/lib.rs` (SOURCE): batch-1 dropped the
   baked `PINNED_REVISION: {revision}` env for runtime pin parsing from
   `.github-gen/velnor-workflow.toml`; a2 added the `(.revision | test)`
   jq clause + `--revision` probe to the same format string.
   Resolution (script `/tmp/resolve-lib.py`, all assertions held): HEAD
   body kept, a2 filter clause + 3-line revision probe copied verbatim
   from the a2 side; rendered filter proven byte-equal to a2's; probe
   ordering closure-gate → manifest probe → revision probe → revision
   gate asserted. A2's doc + test hunks auto-merged coherently
   (single-arg call + revision assertions).
2. `.github/ci/.github-actions-generator-state` (GENERATED): reset to
   HEAD side, then regenerated. Never hand-edited.

Post-merge compile fix (source only): a2 added a test at
`primitives/release.rs:3420` calling
`workflow_pinned_policy_runtime_velnor(FIXTURE_REVISION, "checkout")`
with the pre-batch-1 2-arg signature → E0061. Dropped the removed arg
(test only needs rendered text; `FIXTURE_REVISION` still used 2×
elsewhere in the file).

Regen: rebuilt `cargo build -p velnor-workflow`, ran
`./target/debug/velnor-workflow --plain --force`, then
`--plain --dry-run .` → **0 files would change**.

Rendered-output checks (regenerated bytes, not hand edits):

- `--revision` line counts per file identical to a2 parent (1/1/4/2
  across ci-unit-rust/release/ci-runtime-products/setup-action).
- `velnor-workflow.toml` pin-parse counts identical to batch-1 parent
  (3/2/1 across ci-unit-rust/release/preview).
- The merged Velnor provisioner step in `ci-unit-rust.yml` contains the
  pin parse + filter clause + both probes, and no baked
  `PINNED_REVISION:` env.

Commit `411b8a99` (signed off):

> Merge feat/a2-source-identity into docs/bastion-final-plan

9 files (+360/-51): action source + rendered action, generator state,
3 workflows, lib.rs, release.rs, runtime_products.rs.

Signoff redo note: the first stack (`108f43aa`/`05606dd2`) was rebuilt
because `108f43aa` lacked `Signed-off-by`. Re-merge replayed both
conflict resolutions via rerere (lib.rs proven byte-identical to the
verified resolution); the rebuilt staged tree was proven empty-diff vs
the fully-gated first tree (`git diff int2-backup --quiet` clean), so
all gate results below apply to the pushed bytes. Backup branch
deleted; nothing else pushed.

## Gates (all green, on the pushed tree bytes)

- `--plain --dry-run`: `Dry-run: 0 files would change`, exit 0
  (re-run after the redo).
- `cargo test -p velnor-workflow`: 492 lib + 2/6/5/9/33, 0 failed.
- `cargo test -p velnor-runner --lib`: 2177 passed / 0 failed /
  4 ignored — matches opstore cert exactly.
- `cargo clippy -p velnor-workflow -p velnor-runner -p velnor-model
  --all-targets -- -D warnings`: exit 0, no warnings.
- `cargo fmt -p velnor-workflow -p velnor-runner -p velnor-model --
  --check`: clean.
- `actionlint` 1.7.12 on ci-unit-rust, release, ci-runtime-products,
  preview: exit 0, no findings.
- `git diff --check`: clean. Final `git status`: clean, in sync with
  `origin/docs/bastion-final-plan` @ `411b8a99`.
