# Generated by velnor-workflow. Regenerate; do not hand-edit.
# SPDX-FileCopyrightText: 2026 Alexey Zhokhov
# SPDX-License-Identifier: Apache-2.0

name: Release

on:
  push:
    tags:
      - 'v[0-9]*'
  workflow_dispatch:
    inputs:
      mode:
        description: |
          validate = secret-free assembly + reconciliation fixtures (feature branch OK).
          Stable publication is tag-triggered only.
        type: choice
        default: validate
        options: [validate]
      # Menu-bar signing always uses GitHub-hosted macOS (never Velnor) — the
      # Velnor fleet has no macOS runners (Apple credential boundary).
      lanes:
        description: velnor (default) | github | both
        type: choice
        default: velnor
        options: [velnor, github, both]

permissions:
  actions: read
  contents: read

# Two simultaneous tag pushes (or a tag push racing a workflow_dispatch
# from main) would otherwise both reach `gh release create` and the
# second loses to a 422 from a duplicate tag/release. Group key is
# specifically `release-` (no ref) because we want serialization
# across tags too.
concurrency:
  group: release
  cancel-in-progress: false

jobs:
  matrix-setup:
    name: Select runner lane
    runs-on: ${{ ((github.event_name == 'workflow_dispatch' && inputs.lanes == 'github') || github.event_name == 'pull_request' || github.event_name == 'push') && 'ubuntu-26.04' || fromJSON('["self-hosted","velnor-target-mvp"]') }}
    timeout-minutes: 5
    outputs:
      configs: ${{ steps.set.outputs.configs }}
      has_github: ${{ steps.set.outputs.has_github }}
    steps:
      - id: set
        env:
          LANES: ${{ github.event_name == 'workflow_dispatch' && inputs.lanes || github.event_name == 'push' && 'github' || 'velnor' }}
        run: |
          github='{"lane":"GitHub","runner":"\"ubuntu-26.04\"","runner_os":"Linux","runner_arch":"X64"}'
          velnor='{"lane":"Velnor","runner":"[\"self-hosted\",\"velnor-target-mvp\"]","runner_os":"Linux","runner_arch":"X64"}'
          case "$LANES" in
            velnor)
              configs="[$velnor]"
              has_github=false
              ;;
            both)
              configs="[$velnor,$github]"
              has_github=true
              ;;
            *)
              configs="[$github]"
              has_github=true
              ;;
          esac
          {
            echo "configs=$configs"
            echo "has_github=$has_github"
          } >> "$GITHUB_OUTPUT"

  check-version:
    runs-on: ${{ ((github.event_name == 'workflow_dispatch' && inputs.lanes == 'github') || github.event_name == 'pull_request' || github.event_name == 'push') && 'ubuntu-26.04' || fromJSON('["self-hosted","velnor-target-mvp"]') }}
    timeout-minutes: 5
    outputs:
      published: ${{ steps.published.outputs.published }}
      version: ${{ steps.version.outputs.version }}
      mode: ${{ steps.mode.outputs.mode }}
      app_build: ${{ steps.version.outputs.app_build }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0 # git rev-list derives the immutable CFBundleVersion.

      - name: Resolve mode
        id: mode
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            mode="${{ inputs.mode }}"
          else
            # Tag pushes always publish.
            mode=publish
          fi
          echo "mode=$mode" >> "$GITHUB_OUTPUT"

      - name: Gate branch for mode
        env:
          MODE: ${{ steps.mode.outputs.mode }}
        run: |
          set -euo pipefail
          if [[ "$MODE" == "validate" ]]; then
            echo "validate mode: feature branches allowed; no secrets; no external writes"
          fi

      - name: Determine version
        id: version
        env:
          GH_TOKEN: ${{ github.token }}
          MODE: ${{ steps.mode.outputs.mode }}
        run: |
          set -euo pipefail
          if [[ "$MODE" == "validate" ]]; then
            # Fixed fixture for secret-free assembly + reconciliation drills.
            version="0.0.0"
            app_build="1"
            echo "version=$version" >> "$GITHUB_OUTPUT"
            echo "app_build=$app_build" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            version="${GITHUB_REF#refs/tags/v}"
          else
            version=$(sed -n 's/^version = "\(.*\)"/\1/p' Cargo.toml | head -1)
            if [[ "$version" == *"-dev"* ]]; then
              echo "::error::Cannot release a dev version ($version)"
              exit 1
            fi
          fi
          if ! [[ "$version" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "::error::Stable app version must be X.Y.Z (got $version)"
            exit 1
          fi
          # CFBundleVersion: full-history commit count (main ancestry for tags).
          app_build=$(git rev-list --count HEAD)
          if ! [[ "$app_build" =~ ^[0-9]+$ ]]; then
            echo "::error::CFBundleVersion commit count is not numeric"
            exit 1
          fi
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "app_build=$app_build" >> "$GITHUB_OUTPUT"

      - name: Reuse a completely published release
        id: published
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ steps.version.outputs.version }}
          MODE: ${{ steps.mode.outputs.mode }}
        run: |
          set -euo pipefail
          if [[ "$MODE" == "validate" ]]; then
            echo "published=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          if ! gh release view "v${VERSION}" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            echo "published=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # Independent states: formula alone is not enough when menu-bar assets exist.
          state="$(mise run desktop-release-state "$VERSION" "$GITHUB_REPOSITORY" || true)"
          echo "$state"
          cask_complete=$(printf '%s\n' "$state" | sed -n 's/^cask_complete=//p')
          app_complete=$(printf '%s\n' "$state" | sed -n 's/^app_file_assets_complete=//p')
          formula=$(
            curl -fsSL --max-time 30 \
              "https://raw.githubusercontent.com/jackin-project/homebrew-tap/main/Formula/jackin.rb"
          )
          formula_version=$(printf '%s\n' "$formula" | sed -n 's/^[[:space:]]*version "\(.*\)"/\1/p' | head -1)
          if [ "$formula_version" = "$VERSION" ] && [ "$app_complete" = "true" ] && [ "$cask_complete" = "true" ]; then
            echo "published=true" >> "$GITHUB_OUTPUT"
            echo "jackin❯ v${VERSION} release, formula, and usage menu-bar cask already complete; reusing"
          elif [ "$formula_version" = "$VERSION" ] && [ "$app_complete" != "true" ]; then
            # Legacy CLI-only complete release without menu-bar assets yet — continue to add app.
            echo "published=false" >> "$GITHUB_OUTPUT"
          elif [ "$formula_version" = "$VERSION" ]; then
            echo "published=false" >> "$GITHUB_OUTPUT"
          else
            echo "published=false" >> "$GITHUB_OUTPUT"
          fi

  test:
    if: >-
      needs.check-version.outputs.published != 'true' &&
      needs.check-version.outputs.mode == 'publish'
    needs: [check-version, matrix-setup]
    strategy:
      fail-fast: false
      matrix:
        config: ${{ fromJSON(needs.matrix-setup.outputs.configs) }}
    runs-on: ${{ fromJSON(matrix.config.runner) }}
    timeout-minutes: 60
    name: test release workspace (${{ matrix.config.lane }})
    env:
      CARGO_INCREMENTAL: "0"
      CARGO_TERM_COLOR: always
      RUSTC_WRAPPER: sccache
      SCCACHE_GHA_ENABLED: "false"
      SCCACHE_CACHE_SIZE: 20G
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Bound the Mr. Boxington store
        shell: bash
        run: echo "MBX_GC_MAX_SIZE=12GiB" >> "$GITHUB_ENV"
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-static-mbx-v3-ae157df2a7ca-${{ runner.os }}-${{ runner.arch }}-${{ github.workflow }}-${{ github.job }}-${{ hashFiles('Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain', 'mise.toml', 'mise.lock', '**/Cargo.toml') }}-${{ hashFiles('./**/*.rs', 'crates/jackin-agent-status/**/*.rs', 'crates/jackin-agent-status/benches/**', 'crates/jackin-agent-status/examples/**', 'crates/jackin-agent-status/src/**', 'crates/jackin-agent-status/tests/**', 'crates/jackin-brand/**/*.rs', 'crates/jackin-brand/benches/**', 'crates/jackin-brand/examples/**', 'crates/jackin-brand/src/**', 'crates/jackin-brand/tests/**', 'crates/jackin-build-meta/**/*.rs', 'crates/jackin-build-meta/benches/**', 'crates/jackin-build-meta/examples/**', 'crates/jackin-build-meta/src/**', 'crates/jackin-build-meta/tests/**', 'crates/jackin-capsule/**/*.rs', 'crates/jackin-capsule/benches/**', 'crates/jackin-capsule/build.rs', 'crates/jackin-capsule/examples/**', 'crates/jackin-capsule/src/**', 'crates/jackin-capsule/src/daemon.rs', 'crates/jackin-capsule/tests/**', 'crates/jackin-config/**/*.rs', 'crates/jackin-config/benches/**', 'crates/jackin-config/examples/**', 'crates/jackin-config/fuzz/**/*.rs', 'crates/jackin-config/fuzz/benches/**', 'crates/jackin-config/fuzz/examples/**', 'crates/jackin-config/fuzz/src/**', 'crates/jackin-config/fuzz/tests/**', 'crates/jackin-config/src/**', 'crates/jackin-config/tests/**', 'crates/jackin-console/**/*.rs', 'crates/jackin-console/benches/**', 'crates/jackin-console/examples/**', 'crates/jackin-console/src/**', 'crates/jackin-console/tests/**', 'crates/jackin-core/**/*.rs', 'crates/jackin-core/benches/**', 'crates/jackin-core/examples/**', 'crates/jackin-core/src/**', 'crates/jackin-core/tests/**', 'crates/jackin-dev/**/*.rs', 'crates/jackin-dev/benches/**', 'crates/jackin-dev/examples/**', 'crates/jackin-dev/src/**', 'crates/jackin-dev/tests/**', 'crates/jackin-diagnostics/**/*.rs', 'crates/jackin-diagnostics/benches/**', 'crates/jackin-diagnostics/build.rs', 'crates/jackin-diagnostics/examples/**', 'crates/jackin-diagnostics/src/**', 'crates/jackin-diagnostics/tests/**', 'crates/jackin-docker/**/*.rs', 'crates/jackin-docker/benches/**', 'crates/jackin-docker/build.rs', 'crates/jackin-docker/examples/**', 'crates/jackin-docker/src/**', 'crates/jackin-docker/tests/**', 'crates/jackin-env/**/*.rs', 'crates/jackin-env/benches/**', 'crates/jackin-env/examples/**', 'crates/jackin-env/fuzz/**/*.rs', 'crates/jackin-env/fuzz/benches/**', 'crates/jackin-env/fuzz/examples/**', 'crates/jackin-env/fuzz/src/**', 'crates/jackin-env/fuzz/tests/**', 'crates/jackin-env/src/**', 'crates/jackin-env/tests/**', 'crates/jackin-host/**/*.rs', 'crates/jackin-host/benches/**', 'crates/jackin-host/examples/**', 'crates/jackin-host/src/**', 'crates/jackin-host/tests/**', 'crates/jackin-image/**/*.rs', 'crates/jackin-image/benches/**', 'crates/jackin-image/build.rs', 'crates/jackin-image/examples/**', 'crates/jackin-image/src/**', 'crates/jackin-image/tests/**', 'crates/jackin-instance/**/*.rs', 'crates/jackin-instance/benches/**', 'crates/jackin-instance/examples/**', 'crates/jackin-instance/src/**', 'crates/jackin-instance/tests/**', 'crates/jackin-isolation/**/*.rs', 'crates/jackin-isolation/benches/**', 'crates/jackin-isolation/examples/**', 'crates/jackin-isolation/src/**', 'crates/jackin-isolation/tests/**', 'crates/jackin-launch/**/*.rs', 'crates/jackin-launch/benches/**', 'crates/jackin-launch/build.rs', 'crates/jackin-launch/examples/**', 'crates/jackin-launch/src/**', 'crates/jackin-launch/tests/**', 'crates/jackin-lints/**/*.rs', 'crates/jackin-lints/benches/**', 'crates/jackin-lints/examples/**', 'crates/jackin-lints/src/**', 'crates/jackin-lints/tests/**', 'crates/jackin-manifest/**/*.rs', 'crates/jackin-manifest/benches/**', 'crates/jackin-manifest/examples/**', 'crates/jackin-manifest/fuzz/**/*.rs', 'crates/jackin-manifest/fuzz/benches/**', 'crates/jackin-manifest/fuzz/examples/**', 'crates/jackin-manifest/fuzz/src/**', 'crates/jackin-manifest/fuzz/tests/**', 'crates/jackin-manifest/src/**', 'crates/jackin-manifest/tests/**', 'crates/jackin-oppicker/**/*.rs', 'crates/jackin-oppicker/benches/**', 'crates/jackin-oppicker/examples/**', 'crates/jackin-oppicker/src/**', 'crates/jackin-oppicker/tests/**', 'crates/jackin-otlp-testbed/**/*.rs', 'crates/jackin-otlp-testbed/benches/**', 'crates/jackin-otlp-testbed/examples/**', 'crates/jackin-otlp-testbed/src/**', 'crates/jackin-otlp-testbed/tests/**', 'crates/jackin-pr-trailers/**/*.rs', 'crates/jackin-pr-trailers/benches/**', 'crates/jackin-pr-trailers/examples/**', 'crates/jackin-pr-trailers/src/**', 'crates/jackin-pr-trailers/tests/**', 'crates/jackin-process/**/*.rs', 'crates/jackin-process/benches/**', 'crates/jackin-process/examples/**', 'crates/jackin-process/src/**', 'crates/jackin-process/tests/**', 'crates/jackin-protocol/**/*.rs', 'crates/jackin-protocol/benches/**', 'crates/jackin-protocol/examples/**', 'crates/jackin-protocol/fuzz/**/*.rs', 'crates/jackin-protocol/fuzz/benches/**', 'crates/jackin-protocol/fuzz/examples/**', 'crates/jackin-protocol/fuzz/src/**', 'crates/jackin-protocol/fuzz/tests/**', 'crates/jackin-protocol/src/**', 'crates/jackin-protocol/tests/**', 'crates/jackin-runtime/**/*.rs', 'crates/jackin-runtime/benches/**', 'crates/jackin-runtime/build.rs', 'crates/jackin-runtime/examples/**', 'crates/jackin-runtime/src/**', 'crates/jackin-runtime/tests/**', 'crates/jackin-telemetry/**/*.rs', 'crates/jackin-telemetry/benches/**', 'crates/jackin-telemetry/examples/**', 'crates/jackin-telemetry/src/**', 'crates/jackin-telemetry/tests/**', 'crates/jackin-test-support/**/*.rs', 'crates/jackin-test-support/benches/**', 'crates/jackin-test-support/examples/**', 'crates/jackin-test-support/src/**', 'crates/jackin-test-support/tests/**', 'crates/jackin-tui/**/*.rs', 'crates/jackin-tui/benches/**', 'crates/jackin-tui/examples/**', 'crates/jackin-tui/src/**', 'crates/jackin-tui/tests/**', 'crates/jackin-usage-ffi/**/*.rs', 'crates/jackin-usage-ffi/benches/**', 'crates/jackin-usage-ffi/examples/**', 'crates/jackin-usage-ffi/src/**', 'crates/jackin-usage-ffi/tests/**', 'crates/jackin-usage/**/*.rs', 'crates/jackin-usage/benches/**', 'crates/jackin-usage/examples/**', 'crates/jackin-usage/src/**', 'crates/jackin-usage/tests/**', 'crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json', 'crates/jackin-xtask/**/*.rs', 'crates/jackin-xtask/benches/**', 'crates/jackin-xtask/examples/**', 'crates/jackin-xtask/src/**', 'crates/jackin-xtask/tests/**', 'crates/jackin/**/*.rs', 'crates/jackin/benches/**', 'crates/jackin/build.rs', 'crates/jackin/examples/**', 'crates/jackin/src/**', 'crates/jackin/tests/**', 'docker/construct/Dockerfile', 'docker/runtime/agent-status/hooks/claude/report-hook.sh', 'docker/runtime/agent-status/hooks/codex/report-hook.sh', 'docker/runtime/agent-status/hooks/opencode/plugin.js', 'docker/runtime/agent-status/hooks/opencode/report-hook.sh', 'docker/runtime/entrypoint.sh', 'mise.lock', 'vendor/arrayref/**/*.rs', 'vendor/arrayref/benches/**', 'vendor/arrayref/examples/**', 'vendor/arrayref/src/**', 'vendor/arrayref/tests/**') }}
          restore-keys: |
            velnor-static-mbx-v3-ae157df2a7ca-${{ runner.os }}-${{ runner.arch }}-${{ github.workflow }}-${{ github.job }}-${{ hashFiles('Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain', 'mise.toml', 'mise.lock', '**/Cargo.toml') }}-
            velnor-static-mbx-v3-ae157df2a7ca-${{ runner.os }}-${{ runner.arch }}-${{ github.workflow }}-${{ github.job }}-
          save-on-workflow-dispatch: true
      - uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba # v0.0.11
        with:
          version: v0.16.0
          disable_annotations: "false"
      - uses: jdx/mise-action@3c2e0cf82a5b2e5249f0d3635a4d83d0ae861518 # v4.2.5
        with:
          version: "2026.7.12"
          cache_key_prefix: "mise-v2"
          cache_save: false
          install_args: "cargo-binstall rust aqua:nextest-rs/nextest/cargo-nextest github:open-telemetry/weaver"
          github_token: ${{ secrets.GH_READONLY_TOKEN }}
      - name: Bind Cargo cache actions into admitted closure
        if: github.event_name == 'velnor-admission-closure'
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      - name: Bind Cargo cache save into admitted closure
        if: github.event_name == 'velnor-admission-closure'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      - uses: ./.github/actions/cache-cargo-registry
      - name: Cache Cargo target
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: target
          key: cargo-target-${{ matrix.config.lane }}-${{ runner.os }}-${{ github.job }}-${{ hashFiles('rust-toolchain.toml', '.cargo/config.toml', '**/Cargo.toml', '**/Cargo.lock') }}-${{ github.sha }}
          restore-keys: cargo-target-${{ matrix.config.lane }}-${{ runner.os }}-${{ github.job }}-${{ hashFiles('rust-toolchain.toml', '.cargo/config.toml', '**/Cargo.toml', '**/Cargo.lock') }}-
      - run: mbx nextest run --workspace --locked --no-fail-fast

  build:
    if: >-
      needs.check-version.outputs.published != 'true' &&
      needs.check-version.outputs.mode == 'publish'
    needs: [check-version, matrix-setup]
    strategy:
      fail-fast: false
      matrix:
        config: ${{ fromJSON(needs.matrix-setup.outputs.configs) }}
        target:
          - triple: x86_64-apple-darwin
            zigbuild_target: x86_64-apple-darwin
          - triple: aarch64-apple-darwin
            zigbuild_target: aarch64-apple-darwin
          - triple: x86_64-unknown-linux-gnu
            zigbuild_target: x86_64-unknown-linux-gnu.2.17
          - triple: aarch64-unknown-linux-gnu
            zigbuild_target: aarch64-unknown-linux-gnu.2.17
    runs-on: ${{ fromJSON(matrix.config.runner) }}
    timeout-minutes: 60
    name: build jackin ${{ matrix.target.triple }} (${{ matrix.config.lane }})
    permissions:
      contents: read
      id-token: write
      attestations: write
    env:
      CARGO_INCREMENTAL: "0"
      CARGO_TERM_COLOR: always
      RUSTC_WRAPPER: sccache
      SCCACHE_BASEDIRS: ${{ github.workspace }}
      SCCACHE_GHA_ENABLED: "off"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Cache Rust toolchain
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup/toolchains
            ~/.rustup/update-hashes
          key: rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml') }}
          restore-keys: |
            rustup-${{ runner.os }}-${{ runner.arch }}-
      - uses: jdx/mise-action@3c2e0cf82a5b2e5249f0d3635a4d83d0ae861518 # v4.2.5
        env:
          # sccache install: blank wrapper in case binstall source-falls-back
          # to cargo, which would probe rustc through a missing sccache.
          RUSTC_WRAPPER: ""
        with:
          version: "2026.7.12"
          cache_key_prefix: "mise-v2"
          cache_save: ${{ github.ref == 'refs/heads/main' }}
          install_args: "cargo-binstall rust zig cargo:cargo-zigbuild cosign syft cargo:sccache"
          github_token: ${{ secrets.GH_READONLY_TOKEN }}
      - name: Bind Cargo cache actions into admitted closure
        if: github.event_name == 'velnor-admission-closure'
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      - name: Bind Cargo cache save into admitted closure
        if: github.event_name == 'velnor-admission-closure'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      - id: cargo-registry-cache
        uses: ./.github/actions/cache-cargo-registry
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          shared-key: release-archive-jackin-${{ matrix.target.triple }}-workspace-v2
          cache-all-crates: "true"
          cache-workspace-crates: "true"
          env-vars: "CARGO CC CFLAGS CXX CMAKE RUSTC_WRAPPER RUSTFLAGS RUSTDOC RUSTUP"
          save-if: ${{ github.ref == 'refs/heads/main' }}
          # No branch in the cache key: GitHub isolates branch caches, so every
          # PR/branch/release reads the default branch's warm cache via fallback.
          # Only main saves, preserving limited cache quota for reusable entries.
      - run: rustup target add ${{ matrix.target.triple }}
      - name: Cache macOS SDK
        if: contains(matrix.target.triple, 'apple')
        id: sdk-cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: /tmp/macos-sdk
          key: macos-sdk-26.1
      - name: Download macOS SDK
        if: contains(matrix.target.triple, 'apple') && steps.sdk-cache.outputs.cache-hit != 'true'
        run: |
          mkdir -p /tmp/macos-sdk
          curl -fsSL "https://github.com/joseluisq/macosx-sdks/releases/download/26.1/MacOSX26.1.sdk.tar.xz" \
            | tar -xJ -C /tmp/macos-sdk
      - name: Set macOS SDKROOT
        if: contains(matrix.target.triple, 'apple')
        run: |
          echo "SDKROOT=/tmp/macos-sdk/MacOSX26.1.sdk" >> "$GITHUB_ENV"
          echo "MACOSX_DEPLOYMENT_TARGET=12.0" >> "$GITHUB_ENV"
      - name: Build signed archive
        uses: ./.github/actions/build-release-archive
        with:
          package: jackin
          target: ${{ matrix.target.triple }}
          zigbuild-target: ${{ matrix.target.zigbuild_target }}
          version: ${{ needs.check-version.outputs.version }}
          archive-basename: jackin-${{ needs.check-version.outputs.version }}-${{ matrix.target.triple }}
          artifact-name: release-${{ matrix.config.lane }}-jackin-${{ matrix.target.triple }}
          timings-artifact-name: cargo-timings-release-jackin-${{ matrix.target.triple }}-${{ matrix.config.lane }}
          sccache-stats-artifact-name: sccache-stats-release-jackin-${{ matrix.target.triple }}-${{ matrix.config.lane }}
          sccache-stats-heading: sccache release jackin ${{ matrix.target.triple }} (${{ matrix.config.lane }})

  build-jackin-capsule:
    if: >-
      needs.check-version.outputs.published != 'true' &&
      needs.check-version.outputs.mode == 'publish'
    needs: [check-version, matrix-setup]
    runs-on: ${{ fromJSON(matrix.config.runner) }}
    timeout-minutes: 60
    name: build jackin-capsule ${{ matrix.target.triple }} (${{ matrix.config.lane }})
    permissions:
      contents: read
      id-token: write
      attestations: write
    env:
      CARGO_INCREMENTAL: "0"
      CARGO_TERM_COLOR: always
      RUSTC_WRAPPER: sccache
      SCCACHE_BASEDIRS: ${{ github.workspace }}
      SCCACHE_GHA_ENABLED: "off"
    strategy:
      fail-fast: false
      matrix:
        config: ${{ fromJSON(needs.matrix-setup.outputs.configs) }}
        target:
          - triple: aarch64-unknown-linux-gnu
            zigbuild_target: aarch64-unknown-linux-gnu.2.17
          - triple: x86_64-unknown-linux-gnu
            zigbuild_target: x86_64-unknown-linux-gnu.2.17
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Cache Rust toolchain
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup/toolchains
            ~/.rustup/update-hashes
          key: rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml') }}
          restore-keys: |
            rustup-${{ runner.os }}-${{ runner.arch }}-
      - uses: jdx/mise-action@3c2e0cf82a5b2e5249f0d3635a4d83d0ae861518 # v4.2.5
        env:
          # sccache install: blank wrapper in case binstall source-falls-back
          # to cargo, which would probe rustc through a missing sccache.
          RUSTC_WRAPPER: ""
        with:
          version: "2026.7.12"
          cache_key_prefix: "mise-v2"
          cache_save: ${{ github.ref == 'refs/heads/main' }}
          install_args: "cargo-binstall rust zig cargo:cargo-zigbuild cosign syft cargo:sccache"
          github_token: ${{ secrets.GH_READONLY_TOKEN }}
      - name: Bind Cargo cache actions into admitted closure
        if: github.event_name == 'velnor-admission-closure'
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      - name: Bind Cargo cache save into admitted closure
        if: github.event_name == 'velnor-admission-closure'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      - id: cargo-registry-cache
        uses: ./.github/actions/cache-cargo-registry
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          shared-key: release-archive-capsule-${{ matrix.target.triple }}-workspace-v2
          cache-all-crates: "true"
          cache-workspace-crates: "true"
          env-vars: "CARGO CC CFLAGS CXX CMAKE RUSTC_WRAPPER RUSTFLAGS RUSTDOC RUSTUP"
          save-if: ${{ github.ref == 'refs/heads/main' }}
          # No branch in the cache key: GitHub isolates branch caches, so every
          # PR/branch/release reads the default branch's warm cache via fallback.
          # Only main saves, preserving limited cache quota for reusable entries.
      - run: rustup target add ${{ matrix.target.triple }}
      - name: Build signed capsule archive
        uses: ./.github/actions/build-release-archive
        with:
          package: jackin-capsule
          target: ${{ matrix.target.triple }}
          zigbuild-target: ${{ matrix.target.zigbuild_target }}
          version: ${{ needs.check-version.outputs.version }}
          archive-basename: jackin-capsule-${{ needs.check-version.outputs.version }}-${{ matrix.target.triple }}
          artifact-name: release-${{ matrix.config.lane }}-jackin-capsule-${{ matrix.target.triple }}
          timings-artifact-name: cargo-timings-release-capsule-${{ matrix.target.triple }}-${{ matrix.config.lane }}
          sccache-stats-artifact-name: sccache-stats-release-capsule-${{ matrix.target.triple }}-${{ matrix.config.lane }}
          sccache-stats-heading: sccache release capsule ${{ matrix.target.triple }} (${{ matrix.config.lane }})

  # Secret-free (validate) or credentialed (publish) arm64 menu-bar app.
  # GitHub-hosted macOS only — never Velnor (Apple credential boundary).
  build-usage-menu-bar:
    if: >-
      needs.check-version.outputs.published != 'true' &&
      (github.event_name != 'workflow_dispatch' || inputs.lanes != 'velnor')
    needs: [check-version]
    # Fixed shipping baseline: Tahoe host and Xcode 26.6, never a moving latest label.
    runs-on: macos-26
    timeout-minutes: 90
    environment: ${{ needs.check-version.outputs.mode == 'publish' && 'release-macos' || '' }}
    permissions:
      contents: read
      id-token: write
      attestations: write
    env:
      VERSION: ${{ needs.check-version.outputs.version }}
      APP_BUILD: ${{ needs.check-version.outputs.app_build }}
      MODE: ${{ needs.check-version.outputs.mode }}
      CARGO_TERM_COLOR: always
      RUSTC_WRAPPER: sccache
      SCCACHE_GHA_ENABLED: "false"
      SCCACHE_CACHE_SIZE: 20G
      DEVELOPER_DIR: /Applications/Xcode_26.6.app/Contents/Developer
      MACOSX_DEPLOYMENT_TARGET: "26.0"
    outputs:
      app_sha256: ${{ steps.sha.outputs.app_sha256 }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Bound the Mr. Boxington store
        shell: bash
        run: echo "MBX_GC_MAX_SIZE=12GiB" >> "$GITHUB_ENV"
      - name: Set up Mr. Boxington
        uses: jdx/mr-boxington-action@7234d3dd1a6ca8f6c381eea8e4dfb03f18fcf777 # v1.3.0
        with:
          backend: github
          github-cache-mode: objects
          version: 1.8.3
          cache-key: velnor-static-mbx-v3-ae157df2a7ca-${{ runner.os }}-${{ runner.arch }}-${{ github.workflow }}-${{ github.job }}-${{ hashFiles('Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain', 'mise.toml', 'mise.lock', '**/Cargo.toml') }}-${{ hashFiles('./**/*.rs', 'crates/jackin-agent-status/**/*.rs', 'crates/jackin-agent-status/benches/**', 'crates/jackin-agent-status/examples/**', 'crates/jackin-agent-status/src/**', 'crates/jackin-agent-status/tests/**', 'crates/jackin-brand/**/*.rs', 'crates/jackin-brand/benches/**', 'crates/jackin-brand/examples/**', 'crates/jackin-brand/src/**', 'crates/jackin-brand/tests/**', 'crates/jackin-build-meta/**/*.rs', 'crates/jackin-build-meta/benches/**', 'crates/jackin-build-meta/examples/**', 'crates/jackin-build-meta/src/**', 'crates/jackin-build-meta/tests/**', 'crates/jackin-capsule/**/*.rs', 'crates/jackin-capsule/benches/**', 'crates/jackin-capsule/build.rs', 'crates/jackin-capsule/examples/**', 'crates/jackin-capsule/src/**', 'crates/jackin-capsule/src/daemon.rs', 'crates/jackin-capsule/tests/**', 'crates/jackin-config/**/*.rs', 'crates/jackin-config/benches/**', 'crates/jackin-config/examples/**', 'crates/jackin-config/fuzz/**/*.rs', 'crates/jackin-config/fuzz/benches/**', 'crates/jackin-config/fuzz/examples/**', 'crates/jackin-config/fuzz/src/**', 'crates/jackin-config/fuzz/tests/**', 'crates/jackin-config/src/**', 'crates/jackin-config/tests/**', 'crates/jackin-console/**/*.rs', 'crates/jackin-console/benches/**', 'crates/jackin-console/examples/**', 'crates/jackin-console/src/**', 'crates/jackin-console/tests/**', 'crates/jackin-core/**/*.rs', 'crates/jackin-core/benches/**', 'crates/jackin-core/examples/**', 'crates/jackin-core/src/**', 'crates/jackin-core/tests/**', 'crates/jackin-dev/**/*.rs', 'crates/jackin-dev/benches/**', 'crates/jackin-dev/examples/**', 'crates/jackin-dev/src/**', 'crates/jackin-dev/tests/**', 'crates/jackin-diagnostics/**/*.rs', 'crates/jackin-diagnostics/benches/**', 'crates/jackin-diagnostics/build.rs', 'crates/jackin-diagnostics/examples/**', 'crates/jackin-diagnostics/src/**', 'crates/jackin-diagnostics/tests/**', 'crates/jackin-docker/**/*.rs', 'crates/jackin-docker/benches/**', 'crates/jackin-docker/build.rs', 'crates/jackin-docker/examples/**', 'crates/jackin-docker/src/**', 'crates/jackin-docker/tests/**', 'crates/jackin-env/**/*.rs', 'crates/jackin-env/benches/**', 'crates/jackin-env/examples/**', 'crates/jackin-env/fuzz/**/*.rs', 'crates/jackin-env/fuzz/benches/**', 'crates/jackin-env/fuzz/examples/**', 'crates/jackin-env/fuzz/src/**', 'crates/jackin-env/fuzz/tests/**', 'crates/jackin-env/src/**', 'crates/jackin-env/tests/**', 'crates/jackin-host/**/*.rs', 'crates/jackin-host/benches/**', 'crates/jackin-host/examples/**', 'crates/jackin-host/src/**', 'crates/jackin-host/tests/**', 'crates/jackin-image/**/*.rs', 'crates/jackin-image/benches/**', 'crates/jackin-image/build.rs', 'crates/jackin-image/examples/**', 'crates/jackin-image/src/**', 'crates/jackin-image/tests/**', 'crates/jackin-instance/**/*.rs', 'crates/jackin-instance/benches/**', 'crates/jackin-instance/examples/**', 'crates/jackin-instance/src/**', 'crates/jackin-instance/tests/**', 'crates/jackin-isolation/**/*.rs', 'crates/jackin-isolation/benches/**', 'crates/jackin-isolation/examples/**', 'crates/jackin-isolation/src/**', 'crates/jackin-isolation/tests/**', 'crates/jackin-launch/**/*.rs', 'crates/jackin-launch/benches/**', 'crates/jackin-launch/build.rs', 'crates/jackin-launch/examples/**', 'crates/jackin-launch/src/**', 'crates/jackin-launch/tests/**', 'crates/jackin-lints/**/*.rs', 'crates/jackin-lints/benches/**', 'crates/jackin-lints/examples/**', 'crates/jackin-lints/src/**', 'crates/jackin-lints/tests/**', 'crates/jackin-manifest/**/*.rs', 'crates/jackin-manifest/benches/**', 'crates/jackin-manifest/examples/**', 'crates/jackin-manifest/fuzz/**/*.rs', 'crates/jackin-manifest/fuzz/benches/**', 'crates/jackin-manifest/fuzz/examples/**', 'crates/jackin-manifest/fuzz/src/**', 'crates/jackin-manifest/fuzz/tests/**', 'crates/jackin-manifest/src/**', 'crates/jackin-manifest/tests/**', 'crates/jackin-oppicker/**/*.rs', 'crates/jackin-oppicker/benches/**', 'crates/jackin-oppicker/examples/**', 'crates/jackin-oppicker/src/**', 'crates/jackin-oppicker/tests/**', 'crates/jackin-otlp-testbed/**/*.rs', 'crates/jackin-otlp-testbed/benches/**', 'crates/jackin-otlp-testbed/examples/**', 'crates/jackin-otlp-testbed/src/**', 'crates/jackin-otlp-testbed/tests/**', 'crates/jackin-pr-trailers/**/*.rs', 'crates/jackin-pr-trailers/benches/**', 'crates/jackin-pr-trailers/examples/**', 'crates/jackin-pr-trailers/src/**', 'crates/jackin-pr-trailers/tests/**', 'crates/jackin-process/**/*.rs', 'crates/jackin-process/benches/**', 'crates/jackin-process/examples/**', 'crates/jackin-process/src/**', 'crates/jackin-process/tests/**', 'crates/jackin-protocol/**/*.rs', 'crates/jackin-protocol/benches/**', 'crates/jackin-protocol/examples/**', 'crates/jackin-protocol/fuzz/**/*.rs', 'crates/jackin-protocol/fuzz/benches/**', 'crates/jackin-protocol/fuzz/examples/**', 'crates/jackin-protocol/fuzz/src/**', 'crates/jackin-protocol/fuzz/tests/**', 'crates/jackin-protocol/src/**', 'crates/jackin-protocol/tests/**', 'crates/jackin-runtime/**/*.rs', 'crates/jackin-runtime/benches/**', 'crates/jackin-runtime/build.rs', 'crates/jackin-runtime/examples/**', 'crates/jackin-runtime/src/**', 'crates/jackin-runtime/tests/**', 'crates/jackin-telemetry/**/*.rs', 'crates/jackin-telemetry/benches/**', 'crates/jackin-telemetry/examples/**', 'crates/jackin-telemetry/src/**', 'crates/jackin-telemetry/tests/**', 'crates/jackin-test-support/**/*.rs', 'crates/jackin-test-support/benches/**', 'crates/jackin-test-support/examples/**', 'crates/jackin-test-support/src/**', 'crates/jackin-test-support/tests/**', 'crates/jackin-tui/**/*.rs', 'crates/jackin-tui/benches/**', 'crates/jackin-tui/examples/**', 'crates/jackin-tui/src/**', 'crates/jackin-tui/tests/**', 'crates/jackin-usage-ffi/**/*.rs', 'crates/jackin-usage-ffi/benches/**', 'crates/jackin-usage-ffi/examples/**', 'crates/jackin-usage-ffi/src/**', 'crates/jackin-usage-ffi/tests/**', 'crates/jackin-usage/**/*.rs', 'crates/jackin-usage/benches/**', 'crates/jackin-usage/examples/**', 'crates/jackin-usage/src/**', 'crates/jackin-usage/tests/**', 'crates/jackin-usage/tests/fixtures/contracts/usage-projection-v1-current.json', 'crates/jackin-xtask/**/*.rs', 'crates/jackin-xtask/benches/**', 'crates/jackin-xtask/examples/**', 'crates/jackin-xtask/src/**', 'crates/jackin-xtask/tests/**', 'crates/jackin/**/*.rs', 'crates/jackin/benches/**', 'crates/jackin/build.rs', 'crates/jackin/examples/**', 'crates/jackin/src/**', 'crates/jackin/tests/**', 'docker/construct/Dockerfile', 'docker/runtime/agent-status/hooks/claude/report-hook.sh', 'docker/runtime/agent-status/hooks/codex/report-hook.sh', 'docker/runtime/agent-status/hooks/opencode/plugin.js', 'docker/runtime/agent-status/hooks/opencode/report-hook.sh', 'docker/runtime/entrypoint.sh', 'mise.lock', 'vendor/arrayref/**/*.rs', 'vendor/arrayref/benches/**', 'vendor/arrayref/examples/**', 'vendor/arrayref/src/**', 'vendor/arrayref/tests/**') }}
          restore-keys: |
            velnor-static-mbx-v3-ae157df2a7ca-${{ runner.os }}-${{ runner.arch }}-${{ github.workflow }}-${{ github.job }}-${{ hashFiles('Cargo.lock', 'rust-toolchain.toml', 'rust-toolchain', 'mise.toml', 'mise.lock', '**/Cargo.toml') }}-
            velnor-static-mbx-v3-ae157df2a7ca-${{ runner.os }}-${{ runner.arch }}-${{ github.workflow }}-${{ github.job }}-
          save-on-workflow-dispatch: true
      - uses: mozilla-actions/sccache-action@fc920bf0ec8de6ee65d409111f7ec508035751ba # v0.0.11
        with:
          version: v0.16.0
          disable_annotations: "false"

      - name: Cache Rust toolchain
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup/toolchains
            ~/.rustup/update-hashes
          key: rustup-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('rust-toolchain.toml') }}
          restore-keys: |
            rustup-${{ runner.os }}-${{ runner.arch }}-

      - uses: jdx/mise-action@3c2e0cf82a5b2e5249f0d3635a4d83d0ae861518 # v4.2.5
        with:
          version: "2026.7.12"
          cache_key_prefix: "mise-v2"
          cache_save: ${{ github.ref == 'refs/heads/main' }}
          install_args: "cargo-binstall rust cosign syft aqua:nextest-rs/nextest/cargo-nextest cargo:boltffi_cli"
          github_token: ${{ secrets.GH_READONLY_TOKEN }}

      - name: Bind Cargo cache actions into admitted closure
        if: github.event_name == 'velnor-admission-closure'
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      - name: Bind Cargo cache save into admitted closure
        if: github.event_name == 'velnor-admission-closure'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      - uses: ./.github/actions/cache-cargo-registry

      - name: Cache Cargo target
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: target
          key: cargo-target-GitHub-${{ runner.os }}-${{ github.job }}-${{ hashFiles('rust-toolchain.toml', '.cargo/config.toml', '**/Cargo.toml', '**/Cargo.lock') }}-${{ github.sha }}
          restore-keys: cargo-target-GitHub-${{ runner.os }}-${{ github.job }}-${{ hashFiles('rust-toolchain.toml', '.cargo/config.toml', '**/Cargo.toml', '**/Cargo.lock') }}-

      - name: Verify mise-managed boltffi
        run: boltffi --version

      - name: Verify Xcode 26.6
        run: xcodebuild -version

      - name: Verify Swift toolchain
        run: swift --version

      - name: Assemble arm64 static app
        env:
          JACKIN_APP_VERSION: ${{ env.VERSION }}
          JACKIN_APP_BUILD: ${{ env.APP_BUILD }}
        run: mise run desktop-build "${JACKIN_APP_VERSION}" "${JACKIN_APP_BUILD}"

      - name: Verify ad-hoc app
        env:
          JACKIN_APP_VERSION: ${{ env.VERSION }}
          JACKIN_APP_BUILD: ${{ env.APP_BUILD }}
        run: mise run desktop-verify native/dist/JackinDesktop.app "${JACKIN_APP_VERSION}" "${JACKIN_APP_BUILD}"

      - name: Ad-hoc must fail release-mode verifier
        if: env.MODE == 'validate'
        env:
          JACKIN_APP_VERSION: ${{ env.VERSION }}
          JACKIN_APP_BUILD: ${{ env.APP_BUILD }}
        run: |
          set -euo pipefail
          if mise run desktop-verify native/dist/JackinDesktop.app "${JACKIN_APP_VERSION}" "${JACKIN_APP_BUILD}" --release; then
            echo "::error::ad-hoc app must not pass --release"
            exit 1
          fi
          echo "ok: ad-hoc correctly fails release-mode checks"

      - name: Offline reconciliation fixtures
        run: mbx nextest run -p jackin-xtask --locked -E 'test(desktop::release_state)'

      - name: Reconciliation state (read-only)
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mise run desktop-release-state "$VERSION" "$GITHUB_REPOSITORY" | tee state.env
          # Second run is identical (no writes).
          mise run desktop-release-state "$VERSION" "$GITHUB_REPOSITORY" | tee state2.env
          diff -u state.env state2.env

      - name: Import Developer ID + notarize (publish only)
        if: env.MODE == 'publish'
        env:
          P12_BASE64: ${{ secrets.DEVELOPER_ID_APPLICATION_P12_BASE64 }}
          P12_PASSWORD: ${{ secrets.DEVELOPER_ID_APPLICATION_P12_PASSWORD }}
          ASC_KEY_P8: ${{ secrets.APP_STORE_CONNECT_API_KEY_P8 }}
          ASC_KEY_ID: ${{ secrets.APP_STORE_CONNECT_KEY_ID }}
          ASC_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
          EXPECTED_CERT_SHA256: ${{ vars.JACKIN_DEVELOPER_ID_CERT_SHA256 }}
          EXPECTED_TEAM_ID: ${{ vars.JACKIN_DEVELOPER_ID_TEAM_ID }}
          JACKIN_APP_VERSION: ${{ env.VERSION }}
          JACKIN_APP_BUILD: ${{ env.APP_BUILD }}
          NOTARY_LOG_DIR: ${{ runner.temp }}/notary
        run: |
          set -euo pipefail
          : "${P12_BASE64:?missing DEVELOPER_ID_APPLICATION_P12_BASE64}"
          : "${P12_PASSWORD:?missing DEVELOPER_ID_APPLICATION_P12_PASSWORD}"
          : "${ASC_KEY_P8:?missing APP_STORE_CONNECT_API_KEY_P8}"
          : "${ASC_KEY_ID:?missing APP_STORE_CONNECT_KEY_ID}"
          : "${ASC_ISSUER_ID:?missing APP_STORE_CONNECT_ISSUER_ID}"
          KEYCHAIN_PATH="${RUNNER_TEMP}/jackin-signing.keychain-db"
          KEYCHAIN_PASSWORD="$(openssl rand -base64 32)"
          P12_PATH="${RUNNER_TEMP}/developer-id.p12"
          P8_PATH="${RUNNER_TEMP}/AuthKey_${ASC_KEY_ID}.p8"
          cleanup() {
            security delete-keychain "$KEYCHAIN_PATH" 2>/dev/null || true
            rm -f "$P12_PATH" "$P8_PATH"
          }
          trap cleanup EXIT
          printf '%s' "$P12_BASE64" | base64 --decode > "$P12_PATH"
          printf '%s' "$ASC_KEY_P8" > "$P8_PATH"
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          security import "$P12_PATH" -P "$P12_PASSWORD" -A -t cert -f pkcs12 \
            -k "$KEYCHAIN_PATH" -T /usr/bin/codesign
          # Prepend signing keychain without unquoted word-splitting.
          mapfile -t existing_keychains < <(security list-keychain -d user | tr -d '"')
          security list-keychain -d user -s "$KEYCHAIN_PATH" "${existing_keychains[@]}"
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s \
            -k "$KEYCHAIN_PASSWORD" "$KEYCHAIN_PATH"
          IDENTITY="$(security find-identity -v -p codesigning "$KEYCHAIN_PATH" | awk -F'\"' '/Developer ID Application/ {print $2; exit}')"
          [[ -n "$IDENTITY" ]] || { echo "::error::no Developer ID Application identity in imported keychain"; exit 1; }
          export DEVELOPER_ID_APPLICATION="$IDENTITY"
          export APP_STORE_CONNECT_API_KEY_PATH="$P8_PATH"
          export APP_STORE_CONNECT_KEY_ID="$ASC_KEY_ID"
          export APP_STORE_CONNECT_ISSUER_ID="$ASC_ISSUER_ID"
          OUT_ZIP="${RUNNER_TEMP}/jackin-desktop-${VERSION}-aarch64-apple-darwin.zip"
          mise run desktop-sign-notarize native/dist/JackinDesktop.app "$OUT_ZIP" \
            "${JACKIN_APP_VERSION}" "${JACKIN_APP_BUILD}"
          mkdir -p dist-app
          cp "$OUT_ZIP" "dist-app/jackin-desktop-${VERSION}-aarch64-apple-darwin.zip"
          # Credentials must be gone before supply-chain tooling.
          cleanup
          trap - EXIT

      - name: Package ad-hoc ZIP (validate only)
        if: env.MODE == 'validate'
        run: |
          set -euo pipefail
          mkdir -p dist-app
          OUT="dist-app/jackin-desktop-${VERSION}-aarch64-apple-darwin.zip"
          (
            cd native/dist
            ditto -c -k --keepParent JackinDesktop.app "$GITHUB_WORKSPACE/$OUT"
          )
          echo "validate ZIP (ad-hoc, not for release): $OUT"

      - name: Supply-chain sidecars after credential cleanup
        if: env.MODE == 'publish'
        working-directory: dist-app
        run: |
          set -euo pipefail
          ARCHIVE="jackin-desktop-${VERSION}-aarch64-apple-darwin.zip"
          shasum -a 256 "$ARCHIVE" | awk '{print $1}' > "${ARCHIVE}.sha256"
          cosign sign-blob --bundle "${ARCHIVE}.bundle" --yes "$ARCHIVE"
          syft scan "$ARCHIVE" -o cyclonedx-json > "${ARCHIVE}.sbom.json"
          test -s "${ARCHIVE}.sbom.json"
          grep -q 'JackinDesktop\|jackin-desktop' "${ARCHIVE}.sbom.json" \
            || grep -qi 'macos\|software' "${ARCHIVE}.sbom.json" \
            || { echo "::error::SBOM does not meaningfully identify the app archive"; exit 1; }

      - name: Attest build provenance
        if: env.MODE == 'publish'
        uses: actions/attest-build-provenance@0f67c3f4856b2e3261c31976d6725780e5e4c373 # v4.1.1
        with:
          create-storage-record: "true"
          push-to-registry: "false"
          show-summary: "true"
          subject-path: dist-app/jackin-desktop-${{ env.VERSION }}-aarch64-apple-darwin.zip

      - name: SHA output
        id: sha
        if: env.MODE == 'publish'
        run: |
          set -euo pipefail
          echo "app_sha256=$(cat "dist-app/jackin-desktop-${VERSION}-aarch64-apple-darwin.zip.sha256")" >> "$GITHUB_OUTPUT"

      - name: Upload menu-bar artifacts
        if: env.MODE == 'publish'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-GitHub-usage-menu-bar
          path: dist-app/*
          if-no-files-found: error

      - name: Stage release symbols
        run: |
          set -euo pipefail
          mkdir -p dist-symbols
          ditto native/dist/JackinDesktop.app.dSYM dist-symbols/JackinDesktop.app.dSYM
          tar -czf dist-symbols/libjackin_usage_ffi.a.tar.gz \
            -C target/aarch64-apple-darwin/desktop-release libjackin_usage_ffi.a
          test -s dist-symbols/libjackin_usage_ffi.a.tar.gz

      - name: Upload release symbols
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: symbols-usage-menu-bar
          path: dist-symbols/**
          if-no-files-found: error
          retention-days: 90

      - name: Upload notary log (short retention)
        if: env.MODE == 'publish' && always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: notary-log-usage-menu-bar
          path: ${{ runner.temp }}/notary/**
          if-no-files-found: ignore
          retention-days: 14

  release:
    if: >-
      needs.matrix-setup.outputs.has_github == 'true' &&
      needs.check-version.outputs.published != 'true' &&
      needs.check-version.outputs.mode == 'publish'
    needs: [check-version, matrix-setup, test, build, build-jackin-capsule, build-usage-menu-bar]
    runs-on: ${{ ((github.event_name == 'workflow_dispatch' && inputs.lanes == 'github') || github.event_name == 'pull_request' || github.event_name == 'push') && 'ubuntu-26.04' || fromJSON('["self-hosted","velnor-target-mvp"]') }}
    timeout-minutes: 60
    permissions:
      contents: write
      id-token: write
    outputs:
      version: ${{ needs.check-version.outputs.version }}
      arm64_macos: ${{ steps.shas.outputs.arm64_macos }}
      x86_macos: ${{ steps.shas.outputs.x86_macos }}
      arm64_linux: ${{ steps.shas.outputs.arm64_linux }}
      x86_linux: ${{ steps.shas.outputs.x86_linux }}
      capsule_arm64_linux: ${{ steps.shas.outputs.capsule_arm64_linux }}
      capsule_x86_linux: ${{ steps.shas.outputs.capsule_x86_linux }}
      app_sha256: ${{ steps.shas.outputs.app_sha256 }}
    env:
      VERSION: ${{ needs.check-version.outputs.version }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: release-GitHub-*
          path: artifacts
          merge-multiple: true

      - name: Read platform SHA256s
        id: shas
        run: |
          read_sha() {
            awk '{print $1}' "artifacts/$1.sha256"
          }
          {
            echo "arm64_macos=$(read_sha "jackin-${VERSION}-aarch64-apple-darwin.tar.gz")"
            echo "x86_macos=$(read_sha "jackin-${VERSION}-x86_64-apple-darwin.tar.gz")"
            echo "arm64_linux=$(read_sha "jackin-${VERSION}-aarch64-unknown-linux-gnu.tar.gz")"
            echo "x86_linux=$(read_sha "jackin-${VERSION}-x86_64-unknown-linux-gnu.tar.gz")"
            echo "capsule_arm64_linux=$(read_sha "jackin-capsule-${VERSION}-aarch64-unknown-linux-gnu.tar.gz")"
            echo "capsule_x86_linux=$(read_sha "jackin-capsule-${VERSION}-x86_64-unknown-linux-gnu.tar.gz")"
            if [ -f "artifacts/jackin-desktop-${VERSION}-aarch64-apple-darwin.zip.sha256" ]; then
              echo "app_sha256=$(read_sha "jackin-desktop-${VERSION}-aarch64-apple-darwin.zip")"
            else
              echo "app_sha256="
            fi
          } >> "$GITHUB_OUTPUT"

      - name: Produce and sign capsule manifest
        uses: ./.github/actions/sign-capsule-manifest
        with:
          version: ${{ needs.check-version.outputs.version }}
          arm64-sha: ${{ steps.shas.outputs.capsule_arm64_linux }}
          x86-sha: ${{ steps.shas.outputs.capsule_x86_linux }}
          github-token: ${{ github.token }}

      - name: Assemble consumer release manifest and independent checksums
        run: |
          set -euo pipefail
          mapfile -d '' -t subjects < <(
            find artifacts -maxdepth 1 -type f \
              \( -name "jackin-${VERSION}-*.tar.gz" \
                 -o -name "jackin-capsule-${VERSION}-*.tar.gz" \
                 -o -name "jackin-desktop-${VERSION}-*.zip" \) \
              -print0 | sort -z
          )
          test "${#subjects[@]}" -eq 7
          : > artifacts/SHA256SUMS
          : > assets.jsonl
          for subject in "${subjects[@]}"; do
            name=$(basename "$subject")
            digest=$(sha256sum "$subject" | awk '{print $1}')
            printf '%s  %s\n' "$digest" "$name" >> artifacts/SHA256SUMS
            jq -cn --arg name "$name" --arg sha256 "$digest" \
              '{name:$name,sha256:$sha256}' >> assets.jsonl
          done
          jq -S -n \
            --arg source_repository "$GITHUB_REPOSITORY" \
            --arg source_ref "refs/tags/v${VERSION}" \
            --arg source_commit "$GITHUB_SHA" \
            --arg version "$VERSION" \
            --slurpfile assets assets.jsonl \
            '{schema:"velnor.package-release.v1",source_repository:$source_repository,source_ref:$source_ref,source_commit:$source_commit,version:$version,assets:$assets}' \
            > artifacts/release-manifest.json
          (cd artifacts && sha256sum --check --strict SHA256SUMS)

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: package-release-assets
          path: artifacts
          if-no-files-found: error

  sign-packages:
    needs: release
    strategy:
      fail-fast: false
      matrix:
        subject:
          - jackin-${{ needs.release.outputs.version }}-aarch64-apple-darwin.tar.gz
          - jackin-${{ needs.release.outputs.version }}-x86_64-apple-darwin.tar.gz
          - jackin-${{ needs.release.outputs.version }}-aarch64-unknown-linux-gnu.tar.gz
          - jackin-${{ needs.release.outputs.version }}-x86_64-unknown-linux-gnu.tar.gz
          - jackin-capsule-${{ needs.release.outputs.version }}-aarch64-unknown-linux-gnu.tar.gz
          - jackin-capsule-${{ needs.release.outputs.version }}-x86_64-unknown-linux-gnu.tar.gz
          - jackin-desktop-${{ needs.release.outputs.version }}-aarch64-apple-darwin.zip
    permissions:
      attestations: write
      contents: read
      id-token: write
    uses: jackin-project/velnor-actions/.github/workflows/package-signer.yml@851ef541d67f9cabebf2ddb2a2a02f51f6c54130 # 2026.8.33
    with:
      artifact-name: package-release-assets
      subject-path: ${{ matrix.subject }}
      source-ref: refs/tags/v${{ needs.release.outputs.version }}

  publish-release:
    needs: [release, sign-packages]
    runs-on: ${{ ((github.event_name == 'workflow_dispatch' && inputs.lanes == 'github') || github.event_name == 'pull_request' || github.event_name == 'push') && 'ubuntu-26.04' || fromJSON('["self-hosted","velnor-target-mvp"]') }}
    timeout-minutes: 15
    permissions:
      contents: write
    env:
      VERSION: ${{ needs.release.outputs.version }}
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: package-release-assets
          path: artifacts

      - name: Create or repair GitHub Release (no clobber)
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          tag="v${VERSION}"
          upload_args=(
            artifacts/release-manifest.json
            artifacts/SHA256SUMS
            artifacts/jackin-[0-9]*.tar.gz
            artifacts/jackin-[0-9]*.tar.gz.sha256
            artifacts/jackin-[0-9]*.tar.gz.bundle
            artifacts/jackin-[0-9]*.tar.gz.sbom.json
            artifacts/jackin-capsule-*.tar.gz
            artifacts/jackin-capsule-*.tar.gz.sha256
            artifacts/jackin-capsule-*.tar.gz.bundle
            artifacts/jackin-capsule-*.tar.gz.sbom.json
            artifacts/capsule-manifest.json
            artifacts/capsule-manifest.json.bundle
          )
          if ls artifacts/jackin-desktop-*.zip >/dev/null 2>&1; then
            upload_args+=(
              artifacts/jackin-desktop-*.zip
              artifacts/jackin-desktop-*.zip.sha256
              artifacts/jackin-desktop-*.zip.bundle
              artifacts/jackin-desktop-*.zip.sbom.json
            )
          fi
          if gh release view "$tag" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            echo "Release $tag exists — uploading only missing assets (no --clobber)"
            # Fail closed on conflicting bytes for the menu-bar ZIP if present.
            if [ -f "artifacts/jackin-desktop-${VERSION}-aarch64-apple-darwin.zip" ]; then
              existing="$(gh release download "$tag" --repo "$GITHUB_REPOSITORY" \
                --pattern "jackin-desktop-${VERSION}-aarch64-apple-darwin.zip" \
                --dir /tmp/existing-release 2>/dev/null && shasum -a 256 /tmp/existing-release/*.zip | awk '{print $1}' || true)"
              new="$(shasum -a 256 "artifacts/jackin-desktop-${VERSION}-aarch64-apple-darwin.zip" | awk '{print $1}')"
              if [ -n "$existing" ] && [ "$existing" != "$new" ]; then
                echo "::error::conflicting menu-bar ZIP for $tag — refuse clobber; bump version"
                exit 1
              fi
            fi
            gh release upload "$tag" "${upload_args[@]}" --repo "$GITHUB_REPOSITORY" || true
          else
            gh release create "$tag" \
              "${upload_args[@]}" \
              --title "$tag" \
              --generate-notes
          fi
