# Generated by velnor-workflow. Regenerate; do not hand-edit.
name: Construct Image

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      lanes:
        description: velnor (default) | github | both
        type: choice
        default: velnor
        options: [velnor, github, both]

permissions:
  actions: read
  contents: read

concurrency:
  group: construct-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  # Single source of truth for the digest directory — `build` writes
  # into it, `publish-manifest` reads from it.
  DIGEST_DIR: /tmp/jackin-construct-digests
  # Single source of truth for the registry coordinate — keeps the
  # two buildcache refs in sync.
  REGISTRY_IMAGE: projectjackin/construct
  # NOTE: BUILDX_BUILDER is set at the `build` job level only, not
  # workflow-wide. Setting it workflow-wide leaks into `publish-manifest`,
  # which has no buildx builder and would then fail with
  # `ERROR: no builder "jackin-construct" found` because docker buildx
  # treats BUILDX_BUILDER as the default-builder selection.

jobs:
  changes:
    runs-on: ${{ ((github.event_name == 'workflow_dispatch' && inputs.lanes == 'github') || github.event_name == 'pull_request' || github.event_name == 'push') && 'ubuntu-26.04' || fromJSON('["self-hosted","velnor-target-mvp"]') }}
    timeout-minutes: 5
    outputs:
      reuse: ${{ steps.construct-result.outputs.hit }}
      exact-reuse: ${{ steps.construct-result.outputs.exact }}
      result-name: ${{ steps.construct-result.outputs.name }}
      construct: ${{ steps.dispatch.outputs.construct || steps.filter.outputs.construct }}
      construct_image: ${{ steps.dispatch.outputs.construct_image || steps.filter.outputs.construct_image }}
      registry-contract: ${{ steps.construct-result.outputs.registry-contract }}
      rustup-contract: ${{ steps.construct-result.outputs.rustup-contract }}
      build-configs: ${{ (github.event_name == 'workflow_dispatch' && inputs.lanes == 'both') && '[{"lane":"Velnor","runner":"[\"self-hosted\",\"velnor-target-mvp\"]","runner_os":"Linux","runner_arch":"X64","platform":"amd64","writer":true},{"lane":"GitHub","runner":"\"ubuntu-26.04\"","runner_os":"Linux","runner_arch":"X64","platform":"amd64","writer":false},{"lane":"GitHub","runner":"\"ubuntu-26.04-arm\"","runner_os":"Linux","runner_arch":"ARM64","platform":"arm64","writer":false}]' || ((github.event_name == 'workflow_dispatch' && inputs.lanes == 'github') || github.event_name == 'pull_request' || github.event_name == 'push') && '[{"lane":"GitHub","runner":"\"ubuntu-26.04\"","runner_os":"Linux","runner_arch":"X64","platform":"amd64","writer":true},{"lane":"GitHub","runner":"\"ubuntu-26.04-arm\"","runner_os":"Linux","runner_arch":"ARM64","platform":"arm64","writer":true}]' || '[{"lane":"Velnor","runner":"[\"self-hosted\",\"velnor-target-mvp\"]","runner_os":"Linux","runner_arch":"X64","platform":"amd64","writer":true}]' }}
      lane-configs: ${{ (github.event_name == 'workflow_dispatch' && inputs.lanes == 'both') && '[{"lane":"Velnor","runner":"[\"self-hosted\",\"velnor-target-mvp\"]","runner_os":"Linux","runner_arch":"X64"},{"lane":"GitHub","runner":"\"ubuntu-26.04\"","runner_os":"Linux","runner_arch":"X64"}]' || ((github.event_name == 'workflow_dispatch' && inputs.lanes == 'github') || github.event_name == 'pull_request' || github.event_name == 'push') && '[{"lane":"GitHub","runner":"\"ubuntu-26.04\"","runner_os":"Linux","runner_arch":"X64"}]' || '[{"lane":"Velnor","runner":"[\"self-hosted\",\"velnor-target-mvp\"]","runner_os":"Linux","runner_arch":"X64"}]' }}
      # Single source of truth for "should this run produce / publish
      # registry artifacts" — true on push-to-main and on
      # workflow_dispatch from main, false everywhere else (PRs,
      # dispatch from feature branches). Downstream steps gate on
      # `needs.changes.outputs.is_publish` instead of restating the
      # same boolean five times.
      is_publish: ${{ steps.flags.outputs.is_publish }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # Construct result reuse compares earlier semantic source commits.
          fetch-depth: 0 # required by construct result-reuse comparison

      - name: Find input-identical successful construct result
        id: construct-result
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          LANES: ${{ github.event_name == 'pull_request' && 'github' || (github.event_name == 'workflow_dispatch' && inputs.lanes || 'velnor') }}
          REGISTRY_CONTRACT: ${{ hashFiles('Cargo.lock', 'crates/**/fuzz/Cargo.lock', 'crates/jackin-lints/Cargo.lock', 'rust-toolchain.toml', '.cargo/config.toml') }}
          REPOSITORY: ${{ github.repository }}
          RUSTUP_CONTRACT: ${{ hashFiles('rust-toolchain.toml') }}
        run: |
          set -euo pipefail
          mode=rehearsal
          if [ "$GITHUB_EVENT_NAME" = "push" ] || { [ "$GITHUB_EVENT_NAME" = "workflow_dispatch" ] && [ "$GITHUB_REF" = "refs/heads/main" ]; }; then
            mode=publish
          fi
          name="construct-result-v2-$(scripts/ci/construct-result-contract.sh)-${mode}-${LANES}"
          artifact_id=
          exact=false
          if ! artifact_id=$(
            gh api "repos/${REPOSITORY}/actions/artifacts?name=${name}&per_page=10" \
              --jq '.artifacts[] | select(.expired == false) | .id' \
              | head -n 1
          ); then
            echo "::warning::construct-result lookup failed; running construct in full"
            artifact_id=
          fi
          if [ -n "$artifact_id" ]; then
            exact=true
          else
            latest=$(
              gh api "repos/${REPOSITORY}/actions/artifacts?per_page=100" \
                --jq '[.artifacts[] | select(.expired == false and (.name | startswith("construct-result-v1-") and endswith("-'"${mode}-${LANES}"'")))][0] | [.id, .workflow_run.head_sha] | @tsv'
            )
            latest_id=${latest%%$'\t'*}
            source_sha=${latest#*$'\t'}
            if [ -n "$latest_id" ] && [ -n "$source_sha" ]; then
              git fetch --no-tags --depth=1 origin "$source_sha"
              if git diff --quiet "$source_sha" HEAD -- \
                .cargo Cargo.lock Cargo.toml 'crates/*/Cargo.toml' \
                crates/jackin-process/src crates/jackin-xtask/src/cmd.rs \
                crates/jackin-xtask/src/construct.rs crates/jackin-xtask/src/construct \
                crates/jackin-xtask/src/main.rs docker-bake.hcl docker/construct \
                rust-toolchain.toml; then
                echo "::notice::promoting a Construct result with identical semantic inputs"
                artifact_id=$latest_id
              fi
            fi
          fi
          if [ -n "$artifact_id" ]; then
            echo "::notice::reusing input-identical successful construct result"
          fi
          {
            echo "name=$name"
            echo "exact=$exact"
            echo "registry-contract=$REGISTRY_CONTRACT"
            echo "rustup-contract=$RUSTUP_CONTRACT"
            if [ -n "$artifact_id" ]; then
              echo "hit=true"
            else
              echo "hit=false"
            fi
          } >> "$GITHUB_OUTPUT"

      - name: Force construct=true on workflow_dispatch
        id: dispatch
        if: >-
          steps.construct-result.outputs.hit != 'true' &&
          github.event_name == 'workflow_dispatch'
        shell: bash
        run: |
          echo "construct=true" >> "$GITHUB_OUTPUT"
          if [ "$GITHUB_REF" = "refs/heads/main" ]; then
            echo "construct_image=true" >> "$GITHUB_OUTPUT"
          else
            echo "construct_image=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Classify changed paths
        id: filter
        if: >-
          steps.construct-result.outputs.hit != 'true' &&
          github.event_name != 'workflow_dispatch'
        uses: dorny/paths-filter@ceb8a2b8f2d89434be7ff52d3de7ec3738c5cc9d # v4
        with:
          token: ""
          base: ${{ github.event_name == 'pull_request' && github.event.pull_request.base.sha || github.event.before }}
          filters: |
            # Build the construct image when build plumbing changes, even if the
            # change should not publish a new immutable construct tag.
            construct:
              - '.github/workflows/construct.yml'
              - 'docker-bake.hcl'
              - 'docker/construct/**'
              - 'crates/jackin-xtask/Cargo.toml'
              - 'crates/jackin-xtask/src/main.rs'
              - 'crates/jackin-xtask/src/construct.rs'
              - 'crates/jackin-xtask/src/construct/**'
              - 'scripts/ci/construct-result-contract.sh'
            # Require an unpublished docker/construct/VERSION only when inputs
            # baked into the construct image changed.
            construct_image:
              - 'docker-bake.hcl'
              - 'docker/construct/Dockerfile'
              - 'docker/construct/fish-config.fish'
              - 'docker/construct/versions.env'
              - 'docker/construct/zshrc'

      - name: Compute publish flag
        id: flags
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          REF: ${{ github.ref }}
        run: |
          if [ "$EVENT_NAME" = "push" ] || { [ "$EVENT_NAME" = "workflow_dispatch" ] && [ "$REF" = "refs/heads/main" ]; }; then
            echo "is_publish=true" >> "$GITHUB_OUTPUT"
          else
            echo "is_publish=false" >> "$GITHUB_OUTPUT"
          fi

  # amd64 and arm64 share every step except runner label and platform
  # string. Matrix collapse keeps the per-platform fail-fast=false
  # behavior (one arch failing must not cancel the other) while
  # eliminating ~60 lines of cross-job copy-paste.
  build:
    name: ${{ matrix.config.platform }} (${{ matrix.config.lane }})
    needs: [changes]
    if: needs.changes.outputs.construct == 'true'
    runs-on: ${{ fromJSON(matrix.config.runner) }}
    timeout-minutes: 60
    permissions:
      contents: read
    env:
      # Scoped to the build job only. Setting it workflow-wide leaks
      # into `publish-manifest`, which has no buildx builder and would
      # then fail with `ERROR: no builder "jackin-construct" found`.
      BUILDX_BUILDER: jackin-construct-${{ github.run_id }}-${{ github.run_attempt }}-${{ strategy.job-index }}
      # `mise run` activates the whole mise.toml tool set and, by default,
      # installs every missing one before the task. This job only needs the
      # rust toolchain mise-action already installed to compile `cargo xtask`;
      # without this, an unrelated tool's install (e.g. a flaky rustup
      # component download for cargo-zigbuild) reds the construct build.
      MISE_TASK_RUN_AUTO_INSTALL: "false"
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
    strategy:
      fail-fast: false
      matrix:
        config: ${{ fromJSON(needs.changes.outputs.build-configs) }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Bind Cargo cache actions into admitted closure
        if: github.event_name == 'velnor-admission-closure'
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      - name: Bind Cargo cache save into admitted closure
        if: github.event_name == 'velnor-admission-closure'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0

      - name: Cache Rust toolchain
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup/toolchains
            ~/.rustup/update-hashes
          key: rustup-${{ runner.os }}-${{ runner.arch }}-${{ needs.changes.outputs.rustup-contract }}
          restore-keys: |
            rustup-${{ runner.os }}-${{ runner.arch }}-

      - name: Setup mise
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4
        with:
          version: "2026.7.12"
          cache_key_prefix: "mise-v2"
          cache_save: ${{ github.ref == 'refs/heads/main' }}
          install_args: "rust"
          # Fork PRs have no GH_READONLY_TOKEN; the workflow token covers
          # same-repository reads on the pull_request lane.
          github_token: ${{ github.event_name == 'pull_request' && github.token || secrets.GH_READONLY_TOKEN }}
      - id: cargo-registry-cache
        uses: ./.github/actions/cache-cargo-registry
        with:
          contract-key: ${{ needs.changes.outputs.registry-contract }}

      - name: Prefetch Cargo dependencies
        shell: bash
        run: |
          set -euo pipefail
          cargo fetch --locked --offline
      - name: Resolve construct tool versions
        id: versions
        shell: bash
        run: |
          set -euo pipefail
          source docker/construct/versions.env
          echo "shellfirm-version=${SHELLFIRM_VERSION}" >> "$GITHUB_OUTPUT"
      - name: Restore prebuilt shellfirm
        id: shellfirm-cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: docker/construct/prebuilt
          key: shellfirm-${{ runner.os }}-${{ runner.arch }}-${{ steps.versions.outputs.shellfirm-version }}-${{ needs.changes.outputs.rustup-contract }}
      - name: Log in to Docker Hub
        if: needs.changes.outputs.is_publish == 'true' && env.DOCKERHUB_USERNAME != '' && env.DOCKERHUB_TOKEN != ''
        uses: docker/login-action@af1e73f918a031802d376d3c8bbc3fe56130a9b0 # v4.4.0
        with:
          username: ${{ env.DOCKERHUB_USERNAME }}
          password: ${{ env.DOCKERHUB_TOKEN }}

      - name: Install Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0
        with:
          name: ${{ env.BUILDX_BUILDER }}
          driver: docker-container
          # Route docker.io base-image pulls (rust, debian) through Google's
          # pull-through mirror so construct builds don't hit Docker Hub's
          # per-account pull-rate limit (429 Too Many Requests). Bases stay
          # digest-pinned, so the mirror only serves bytes — integrity is still
          # verified against the pinned sha256.
          buildkitd-config-inline: |
            [registry."docker.io"]
              mirrors = ["mirror.gcr.io"]

      - name: Bootstrap buildx
        run: mise run construct-init-buildx

      - name: Build ${{ matrix.config.platform }} without push
        if: matrix.config.writer != true || needs.changes.outputs.is_publish != 'true' || needs.changes.outputs.construct_image != 'true'
        env:
          # Both runner lanes restore the same durable cache written by main.
          # Persistent BuildKit state on Velnor remains a transparent local
          # accelerator; it does not change this workflow configuration.
          CACHE_FROM: type=registry,ref=${{ env.REGISTRY_IMAGE }}:buildcache-${{ matrix.config.platform }}
        run: mise run construct-build-platform ${{ matrix.config.platform }}

      - name: Push ${{ matrix.config.platform }} image by digest
        if: matrix.config.writer && needs.changes.outputs.is_publish == 'true' && needs.changes.outputs.construct_image == 'true'
        env:
          CACHE_FROM: type=registry,ref=${{ env.REGISTRY_IMAGE }}:buildcache-${{ matrix.config.platform }}
          CACHE_TO: type=registry,ref=${{ env.REGISTRY_IMAGE }}:buildcache-${{ matrix.config.platform }},mode=max
        run: mise run construct-push-platform ${{ matrix.config.platform }}

      - name: Upload ${{ matrix.config.platform }} digest
        if: matrix.config.writer && needs.changes.outputs.is_publish == 'true' && needs.changes.outputs.construct_image == 'true'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: construct-digest-${{ matrix.config.lane }}-${{ matrix.config.platform }}
          path: ${{ env.DIGEST_DIR }}/${{ matrix.config.platform }}.digest
          if-no-files-found: error
          retention-days: 1

  publish-manifest:
    name: publish manifest
    if: needs.changes.outputs.is_publish == 'true' && needs.changes.outputs.construct_image == 'true'
    needs: [changes, build]
    runs-on: ${{ ((github.event_name == 'workflow_dispatch' && inputs.lanes == 'github') || github.event_name == 'pull_request' || github.event_name == 'push') && 'ubuntu-26.04' || fromJSON('["self-hosted","velnor-target-mvp"]') }}
    timeout-minutes: 60
    permissions:
      contents: read
    env:
      # Keep `mise run` on the rust toolchain mise-action installed instead of
      # installing the whole mise.toml tool set for the rust-only xtask task.
      MISE_TASK_RUN_AUTO_INSTALL: "false"

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Bind Cargo cache actions into admitted closure
        if: github.event_name == 'velnor-admission-closure'
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      - name: Bind Cargo cache save into admitted closure
        if: github.event_name == 'velnor-admission-closure'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0

      - name: Cache Rust toolchain
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup/toolchains
            ~/.rustup/update-hashes
          key: rustup-${{ runner.os }}-${{ runner.arch }}-${{ needs.changes.outputs.rustup-contract }}
          restore-keys: |
            rustup-${{ runner.os }}-${{ runner.arch }}-

      - name: Setup mise
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4
        with:
          version: "2026.7.12"
          cache_key_prefix: "mise-v2"
          cache_save: ${{ github.ref == 'refs/heads/main' }}
          install_args: "rust"
          # Fork PRs have no GH_READONLY_TOKEN; the workflow token covers
          # same-repository reads on the pull_request lane.
          github_token: ${{ github.event_name == 'pull_request' && github.token || secrets.GH_READONLY_TOKEN }}
      - id: cargo-registry-cache
        uses: ./.github/actions/cache-cargo-registry
        with:
          contract-key: ${{ needs.changes.outputs.registry-contract }}

      - name: Prefetch Cargo dependencies
        shell: bash
        run: |
          set -euo pipefail
          cargo fetch --locked --offline
      - name: Log in to Docker Hub
        uses: docker/login-action@af1e73f918a031802d376d3c8bbc3fe56130a9b0 # v4.4.0
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Download platform digests
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: construct-digest-*
          path: ${{ env.DIGEST_DIR }}
          merge-multiple: true

      - name: Publish multi-platform manifest
        run: mise run construct-publish-manifest

  # PR-time rehearsal of `publish-manifest`'s setup. Runs the same
  # docker buildx CLI plumbing publish-manifest depends on, without
  # logging in to Docker Hub or writing a manifest to the registry,
  # so a workflow- or job-level env-var leak that breaks
  # `docker buildx` resolution surfaces in PR rather than
  # post-merge on main. Catches the exact failure class from
  # jackin-project/jackin#266 — `docker buildx ls` evaluates
  # BUILDX_BUILDER at startup and exits non-zero on a missing-builder
  # reference, regardless of whether the rest of the command would
  # have hit the network. The rehearsal runs on the events
  # publish-manifest itself skips: PR and feature-branch dispatch
  # that touch construct paths.
  publish-manifest-rehearsal:
    name: publish manifest (rehearsal, ${{ matrix.config.lane }})
    if: needs.changes.outputs.construct == 'true' && needs.changes.outputs.is_publish != 'true'
    needs: [changes, build]
    runs-on: ${{ fromJSON(matrix.config.runner) }}
    timeout-minutes: 60
    permissions:
      contents: read
    env:
      # Keep `mise run` on the rust toolchain mise-action installed instead of
      # installing the whole mise.toml tool set for the rust-only xtask task.
      MISE_TASK_RUN_AUTO_INSTALL: "false"
    strategy:
      fail-fast: false
      matrix:
        config: ${{ fromJSON(needs.changes.outputs.lane-configs) }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Bind Cargo cache actions into admitted closure
        if: github.event_name == 'velnor-admission-closure'
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      - name: Bind Cargo cache save into admitted closure
        if: github.event_name == 'velnor-admission-closure'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0

      - name: Cache Rust toolchain
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.rustup/toolchains
            ~/.rustup/update-hashes
          key: rustup-${{ runner.os }}-${{ runner.arch }}-${{ needs.changes.outputs.rustup-contract }}
          restore-keys: |
            rustup-${{ runner.os }}-${{ runner.arch }}-

      - name: Setup mise
        uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4
        with:
          version: "2026.7.12"
          cache_key_prefix: "mise-v2"
          cache_save: ${{ github.ref == 'refs/heads/main' }}
          install_args: "rust"
          # Fork PRs have no GH_READONLY_TOKEN; the workflow token covers
          # same-repository reads on the pull_request lane.
          github_token: ${{ github.event_name == 'pull_request' && github.token || secrets.GH_READONLY_TOKEN }}
      - id: cargo-registry-cache
        uses: ./.github/actions/cache-cargo-registry
        with:
          contract-key: ${{ needs.changes.outputs.registry-contract }}

      - name: Prefetch Cargo dependencies
        shell: bash
        run: |
          set -euo pipefail
          cargo fetch --locked --offline
      - name: Verify docker buildx CLI resolves under workflow env
        # `docker buildx ls` reads BUILDX_BUILDER (and the rest of
        # buildx's env-driven config) and fails fast on a missing
        # builder reference. This is the exact error shape from
        # jackin-project/jackin#266 — running it on PR + feature-branch
        # dispatch surfaces a leak that publish-manifest would
        # otherwise only hit post-merge on main.
        run: docker buildx ls

      - name: Verify imagetools CLI plugin loads
        # Exercises the same plugin path publish-manifest's
        # `docker buildx imagetools create` / `inspect` commands rely
        # on. Cheap, network-free smoke that the plugin is bundled
        # and loadable on the runner.
        run: docker buildx imagetools --help >/dev/null

      - name: Verify construct VERSION is not already published
        if: needs.changes.outputs.construct_image == 'true'
        # PR-time mirror of publish-manifest's version-bump guard. The
        # build job already ran, so reaching here means docker/construct/**
        # content changed; an anonymous public-registry inspect needs no
        # Docker Hub login, so the "changed the image but didn't bump
        # docker/construct/VERSION" failure surfaces on the PR instead of
        # on main, where publish-manifest is the first job to evaluate it.
        run: mise run construct-assert-version-unpublished

  construct-required:
    if: always()
    needs: [changes, build, publish-manifest, publish-manifest-rehearsal]
    runs-on: ${{ ((github.event_name == 'workflow_dispatch' && inputs.lanes == 'github') || github.event_name == 'pull_request' || github.event_name == 'push') && 'ubuntu-26.04' || fromJSON('["self-hosted","velnor-target-mvp"]') }}
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Bind CI xtask dependencies into admitted closure
        if: github.event_name == 'velnor-admission-closure'
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      - name: Bind CI xtask cache save into admitted closure
        if: github.event_name == 'velnor-admission-closure'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
      - name: Bind CI xtask resolver into admitted closure
        if: github.event_name == 'velnor-admission-closure'
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
      - name: Bind CI xtask fallback toolchain into admitted closure
        if: github.event_name == 'velnor-admission-closure'
        uses: jdx/mise-action@3c2e0cf82a5b2e5249f0d3635a4d83d0ae861518 # v4.2.5
      - uses: ./.github/actions/aggregate-needs
        with:
          needs-json: ${{ toJSON(needs) }}
          workflow-label: construct
      - uses: ./.github/actions/download-ci-xtask
        if: always()
        with:
          token: ${{ github.token }}
          lane: ${{ github.event_name == 'pull_request' && 'GitHub' || ((github.event_name == 'workflow_dispatch' && inputs.lanes == 'github') || github.event_name == 'push') && 'GitHub' || 'Velnor' }}
          xtask-contract: ${{ hashFiles('Cargo.lock', 'Cargo.toml', 'rust-toolchain.toml', '.cargo/**', 'crates/*/Cargo.toml', 'crates/jackin-xtask/src/**', 'crates/jackin-process/src/**') }}
          include-tools: "false"
      - name: Audit every Construct job's performance
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
        run: >-
          "$CI_XTASK" ci-audit
          --expect-clean "${{ needs.changes.outputs.reuse == 'true' }}"
          --workflow-label Construct
      - name: Stage input-identical successful construct result
        if: >-
          needs.changes.outputs.exact-reuse != 'true' &&
          (github.ref == 'refs/heads/main' ||
           github.event_name == 'workflow_dispatch' ||
           (github.event_name == 'pull_request' &&
            github.event.pull_request.head.repo.full_name == github.repository))
        run: |
          printf '%s\n' "source-sha=${GITHUB_SHA}" > construct-result.txt
      - name: Publish input-identical successful construct result
        if: >-
          needs.changes.outputs.exact-reuse != 'true' &&
          (github.ref == 'refs/heads/main' ||
           github.event_name == 'workflow_dispatch' ||
           (github.event_name == 'pull_request' &&
            github.event.pull_request.head.repo.full_name == github.repository))
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ${{ needs.changes.outputs.result-name }}
          path: construct-result.txt
          compression-level: 0
          retention-days: 7
