# G1 EXECUTION PLAN — ChainArgos/java-monorepo (spec §9.2, bastion campaign)

Status: READ-ONLY planning. G gate NOT reached. NO ChainArgos writes until gate.
Sources: /tmp/g1-prep.md (a–e) + /tmp/g1-tc-prep.md (T1–T5) + /tmp/g1-actions-prep.md (§§1–4) + /tmp/a0-consumers.md §3 (C-ca-1..5) + spec §9.2.
Live main at prep time: `38a8fb5777fd02fec8b3904d86387aba05940e9a` (audited `235e479b…`; gap is docs-only, C-ca-2). G1 MUST re-pin live main at gate day.

## Phase 0 — Gate entry: auth + scope + typed config + published pin

0.1 Re-read live `main` SHA (ref + ls-remote must agree). Re-resolve every path below against it. Record drift as old→new→consequence.
0.2 Authorization: verify trust/access for ChainArgos org+repo. Add ONLY: authorization, generic scope entries where needed (same Velnor controller, global N — no second controller, no per-scope N, no per-ChainArgos host architecture), typed generator config. No new daemon/VM/pool/script/copied workflow (§9.3 shape).
0.3 Published pin: select then-published velnor-workflow product pin. Hard requirement: pin contains `2678913a` (verify `git merge-base --is-ancestor 2678913a <pin>`). Producer-side provenance (releases/attestations); consumer tree carries no pin today (C-ca-3).
0.4 Declare `[generator] revision = "<gate pin>"` in ChainArgos `.github-gen/velnor-workflow.toml`. `validate_workflow_revision` requires full 40-char SHA.
0.5 Single-writer + release-disabled: mutation single-writer unchanged; `[release] enabled=false` stays; never restore legacy YAML to pass a gate.

## Phase 1 — FIRST: ownership/trust + missing local actions (before ALL §9.2 content work)

Why first: all 6 unit workflows reference nonexistent `./.github/actions/report-velnor-ci-outcomes` (`.github/actions/` 404 at live main); load-bearing for all 71 units / 213 executions (C-ca-4). Nothing else can qualify green while 6/11 workflows point at a missing path. Spec §9.2: "repaired first".
1.1 Ownership/trust failures: repair per spec §9.2 (same priority lane as actions repair; separate item, named here so it is not dropped).
1.2 Regenerate ChainArgos tree with generator built at gate pin (published runtime product; NEVER hand-edit — headers say "Generated by velnor-workflow. Regenerate; do not hand-edit").
1.3 Expected diff (whole repair):
  - 6 unit workflows (`ci-unit-{bun,docker,docs,gradle,node,rust}.yml`): local `uses:` → `tailrocks/velnor/.github/actions/report-velnor-ci-outcomes@<pin>` (remote pinned; cf. jackin healthy shape).
  - NO new `.github/actions/` dir. Explicit non-goal: consumers must not ship local copies (per `2678913a` + `velnor_first_ci.rs:958`). Do NOT hand-create dir as quick fix.
  - `revision` key added (also closes C-ca-3).
1.4 Verify: grep local-path `uses:` → 0 hits; 6 remote-pin hits with exact gate pin; `gh api .../tailrocks/velnor/.../action.yml?ref=<pin>` → 200; policy `generated-tree` byte-identical regen; `velnor-workflow` suite green at gate pin (covers consumer-shape assertion).
1.5 Ledger check: 11 workflows, 71 units (37G/17R/11D/4B/1N/1Docs), Velnor-only defaults, 213 three-provider executions before additional controls (C-ca-1). Preserve or record reviewed coverage-equivalent correction.

## Phase 2 — Job-local PostgreSQL + graph-justified 16-prep dedup (Gradle)

2.1 Provision disposable job-local PG per job (seed per `docker/postgresql/init-databases.sql`: DBs legacy/instix/transfer_monitor/ethereum/tron/bitcoin/coingecko+kestra; schemas eth/tron/temp/report in chainargos). Wire via actual build-supported vars: `POSTGRESQL_DB_HOST/PORT` (defaults 127.0.0.1/40000) and per-module `<NAME>_DATASOURCE_URL/USERNAME/PASSWORD/SCHEMA` where declared. NEVER prod/shared DB.
2.2 Dedup the 16 unqualified `flywayMigrate jooqCodegen` preps (root `gradle-backend` + 7 domain + 8 flyway units; other 21 gradle units run bare `:X:check`). Graph facts: 8 flyway modules own `flyway{}`+migrate; 7 domain modules own jooqCodegen + `compileJava→jooqCodegen`, `api(flyway)`; NO `jooqCodegen→flywayMigrate` edge (CLI order only); today each of the 16 runs ALL 8 migrates + ALL 7 codegens. Replacement (keep CLI order, migrate before codegen):
  - domain unit `:X-domain`: `:X-flyway:flywayMigrate :X-domain:jooqCodegen :X-domain:check`
  - flyway unit `:X-flyway`: `:X-flyway:flywayMigrate :X-flyway:check` (report-flyway: this alone — nothing reads `report` in-graph)
  - root `gradle-backend`: unchanged full `flywayMigrate jooqCodegen check`
  - consumer units (processor-apps, validation jobs, utils — currently rely on luck/caches): ADD scoped `:D-flyway:flywayMigrate` per transitively-read domain D.
2.3 Equal-coverage proof obligations: (1) per unit, every schema its transitive jooqCodegen reads is migrated in same invocation (DB/schema matrix §a: bitcoin public/chainargos; eth eth/chainargos; legacy public/legacy; report chainargos/report; temp temp/chainargos; transfer-monitor public/transfer_monitor; tron tron/chainargos; whitelabel public/instix); (2) `transfer-monitor-app` migrates 4 flyway modules (transferMonitor+eth+temp+legacy); (3) all host/ports resolve to job-local PG; (4) transitive consumer list re-verified at gate (incl. whether model/utils/coingecko/redshift/tailrocks/toolbox/crypto need PG at test runtime via Micronaut test-resources).
2.4 Micronaut: check `api.version=1.44` override (backend/build.gradle.kts) against selected bastion daemon API.

## Phase 3 — Testcontainers: profile + serial RustFS + verified config + digest locks

3.1 `--profile ci` on ALL 17 rust units × all four lanes (pr/full × github/velnor) in `.github/ci/project.toml`, regen workflows. Retain `--locked --all-features --package X --no-tests pass`. (Today no `--profile` → effective `default`, not intended `ci`.)
3.2 RustFS serial group: keep `[test-groups] lightdash-rustfs {max-threads=1}` + `profile.default.overrides` filter as-is (ci inherits default). Gate evidence: `cargo nextest show-config --profile ci` (or mbx equiv) proving `lightdash-csv-delivery-app` resolves to group with max-threads=1 + passing lightdash run under `--profile ci`.
3.3 Digest locks (resolve+lock at gate, never assume immutability): `postgres:18-alpine`, `rabbitmq:3.8.22-management` (module default TAG, EOL), `redis:5.0` (module default TAG), `rustfs/rustfs:1.0.0-beta.8` (compose digest `sha256:fa19…e6cc` = starting candidate, re-verify). Mechanism preference: (a) fixture-level digest pin if testcontainers 0.27 API accepts digest-suffixed ref; (b) job-local pre-pull by digest + fixture tag re-point; (c) registry mirror with immutable tags. Record mechanism + digests. OUT OF SCOPE (name it): upgrading EOL rabbit/redis defaults = separate source change + own test run (spec: lock tested digests).
3.4 Job-local Docker for every rust-unit job (socket or job-scoped daemon = bastion design decision, not fixture change). Fixtures already isolate (ephemeral mapped ports, no fixed names — spot-verify at gate). Root `docker-compose.yml` NEVER started on bastion (policy doc testing.md:37 agrees).
3.5 Suite map (12 PG / 3 RabbitMQ / 2 Redis / 1 RustFS-20-tests; recount vs prep-21 at gate): processor-monitor (`state_machine.rs`, no container refs) + processor-compare (no testcontainers dep) are NOT container tests — keep on Docker-capable job (harmless) unless lanes split.

## Phase 4 — Rust Docker: 12-target root-context bake

4.1 Fix `docker-backend-rust` (today: wrong `backend-rust` context + no `--target` → builds final `yolo-observer` stage only). Target: `docker buildx bake -f backend-rust/docker-bake.hcl` — root context + all 12 service targets (bitcoin/eth/tron-processor, coingecko-pricing, processor-monitor, processor-compare, lightdash-csv-delivery+SOURCE_COMMIT, blockchain-explorer, bitcoin/eth/tron/legacy-grpc-server). Root context mandatory (Dockerfile `COPY . .` ×2; consumes root Cargo workspace + `backend/{eth,tron}-flyway` migrations + `scripts/lightdash-migration`). `mise run build-docker-*` shows correct per-target shape.
4.2 Gate: 12-target bake green.

## Phase 5 — Root Compose: never unchanged on shared bastion

5.1 `docker-compose.yml` (799 lines, `name: chainargos`, 36 fixed `container_name`, global bridge, `restart: always`, fixed host ports incl. 80/443, `~/chainargos-data` home mounts) MUST NOT run unchanged on bastion: guarantees cross-job collisions + shared mutable state. Also inspect `docker-compose.processor-verify.yml` (not yet read). Any compose use needs project isolation + ephemeral ports + job-scoped volumes. Gradle PG + rust suites stay on Phase 2/3 job-local services.

## Phase 6 — Toolchain preservation + browser gates

6.1 Preserve unless separately tested source change updates: GraalVM `oracle-graalvm-25.0.3` (Java 25 nativeImageCapable), Gradle wrapper 9.5.1, Rust 1.98.1 (+clippy/rustfmt), protoc 35.1 / protobuf-java 4.35.0, jOOQ 3.21.4 / Flyway 11.20.3 / PG JDBC 42.7.11, Micronaut platform 4.10.14 / plugins 4.6.2, nextest 0.9.140, Node 24.20.0, Bun 1.3.14 (runner-provided, not mise). Resolve Dockerfile `rust:1.98.0-trixie@sha256:620d…` DRIFT vs 1.98.1 (keep-or-tested-update, not silent).
6.2 Playwright/browser gates AUDITED and explicitly executed: unit `bun-platform` runs only install→lint→typecheck→build→vitest; `test:browser` (`scripts/browser-gate.ts`, zero-test refusal) is in NO unit command — unit green ≠ browser coverage. Wire explicitly (chromium+webkit provision, browserless/chromium v2.56.3 vs local). Platform uses @playwright/test 1.63.0 + axe-core 4.11.1; prototype playwright-core 1.62.1.

## Phase 7 — Failure labeling + full qualification (PR + main)

7.1 Classify every failure: container-start/pull (daemon/registry/digest) vs test assertion vs external-network (ONLY if real remote host in log; httpmock suites — `rpc_client_test.rs`, `network_verification_test.rs`, `pipeline/*` fixture/replay — can NEVER yield external class; their failures are code/fixture). External class fails run as external with host/endpoint quoted — never counted as container regression. Fixture acceptance separated from live Ethereum/Base RPC suites.
7.2 Gate proof bundle: (1) ownership/trust + actions repair verified (Phase 1); (2) 71/213 ledger holds; (3) scoped flyway/jooq coverage proof; (4) `--profile ci` everywhere + show-config evidence; (5) 4 digests locked+recorded; (6) 17-crate rust run green job-local, no root-compose; (7) 12-target bake green; (8) toolchain/browser evidence; (9) failure-taxonomy sample (≥1 induced pull/start failure labeled, or documented taxonomy if infeasible); (10) same controller + global N; single-writer + release-disabled intact.
7.3 Run full qualification: PR lane then main lane. If generic generator/Velnor bug found: fix generator/package → publish → requalify Velnor for that change → continue ChainArgos. No per-ChainArgos host architecture.
