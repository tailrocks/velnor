# V-A1 ownership verification — INDEPENDENT (read-only, no repo edits)
Date: 2026-09-17 (UTC). Target: /tmp/a1-ownership.md (bastion campaign, a1-ownership).
Method: re-listed trees with `find` (dotfiles included), re-counted ledger `[outputs]`,
checked every ledger output exists, grepped headers, diffed static_files sources,
re-read dispatch `generated_files_with_surface` (lib.rs:4842-4953), verified
policy-setup-action checkout context at both sites.

## VERDICT: CERTIFIED (with 2 errata)

Core claims under test both HOLD:
1. Zero unexpected files: every file under .github/ (21), .github-gen/ (4),
   config/fleet/ (7) resolves to a ledger output, generator input, the ledger
   itself, the one documented hand file, fleet-policy-gen outputs, or hand ops docs.
2. policy-setup-action oddity RESOLVED: both `uses:` sites (ci-policy.yml:60,
   ci-main.yml:162 — line numbers confirmed) are preceded by an
   `actions/checkout` step with `path: policy-setup-action` + `sparse-checkout:
   .github/actions/setup-velnor-workflow` pinned to base SHA. CI-runtime-only
   path; nothing missing from repo. Confirmed, not a defect.

## Corroborated specifics
- All 20 ledger outputs exist on disk; zero ledger-listed-but-missing.
- 14 workflows + workflows AGENTS.md + actionlint.yaml + project.toml +
  velnor-host.env all carry `# Generated by velnor-workflow`. (Actions: see erratum E2.)
- static_files byte-identity: 3/3 `diff -q` clean (both actions + workflows AGENTS.md).
- `builtin_generated_actions()` returns `BTreeMap::new()` — empty, static_files sole path. Confirmed.
- static_files rows at config L290-300 match claimed paths.
- `.github/AGENTS.md` self-declares hand-maintained (line 7). Confirmed.
- Dispatch table structure matches: actionlint insert, kind-unit loop, project.toml,
  workflow_files match arms (ci-pr/ci-policy/signer/ci-main/nightly/maintenance/
  preview/release/renovate/ci-runtime-products), workflow_templates, builtins,
  static_files, velnor-host.env. (Line refs ~5 lines stale: fn spans 4842-4953,
  arms at 4905-4919 — substance correct.)
- Fleet siblings: release-refs.toml header names fleet_policy.rs owner; all 3
  policies carry `"generator_version":"fleet-policy-gen/1"`; buildkitd.gc.toml and
  RUNBOOK.md have no Generated header (hand docs). Confirmed.
- Generator pin 7341ef4bdf750c1fbe419e94fb3848c5b8dde718 matches config L9.
- Takeover guard LEGACY_WORKFLOWS_AGENTS_MD const present (~lib.rs:6056).
- `.github/ci/` contains exactly project.toml + ledger (dotfile invisible to plain `ls`).

## ERRATA (incidental claims disproven; ownership unaffected)
- E1 COUNT: a1 §0/§5 say "19 outputs" / "19/19 present". Ledger actually lists
  20 outputs (author miscounted — likely dropped velnor-host.env). All 20 present,
  so the substance is 20/20, stronger than claimed. Fix number to 20.
- E2 ACTION HEADERS: a1 §0 claims "both actions ... carry the # Generated by
  velnor-workflow header". FALSE: `grep 'Generated by'` on both emitted action.yml
  files (and both sources) returns nothing. Expected — they are static_files
  verbatim copies of hand-maintained sources — but the sentence as written is wrong.
  Fix: actions are ledger-listed verbatim copies WITHOUT the generated header.

No unowned files found. No missing ledger outputs. Nothing further to disprove.
