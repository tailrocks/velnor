# A1 ownership inventory — bastion campaign
Date: 2026-09-17 (UTC). Workspace: /Users/donbeave/Projects/tailrocks/velnor-project/velnor3. Read-only; no edits made.

## 0. Method
- `./target/debug/velnor-workflow --help` shows NO `ownership`/`inventory` subcommands — the CLI is a single
  `generate TARGET` entry point. Mapping below is manual, grounded in two oracles:
  1. `.github/ci/.github-actions-generator-state` (schema 2) — the generator's own ownership ledger: 19 outputs.
  2. `generated_files_with_surface` dispatch table, `crates/velnor-workflow/src/lib.rs:4837-4947`.
- Regen command (per `.github/AGENTS.md`): `cd crates/velnor-workflow && mbx run --locked --manifest-path Cargo.toml -- --plain --force ../..`
  (`--plain --check ../..` for the verify gate). Generator pin: `7341ef4b…` (`.github-gen/velnor-workflow.toml` `[generator].revision`).
- All 14 workflows + both actions + `actionlint.yaml` + `project.toml` + `velnor-host.env` carry the
  `# Generated by velnor-workflow` header. All 19 ledger outputs exist on disk; digests not re-verified (read-only audit).

## 1. `.github/workflows/*.yml` → generator owner
| File | `generated_files_with_surface` dispatch (lib.rs) | Terminal renderer / primitive | Config trigger (`.github-gen/velnor-workflow.toml`) |
|---|---|---|---|
| ci-pr.yml | `generated_ci_pr` (:4900, :4972) | `WorkflowIr::render_nested(PullRequest, legacy_plan)` (`primitives/ir.rs:3228`) | `default_workflow_files` (:921) + `[[declare]] primitive="unit-aggregation" file="ci-pr.yml"` (L178) |
| ci-main.yml | `generated_ci_main` (:4905, :5009) | `render_nested(Main, legacy_plan)` | `default_workflow_files` + `unit-aggregation` (L182) |
| ci-policy.yml | `generated_ci_policy` (:4901, :5001) | `render_policy_entrypoint` (lib.rs:4103) | `default_workflow_files` only (no declare row — always emitted) |
| nightly.yml | `generated_nightly` (:4906, :5013) | `render_nightly_dispatcher` (`primitives/ir.rs:3295`) | `default_workflow_files` + `unit-aggregation` (L186) |
| maintenance.yml | `generated_maintenance` (:4907, :5017) | `primitives::release::maintenance_content` | `default_workflow_files` + `primitive="maintenance"` (L242) |
| ci-release-package-signer.yml | `generated_release_package_signer` (:4902, :5005) | `primitives::release::release_signer_content` (+ `templates/release-package-signer.yml`, lib.rs:179) | `primitive="release-signer"` (L234) → `added_files` (`primitives/mod.rs:796`) → `workflow_files` (lib.rs:4778) |
| release.yml | `generated_release` (:4909, :5028) | `primitives::release::release_content` (+ `[release]` section, `render_apt_release`, apt updater templates) | `primitive="release"` (L303) → `added_files` |
| preview.yml | `generated_preview` (:4908, :5024) | `primitives::release::preview_content` | `primitive="preview"` (L307) → `added_files` |
| ci-runtime-products.yml | `generated_runtime_products` (:4914, :5032) | `primitives::runtime_products::runtime_products_content` | NO declare row — `add_owner_runtime_products_file` (lib.rs:4823): auto-added because `repository == tailrocks/velnor` (setup-action owner repo) |
| ci-unit-bun.yml | kind loop (:4862-4881) | `WorkflowIr::render_kind_unit_workflow(Bun)` (`primitives/ir.rs:3856`); filename `ci-unit-{id_prefix}.yml` (lib.rs:2432) | `primitive="bun-package-pipeline"` (L93) |
| ci-unit-docker.yml | kind loop | `render_kind_unit_workflow(Docker)` | `primitive="docker-image-pipeline"` (L97) + `[[units]] id="docker"` (L269, `requires_trusted`) |
| ci-unit-docs.yml | kind loop | `render_kind_unit_workflow(Docs)` | `primitive="docs-lint-pipeline"` (L101) |
| ci-unit-opentofu.yml | kind loop | `render_kind_unit_workflow(Opentofu)` | `primitive="opentofu-pipeline"` (L105) |
| ci-unit-rust.yml | kind loop | `render_kind_unit_workflow(Rust)` | 12× `primitive="rust-crate-pipeline"` rows (L109-157) + `[[units]]` overrides (L247) |
| AGENTS.md | `static_files` emit (:4931) | `read_static_files` (lib.rs:2246); bytes owned by repo | `[[static_files]]` L298 → `source=.github-gen/sources/workflows-AGENTS.md` (byte-identical) |

`declare` rows with `file =` feed `surface.added_files` (`primitives/mod.rs:796-816`), merged into `workflow_files`
(lib.rs:4778-4782), then rendered by the dispatch table — that is the file↔primitive link for non-default files.

## 2. Referenced local actions → owner
| File | Owner |
|---|---|
| `.github/actions/setup-velnor-workflow/action.yml` | velnor-workflow `static_files` row (config L290-292); source `.github-gen/sources/actions/setup-velnor-workflow/action.yml` — **byte-identical** (`diff -q` clean). `builtin_generated_actions()` (lib.rs:4952) is currently empty, so static_files is the sole path. Consumed by release.yml ×21, ci-pr, ci-main, maintenance, preview ×3. |
| `.github/actions/report-velnor-ci-outcomes/action.yml` | velnor-workflow `static_files` row (config L294-296); source byte-identical. Consumed by all 5 ci-unit-*.yml ×2 each. |

## 3. Generated manifests → owner
| File | Owner |
|---|---|
| `.github/ci/project.toml` | `ProjectConfig::toml()` (lib.rs:936), inserted lib.rs:4883. Checked-in runtime contract. |
| `.github/ci/.github-actions-generator-state` | The ledger itself, written by `write_generated_with_options` (lib.rs:4785). |
| `.github/actionlint.yaml` | `render_actionlint_config` (lib.rs:5036). Runner-label contract. |
| `config/fleet/velnor-host.env` | `config::render_velnor_host_env` (`config/mod.rs:649`) from `[cache.velnor]` (lib.rs:4934). Generator-only budgets, never in project.toml. |
| `.github-gen/velnor-workflow.toml` | INPUT (generation config), not output. Hand-maintained. |
| `.github-gen/sources/**` | INPUTS (action bytes + workflows-AGENTS.md template), not outputs. Hand-maintained; generator copies verbatim. |

## 4. Oddity verdicts (A0 §3 / §5 follow-ups)
- **`policy-setup-action` path (ci-policy.yml:60, ci-main.yml:162) — RESOLVED, not a defect.** Both sites are preceded
  by an `actions/checkout` step with `path: policy-setup-action` + `sparse-checkout: .github/actions/setup-velnor-workflow`
  pinned to the PR base SHA (ci-policy.yml:52-58, ci-main.yml:154-160). The directory exists only at CI runtime as the
  trust boundary (base-owned setup action, `pull_request_target`-safe). No repo path is missing; `ls policy-setup-action`
  correctly returns nothing locally.
- **`.github/workflows/AGENTS.md` — GENERATED** (ledger-listed, static_files-sourced). Takeover guard for the legacy
  wording lives at lib.rs:6054-6061.
- **`.github/AGENTS.md` — HAND-MAINTAINED** (self-declares: "This file is the only hand-maintained file under `.github`").
  Not in ledger. Expected.
- **Root `AGENTS.md` — HAND-MAINTAINED** project rules. Out of generator scope. Expected.
- **`config/fleet/` siblings (NOT velnor-workflow outputs, all expected):**
  - `release-refs.toml` — ledger owned by `crates/velnor-tools/src/fleet_policy.rs` (Plan 039), input to fleet-policy-gen.
  - `policies/*-desired-policy.json` — generated by **velnor-tools `fleet-policy-gen`** (`"generator_version":"fleet-policy-gen/1"`), not velnor-workflow.
  - `buildkitd.gc.toml`, `RUNBOOK.md` — hand-maintained ops docs (no `Generated by` header).

## 5. UNEXPECTED files
**None.** Every file under `.github/`, `.github-gen/`, and `config/fleet/` resolves to exactly one of: ledger-listed
velnor-workflow output (19/19 present), generator input (`.github-gen`), the ledger file itself, the one documented
hand file (`.github/AGENTS.md`), velnor-tools fleet-policy outputs, or hand-maintained fleet ops docs. No present-but-unowned
files; no ledger-listed-but-missing files.
