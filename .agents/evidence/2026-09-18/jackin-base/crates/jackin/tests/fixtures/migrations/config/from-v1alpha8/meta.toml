from_version = "v1alpha8"
target_version = "v1alpha10"
target_version_shipped = "2026-07-06"
summary = "Restamps config as v1alpha9. Adds optional [telemetry].level and [telemetry].categories to AppConfig; additive with serde defaults, no transformation needed."
